/**
 * Orchestrator — top-level session driver.
 *
 * Implementation: Unit 6. Unit 1 ships the signature only.
 */
import type { Config, SessionResult } from './types'

export function runSession(_config: Config): Promise<SessionResult> {
  throw new Error('not implemented')
}
