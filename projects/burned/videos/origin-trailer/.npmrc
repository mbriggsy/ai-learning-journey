# Isolated package — opt out of the BURNED-root pnpm workspace so `pnpm
# install` here installs locally instead of no-op'ing by walking up to the
# root workspace (which has no `packages:` entry covering this dir).
# Carried forward from the v1 trailer package.
ignore-workspace=true
