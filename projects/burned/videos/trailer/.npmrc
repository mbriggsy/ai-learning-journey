# Trailer is an isolated package (roadmap ADR #2). The BURNED root carries
# pnpm-workspace.yaml for its own onlyBuiltDependencies config; without this
# flag, `pnpm install` from videos/trailer is a no-op because pnpm walks up
# to the workspace root and finds no `packages:` entry covering this dir.
ignore-workspace=true
