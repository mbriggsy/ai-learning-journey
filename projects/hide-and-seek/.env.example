# Gemini API key for art generation pipeline (Phase 7)
# Get one at https://aistudio.google.com/app/apikey
# IMPORTANT: Do NOT use the VITE_ prefix — that exposes to the client bundle
GEMINI_API_KEY=your_key_here
