# The Back Nine — TODO

> Actionable next-actions only. **Where we are** ("you are here") lives in [`docs/roadmap.md`](docs/roadmap.md) — the maintained per-unit status table. **What we're building + why** is [`docs/README.md`](docs/README.md). No session history here; git log has it.

## Next, in priority order

1. **U7 — the confidence-statement surface (IN PROGRESS; building via Path B = the band on real data first).** Done this session: the **engine per-year band fan** (opt-in `simulate(params, seed, {bandFan:true})` → `distribution.bandFan`; `BandFan`/`BandFanYear` in `src/shared/model.ts`; living-cohort + $0-ruin + `cohortFraction` semantics; byte-identical to a fan-off run); the **producer** `resolveBandData(fan, outcomeState, {formatDollar, annotations?, callouts?})` in `src/viz/bandData.ts` (resample → fixed 49-pt lattice, `dollarMax ≥ max(p90)`, `isFixedLattice` throw, `cohortFraction` carried per `BandSample`) — **the deferred U6-review producer-seam guards are now WIRED here**; and the **non-color verdict signals** `src/ui/verdictSignal.tsx` (six silhouettes + `VerdictIcon`, decorative-by-default). **Remaining U7 build, in order:**
   - **(a)** `src/ui/outcomeStates.ts` — pure presentation lookup keyed by `OutcomeState`, **exhaustive** (compile-error on a gap; re-derives NO thresholds/clamp — the engine already tags the distribution). state → {verdict wordKey, framing `outcome-first | action-first | range`, in-tool nextAction}.
   - **(b)** Extend `src/ui/copy.ts` — the per-state verdict grammar as slot fns (verdict / magnitude / next-action clauses). Outcome WORDS already exist (`outcomeOnTrack`…`outcomeAlreadyFailing`; indeterminate has none — it's range/incomplete). Survivor copy: SS step-down as a $ drop; single-filer effect with **NO "widow's penalty" jargon**. First-draft strings — exact wording is the cold-read's call.
   - **(c)** `src/ui/copyGuard.ts` + test — categories (imperative-advice verbs / advice superlatives / false-certainty verbs) + slot-discipline certainty-hygiene + catastrophe-lexicon tone-lint on survivor copy. ENUMERATE `copy`, assert every entry passes; keep `staticDisclosures` OUT. **LANDMINE: field-operation imperatives are an allowlisted class** ("Skip this if none applies", "Set an age only if…", "Lower it only if…") — recorded R40-U4 decision (plan §cross-cutting 4). Scope the advice-verb regex to verdict/recommendation copy; do NOT flag the existing R40 catalog.
   - **(d)** `src/ui/ConfidenceStatement.tsx` — verdict-text-first (word + dollar magnitude + `VerdictIcon`); band is the on-demand "show me the range" reveal (mounts `ConfidenceBandPanel`, fed by `resolveBandData`); pending/compute-error are non-verdict modes; entrance + crossfade motion, **numbers static (no odometer)**, reduced-motion. Spine-lead for U7 (date-first lead is D2). **`focusHeading` reuse is lint-clean today** — verified 2026-06-24: `eslint.config.js` has NO `src/ui/**` import-ban block (the ui/intake blocks were consciously *deferred*, see the store block's comment ~L108-112), and `src/intake/a11y.ts:2` already pre-declares "U7 reuses focusHeading". So importing it from `@intake/a11y` passes lint. The "move to a shared home" is a *cleanliness* option only (ui→intake inverts the conceptual stack), NOT a gate — and it can **NOT** be `src/shared/` (`a11y.ts` imports React; shared is the pure leaf the engine depends on); if ever moved, a ui/viz-tier a11y module. Don't block on it.
   - **(e)** `src/ui/SurvivorReadout.tsx` — second confidence statement in the same vocabulary on the engine's SURVIVOR distribution. **LANDMINE (verify FIRST): the engine emits NO survivor-specific distribution today** — `Distribution` carries only JOINT `terminalValuesReal`/`depletionYears`/`survivalFraction` (+ `taxAware` + `bandFan`); the survivor case lives WITHIN the paths (the step-down), not as its own output. SurvivorReadout likely needs a NEW additive presence-keyed engine output — the SAME shape of work the band fan just was (which the TODO's old "couples with U7" note under-scoped). Trace `simulate.ts`/`confidence.ts` before building; don't fake a survivor stat UI-side.
   - **(f)** A dev-only **preview harness** rendering all six states + the survivor readout (drive the lookup/`VerdictIcon`/`ConfidenceStatement` with representative fixtures; feed `resolveBandData` representative fans). **Drive it through Playwright/Chromium + screenshot-verify EVERY state before the cold-read** (manifesto: see it render before claiming it — the verdict icons were designed BLIND and have never been visually rendered).
   - **Then the N=1 laptop cold-read** across all six states + survivor — the **Act-2 EXIT GATE**. The silhouette-legibility call is Briggsy's (color-blind = the exact user this channel serves). Visual direction is RATIFIED — "The Clubhouse Ledger" (`src/ui/styles/tokens.css`: Fraunces + Source Sans 3, cream/green, tabular-nums global), already cold-read-approved on intake; U7 EXTENDS it. (`compound-engineering:frontend-design` skill won't load post-reload — name glitch; not blocking since direction's ratified. Do NOT fall back to the official `frontend-design` plugin.)

2. **Phase-3 render, owed when U7 wires the band live** (`src/viz/ConfidenceBand.tsx` `ResolvedFan`): treat a `dollarMax`/`horizonYears` change across recomputes as a **re-draw, not a cross-scale morph** (still owed from the U6-review); and drive **thin-late-year de-emphasis** off the now-carried per-sample `cohortFraction` so a band narrowing because couples died never reads as rising certainty. The remaining ~8 advisory U6-review P3s (drop the inert `outcomeState` from `ResolvedBandData`; the modal reduced-motion symmetry test) stay low-priority — [insight 043](docs/insights/043-a-domain-guard-loud-on-one-degenerate-input-fails-calm-on-its-sibling.md).

3. **D2 — the state-adaptive first answer + two-pane laptop layout.** Date-first for not-yet-retired, spine-first for already-retired; one intake flow, two leads. The showcase surface (laptop is primary). Composes over the U6 band + U7.

4. **U8 — save/load (the first encrypted Save).** `ScenarioV3` is defined; remaining = the codec v3 arm (a *separate* `checkPersonV3`, never mutate the frozen `checkPerson`), `AnyScenario` membership, the user-facing **first-Save ceremony** (passphrase-strength gate `zxcvbn-ts` ≥3 ∧ len ≥12 + recovery-phrase display + mandatory export), and load-on-return. NOTE: the encrypted-vault IndexedDB writer (`writeVault`/`rewriteModel` in `store/db.ts`) already exists (the Act-1 encrypted store) and is wired into `backup.ts`/`session.ts` — U8 is the *flow* + the v3 writer, NOT a new persistence layer. Fold in the portfolio-holdings model field ([`docs/decisions/portfolio-holdings.md`](docs/decisions/portfolio-holdings.md)) on the same persisted shape — no v3→v4 migration.

5. **Single-user "just me" mode** (deferred). A real people-of-one path — NOT "zeros for the spouse" (a phantom zero-spouse still carries a mortality curve and warps joint-survivor + the MFJ→single transition).

Then Act 3 (Controls, U9–U13) → Act 4 (Solver & Recommendation, U14–U17). See [`docs/roadmap.md`](docs/roadmap.md).

## Standing cadences
- `/ultramode-code-review` at every unit boundary; the four-skill UI loadout before ANY user-facing surface (CLAUDE.md "UI design skills").
- `/brief` (read `docs/insights/`) before a unit; `/distill` after.
- **Delegated build:** use **native Agent Teams** for live-steer, **eye-oracle** units (Briggsy steers mid-build) and the **Workflow** tool for fire-and-forget, **test-oracle** fan-out. The durable laws live in memory `feedback-delegated-build-laws` (decide-before-dispatch · loop=unit-of-trust · independent-verify-by-a-different-agent · task-list-canonical · coordinator-restraint · eye-vs-test-oracle). *(The `loop-engineer` + `swarm` skill wrappers were archived 2026-06-24 — thin wrappers over the now-native capabilities; don't reach for them.)*

## Verify-owed
- The OOP-medical hint figure (`src/intake/referenceData.ts` → `OOP_MEDICAL_TYPICAL_HOUSEHOLD`) is grounded-search-sourced, **not** primary-table-verified (`directionalUntilPinned`). BLS bot-walls `curl` — ask Briggsy to pull the CE "Age of reference person" table to pin it cell-by-cell. Conservative-by-design while it stays directional.
