# The Back Nine — TODO

> Actionable items only. Live plan: `docs/plans/back-nine-mvp/roadmap.md` (4-phase spine). Product: `docs/plans/direction-reset-2026-06-04.md` (north-star) + `docs/brainstorms/the-back-nine-requirements.md` (v2). Landmines + solver-blocking gates live in the roadmap's "Risks" + "Validation Gates". Project insights: `docs/insights/`. No session history here — git log has it.

## Current State
**P1·U1 (Monte Carlo engine core) SHIPPED + CI green on Linux (2026-06-06).** Pure, deterministic `(params, seed)` engine: vendored `mulberry32` + stateless Box-Muller + exact simple→log moment conversion (`rng.ts`); the single shared per-year update (`decumulation.ts`, contract #3, used by both backtest and MC); Trinity rolling-window backtest on **real 1925–1995 Shiller data** vendored from cFIREsim-open (`historical.ts` + `reference/shillerSeries.ts`); sex-specific cohort longevity with per-path survivor identity (`longevity.ts` + `reference/mortality.ts`); 4 CRN-neutral drawdown policies (`sequencing.ts`); the MC spine — dimension-only CRN draw schedule + earned-income bridge + SS step-down + R19 gate (`simulate.ts`); pure distribution→reading with coarse-grid quantization + 10/10 clamp (`confidence.ts`); typed-array worker transfer with a structural main↔worker split (`engineProtocol.ts`/`engineWire.ts`/`engine.worker.ts`). **Verified: typecheck clean · 113 tests pass (15 files) · initial JS 195.4 KiB ≤ 300 KiB · CI green on Linux · seen in-browser (real engine through the worker → "9 of 10 · over-funded").** A 10-skeptic adversarial review folded 3 R19 gaps in `validateParams`.

**P1·U0 (foundation) SHIPPED earlier** — scaffold, PWA, engine-purity lint, constants module, ACA re-verify gate, strict CSP, full CI. 5 U0 insights in `docs/insights/`.

**P1·U2 (the tax-and-accounts overlay) COMPLETE — `taxOverlay.ts`, wired into `simulate.ts` (2026-06-06).** A per-year deterministic cash-term transform (contract #3, ZERO draws) that grosses the withdrawal up for tax + RMD + Roth conversions over per-person buckets, reducing **byte-identically to the spine** under the EXHAUSTIVE OFF condition. The arc (per-milestone detail is in git log): M1 seam → M2 RMD → M3 ordinary-income tax (the `solveGrossWithdrawal` per-year fixed point) → M4 SS provisional income (Pub 915 Worksheet 1) → M5 Roth conversion + cap-gains/QD stacking → M6a wire into `simulate` + MFJ→single survivor switch + bracket-fill injected-ceiling → **M6b the Joint-Life & Last-Survivor RMD divisor + per-person pre-tax splitting**. M6b detail: the JLLS Table II grid is now **SOURCED** (`constants/jointLifeLastSurvivor.data.ts` — transcribed from eCFR §1.401(a)(9)-9(d), cross-verified vs Pub 590-B Table II, the **last constants gap closed**); `selectRmdDivisor` switches off ULT for a >10yr-younger sole spouse (gap ≥ 11 → smaller forced RMD); and per-person pre-tax (`OverlayParams.pretaxByPerson`) makes each spouse's IRA force its own RMD on its own age, a deceased spouse's IRA rolling to the survivor. Every milestone got a multi-lens adversarial review; M6b's caught insight-008 recurring in a relational guard (→ insight 010) + a calm-but-wrong reference-mismatch path. **Verified: typecheck clean · 268 tests (16 files) · lint clean · bundle 195.4 KiB (overlay is worker-side). 10 insights in `docs/insights/`.**
- **Load-bearing U2 contracts (don't break):** reduce-to-spine byte-identity under EXHAUSTIVE OFF (buckets collapsed AND conversion 0 AND tax off AND RMD-inert); one shared market draw (CRN, no asset-location); the gross-up contraction k≈0.74 / `GROSS_UP_MAX_PASSES=128` (insights 006/007 — `rmd` is a pre-loop constant); `validateParams` is the R19 gate (a NaN passes a relational guard — insight 010); every tax figure read from `@engine/constants`, all `directionalUntilPinned` until the P1-exit pin pass; the per-person ledger sums to `buckets.pretax` and is gated on `initialPretaxByPerson` (absent ⇒ the aggregate path, byte-identical).

**P1·U3 (the healthcare overlay) IN PROGRESS — M1 (constants) + M2 (MAGI calcs) SHIPPED 2026-06-06; M3 (the pre-65 ACA fixed-point + 400%-FPL cliff) COMPLETE 2026-06-07 (Slices 1/E/2/3/4 wiring + Slice 5 value-correctness battery); M4 (IRMAA post-65 2yr-lagged feed-forward) COMPLETE 2026-06-07.** The data foundation: `src/engine/constants/health.ts` now carries the FULL federal tables the ACA fixed-point + IRMAA lag need (U0 had stubbed only endpoints/first-tier). `acaApplicablePercentage` = a 6-band interpolable schedule (IRS Rev. Proc. 2025-25 §3.01, read verbatim + secondary cross-verified; the one missing value was the 133% lower bound = 3.14%); `federalPovertyGuidelines` = the 2025 HHS base ($15,650) + per-person increment ($5,500) — the 400% cliff dollar is now DERIVED (4×FPL = $84,600 exact, retiring the rounded `aca400FplCliff`); `irmaa` = the full 5-tier schedule (CMS + The Finance Buff + an internal cost-share check), per-tier Part B/D surcharges, totals derived from `partB2026`. 5 new typed interfaces in `types.ts`; 3 golden tests pin EVERY interior value (externally-derived, DND/012) + the cross-table identities (FPL→cliff, IRMAA→partB2026 cost-share, MFJ=2×single tiers 1-4). All `directionalUntilPinned: true` (flip at the P1-exit pin pass); ACA tables keep `reVerifyEveryBuild`. **M2:** `healthOverlay.ts` now carries the two PURE MAGI calculators — `acaMagi` = nonSSordinary + realizedGain + **FULL** ss; `irmaaMagi` = same but **TAXABLE** ss (= AGI); they differ by EXACTLY the non-taxable SS portion (the load-bearing distinctness fact, research §4a, primary-source-verified). Pure functions of the tax overlay's converged quantities; NOT yet wired into `simulate` (no ACA/IRMAA cost — M3/M4). 5-case externally-derived distinctness test (DND/012). **M3 (the ACA pre-65 fixed-point, SLICED — design settled by a 16-agent workflow, verdict fix-then-ship): Slice 1** = `solveGrossWithdrawal` now returns `{gross, components: MagiComponents}`, surfacing the floored converged locals — byte-identical `.gross`, reduce-to-spine untouched; **Slice E** = the ARPA/IRA ENHANCED applicable-% table (`acaApplicablePercentageEnhanced`) sourced verbatim from IRC §36B(b)(3)(A)(iii) + cross-verified (the no-cliff / flat-8.5% open-top-band toggle regime — Briggsy's call to ship the toggle on REAL numbers, never faked); **Slice 2** = pure `solveAcaFundedGross` — bisection over net-premium `[0,enrolled]` + explicit cliff branch (probe-at-0 short-circuit, ceil-quantized feasibility, over-cliff PTC=0, **conservative** below-100%-FPL PTC=0 + `belowFloor` flag), regime-aware (enhanced skips the cliff), finiteness-FIRST guards, 10 unit tests via a synthetic `fundNet`. **Slice 3** = the 4 health streams on `OverlayParams` (`healthcareEnabled`/`enhancedSubsidies`/`slcsp`/`enrolledPremium`) + finiteness-FIRST R19 element guards in `validateParams` (mirror `conversions`, NOT `bracketFillCeilings` — +Infinity REJECTED). **Slice 4** = `solveAcaFundedGross` WIRED into `runTaxAwareDecumulation` + `simulate`: the OUTER ACA fixed point wraps the inner gross-up in the EXISTING per-year loop; the net premium funds inside `grossWithdrawal` (presence — terminalReal drops) + accrues to `TaxAwareResult.totalNetPremiumReal`; the AGE GATE (`pre65 = livingCount − count65`, prices ONLY with ≥1 pre-65 member — `ResolvedYear` gained `livingCount`) + reduce-to-spine byte-identity + the healthcare-requires-tax fail-loud at BOTH `validateParams` (indeterminate) AND the overlay backstop (throw). A 6-lens/18-agent adversarial review caught 4 minor findings (the enrolled/slcsp streams lacked the symmetric overlay fail-loud the standing decision requires — a +Infinity enrolled silently un-priced for a direct caller; a missing slcsp threw mid-path instead of indeterminate) — folded + tested (up-front overlay backstop + slcsp-coverage pre-check + mixed-age gate test). M3 Slice 5 then added the integrated value-correctness battery (10 tests). **U3-EXIT CODE-REVIEW PILOT (2026-06-07) — 8 NEW×MATERIAL findings FIXED:** the R19 gate (`validateParams`) now validates the enum params (drawdownPolicy/longevityMode) + person `retirementAge`/`socialSecurityClaimAge`/`sex` + `initialTaxableBasis` finiteness UNCONDITIONALLY (not gated on `taxable>0`); `solveAcaFundedGross` SEGMENTS `[0,enrolled]` at the 133%-FPL applicable-% discontinuity and returns the cheapest feasible root (the residual is NOT globally monotone — insight 013); `totalNetPremiumReal` accrues AFTER the depletion check (no over-accrual in a failed year); the per-person ledger backstop throws on a COUNT mismatch (catches PARTIAL ref mismatch, not just all-dead); `solveGrossWithdrawal`'s 10 positional args bundled into a named `GrossUpContext` (anti-transposition); `allocateWithdrawal` switch gained a `default: throw`; `buildDollar` magnitudes are now value-tested + an on-track plan never emits a contradictory 'trim'. **U3 CODE-REVIEW PILOT #2 (2026-06-07) — 1 test gap + 4 fail-loud hardenings FIXED (`530244e8`):** the survivor ACA-cliff transition (`fplForHousehold` drops 2→1 at a death → cliff 84,600→62,600 → a danger-band MAGI flips subsidized→PTC=0) is now COVERED (insight 014); `findPForMagi` + `applicableContributionFraction`-empty-bands now fail loud (burned/062); the overlay backstop mirrors `validateParams`'s slcsp-COVERAGE gate; `validateParams` validates `overlay.filing` membership. **ZERO live production bugs found across both review tracks** — see "Code-review cadence — DECIDED". **Verified: typecheck clean · 337 tests (17 files) · lint · verify:aca · bundle 195.4 KiB.**

**P1·U3 M4 (the post-65 IRMAA feed-forward) COMPLETE — `healthOverlay.ts` (pure) + `taxOverlay.ts` (wired) + `simulate.ts` (R19 gate), 2026-06-07.** IRMAA is a 2-year-LAGGED FEED-FORWARD, NOT a fixed point (research §4c): the year-t Medicare cost keys off `irmaaMagi[t−2]`, a CONSTANT addend to the spending the gross-up funds. Pure pieces: `irmaaTierSurchargeMonthly` (step fn — highest tier strictly exceeded, finiteness-FIRST, NO ceil on the integer thresholds per insight 012) + `medicareAnnualCost` (base Part B + surcharge, × Medicare-enrolled count × 12 — Briggsy's "FULL Medicare cost" call, the post-65 mirror of funding the full pre-65 net ACA premium). Wiring in `runTaxAwareDecumulation`: a per-year `irmaaMagiHistory` recorded at every healthcare-on year's converged FLOORED components (`irmaaMagi`, NOT `acaMagi` — the dead code at `healthOverlay.ts:103` is now LIVE), read at `[t−lookback]` (lookback READ from the constant, never hard-coded 2) or `irmaaMagiSeed` for t<lookback; `regime.count65` = the Medicare-enrolled count (the post-65 mirror of the ACA `pre65` gate); `filing[t−2]` selects the threshold column (so the survivor MFJ→single flip is itself lagged +2yr — source-confirmed via gemini-grounding: SSA uses the N−2 return's filing status); accrues to a new `TaxAwareResult.totalMedicareCostReal`. The second-order self-funding loop the research said the MVP "may ignore" is captured FOR FREE (the surcharge folds into `net` before the MAGI is recorded). **Verified: 337 tests · 7 new M4 integration goldens passed first-try (externally-derived, DND/012): presence+base-value, surcharge-from-seed, the EXACTLY-2yr lag, irmaaMagi-not-acaMagi (the SS-vs-conversion differential), the survivor +2yr flip (insight 014), reduce-to-spine, fail-loud.** DISCLOSED boundaries (never silent): Part D BASE plan premium (plan-specific, user-budgeted), the Part B deductible/OOP (HSA/M5), premium-free Part A assumed, the SSA-44 appeal not modeled (conservative — overstates the widow's penalty).
- **Load-bearing U3·M4 contracts (now permanent tested contracts; do NOT regress):** (1) the surcharge is a 2yr-lagged CONSTANT addend to `net`, NEVER a search variable — insight 013 only bites if a solver searches over an IRMAA-affected quantity, so it must stay OUT of the ACA bisection's residual (it rides in `baseNet`). (2) record `irmaaMagi(solComponents)` (TAXABLE SS) — `acaMagi` (FULL SS) is the WRONG one and the SS-vs-conversion differential test guards it. (3) the seed is REQUIRED (fail-loud at BOTH `validateParams` and the overlay backstop) iff a member is Medicare-enrolled (≥65) in a year < lookback — never default 0 (a phantom $0 surcharge → understated cost → overstated survival). (4) `fundingNet = net + medicareCost` is `net + 0` exactly when no one is ≥65 ⇒ reduce-to-spine + every pre-65 fixture byte-identical. (5) the one-pass MAGI-components lag (<1e-7) is far below the $1 INTEGER IRMAA grid (insight 012) ⇒ measure-zero tier-flip; verified-and-documented, NOT recompute-at-converged-gross. (6) the pre-M4 ACA age-gate fixtures that asserted post-65 byte-identity are now ACA→IRMAA HANDOFF tests (post-65: `totalNetPremiumReal === 0` but `totalMedicareCostReal > 0`).
- **Load-bearing U3 architecture (M2+), from the understand-map:** ACA = an OUTER fixed-point (bisection on a PIECEWISE-monotone funding-gap residual — the map is non-smooth: the 400% cliff + a 133%-FPL applicable-% DISCONTINUITY [the residual jumps UP there, so the solver SEGMENTS the bracket at each discontinuity and picks the cheapest feasible root — insight 013, fixed 2026-06-07] + sliding-band kinks + the `max(0,·)` floor) WRAPPING the inner tax gross-up (`solveGrossWithdrawal`, untouched): premium→net→gross-up→MAGI→premium. IRMAA = a 2yr-lagged feed-forward (NO fixed point), an outer addend to `net[t]`. There is ONE death-event signal (`t < deathOffsets[i]` / the `living` 2→1 shrink) — ACA-FPL flips immediately, IRMAA at +2yr; NO new boundary. Piggyback the per-year health regime onto the EXISTING `living`/`HouseholdYear` loop (never a second per-(path×year) loop). The two MAGIs derive from quantities the tax overlay computes at the CONVERGED gross — the locals INSIDE `solveGrossWithdrawal` (`taxOverlay.ts` ~719-727: `nonSSordinary`, `realizedGain` AFTER its `max(0,·)` floor at ~720, ssBenefit, taxableSS), now surfaced on a `GrossUpSolution` (`solveGrossWithdrawal` returns `{gross, components}` — M3 SHIPPED). **The locked contract: feed `acaMagi`/`irmaaMagi` THOSE surfaced floored locals — never recompute MAGI off a raw-gain ledger (a down-market negative gain → understated MAGI → the named sign-inversion).** ACA-MAGI = nonSSordinary + realizedGain + **FULL** ss; IRMAA-MAGI = same but taxableSS (= AGI). `taxExemptInterest`/foreign income are 0 in the MVP and OMITTED from `MagiComponents` (as-we-go — no producer); **if a muni bucket is ever added, muni interest goes into BOTH MAGIs AND §86 provisional income (`taxableSocialSecurity`) in ONE change — else IRMAA-MAGI understates.**

**Plan set remains LOCKED** (roadmap + 4 phase docs).

## ⚑ NEXT SESSION — START HERE

**State:** clean tree · all gates green AND **CI green** at last commit (typecheck · **342 vitest tests (17 files)** + **7 Playwright e2e (`verify:csp`)** · lint · verify:aca · bundle 195.4 KiB). A SECOND `/ultramode-code-review` pass on the committed CSP track found + folded a real gap (the exact-directive guard was a hand-picked subset → now a deny-by-default whole-policy table covering img-src/form-action; + an img-src image-beacon e2e arm; + duplicate-directive reject — insight 016 deepened). Last session (2026-06-08) shipped the **CSP browser-enforcement harness** — the prior tracks 1+2, now DONE: the false "browser-enforcement verified" claim is now **TRUE + CI-gated** (`scripts/serve-dist-with-headers.ts` two-port harness + `e2e/csp.spec.ts` 5 tests + `verify:csp` in `verify-the-back-nine.yml`; a `connect-src` cross-origin-**exfil** proof + an **exact-directive** vitest guard came out of the adversarial review; mutation-verified teeth; **insight 016**) **and** the 2 decision-free strategic-review doc fixes. The maiden STRATEGIC/phase-level review (7 confirmed of 35) results are below.

### ⚑⚑ FOUNDATIONAL PIVOT (Briggsy ATC, 2026-06-08): FULL PROJECTION — accumulation is now IN SCOPE
The thesis EXPANDED from decumulation-only to **accumulation → decumulation** (full projection). **Trigger:** the M5/HSA contributions question surfaced that the engine models **ZERO contributions to ANY bucket** — `cashTermsForYear` (`simulate.ts:99-136`) clamps `net = max(0, spending − earned − ss)` ("never a contribution back"); it starts at `currentAge` with `initialPortfolio`, grows only by market returns, earned income offsets spending in working years, but **ongoing savings are never added**. Briggsy wants the pre-retirement saving phase modeled (project balances forward from current state THROUGH retirement). This **reopens the locked thesis** — the requirements (R20 "from retirement age to 65"; R9 sequencing+conversion) + the U0–U17 plan are all decumulation-shaped.

**BRAINSTORM + `/ce:plan` BOTH DONE (2026-06-08).** Requirements: `docs/brainstorms/the-fuck-off-date-requirements.md` (R26–R39, 6-persona reviewed). The re-plan: **`docs/plans/2026-06-08-001-feat-fuck-off-date-accumulation-plan.md`** — 4 tracks / ~9 units (A: fold R26–R39 into master+roadmap+phase docs · B1: resume U3·M5 HSA *spend* · C: accumulation engine [contribution/ticker constants → signed inflow → date-search] · D: ~5-min account-level intake + the state-adaptive date surface). Engine premise VERIFIED against source (`cashTermsForYear`/`stepYear` subtraction-only). All "Deferred to Planning" decisions resolved (contribution-inflow signed term + golden; date-search = exhaustive non-monotone sweep, not the solver, profile gate; per-account not per-lot basis; ticker→blend bundled table cash→bond + TDF-static; 2026 constants = Notice 2025-67 / Rev. Proc. 2025-19 / SECURE 2.0 §§108/109; retired-but-contributing = disclose + empty-overlap invariant).

**⚠ THE PLAN IS NOT YET CLEAN — round-3 doc-review fold PENDING before `ce:work`.** Two review rounds ran (adversarial → folded 15; then 6-lens doc-review on the folded doc → 25 confirmed, several P1). **The 7 root findings are captured as precise prescriptions in the plan's "⚠ STATUS" section at the top.** Two are regressions the round-2 fold itself introduced.

**⚑ NEXT = ROUND-3 FOLD in a FRESH SESSION**, then `ce:work`:
1. **Round-3 (coherent REWRITE, not patches — patching caused the round-2 regressions):** open the plan, fold the 7 roots in its "⚠ STATUS" section together (they interact — A's date-offset model `Y` reshapes C/D/E). Roots: **A** search var = household date-offset `Y` not an age (per-person engine offsets); **B** [P1, my regression] fold the overlay contribution AFTER the bucket-scale; **C** [P1] §7 clamp gate-on-accumulation + truncate-at-death + disclose; **D** [P1] IRMAA working-year MAGI override (≈$0 history → optimistic); **E** [P1] per-person Medicare-onset as a SEPARATE predicate (not `count65`) + re-key validateParams seed loop; **F** [P1] build/sequence U4 before D1 (PII at rest); **G** [P2] lower-bound date selection's pessimistic/no-date case. Full evidence: workflow outputs `wf_0feee122-3bf` + `wf_f8c188a6-eee`.
2. **Re-run the doc-review** to confirm clean → delete the "⚠ STATUS" section → set the plan's `deepened`/`doc-reviewed` stamps to clean.
3. **THEN `ce:work`** — Track **A** (doc reconciliation) or **B1** (HSA spend) are both independent and can start first.

**M5 (HSA) = Track B1** — HSA *spend* (decumulation), independent of accumulation, can resume right after round-3. HSA *contributions* fold into Track C (accumulation). Full M5 spend understanding-map recoverable from run `wf_29e52525-1d0`.

**On hold behind the pivot (was START HERE):**
1. **`/ce:work` M5 (HSA 4th bucket)** — PAUSED (see above). When it resumes: read `docs/research/pre65-healthcare-aca-hsa-2026-06-04.md` (HSA strand) + `phase-1-foundation.md` Unit-3 HSA scope; the SPEND-side 5-slice plan (type+reduce-to-spine → qualified-spend-reduces-fundingNet MAGI-invisibly → owner-65+ Medicare-premium privilege + the ACA-premium trap → post-65 non-qualified laundering negative test → ledger reconciliation) is synthesized in this session's history. The `generalDrawableTotal` (taxable+pretax+roth) vs hsa-inclusive total split is the subtle correctness point (dark at hsa=0 — insight 014 class).
2. **DISTILL the strategic-review cadence → a skill** (mirror how `/ultramode-code-review` was built FROM its pilots): a `/strategic-review` (or `/phase-review`) skill — document-review personas at WHOLE-PHASE scope, fed the north-star + plan + as-built + the TRACKED-open-items list (the "is-it-NEW?" filter is THE calibration — it dropped 27/35), adversarial-verify, a "this is NOT a code review" altitude guard, design-lens skipped pre-UI, model=latest/greatest. The maiden script is recoverable from the `wf_70789fa9-026` run journal.

> **Note — UI work hasn't started yet.** It begins at **P2·U5** (the intake flow — the first user-facing React surface), with **U6/U7** the headline design surfaces (confidence band + "magic moment" statement). When the first UI component lands, load `/frontend-design` + `/emil-design-eng` (memory: `feedback-ui-work-requires-design-skills`). Phase 1 (M5, M6, U4) is still headless engine.

## ⚑ Strategic review — RESULTS (maiden run, 2026-06-07; 7 confirmed of 35)
**The unifying theme:** the foundation is correctness-solid but its **output / per-candidate-evaluator contract is shaped for the SPINE answer, not the SOLVER.** Dispositions = Briggsy's ATC calls.
- **[P1, adversarial] Candidate-throw vs rank-K (DECIDE-SOONER):** the overlay has 3 fail-loud `throw` sites (gross-up 128-pass `taxOverlay.ts:806`; ACA bisection `healthOverlay.ts:326`; `findPForMagi` `:358`). The P4 solver "ranks K candidates" but NOTHING defines what happens when one candidate THROWS mid-batch — abort (deny the user any rec) vs silent-drop (the banned silent measurement; dropped class = the aggressive near-cliff candidates the curse bites hardest). Decide a typed per-candidate `infeasible/non-converged` sentinel (cheaper before the solver layers on) + a U14 "a candidate that throws" planted-fail case. → **TRACKED, decide at M6/P4-entry (flagged sooner).**
- **[P2, feasibility] Lifetime-tax has no engine producer:** `simulate` emits only `terminalReal+depletion`; lifetime tax is accumulated NOWHERE; `objective ≡ headline` forbids recomputing it in `objective.ts` (the M3 sign-inversion class). → TRACKED (output contract).
- **[P2, architecture] Death-year basis snapshot missing:** only horizon-end basis exists (`taxOverlay.ts:155-157` self-flags it); the leave-more after-tax-to-heirs objective needs per-path basis at the SAMPLED death year. → TRACKED (output contract).
- **[P2, security] Restore semantic validation unspecified:** U4 lists "no-falsely-confident-restore" but specs only GCM crypto integrity — no structural validation of the decoded Scenario before it's trusted (GCM-valid ≠ semantically valid). → TRACKED (decide in U4).
- **[P2, security] CSP "verified" was false as-built** → **SHIPPED 2026-06-08** (two-port harness + 5-test browser e2e + `verify:csp` in CI; the review added a `connect-src` exfil proof + exact-directive vitest guard; insight 016).
- **[P3, coherence] `survivalFraction` docstring drift** → **FIXED 2026-06-08.**
- **[P3, coherence] stale M3 "must refactor" comments** → **FIXED 2026-06-08.**

**Eval of the cadence (maiden voyage):** WORKS — 7 genuine strategic findings a CODE review structurally cannot reach; the verify "is-it-new?" filter dropped 27/35 (the noise = re-statements of tracked items: survivor-SS ×3, ScenarioV3 ×5, death-year-basis ×3, the directional pins ×3, healthcare-wire ×3); the standout P1 came from the ADVERSARIAL lens (adversary earns its keep at strategic altitude too); personas stayed strategic (no code-nits leaked). → institutionalize as a skill (#4).

## ✅ Code-review cadence — DECIDED (2026-06-07, two pilots): lean holistic + adversarial-verify is the rule; turnkey `ce:review` demoted to optional breadth

**Pilot #1** (custom-Workflow of ce's review AGENTS, on the pre-fix engine): 43 raised · **8 NEW×MATERIAL** fixed (incl. the 133% ACA bisection gap → insight [013]). **Pilot #2** (2026-06-07 — the head-to-head that settled the fork): the **GENUINE `ce:review` 11-persona skill** (its real catalog, hand-orchestrated per the skill's stages) vs an **ultracode 6-lens HOLISTIC pass** (incl. the architecture lens ce:review lacks), both on U3, **every finding adversarially verified against source**. Across 17 reviewer agents / ~80 raw findings: **ZERO live production bugs** — the catches were **1 material test gap** (the survivor ACA-cliff transition → insight [014]) + **4 cheap fail-loud hardenings** + forward landmines. All fixed in `530244e8`.

**What the head-to-head settled:**
- Turnkey `ce:review` is **NOT theater** (it independently caught the survivor-cliff gap + a unique ScenarioV3/wire forward item) but is **NOT worth the recurring full run**: ~half its 11-persona team added no material signal on a pure engine (project-standards **0**, agent-native **0**, maintainability/learnings/perf = nits + re-finds of the deferred perf item), it **structurally lacks an architecture lens** (which found 3/8 of pilot #1's keepers), and it has **NO adversarial-verify stage**.
- **ultracode adds real value on its own:** the holistic + architecture framing caught 2 items ce:review missed, AND the instinct to adversarially-verify every finding is what **right-sized two overstated "P1s"** — `overlay.filing` (overridden by `resolveYear`, so direct-caller-only) and the MAGI-components lag (hits only unwired `irmaaMagi`). Folded on faith, the turnkey output would have "fixed" a dead field (insight 005: a review is an idea generator, not an authority). One reviewer's suggested fix was even **directionally wrong** (a sub-floor `return 0` = phantom FULL subsidy) — caught only by tracing.

### THE STANDING CADENCE (this is the rule)
A read-only HOLISTIC Workflow at the UNIT boundary (whole-file, not diff-scoped), each reviewer fed the project's load-bearing contracts (from CLAUDE.md + docs/insights/). The lean lenses are **table stakes (always-on)**; the verify + distill steps are **MANDATORY**; OTHER ce agents are **conditionally added by the nature of the diff** (ce:review's own Stage-3 selection, applied to our baseline — don't run them all every time).
1. **ALWAYS-ON lenses (table stakes):** correctness · architecture · testing · ts-idiom · **simplicity/YAGNI** · one api-contract/persistence glance.
2. **CONDITIONAL — evaluate per review; add the ce agent only when the diff warrants:** **security** (auth/crypto/input — e.g. U4 KDF/recovery) · **reliability** (error-handling/async/background jobs — the worker/store layers) · **performance** (when the deferred measured pass runs / the P4 solver makes `simulate` hot) · **data-integrity + data-migrations** (U4 IndexedDB + the `ScenarioV2→V3` migration, any schema change) · **julik-frontend-races** (P2+ async UI) · **previous-comments** (a PR re-review). Skip the ones the diff doesn't touch.
3. **MANDATORY VERIFY:** adversarially verify EVERY acted-on finding against source before folding (insight 005) — a confident finding is a hypothesis, not a verdict (this pilot's two overstated 'P1s' + a directionally-wrong suggested fix were caught ONLY here).
4. **Fix + full gate** (typecheck · test · lint · verify:aca · verify:bundle); sweep scratch files (insight 005).
5. **MANDATORY `/distill` afterward** (Briggsy 2026-06-07 — "very important"): capture the session's hard-won insight before it evaporates (this pilot → insight 014). The Stop hook enforces /distill, but the review OWNS it explicitly.
- **Simplicity lens — CALIBRATED to this engine** (Briggsy's add, 2026-06-07): target YAGNI · premature abstraction · dead code · unnecessary indirection (e.g. the dead `overlay.filing` field + the redundant eager `probeAt(enrolled)` this pilot surfaced) — distinct from `maintainability` (which scored ZERO: it asks "is it tidy", simplicity asks "should this EXIST / can it be simpler"). Do **NOT** let it flag the project's DELIBERATE dense intent-comments or its required fail-loud guards (those are values, not debt; "WOW over simplicity" governs the VISUAL layers, never the engine).
- **DON'T** run the turnkey 11-persona `ce:review` per milestone (half noise, ~6–7M tokens, no verify stage). `/code-review ultra` (billed, Briggsy-triggered, Claude can't launch) stays available for a deeper cloud pass on a unit if wanted.
- **⚑ INSTITUTIONALIZED → `/ultramode-code-review`** (BUILT 2026-06-07; global skill, source `projects/skills/ultramode-code-review/`, junctioned into `~/.claude/skills/` like `/window`+`/distill`). Encodes this whole cadence: auto-calibrates per project (reads `CLAUDE.md` + `docs/insights/` for the contract brief) → always-on + conditional lenses → adversarial-verify → fix+gate → `/distill`. **BATTLE-TESTED 2026-06-07 (first live spin, on M4):** the cadence works — the brief landed, calibration held (zero noise on dense comments / fail-loud guards), adversarial-verify right-sized 4 findings P2→P3 + dropped a factually-wrong one. BUT the two highest-value refinements came from **Briggsy, not the skill's output** — the review-side adversary had been dropped (only verify-side survived) and reviewers defaulted to **sonnet**; BOTH now folded (adversary ALWAYS-ON + diverse-panel escalation; latest-model fan-out — see insight 015 + `feedback-always-latest-model` memory). (Optional later: the triggering-description optimizer, a background A/B run.)

## Next Steps (priority order)
1. **P1·U3 — the healthcare overlay (IN PROGRESS; M1+M2+M3+M4 COMPLETE 2026-06-07; M4·G review spin DONE `eb0f2ce3`/`ec685963`).** ⚑ **M5 (HSA 4th bucket)** is the next product milestone (after the strategic-review decision in NEXT SESSION track 1). Spec: `phase-1-foundation.md` (Unit 3) + `docs/research/pre65-healthcare-aca-hsa-2026-06-04.md`. Remaining milestones — mirror U2's M1→M6b; **test-first on the golden anchors**; multi-lens read-only review per milestone (insight 005, sweep scratch files). **STANDING DECISION — AS-WE-GO (supersedes the original seam-first M2):** the `healthcareEnabled` toggle + each cost-input stream (SLCSP, enrolled premium, OOP-medical, MAGI-seed) + the reduce-to-spine wiring land WITH their first consumer (M3+), each carrying its own **`Number.isFinite`-FIRST** R19 guard at BOTH `validateParams` (simulate.ts) AND the overlay backstop (insights 008/010) — the proven U2 pattern; never front-load a guessed input shape (e.g. SLCSP may need per-enrollee structure M3 reveals).
   - **M3 (COMPLETE 2026-06-07) — ACA pre-65 fixed-point + 400%-FPL cliff.** Slices 1/E/2/3/4 (the wiring) + Slice 5 (the value-correctness battery) all SHIPPED. Slice 5 = the 4 externally-derived integrated PTC fixtures (under-cliff-clean 250%-FPL · interpolation-interior 175%-FPL · cliff-pair just-under/over · **D6 inversion**) + near-cliff ceil-quantization + the enhanced-subsidy toggle (`taxOverlay.test.ts`) + CRN stability across the ACA wiring (`simulate.test.ts`) — 10 tests via a single zero-return year (terminalReal = P − gross), triangulated hand-math = engine = a 12-agent independent §36B re-derivation (zero mismatches, DND/012). **D6 figure CORRECTED:** the stale "112.32%" was an unsourced back-of-envelope; the engine-faithful effective rate on a $10k conversion crossing the cliff is **~123.1% healthcare-on** (lost PTC 9,633.60 + grossed-up tax 2,677.31, over $10k) vs **~13.6% tax-only** (= 12%/(1−12%)) — the omission inverts the ranking ~9×, the load-bearing D6 proof.
   - **M4 (COMPLETE 2026-06-07) — IRMAA 2yr-lagged feed-forward.** SHIPPED — full detail + the load-bearing contracts are in the Current State block above. Briggsy's scope call: fund the FULL post-65 Medicare cost (base Part B + both surcharges), the post-65 mirror of funding the full pre-65 net ACA premium. All planned contracts landed (per-filing-unit MAGI history; surcharge from IRMAA-MAGI[t−2] × Medicare-enrolled count, ×count never ×2; seed fail-loud near-65 at BOTH layers; survivor MFJ→single flip lagged +2yr — source-confirmed). 7 externally-derived integration goldens passed first-try.
   - **M5 — HSA 4th bucket.** Extend `AccountBuckets` (engine) + `AccountBalances`/`PersonAccounts` (model) with `hsa`; MAGI-invisible ONLY for qualified spend (capped at OOP-medical + owner-65+ Medicare premiums); post-65 NON-qualified withdrawal = ordinary income raising BOTH MAGIs (the laundering negative test); Medicare zeroes contributions; owner-age-keyed Medicare-premium privilege; the ACA-premium-not-qualified trap. Shares the one market draw (contract #2).
   - **M6 — FINAL cross-overlay integration + survivor flip.** (M3's own `simulate` wiring + the enhanced-subsidy toggle moved INTO M3 Slices 4-5 per the as-we-go decision + Briggsy's "source enhanced now"; M6 is now the FULL integration once IRMAA + HSA also land.) Survivor flip both regimes on different clocks (ACA immediate, IRMAA +2yr); full reduce-to-spine + CRN across ALL boundaries + presence companions (premium + surcharge + HSA). Final verify: typecheck + suite + lint + verify:aca + verify:bundle.
2. **P1-EXIT dataset pins (directional→exact; parallelizable, must clear before the PHASE closes).** Three named gates flip U1's directional fixtures to pinned-exact:
   - **SSA `table4c7`** cohort curves → replace `reference/mortality.ts` (currently a Gompertz fit to grounded anchors) 1:1 with a committed table4c7 snapshot (SSA bot-blocks curl — manual fetch + commit).
   - **Trinity long-term-CORPORATE** bond series → `shillerSeries.ts` is GOVERNMENT-bond yield-as-return; pin to flip 50/50 from the govt 100% toward the corporate 95%.
   - **Ibbotson intermediate-GOVERNMENT** total-return series → pin Bengen's 4.15% to bit-exact.
   Each fixture stays marked directional in code until its gate clears. Name who/what clears each.

## Open Items (decide at the named unit)
- **⚑ SOLVER OUTPUT CONTRACT (strategic-review 2026-06-07; decide as ONE step at M6 / P4-entry).** The engine's output/evaluator surface is shaped for the spine answer, not the solver. Decide together (preserving "ranks, does not re-decumulate"): (1) accumulate **per-path lifetime tax paid** (mirror the `totalNetPremiumReal`/`totalMedicareCostReal` parallel-accounting pattern — `objective ≡ headline` forbids recomputing it in `objective.ts`, the M3 sign-inversion class); (2) capture **taxable basis + character at each path's SAMPLED death year** (not horizon-end; `taxOverlay.ts:155-157` self-flags it) for the §1014/IRD leave-more objective; (3) a typed **per-candidate `infeasible/non-converged` sentinel** so a thrown candidate ranks worst instead of aborting or silent-dropping the K-batch (the **P1**, decide-sooner) + a U14 planted "candidate that throws" case; (4) surface these on `Distribution` + the worker wire. This EXTENDS item (c) below (that is P3 *display*; this is the *argmax* surface). M6 (cross-overlay integration) is the natural home for (1)/(2)/(4).
- **U3 review pilot #2 — forward landmines.** (a) **M4 — RESOLVED 2026-06-07:** `irmaaMagi` (NOT `acaMagi`) is wired to the surcharge, guarded by the SS-vs-conversion differential test; the one-pass MAGI-components lag (<1e-7) was verified measure-zero vs the $1 INTEGER IRMAA grid (insight 012 — a `> N` compare is ceil-invariant), so it is verified-and-documented, NOT recompute-at-converged-gross (an extra pass would risk returning the wrong `.gross`). (b) **U4 persistence:** `OverlayParams`'s health streams (`healthcareEnabled`/`slcsp`/`enrolledPremium`/`enhancedSubsidies`/**`irmaaMagiSeed`** — M4's new field) are NOT in `ScenarioV2` — define `ScenarioV3` + a v2→v3 migration before persisting a healthcare-enabled plan, else a saved plan reopens with healthcare silently OFF (the stale-rec = executed-action landmine). (c) **P3 display:** `TaxAwareResult.totalNetPremiumReal` + **`totalMedicareCostReal`** (M4) + the per-year `overCliff`/`belowFloor` disclosure flags are not surfaced to `SimOutput`/`Distribution`/the wire — surface before the first healthcare-cost consumer. (d) **P3/P4 disclosure:** the survivor cliff-drop ($84,600→$62,600) is correct but a strategy calibrated to the couple cliff detonates for the survivor — surface it. (e) **direct-caller hardening (deferred, low priority):** a direct `runTaxAwareDecumulation` caller with healthcare on + an absent/short `ssBenefits` understates ACA-MAGI (overstates PTC), and a short `householdYears` silently falls back to the static household — add backstops when the P3/P4 direct caller lands.
- **`seedB` held-out construction — DEFERRED to U14** (not U1). U1 ships single-seed CRN via `buildDraws(seed, paths, maxHorizon, peopleCount)` — one `mulberry32` consumed in dimension-only fixed order (all market normals, then longevity uniforms), so two candidates under one seed draw byte-identically. When U14's held-out grade lands, pin the `seedB` construction (SplitMix64-high-32 *or* SHA-256(seedA) mod 2³² — pin one) + a statistical-decorrelation acceptance bar + the deterministic `seedB[0..m-1]` B-family (re-derivable from the one persisted `seedB`).
- **`market.space`/`returnsAreReal` are spine-REJECTED, not implemented.** The U1 spine models SIMPLE-space + REAL moments ONLY; `validateParams` rejects `space:'log'` and `returnsAreReal:false` as indeterminate (the type still advertises them, the worker boundary is untyped). Implementing log-space / nominal-deflation is a SCOPE EXPANSION needing new externally-derived golden fixtures (DND/012) — never a guard-only flip.
- **Pin gate (P1-exit, parallelizable):** every tax figure now lives in `@engine/constants` (the JLLS grid was the last gap, closed in M6b) and stays `directionalUntilPinned: true` — flip them together (to `false`) only at the deliberate P1-exit pin pass that confirms against the IRS primaries. The JLLS grid is already eCFR-sourced + Pub-590-B-cross-verified, so its pin is largely a formality; don't flip piecemeal. The solver token (U14) requires `directionalUntilPinned === false` on every rec-relevant figure, so this is load-bearing.
- **Exact ε** for the survival-equivalent band — calibrated against the U14 oracle (a gate; the oracle-cleared token is withheld until ε is calibrated).
- **Survivor SS before the survivor's OWN claim age = $0 (no early §202 survivor claim) — a disclosed MVP simplification, decide at P4.** `cashTermsForYear` (`simulate.ts`) withholds the survivor benefit until the survivor reaches their own claim age; a real widow(er) could claim on the deceased's record from age 60. UNDERSTATES income → harder horizon → the CONSERVATIVE direction for the survival FLOOR (safe). BUT it suppresses several years of SS-torpedo interaction the sequencing/conversion controls exist to exploit — so it can move a RELATIVE strategy ranking (an optimizer's-curse surface). `net` and `ss` use the same figure (no internal disagreement). P4 ranking must be aware, or model the early survivor benefit (needs a sourced §202 rule).
- **Overlay hot-loop perf — DEFERRED to a measured pass (when the P4 solver makes `simulate` hot, K calls/candidate).** The 3-lens review quantified: `simulate`'s per-(path×year) `living` array + `{ living }` `HouseholdYear` object are rebuilt every year (~440k short-lived allocations at 4000 paths × 55yr), yet `living`'s CONTENT is piecewise-constant (≤ 2^peopleCount = 4 distinct arrays for a couple, switching only at death years) — memoizable by alive-bitmask ONCE per `simulate` call (path-independent content; only the switch-year varies). Also: pre-size the 5 per-path arrays (`new Array(horizon)` vs `push`-grow). Both are byte-identity-SAFE (same references/order). **LANDMINE — do NOT "optimize" the double `allocateWithdrawal`** (`taxOverlay.ts` fixed point + the ledger): the solver's last-pass `alloc` LAGS the converged gross by one iteration, so reusing it instead of re-allocating at the converged value is NOT byte-identical → would perturb a reduce-to-spine fixture. The current re-alloc at line ~719 is correct; leave it.
- **U4 KDF-location spike:** main-thread + a rendered "unlocking…" pending state vs a dedicated crypto worker — decided by measured jank on the reference device.
- **WASM** compute-budget threshold + mid-tier reference device (instrument-first). When WASM lands: add `'wasm-unsafe-eval'` to CSP `script-src`. WASM is ALSO the only path to a TRUE cross-engine bit-identical headline (see the cross-engine landmine).
- **CI housekeeping (dated):** `verify-the-back-nine.yml` uses Node-20 actions (`actions/checkout@v4`, `actions/setup-node@v4`, `pnpm/action-setup@v4`). GitHub forces Node-24 default **2026-06-16** and removes Node-20 **2026-09-16** — bump these action majors before then (a non-blocking deprecation warning today, not a failure).
- **Federal-data freshness: runtime fetch is REJECTED (do not re-propose) — freshness is a BUILD-time gate.** Fetching IRS/CMS/HHS tables at runtime to "stay current" breaks four contracts at once: engine purity (no network/clock/env in `src/engine/**`), the CSP (`connect-src 'self'` blocks any external fetch), offline-first, and determinism (a saved plan must reproduce byte-identically against its OWN `taxVintage` — silent data drift is the calm-but-wrong vintage-mismatch the constants discipline exists to prevent; "always current" would be wrong for the user's actual tax year). Freshness is a build-time mechanism: `reVerifyEveryBuild` + `verify:aca`'s 30-day staleness fail, the `{value,citation,directionalUntilPinned}` + vintage stamps, the annual pin pass — data updates only via a deliberate, dated, committed snapshot. **FUTURE (worth building):** (a) a dev-only fetch-and-diff helper (pulls the latest published tables → human-reviewable diff → commit; stays build/dev-time, never a runtime dep); (b) broaden `verify:aca`'s staleness pattern into a general constants-vintage gate that flags when `TAX_YEAR`/`COVERAGE_YEAR` lag the calendar year.

## Landmines
- **Cardinal:** *"it's just for friends" must NEVER soften validation* — calm-but-wrong is the sin; the bar RISES for a recommender. The N=1 cold-read judges *tone*, never *correctness*.
- **M3 Slice-4 contracts SHIPPED + TESTED — do NOT regress** (`taxOverlay.ts` `runTaxAwareDecumulation` + `simulate.ts` `validateParams`): (1) the per-year ACA predicate is `acaTable !== undefined && enrolledThisYear !== undefined && Number.isFinite(enrolledThisYear) && enrolledThisYear > 0 && pre65 > 0` (finiteness BEFORE the relational; insight 010). (2) **AGE GATE:** `pre65 = regime.livingCount − regime.count65`; ACA prices ONLY with ≥1 pre-65 member; FPL denominator = `fplForHousehold(livingCount)` (full living household). (3) **reduce-to-spine:** healthcare off/empty is byte-identical to the spine. (4) **Health-stream fail-loud at BOTH layers (the standing decision, now COMPLETE):** a PRESENT non-finite/negative `enrolled`/`slcsp` throws at the up-front overlay backstop (a `+Infinity` would otherwise suppress the solver's own throw → a silent un-priced premium); a MISSING `slcsp` in a priced year → `validateParams` indeterminate (the slcsp-coverage pre-check), never a mid-path throw. (5) **healthcare REQUIRES tax** (ACA-MAGI comes only from the gross-up) — rejected at both `validateParams` and the overlay backstop.
- **U3-exit pilot contracts (SHIPPED + TESTED 2026-06-07 — do NOT regress):** (1) the R19 gate validates the **enum** params (`drawdownPolicy` ∈ DRAWDOWN_POLICIES, `longevityMode` ∈ {sampled,fixed-horizon}) + **person** `retirementAge`/`socialSecurityClaimAge` finiteness + `sex` membership + `initialTaxableBasis` finiteness **UNCONDITIONALLY** (NOT gated on `taxable>0` — the empty-bucket NaN slip); a NaN in any of these silently produced calm-but-wrong, not indeterminate. (2) `solveAcaFundedGross` is **piecewise** — it segments `[0,enrolled]` at applicable-% **discontinuities** (`applicablePctDiscontinuityFractions`) and returns the **cheapest feasible** self-consistent root; the residual is NOT globally monotone (insight 013). (3) `totalNetPremiumReal += …` sits **AFTER** the depletion check (a failed year accrues nothing). (4) the per-person ledger backstop throws on `alive.filter(Boolean).length !== living.length` (**partial** ref mismatch, not just all-dead). (5) `allocateWithdrawal`'s switch has a `default: throw`. (6) `buildDollar`: an **on-track** plan with a depleted bad decile (p10≤0) emits **'on-the-line', never 'trim'** (no self-contradicting message); the $/month magnitudes are value-tested.
- **U1 calibration is DIRECTIONAL, not pinned.** The MC's absolute numbers are Shiller-GOVERNMENT-bond calibrated (50/50 MC ≈ 95% vs the govt 100% historical) — NOT the Trinity-CORPORATE 95%. The 3 P1-exit pins flip them. Don't read the directional bands as exact.
- **MC-vs-history is TWO-SIDED (Kitces):** the MC sits below history at 4% (i.i.d. tail penalty) but crosses ABOVE at 5% (overstates sustainable income in the 70–90% range). Never assert a blanket "MC < historical" — only in the stress region (`simulate.test.ts`).
- **Cross-engine headline is NOT bit-identical in plain-TS.** `survivors/paths` is integer-exact, but a path within transcendental-rounding-noise of depletion can flip the COUNT by ±1 across engines. Quantization to a 1% grid REDUCES (does not eliminate) the headline-flip risk; the absolute guarantee is the WASM trigger (`confidence.ts` header).
- **`validateParams` is the engine's R19 gate** — the worker boundary is untyped (structured clone), so every incomputable input (mean ≤ -1, |ρ| > 1, NaN moments, unsupported space/real) must be rejected THERE, not assumed away by the type. The adversarial review caught 3 such escapes; keep the gate tight when adding params.
- **`App.tsx` SMOKE_PARAMS is a scaffold liveness demo** — P2·U7 replaces the App body with the real first-answer surface; don't build on the smoke readout.
- **Oracle-before-solver:** U14's optimality oracle must exist + pass BEFORE U15 can recommend (harness-minted opaque token; compile error to skip).
- **Read `docs/insights/` before U3:** engine-purity lint is airtight (keep it); don't compute golden fixtures via the engine (DND/012); a NaN passes a relational/tolerance guard so finiteness must come first (insight 010 — directly relevant to U3's ACA/IRMAA fixed points). Full Risks set in the roadmap.
