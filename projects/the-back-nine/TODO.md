# The Back Nine — TODO

> Actionable next-actions only. **Where we are** ("you are here") lives in [`docs/roadmap.md`](docs/roadmap.md) — the maintained per-unit status table. **What we're building + why** is [`docs/README.md`](docs/README.md). No session history here; git log has it.

> **Dev seeds are LIVE** (`?seed=retired` = all-retired on-track spine band; `?seed=date` = still-working fuck-off-date band; `?seed=borderline` = **borderline retired ("On the line, 7 of 10") whose band descends to $0 — the two-pane honesty cold-read**; `?seed=dateborder` = borderline date with a $0 tail, but long-horizon-squashed). Jump straight to a worded result + band to cold-read any change. DEV-only, DCE'd from prod (`src/ui/devSeeds.ts`). **`?vault=<key>` (e.g. `?vault=retired`) PLANTS an encrypted vault from that seed + drops on the Unlock screen with the passphrase PRE-FILLED — one click opens it, no re-driving intake + the Save ceremony. The param STRIPS ITSELF once the plant lands (history.replaceState), so a plain refresh probes the REAL vault like prod — re-planting is an explicit re-entry of the `?vault` URL, never a refresh side effect (the plant clears the vault first; leaving it armed silently reverted a saved edit — Briggsy, 2026-07-02). DCE-proven (grep dist/ for the dev passphrase = 0).** `plantDevVault` is memoized so StrictMode's double-invoke plants once (a concurrent second plant races the session epoch → spurious write-failed).

## Next, in priority order

> **U8 — SHIPPED + CLOSED (2026-07-02).** The whole save / recovery / decrypt-on-return arc is live-verified in real Chromium and review-hardened (three ultramode passes across the arc; the closeout review ran 10 lenses + per-finding adversarial verifiers — 16 findings ALL verified real, zero false alarms, 7 lenses converged on one defect). Closeout landed: **Fork B** (the commit→export update-apply ceremony hold — `holdUpdateApply` in the ONE `updateGate` seam, raised across securing+export), **Fork C(ii)** (the standing View-only banner — both unlock paths thread `describeUnlockReadOnly`; two-tab verified live), **the proper edit-and-re-save** (`resultSave.ts` persist machine → `session.save()` update path, edit-aware badge; the hydrated session STARTS saved — the return path was offering the ceremony against an existing vault, the worst instance of the ② dead-end), plus the review's 4 fixes: **reload-not-retry** on the read-only refusal (a "Try again" that deterministically re-refuses is the lying-remedy shape; verified live), **AT transition announcements** for the re-save (persistent polite region — the swapped status nodes mounted populated and were silent), **edit-back clears a save failure** (disk matches ⇒ no alarm), and the **reference-equality mutant test** kill. Toast chrome (the deferred U0 CSS pass) also landed. **Post-closeout, same day (Briggsy cold-reads):** the `?vault` param now **self-strips after planting** (a refresh probes the REAL vault — his 1969 edit "reverting" was the re-plant clobbering it, not the save); the result **actions row is withheld while nothing has resolved** (remove-the-opportunity — `computing` prop, epoch-safety verified first); and the **thinking-breathe** motion family (one 2.1s opacity breath on every "working" surface incl. the provisional tag — the heartbeat while the final sharpens; `.fod-pending`/`.cs-pending` were the missed same-words siblings, insight 054's shape caught live). **1473 tests / 84 files, bundle 220.5 KiB.** Detail: git log (95f11695 · 291b014e · 2bcaf4f9 · ea077168 · 48411c1f · 0a5096f9 · b670bc8c) + `docs/roadmap.md` You-Are-Here + `docs/council-log.md`.

1. **⚑ COLD-READS OWED (Briggsy's eye — the only U8 residue):**
   - **✓ The working-household return beat — CLEARED (2026-07-02, Briggsy: "time is better than fine")**: the wait is acceptable at any length *provided there's visual feedback that it's crunching* → shipped the **thinking-breathe** family (his ask) + the provisional tag as the background-sharpen heartbeat. **Residual = his eye on the breath itself** (tempo/depth — one token `--dur-breathe: 2100ms` in `tokens.css`, trough `0.65` in `base.css`) and the `answerProvisionalTag` wording on the RETURN path ("with what you've entered so far" reads intake-flavored when everything IS entered — a return-variant sentence is a copy.ts slot away, flagged 2026-07-02, no call made). Still not driven on a slower device.
   - **The closeout's new strings** (all copyGuard-clean, shipped craftsman's-lead; tweak path `copy.ts`): the View-only banner (`unlockReadOnlyLead` "View-only" + the de-doubled `unlockReadOnly` body), the re-save beat (`resaveCta` "Save your changes" · `resaveHint` · `resavePending`), and the read-only refusal (`saveErrorReadOnly` — reload steer, never "close the other tab"). (He has SEEN resaveCta/resaveHint live 2026-07-02 without objection — not formally cleared.)
   - Standing older tone reads (D2c ladder / two-pane whitespace / squash) live in **Parked** below.

2. **Act 3 (Controls, U9–U13) — THE NEXT BUILD.** Start at [`docs/plans/3-controls.md`](docs/plans/3-controls.md). The **HARD pre-Act-3 gate stands** (council 2026-06-29): before the U10 Roth-conversion lever ships, drive a real non-monotone seed through the LIVE ladder render + N=1 cold-read — "tests green" ≠ "the warning was seen to draw."

3. **DISCLOSED FOLLOW-UPS (filed, NOT blockers — the U8 tail):**
   - (1) **Re-offer export on return** when a committed vault has no recorded backup — needs a persisted backup-made sentinel (numeric, never `Infinity`/`NaN` per DND 009). **⚑ DISSENT preserved (sec-engineer + fiduciary + craftsman):** the Fork B hold only closes the app-initiated "Update now" vector; a close-tab mid-export user still lands unlocked believing the off-device promise — **FLIPS to a blocker IF the close-tab vector proves common.**
   - (2) **Re-sync `docs/architecture.md`'s stale BIP-39/HKDF-128 recovery description** → the shipped PBKDF2-600k user-passphrase reality.
   - (3) **The ColdStart/no-vault restore door** — `savedBody` promises "restore it on another device", but a WIPED device probes `no-vault`→ColdStart, which has no restore affordance. Backend-free: `session.restore` already flows over no-vault; the work is a quiet ColdStart affordance mounting the SAME `RestoreFlow` + its design cold-read.
   - (4) **Extract `useLiveAnnouncer()`** — the liveRef+forwarder idiom is now at FIVE sites (`SaveFlow`/`UnlockScreen`/`RecoveryFlow`/`RestoreFlow`/`Result`) and `intake/flow.tsx` spells the contract differently. Prescription: a hook next to `createAnnouncer` in `src/intake/a11y.ts` returning `{announcer, liveRegionProps}`; migrate all 6 sites; its own verify pass (touches the shipped intake region). **Fold in the closeout review's sibling advisory:** extract a shared `PendingPanel({status})` — the `save-step--pending` section is verbatim at 6 sites.
   - (5) **Closeout-review advisories (verified real, not material):** thread the read-only verdict into `deriveResultSave` so a read-only session never derives the dirty "Save your changes" CTA at all (today the CTA appears, fails honestly, and the failed arm reloads — coherence, not correctness); `UpdateToast` component tests (the `:empty` zero-footprint contract + apply-gate wiring — ViewOnlyBanner's twin contract IS pinned, the toast's isn't); an App-level integration test locking the notice threading end-to-end (emit + render are each seam-tested; the App glue between them is unguarded); simplify `describeUnlockReadOnly` to return `UnlockCopyKey | null` (the 'silent' wrapper arm forces the same collapse at both call sites).

4. **Single-user "just me" mode** (deferred). A real people-of-one path — NOT "zeros for the spouse". (NB: `deriveSpineBandAnnotations` / `deriveDateBandAnnotations` currently assume a two-person household for the ages string — generalize both when this lands.)

Then Act 3 (Controls, U9–U13) → Act 4 (Solver & Recommendation, U14–U17). See [`docs/roadmap.md`](docs/roadmap.md).

## Done this arc (state pointers — git log has the detail)
- **COUNCIL EXPANDED + DESIGN-REVIEW FIXES SHIPPED** (2026-06-29; `docs/council-log.md`). Added 3 permanent UI elders to `.claude/workflows/council.js` (`design-engineer`, `motion-designer`, `a11y-auditor`) — they self-seat on a UI council via a keyword heuristic, stay out of engine/scope councils. First design council reviewed the 2 shipped result surfaces (9/10, no veto, red team could-not-break). FIX-NOW shipped + **live-verified** (Playwright, real Chromium): (1) `:hover` on `.fod-ladder__summary` (date route's primary drawer had only `:focus-visible`) — text+chevron `--ink-muted`→`--ink`; (2) persistent `sr-only <h1>{copy.appTitle}` in `App.tsx` gated on `began` (result views were `h1`-less, started at `h2`) — verified single h1, h1→h2; (3) `.band-modal__close` hover. DROPPED the inert `.band-enlarge-surface` hover (red-team: `currentColor`=0 in src/viz → phantom affordance). 1356 tests; typecheck+lint green.
- **THE TWO-PANE LAPTOP SHOWCASE (D2d) ✓ BUILT + VERIFIED LIVE** (2026-06-29; council-decided 8/10, full roster — `docs/council-log.md`). The landed magic moment reflows on the laptop (≥`--bp-laptop`/68rem) from a single reading-measure column to a verdict-dominant asymmetric two-pane (words left at `--measure`, projection band a top-pinned hero right), as a pure CSS-grid reflow of the IDENTICAL DOM — phone byte-identical. Tokens `--bp-laptop`/`--content-wide`/`--pane-band-min` (tokens.css); `.reveal__lead` + `data-twopane` (stamped off `resolved`, so no-date/indeterminate/pending/error stay single column); grid in result.css (frame) + fuckOffDate.css/confidence.css (route placement). Council's red-team hits all fixed: ≥68rem floor (band-figure 358px, proven > the 260px label-drop) not 60–64rem; `data-twopane` JS gate (CSS `@media` can't read `resolved`); seam unforked. **Honesty-floor guard** `twoPaneHonestyFloor.test.ts` (source-bind). New honesty seeds `?seed=borderline` (spine) + `?seed=dateborder` (date). Also: hardened the clerk charter to auto-load the UI design skills on UI councils; fixed a pre-existing rng-test timeout flake; updated the frontend-design skill notes (CE dropped their variant). 1352 → **1356 tests**; typecheck + lint green.
- **THE CURVE-DRAWER (D2c) ✓ BUILT + VERIFIED LIVE** (2026-06-29; council-decided 8/10). The date route's secondary "how your odds shift by *when* you stop" as a DISCRETE integer-rung odds-ladder (council ruled OUT a smooth curve). Layering: pure `src/viz/curveMarks.ts` (moved from ui — viz can't import ui) → `oddsLadderGeometry.ts` → `OddsLadder.tsx` (hand-rolled SVG, mirrors ConfidenceBand) → `oddsLadderChrome.ts` → `FuckOffDate.tsx` `<details>` drawer. Honesty: bar at true 8.5 (clears/dips by position not hue), crown = engine's durable date (vermilion+ring+"your date", never max-rung), dips ABOVE the bar + "doesn't hold", `atCeiling` never "10 of 10" (slots.xOfTen clamp threads to the a11y tree), draw-once-fade + reduced-motion-identical, CSP-clean. No-date plots NO ladder (Hawk veto) → worded "how close" line. 24 D2c tests (curveMarks 8 + geometry 12 + render 7 + mount 2 + CVD 3 − overlap) → 1352 total. Also fixed a pre-existing `council.js` lint-gate break (Workflow runtime globals undeclared). ⚑ Briggsy's tone cold-read is the only owed item (see Parked).
- **THE SURVIVOR READOUT — WIRED LIVE ✓ COMPLETE + VERIFIED LIVE** (2026-06-28; council-chosen priority, full authority). The "as the survivor" (widow's-cliff) statement was built + cold-read-cleared (U7) but DARK — `survivorConditioned` + `survivorReading` were dropped by the worker→main wire gap. Now crossed via the `bandFan` mirror: `ResolvedWire`/`fromWire` carry both presence-keyed (compact structured clone, never the transfer list); `engineProtocol.run` opt-in widened; `memoryModel` requests `survivorConditioned: true` on the SPINE headline run only; `answerView` threads `survivorReading`; `ConfidenceStatement` mounts `SurvivorReadout` BELOW the band (opacity-only reveal, no layout shift). Verified live at `?seed=retired`: "And if you're on your own → On track, 9 of 10… steps down about $2,000." 7 new tests (wire round-trip byte-identity + presence/absence; answerView threading; CS mount/absence). 1320 tests. ⚑ Briggsy's in-situ tone cold-read is the only owed item (after-the-fact, NOT a blocker).
- **THE BAND HOVER/SCRUB TOOLTIP ✓ COMPLETE + REVIEWED + VERIFIED LIVE** (2026-06-28; commit 99dc2508). Snap-to-lattice scrubber on the SHARED renderer (one seam → both bands): a thin solid rule + p10/p50/p90 dots + a readout of the household **ages** and the **spread** (range-first "Eight in ten land between $X–$Y", median last as "Most likely"). Drift-proof — `tooltipRows` emitted in `resolveBandData`'s same resample loop (byte-equal to the drawn vertex); dollars via `formatAxisDollar`; ages via `deriveBandAgesAt` (single-sourced w/ the axis). **Dead-cohort withdrawal**: below `COHORT_FADE.full` the crisp dollars give way to "Few couples reach these years". CSP-clean (SVG `transform` attribute, zero inline styles), no motion (instant), pointer-only + aria-hidden. Touch: drawer taps-to-enlarge, enlarged modal drag-scrubs (`setPointerCapture`). State inside `ConfidenceBand` → 047 remount resets a stale readout. 8-lens ultramode review (no P0/P1): fixed `placeReadoutBox` flip (was occluding the rule mid-band) + extracted the honesty gate to pure tested helpers (`isThinCohort` + `composeReadoutLines`) → [insight 048]. 1313 tests.
- **THE DATE BAND — D2 slice 2 ✓ COMPLETE + REVIEWED + VERIFIED LIVE** (2026-06-28; commits bf652491 build + 44f8b0c0 review fixes). The fuck-off-date now carries the "show me the range" band with a FUTURE **"work stops"** marker at the crowned date (the accumulation→decumulation inflection). `runDateSearch` emits the crowned candidate's fan ONCE (CRN-identical to the sweep reading) bundled as `DateBand{fan, outcomeState, offsetYears}` (engine-tagged via `summarize`; self-describing offset); rides the wire by structured clone. `answerView.dateBand` + `deriveDateBandAnnotations` + the `FuckOffDate` mount. **Re-draw-not-morph** lives IN `ConfidenceBandPanel` (keys its INNER band on scale, so an open modal survives). **Once-per-landing focus** edge-guard (both surfaces). 1290 tests. 12-lens ultramode review: 3 confirmed fixed + 8 hardened; lesson in [insight 047].
- **PRIME-THE-PUMP DEV SEED ✓ COMPLETE** (2026-06-28; commit 9eef5571). `?seed=retired`/`?seed=date` → straight to a worded result + band. `src/ui/devSeeds.ts` (DEV-only, DCE-proven). Both seeds proven valid against the real engine validator.
- **THE BAND EVIDENCE — spine band ✓ COMPLETE + VERIFIED LIVE** (2026-06-28). `bandFan` crosses the worker wire; `answerView.spineBand` feeds the band + screens the $0-portfolio household; `bandAnnotations.ts` derives the household-clock x-axis; dead-cohort de-emphasis (`bandGeometry.ts` + the SVG mask).
- **U7 confidence statement ✓ COMPLETE** (2026-06-27; [insight 044]). **D2 (a) state-adaptive routing ✓** (61b9d9bf); **D2 (b) live-wiring + reveal-on-provisional ✓** (a6145d1d).

## Parked (Briggsy's cold-read)
- **DESIGN REVIEW — TONE COLD-READS OWED (2026-06-29; `docs/council-log.md` ⚑).** The design council ruled each item's SUBSTANCE already won (full 0..10 ladder axis, show the survivor reading, no axis cap) — only the **tone** read is yours: **CF3** two-pane lower-left whitespace (calm or dead? worst on the DATE route with the ladder closed); **CF4** the coincident "7 of 10" (verdict + survivor echo — the survivor reading STAYS, cutting it would be calm-but-wrong); **CF6 ✓ RESOLVED** (2026-06-29, Briggsy-approved) — shipped a y-axis "X of 10" odds scale (`OddsLadder.tsx` `LadderYAxis`, anchors at rungs 3/5/7, capped below the ceiling) so each dot's height reads off the axis; chose this over per-dot labels (full "X of 10" collides at ~44u dot spacing; bare numbers break the ceiling clamp + confuse with the year axis) and over a line/% (false precision + the headline frame). Live-verified; **CF5** the $20M squash tone (axis cap settled "don't build"); **CF2** band per-year $ range in the a11y tree (enhancement, not a blocker — the AT-reader parity item).
- **THE TWO-PANE (D2d) — FIRST-FRAME TONE COLD-READ ✓ CLEARED (2026-06-29; the council's binding ship gate, now closed).** Briggsy's N=1 verdict on `?seed=borderline` (laptop, wide): **"calm, not an ambush — ship the two-pane."** The two-pane is the **unconditional shipped path** — the single-column-richer fallback (`--text-hero` headline + band-only `--content-wide`) is OFF THE TABLE. The deferred `--text-hero` verdict bump was NOT requested (layout-alone dominance reads calm); the band-at-478 + bottom whitespace read as calm subordinate (accepted within the "calm" verdict). Remaining two-pane items are only the optional design-review nits (CF4 doubled "7 of 10") — available if his eye returns, not blocking.
- **TWO DISCOVERED v1 FACTS (not bugs, surfaced building the seeds):** (a) the **non-monotone ACA-cliff "doesn't hold" dip is UNREACHABLE in v1** — 16 candidates swept, all monotone: proportional drawdown + no conversions never straddles the 400%-FPL cliff non-monotonically (the cliff only bites if the crowned date is pre-65; younger households that get there can't crown a marginal date). Needs Act 3's conversion controls to manufacture, so D2c's never-seen-live dip stays parked. **Now a formal HARD pre-Act-3 gate** (council 2026-06-29): before the U10 Roth-conversion lever ships, drive a real non-monotone seed through the LIVE ladder render + N=1 cold-read — filed at the top of `docs/plans/3-controls.md` + `docs/council-log.md`. "Tests green" ≠ "the warning was seen to draw." (b) the **date route's $0 ruin tail is long-horizon-SQUASHED** (`?seed=dateborder`: ~$6M p90 plume flattens the tail) — the spine `?seed=borderline` is the clean ruin-tail render. This is the same `LONG-HORIZON BAND-SCALE SQUASH` parked item, now seen on the date route.
- **THE TWO-PANE HONESTY-FLOOR e2e (engineering fast-follow, NOT a cold-read):** the band-label-visible guard is currently the source-bind arithmetic test (`twoPaneHonestyFloor.test.ts`) + verified-live, because the dist-served Playwright harness can't reach the DEV-only `?seed=` two-pane (seeds + `?preview` are both DCE'd from prod). A real-browser regression assertion (`.band-droppable-label` VISIBLE at 1088px) needs a **dev-server Playwright project** (a second `webServer` against `pnpm dev`) — belt-and-suspenders, prescription filed here.
- **THE CURVE-DRAWER (D2c) — TONE COLD-READ OWED (2026-06-29):** built + verified live; needs Briggsy's eye on the real render (`?seed=date`, open the "How your odds shift by when you stop" drawer). **Three things to judge:** (1) **tone/layout** — the full 0–10 ladder leaves the bottom ~60% empty (council-mandated no-truncation, but sparse), and below-bar dots carry NO numeric odds (only the crown shows "9 of 10"; others read by position + a11y). Are per-dot odds labels / a leaner rung window wanted? (2) **the DIP not seen live** — the `?seed=date` curve is monotone (household both ≥64, no pre-65 ACA cliff), so the ACA-cliff "doesn't hold" rendering is unit-tested but UNSEEN. *Prescription if wanted: add a pre-65 near-cliff non-monotone dev seed to `src/ui/devSeeds.ts` so the dip cold-reads live.* (3) **the no-date plotted curve** — still DEFERRED behind the Hawk veto (a worded "how close" line ships instead); returns only behind a no-pickable-dot planted-fail + this cold-read (`docs/council-log.md` ⚑).
- **THE SCRUB READOUT'S DEAD-COHORT NOTE — WORDING (council-resolved 2026-06-28 — see `docs/council-log.md`):** `copy.bandReadoutThinNote` is now **"Too few couples to show a range."** (was "Few couples reach these years"). Council drafted + measured the line; Briggsy cold-read-picked it. It names the honest WHY (sample too thin to quote, *not* "you're broke"), is **width-verified** in real Source Sans 3 (164px of ~200px usable — the rejected 'live this long' consensus variant overflowed at 230px), and is now **catastrophe-gated** (`isMortalityKey`, `copyGuard.ts`). FAST-FOLLOW (council-decided, filed): add a durable e2e width assertion for `bandReadoutThinNote` ≤ usable `READOUT_W` in `e2e/design-tokens.spec.ts` — the one-off measure covered THIS string; the e2e guards future edits from clipping the honesty clause.
- **AT-READER PORTFOLIO-RANGE PARITY — ✓ SHIPPED + VERIFIED LIVE (C2, council 2026-06-29, 8/10, `docs/council-log.md`).** A screen-reader user now hears the band's portfolio **dollar range** as ONE static `.sr-only` sentence (the pointer scrub stays aria-hidden). Layering: pure `selectAtRangeColumn` + `AT_RANGE_COHORT_MIN` (`src/viz/bandGeometry.ts`, planted-fail tested) → `composeBandAtRange` (`src/ui/bandPanelChrome.ts`, single-sources the SAME resampled `tooltipRows` the scrub shows) → `atRangeSentence` prop rendered in `ConfidenceBandPanel` (string-free viz) → wired into BOTH routes (`ConfidenceStatement`/`FuckOffDate`, `useMemo([resolved])` so it re-renders with the band on a scale re-key — insight 047). Column = deepest INTERIOR lattice point with cohort ≥ `AT_RANGE_COHORT_MIN`=0.75 (never the survivor-dominated gate edge, never the horizon; withdraws to silence if none qualify). Survivor-neutral anchor (years-from-now), range-first/median-subordinate, **$0-spoken-as-depletion** in THREE shapes (normal range / ruin / total-depletion "gone"). Verified live (real Chromium): `?seed=retired` normal, `?seed=borderline`+`?seed=date` ruin, `?seed=failing` gone — exactly ONE node each. **Margin (0.75) + the three sentences ✓ APPROVED 2026-06-29** (tweakable later — change `AT_RANGE_COHORT_MIN` for the year, the `bandAtRange*` slots for the words).
- **THE LONG-HORIZON BAND-SCALE SQUASH (council-resolved 2026-06-28, conf 8/10 — see `docs/council-log.md`):** ~$20M p90 squashes the median on the date route's ~61y horizon. **CAP LEVER = DON'T BUILD.** The council reversed a unanimous open-to-cap after *measuring*: on the real ~61y route the median lifts only ~9px→19px in a 342px plot (intrinsic ~19:1 living-cohort range; log forbidden by the floor law), the cap silently rescales the shipped spine band 34–50% (both bands share `resolveBandData`, `answerView.ts:65/96`), its no-op guard is vacuous (`linearFan` bottoms at cohortFraction 0.6, never crosses the 0.5 fade — the insight-048 untested-gate trap), and it manufactures a *mirror* false belief (p75–p90 pancaked flat against the ceiling = false hard wall, worst for the color-blind reader). The axis-scale lever is the wrong tool. **Gate institutionalized: no band-scale change ships on an unmeasured readability claim.** ⚑ STILL YOURS: (a) tone cold-read on the real render — is the honest-as-is squash tolerable, or serve first-glance comprehension via a NON-axis aid (make the scrub readout primary/discoverable + a worded deep-tail note reusing `bandReadoutThinNote`)? (b) [VERIFIED 2026-06-28 — not a bug] the ~26% swing is the **expected** provisional(2k)→final(16k) path-count tier change (finer p90 tail); the UI re-draws not morphs (`FuckOffDate.tsx:152`); the 88.88M/65.42M figures are ungrounded (appear nowhere in code). **Latent U8 note:** `seed` is persisted but `tier` is NOT, so a completed date-route scenario could reload at the coarser provisional axis — handle at Save/reload.
- **Spine deriver near-Today tick skip (advisory, review 44f8b0c0):** `deriveDateBandAnnotations` drops a decade tick within the pad of Today; `deriveSpineBandAnnotations` does NOT, so an EARLY-retiree spine band (retired at 58/59) stacks an age-60 tick on Today. Cosmetic (the value is correct). Apply the same skip for consistency, or accept — Briggsy's eye.
- **The work-income field meaning (C3) — ✓ RESOLVED → Option B, SHIPPED + SIMPLIFIED 2026-06-30 (council 8/10 + Briggsy's cold-read, `docs/council-log.md`).** The field means FULL working-year income = pay PLUS working-year investment income; modeled R40 streams (pension/rental/annuity/alimony) stay OUT (KTD-9). The live `model.ts`⟷`copy.ts` contradiction is closed, the optimistic "usually the same as your pay" nudge killed, the "wages-only" comments disambiguated (engine-sense = exclude MODELED streams), the `?seed=dateborder` attribution landmine fixed, KTD-9 record reconciled, source-bound copy regression tests added.
  - **The intake does NOT re-ask pay (simplified 2026-06-30, Briggsy's cold-read).** It was first a two-input split (pay + investment); Briggsy's N=1 read flagged the duplicate pay question as confusing — and the engine can't model a time-varying salary anyway, nor do we ask the quit date (it's the swept output). So: the IRMAA override is `earnedIncomeReal + investment`, derived at `intakeMap.buildDateInput`; the step asks ONE field per working spouse — `workingYearInvestmentByPerson` (the genuinely-new, cardinal-sin-relevant fact — a blank can't become a silent $0, `intakeMap.test` guard). `workingYearWagesByPerson` was removed. The steady-pay simplification (a bonus/RSU spike near 65) is DISCLOSED on the step (conservative-or-disclose). This reversed the council's separate-pay-field call on Briggsy's comprehension signal; the look-back IRMAA-seed fields stay single (a filed return's MAGI is a reported total).
  - **(b) the new wording ✓ APPROVED (2026-06-30 — Briggsy: "wording is great").** C3 is fully closed. Tweak path if his eye returns: the `workIncomeIntro` / `workInvestmentLabel` / `workInvestmentHelp` / `workIncomeDisclosure` slots.

- **⚑ FUTURE — designer pass (Briggsy 2026-06-30, non-blocking): per-spouse help text repeats in `Paired` steps.** On two-person steps the per-field help paragraph renders identically under both "You" and "Your spouse" (the work-income "Investment income on top" help; and similarly the SS / salary steps). Not wrong, just visually repetitive. Have a designer weigh in on hoisting shared per-field help to a single step-level line vs the current per-field repeat — a `Paired`-component pattern decision (`src/intake/questions.tsx`), not C3-specific.

## Deferred (engineering, not cold-read)
- **THE RICHER MARKET DRAW — block-bootstrap + stochastic correlated inflation (`docs/decisions/market-model.md` §7).** The spine is i.i.d. parametric lognormal with a fixed deflator — optimistic on sequence/inflation *regime* risk, the one axis the cardinal rule swears off (the record names each caveat's error direction: §3 no autocorrelation, §4 the US-survivor rail that doesn't bite at the 85% on-track line, §5 inert inflation `stdDev`). **Build when any §7 trigger fires:** (1) the headline's *number* (not just direction) becomes load-bearing at the 85% line; (2) a non-survivor/global series is added to the historical anchor; (3) nominal/inflation display ships (the inert `methodology.ts:36` `stdDev` goes live). **Prescription:** (a) add a block-bootstrap-of-the-real-series and/or regime-switching draw mode in `src/engine/simulate.ts`/`rng.ts` behind the existing `(params, seed)` purity contract — **the CRN dimension-only draw order + single-shared-draw invariant DO NOT move** (`docs/architecture.md`); (b) add stochastic real-return-correlated inflation as a THIRD market factor in `buildDraws` (a new normal per path-year), wired through the deflator the overlay + SS COLA read; (c) **externally-derived fixtures for both** (DND 012 — block-bootstrap success rates + inflation-correlation moments derived by an independent path, committed, proven by hide-the-artifact clean-clone AJS 008; never the engine validating itself); (d) re-scope the MC-below-historical assertion (`simulate.test.ts:66`) to state explicitly which percentile band it constrains, so the over-funded-region rail can't be mistaken for a decision-boundary guarantee. Until then the honest contract is the as-built model in the record.
- **Scrub-interaction e2e (advisory, review 99dc2508):** the scrubber's *render-time* honesty gate is now unit-guarded (insight 048), but the POINTER glue — variant gating (drawer ignores touch / enlarged drag-scrubs), the 047 remount-clears-`idx`, the today-column (idx 0) surviving the `!== null` checks — is unreachable in jsdom (`getScreenCTM` returns null) so it has no regression guard (verified live, not pinned). If wanted: a Playwright e2e (the `scripts/serve-dist` + `csp.spec.ts` harness exists) that scrubs a thinned year (note replaces dollars), a full year (dollars shown), and asserts scrub state resets on a scale-change remount without closing an open modal. Logic is sound + seen-working; this is belt-and-suspenders.
- **DateBand → move `band` INTO the crowned track variants (U9):** today `band?` hangs at the run-level 'dates' outcome and carries its own `offsetYears` (self-describing — done). Once U9 splits floor/lifestyle into two offsets, move `band` onto the crowned `DateTrackOutcome` variant so present-iff-crowned is unrepresentable-when-violated and each band ties to its own track. (Advisory from review 44f8b0c0.)

## Landmines (carry forward)
- **The `?preview` harness does NOT exercise the live deriver** — it renders fixtures with hardcoded annotations. The decade-age ticks + the date "work stops" marker only show on a **real drive / a dev seed** (`?seed=…`), never in `?preview`.
- **The SURVIVOR READOUT is LIVE ✓** (2026-06-28 — was dormant; wired via the `bandFan` mirror). `SurvivorReadout` now mounts below the spine band on the already-retired route; `survivorConditioned` + `survivorReading` cross the wire presence-keyed. Spine-only by design — the date route renders a timing claim, not a joint-solvency verdict (the survivor obligation re-attaches if D2c surfaces a post-stop confidence verdict).
- ONE shared market draw/year (CRN); a fan-on run is byte-identical to fan-off on the goldens (now proven for the DATE candidate config too — `dateSearch.test.ts` reduce-to-spine). Stateless Box-Muller. Externally-derived fixtures (DND 012). The band renderer + producer are DONE — feed them, do not redesign.

## Standing cadences
- `/ultramode-code-review` at every unit boundary; the four-skill UI loadout before ANY user-facing surface (CLAUDE.md "UI design skills").
- `/brief` (read `docs/insights/`) before a unit; `/distill` after.
- **Delegated build:** **native Agent Teams** for live-steer **eye-oracle** units; the **Workflow** tool for fire-and-forget **test-oracle** fan-out. Durable laws in memory `feedback-delegated-build-laws`.

## Verify-owed
- The OOP-medical figures (`src/intake/referenceData.ts` → `OOP_MEDICAL_TYPICAL_HOUSEHOLD`) are grounded-search-sourced, **not** primary-table-verified (`directionalUntilPinned`). BLS bot-walls `curl` — ask Briggsy to pull the CE "Age of reference person" table to pin them cell-by-cell.
