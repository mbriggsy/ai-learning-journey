# The Back Nine — TODO

> Actionable items only. Decisions live in `docs/brainstorms/the-back-nine-requirements.md`; cross-session learnings live in memory + `/distill` insights. No session history here.

## Current State
Requirements locked + 7-persona reviewed. **Foundation research verified** 2026-06-03 (`docs/research/foundation-findings-2026-06-03.md`). **MVP plan written** 2026-06-03 → `docs/plans/mvp-confidence-spine/` (roadmap + 3 phase docs, burned-style frontmatter w/ `deepened`/`doc-reviewed`/`coded`/`code-reviewed` lifecycle stamps, all empty). 9 units across 3 phases. **Phase 1 deepened + doc-reviewed** 2026-06-03 (deepening: 8-agent fan-out; doc-review: 5 personas — coherence/feasibility/security/scope/adversarial — both stamps set; roadmap Output Structure synced — added `historical.ts`, annotated `roth.ts` Phase 3). **Phase 2 deepened + doc-reviewed** 2026-06-03 (broad 8-agent deepening → 47 real findings integrated; 4-lens doc-review → 31 findings, all applied incl. 2 blockers). **Structure changed: Phase 2 grew to Units 3–6** (new Unit 6 = first-Save flow UI) ⇒ **Phase 3 renumbered to Units 7–9**; `TwoFutures` component moved to Phase-3 Unit 8 with its `roth.ts` producer. Phase 3 at depth-tier only (no stamps). New decisions locked: **engine = TypeScript** (WASM = fast-follow); **unlock = passphrase each session, memory-only key, no username**; **KDF = PBKDF2-600k**; magic-moment-first onboarding; shared household credential; married-couple precondition; **+ Phase-2 calls (Briggsy 2026-06-03): progressive intake, earned-income bridge regime (re-opened Phase 1), collect both spouses' sex, display denominator pinned at 10, account-buckets deferred to Phase-3 lever-open.** **Phase 3 deepened + doc-reviewed 2026-06-04** (full broad workflow: 7-agent deepening → 59 findings adjudicated in main loop after the verifier stage hit StructuredOutput brittleness; 7-persona doc-review → ~24 findings auto-fixed) — **ALL 3 phases now done on both gates** (roadmap lifecycle stamps set 2026-06-04). The Phase-3 deepening **re-opened deepened Phase 1** with a **tax-and-accounts overlay** (Briggsy's call: most-complete-picture, **SS-tax IN**) and added **`§Strand 5`** (verified tax reference) to the findings doc; tracked amendments cascaded across roadmap/Phase-1/Phase-2. No code yet.

## Next Steps (priority order)
1. **Cross-phase seam sweep** (Briggsy's call 2026-06-04 — explicitly deferred to "next session"). Phase 3's deepening **re-opened Phase 1** (tax-and-accounts overlay) and amended Phase 2, so this is the final consistency roll-up over all **5** docs (roadmap + 3 phases + `foundation-findings`/§Strand 5) before the set is locked for `ce:work`. Method = grep + targeted reads (light — most cascades were just applied 2026-06-04; the goal is to catch any spot I missed, NOT re-deepen). **Re-verify each seam, focusing on the NEW Phase-3 cascades:**
   - **Death-order** = a CONDITIONAL sub-population FILTER (same draws, no re-draw/re-index) consistent across roadmap/P1/P2/P3; the **Phase-1 longevity per-spouse-death-year-lane requirement** (retain survivor identity per path) is stated wherever death-order is referenced. (I fixed one drift here 2026-06-04 — grep for any surviving "re-index"/"survivor-identity selection".)
   - **Tax overlay** = `roth.ts` orchestrates over a `simulate.ts` overlay (NOT a parallel engine); the **OFF-by-default reduce-to-spine invariant is stated exhaustively** (buckets-collapsed AND conversion=0 AND tax-off AND **RMD-inert**) everywhere it appears; the **single-shared-market-draw-per-year** rule; **no tax number inlined** anywhere (all point to §Strand 5).
   - **schemaVersion ladder** = ONE v2 (buckets + vintage stamps), **written by Unit 8 / read by Unit 9**, Phase-1 Unit 2 migration targets it; the **model-only re-encrypt entry point** (same DK, fresh IV, wraps untouched) is present in Unit 2.
   - **Recovery new-passphrase = mandatory blocking gate** in BOTH Unit 9 and Phase-1 Unit 2.
   - **Staleness clocks** map per-surface (tax→lever only; wall/app-default→spine; balance-drift→both, per-bucket for v2); **SS-COLA clock dropped** everywhere; OBBBA = fixture provenance, not a clock.
   - **Sticky-rounding** = two-margin split (band-edge for headline+state; rounding-step for dollar) + notify-once + capture-at-resolve→verdict + session-only/not-persisted — consistent across Phase-3 Unit 7 ↔ Phase-2 contract #1d ↔ Phase-1 contract #1.
   - **copyGuard** = `src/viz` string-free; literal "RMD age 73" removed from the allowlist (P2 contract #4 + Unit 5); four Roth surfaces routed; new slots added.
   - **Birth-year** derived in Phase-2 Unit 3 (intake year − age) ↔ RMD-age-by-birth-year in Phase-3 Unit 8 + §Strand 5.
   - Confirm the tracked **requirements amendments** (R8 "tightens"→"sharpens"; R10 "~3 years"=example; "~8 inputs"→screen/time; "all three outcomes"→"six states + survivor") are tracked consistently (req-doc stays locked).
   Then stamp the roadmap roll-up (already stamped `deepened`/`doc-reviewed` 2026-06-04 — adjust only if the sweep finds a real gap).
2. **Then `/ce:work` Phase 1** (foundation-first) — run `/brief` first (the `enforce-brief-before-work.sh` hook gates `ce:work` only, not `ce:plan`; verified 2026-06-03).

## Open Items
- [ ] **Back-annotate the requirements doc?** The plan *extended* the requirements: survivor-phase modeling (SS step-down, ~75% spending ratio, two-regime horizon, death-order); the 6-state outcome matrix; married precondition; historical-backtest optional→required; **+ Phase-2 extensions:** progressive intake architecture, earned-income bridge, collect-both-sexes, display denominator 10, the **N=1 cold-read scope "all three outcomes" → "all six states + survivor readout"**, and the **"~8 inputs" success-criterion → a screen-count/time metric** (the deepened required-fact set is ~12–14). Decide whether to fold these back into `the-back-nine-requirements.md` so spec and plan don't drift. (Briggsy to decide.)
- [ ] **Brief-before-planning hook?** Optionally add `ce:plan`/`ce:brainstorm` to `enforce-brief-before-work.sh`'s block-list (currently gates `ce:work` only). Likely redundant since `ce:plan` runs learnings-research natively. (Briggsy to decide.)
- [ ] **Exit gates** (don't block deepening; block calling a fixture "golden" at `ce:work`): (1) confirm SSA cohort curves vs the real `table4c7.html` (bot-blocks fetch → manual + committed snapshot) — **note: the Roth survivor-cliff headline now rests on these, so this gate is load-bearing for the differentiator, not just the spine**; (2) pin Bengen's Ibbotson intermediate-government dataset; (3) pin a true long-term-CORPORATE series for Trinity (cFIREsim is Shiller=government); (4) **NEW — pin the `§Strand 5` tax numbers to the IRS primaries** (the 2026 Rev. Proc. for brackets/standard-deduction; Pub. 501 for filing-status/QSS; Pub. 590-B for the Uniform Lifetime Table; Pub. 915 for SS-taxation) — they are grounded-verified, not primary-pinned. Until cleared, all these fixtures are directional, not golden.
- [ ] **Passphrase-strength floor** — decide a minimum-entropy/strength gate at passphrase creation (Phase 2 onboarding); PBKDF2-600k is the only brute-force defense, so a weak passphrase narrows it.
- [ ] Attorney review of exact Roth-lever copy + the no-securities/no-asset-location boundary before any Terms/marketing goes load-bearing.

## Landmines
- **Roth lever = calculator, NEVER a verdict.** Under Reg BI, a *recommendation* of an account type (Traditional-vs-Roth, rollovers) is treated as an "investment strategy involving securities"; a user-driven calculator isn't. The no-verdict + categorical-trigger + no-securities/no-asset-location guardrail is load-bearing, not stylistic. (findings §Strand 3)
- **Non-extractable key ≠ at-rest security boundary.** `extractable:false` stops *script* exfiltration, not disk access. The real protection is the encrypted blob + KDF. Don't let copy imply otherwise (gates R15). (findings §Strand 2)
- **Jazz is alpha** — don't build the v1 persistence/crypto core on it (2.0 alpha, new API). Single-device MVP likely needs no sync engine at all.
- **Engine validation numbers live in findings §Strand 4 ONLY** (avoid stat-drift). Watch the corrected ones: Trinity 100%-bond/4%/30yr = **~70%** (NOT 20–35%); Bengen 4.15% SAFEMAX = **1966** cohort (NOT 1968); MC runs *more pessimistic* than historical — assert a band, never equality. **cFIREsim can't reproduce Trinity exactly** (Shiller carries GOVERNMENT bonds; Trinity uses CORPORATE) → mode-A is **self-consistent (vs our own committed fixture) + directional** vs published numbers, NOT published-exact, until the corporate series is pinned.
- **`deep-research` workflow broken** — 3× StructuredOutput crashes; "refuted" verdicts are verifier false-negatives. Self-serve via `gemini-grounding` + `curl` (the 2026-06-03 foundation workflow did exactly this — text returns, no StructuredOutput).
- **Web served-JS caveat** — "we can't see your money" covers data at rest / in transit, NOT a malicious-code-update threat model. Disclose, don't overclaim (R15: provable before spoken).
- **No competitive lens** — product is the bar; don't reintroduce moat/defensibility framing.
- **DK key-hierarchy is LOCKED** (Briggsy's call 2026-06-03 over the scope-review's "simplify to flat"). Reason = **one data copy so recovery can never go stale** (flat = two copies that can drift → survivor restores a stale plan). Don't re-recommend flat. DK stays **non-exportable** — "wrap" via `encrypt()` of raw bytes, NEVER `wrapKey()` (forces extractable → XSS can export it).
- **In-memory model is the real XSS surface** — at-rest crypto guards the disk, NOT the cleartext model in JS memory while unlocked. A **strict CSP** (Unit 0, `vercel.json`) is load-bearing for "we can't see your money," separate from the disclosed served-JS caveat.
- **CRN draw schedule must be dimension-only** — pre-allocate the normals matrix to MAX cohort horizon, index by absolute year, stateless Box-Muller (no cached spare). Else the survivor two-regime boundary (moves with sampled longevity) desyncs the Roth with/without arms and the "buys you ~N years" delta jitters.
- **Earned-income bridge: two correctness traps** (re-opened deepened Phase 1, 2026-06-03). (1) Income window truncates at `min(retirementYear, sampledDeathYear)` **per path** — never credit a dead earner (over-inflates exactly the survivor case). (2) The golden-case reduction invariant is **earnedIncome = 0 for both spouses** (pure-retiree case), **NOT** "equal retirement years" (equal-but-future years still net income pre-retirement → not byte-identical to Trinity/Bengen). Bridge = a pre-withdrawal adjustment to the single per-year withdrawal term (`netWithdrawal = max(0, spending − income)`), not a parallel engine.
- **Hysteresis seam is named in Phase 2 but its fields/rule are seated in Phase 3 Unit 7** — Phase-2 intake numbers move FREELY (sharpening must be visible); sticky rounding only activates on editing an *already-complete* answer (Phase 3). Don't seat `lastDisplayed*` in Phase 2. **Two-margin split (P3 deepening): headline + copy-state gate on the band-edge margin; the dollar figure on the independent display-rounding-step margin — conflating them = the "one sticky, one flickers" failure. Write all slices before one notify (`burned/017`); baseline captured at resolve→verdict; session-only, NOT persisted.**
- **Roth lever needs a TAX-AND-ACCOUNTS OVERLAY the spine engine lacks (P3 deepening, re-opens Phase 1).** The spine is tax-blind (`withdrawal==spending`, one pool) → a tax-blind conversion delta is **sign-inverted** (every conversion looks worse). The overlay is a per-year deterministic cash-term transform (grosses-up vs the bridge's net-down), CRN-safe (zero draws), **OFF-by-default reduces byte-identically to the tax-free spine** — exhaustive condition **buckets-collapsed AND conversion=0 AND tax-off AND RMD-inert** ("taxes off" alone does NOT silence RMDs). `roth.ts` orchestrates the arms over the overlay; it does NOT re-implement decumulation. Tax numbers live in **`§Strand 5`** only.
- **ALL account buckets share ONE market draw/year.** Per-bucket draws break CRN (changes draws-per-year) AND = asset-location modeling (the #1 IAA tripwire). Buckets differ only in tax treatment. Structural guard, not copy.
- **Death-order = a CONDITIONAL sub-population FILTER, NOT "swap which spouse is survivor."** Filters the existing paths (same seed/draws, no re-draw/re-index). Requires **Phase-1 longevity to sample per-path, per-spouse death years + retain survivor identity** — the closed-form `pₓ+pᵧ−pₓ·pᵧ` mixture alone does NOT. Thin opposite-order sub-population (big age/sex gap) → widen the hedge, don't show a noisy "X of 10".
- **RMD age is birth-year-derived (72 / 73 / 75-for-born-1960+, effective 2033), NOT a flat 73.** Wrong for a large slice of the 2026 target couple. Phase-2 derives birth year from intake (year − age). The literal "73" is OUT of the copyGuard allowlist (engine-owned slot).
- **SS provisional-income tax is a per-year FIXED-POINT** (tax→taxable-SS→provisional→gross-up→tax; iterate to convergence, zero draws). Thresholds **$32k/$44k MFJ are frozen since 1983/93, NOT inflation-indexed → no staleness clock** (constants, not a dated fixture).
- **v1→v2 vault upgrade = a MODEL-ONLY re-encrypt** (same DK, fresh IV, **wraps UNTOUCHED** — NOT a credential re-wrap; re-minting wraps is for passphrase/recovery ops only). Needs a new Phase-1 Unit-2 atomic entry point. Never on lever-open (no vault mutation as a side effect of exploring) — only on explicit Save. The unsaved-in-memory path persists v2 directly (no v1-on-disk).
- **In-place recovery's set-new-passphrase step is a MANDATORY blocking gate** — no write path until the `passphraseWrap` is re-minted, else the survivor saves data while only the recovery wrap is valid → stranded in recovery-phrase-only access (lose the phrase = data gone).
- **`§Strand 5` is the NEW home for all tax reference numbers** (2026 MFJ brackets, $32,200 std-ded, OBBBA-made-TCJA-permanent, RMD schedule, senior-bonus-sunset-2028, SS thresholds). Don't inline tax numbers in the plan — point to §Strand 5 (avoid stat-drift). Falsifiable IN/OUT line: a tax effect is IN iff it moves the survivor's ordinary-income bracket the conversion stacks into (IRMAA/ACA/NIIT/state = OUT, disclosed).
- **Workflow `agent({schema})` on heavy CE review agents is brittle** (the P3 deepening verifier stage crashed ~110 sub-agents on StructuredOutput — generalizes the `deep-research` landmine above). Return free-text JSON (document-review personas did, cleanly) or return RAW findings and adjudicate in the main loop; never let a crashed verifier read as a "refuted" finding.
