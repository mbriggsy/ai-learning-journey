# The Back Nine — TODO

> Actionable items only. Live plan: `docs/plans/back-nine-mvp/roadmap.md` (4-phase spine). Product: `docs/plans/direction-reset-2026-06-04.md` (north-star) + `docs/brainstorms/the-back-nine-requirements.md` (v2). Project insights: `docs/insights/`. **No session history here — git log has it.** The "DONE" pointers below are one line per unit; the *how* lives in git log + the insights.

## ⚑ NEXT SESSION — START HERE

**State:** clean tree · gates green at HEAD (typecheck · **921 vitest (44 files)** · lint · verify:aca · bundle **197.3 KiB** · verify:csp unchanged-by-construction). **SS benefit sub-engine — PURE CORE + INTEGRATION SEAM BOTH REVIEWED + FIXED.** Pure core `cae139d0`→`7497ad6e`; pure-core review `15f4f63a` (insight 039); integration `2ec80071` (the `pia`/`birthYear` swap + `PersonInputsLegacy` fork + the seam + the 84-site migration); **integration-seam review DONE** (this session — 11-lens + 5-angle adversary panel, 0 false alarms): found + fixed a **P1 cardinal-sin optimistic bug** — the §202 survivor base credited the deceased's PLANNED claim-age DRCs even on paths where death preceded the claim (a plan-70 claimer dying at 68 got 1.24×PIA, not the 1.08× they lived to earn), overstating the early-widowhood floor. Fix = `realizedClaimAgeAtDeath` (seam realizes the claim age at death; pure core untouched) + 6 hand-derived goldens (DND/012) closing the death-before-claim + nonzero-excess-gating + own-wins coverage holes; **insight 040**, sourced 20 CFR §404.313 / RS 00615.301. Reduce-to-spine + CRN still byte-identical. **The SS engine is imported by the app (worker-side — entry bundle unchanged).** **NEXT = SS intake UI polish** (item 1 below). **P2·U5/D1 intake BUILT + reviewed** · **U6 colorblind-safe VIZ FOUNDATION built + reviewed** (`63c7964f`) · **N=1 laptop cold-read still IN PROGRESS** (`fbb03738`). Dev: `pnpm dev`, the app IS the intake.

**⚑ DECISIONS this session (Briggsy ATC) — they reshape the queue:**
1. **Social Security → compute spousal/survivor AUTOMATICALLY — ✅ DONE** (`2ec80071`). The PIA+claim-age input swap, Method-C spousal excess (NOT max()), and §202 survivor are all live. The only remaining piece is the **intake UI polish** (deferred, see NEXT item 2). Note: the build CORRECTED the Briggsy-stated "max(own, spousal)" rule to POMS Method C (own-in-full + reduced excess — the stated rule was calm-but-wrong; verified vs POMS, plan §4).
2. **Single-user → NEEDED, deferred.** Couple-only for now (the honest front door). Implement as a real **"just me" mode** (people-of-ONE — `model.ts` already calls a single person "the degenerate case"; solo mortality, single filing throughout). **LANDMINE: NOT "zeros for the spouse"** — a phantom zero-spouse still carries a mortality curve → warps joint-survivor + the MFJ→single death transition = calm-but-wrong.
3. **Two-pane (laptop) composition** — promote the live answer to a persistent panel BESIDE the questions on wide screens (stacks on phone): uses the real estate, makes "the answer takes shape as you go" literal, and is the home for the held U6 band / D2 surface. **Mockup for Briggsy's eye FIRST** — don't commit the layout blind. (The wrapping he flagged was correct *typography*, ~65ch measure — the gap was composition, not line length.)
4. **Save/load (U8) BROUGHT FORWARD** — stop re-entering each session. Vault (U4) is built; this is the Save ceremony + load-on-return + define `ScenarioV3` + its codec arm + migration IN that change.

**NEXT, in priority order:**
1. **SS intake UI pass — the DEFERRED polish** (four-skill UI loadout STANDING — see below). The plumbing is DONE + correct (`2ec80071`: field binds `pia`, `buildPeople` populates `pia`+`birthYear`, missing-fact keys on `pia`, label references the FRA-67 figure). REMAINING (prescriptions):
   - **`questions.tsx:236-242`** — the SS `CurrencyField` asks YEARLY today; make it the **monthly-entry / store-×12** widget (mirror `spendStep`'s entry-period discipline; the statement shows monthly). Label `copy.ssAmountLabel` → "monthly benefit at full retirement age (67)".
   - **`copy.ts:65-66`** (`ssAmountLabel`/`ssAmountHelp`) — add the **mySSA $0-future-earnings stop-early** instruction (the default statement figure assumes continued earnings → overstates for an early-stopper; the help must route to the mySSA Retirement Estimator, set future earnings to $0, read the age-67 figure).
   - **`sanity.ts`** — the **net-new PIA-ceiling sanity rule** (new rule id + CopyKey + a fire/boundary-pass test; compare in the draft's stored ANNUAL unit; source the ceiling from `@engine/constants` — do NOT inline a dated figure).
2. **Finish the N=1 laptop cold-read** (still in progress — the P2 tone exit bar; needs Briggsy's eye, laptop-primary).
3. **Save/load — U8 brought forward** (decision 4). NOW UNBLOCKED (the `pia` shape is settled — `ScenarioV3.people` is `PersonInputsV3 extends PersonInputs`, persists `pia`+`birthYear` for free). U8's codec arm adds a `checkPersonV3` (`needFinite(v,'pia')` + `needInteger(v,'birthYear')`) — a SEPARATE validator, NEVER mutate `checkPerson` (it validates the frozen legacy shape; a forward note is in `scenarioCodec.ts`).
4. **Two-pane mockup** (decision 3) → the **U6 RENDER** in the right panel → **U7** + **D2** ride the same answer-surface work.
5. **Single-user "just me" mode** (decision 2) — deferred named expansion. NOTE: `householdBenefits` already fail-louds on >2 people and serves people-of-one (own only, no spousal); the seam's survivor branch already guards the people-of-one all-dead case.

**U6 RENDER detail (held; rides the two-pane) —** `palette.ts` + `scale.ts` + the CVD probe are DONE (`63c7964f`). Remaining (four-skill UI loadout FIRST): `src/ui/verdictSignal.tsx` (word + magnitude + shape; redundant color only) · the `ConfidenceBand` SVG (the strip graduates from words to the band) · the two-series render · the verdict-icon silhouettes + their C(6,2) probe.
- **LANDMINE (measured):** the on-band line must be **ink (`--ink`, CVD ≥0.16) or vermilion (≥0.19) — NEVER series-blue** (collapses to 0.024 over the blue band under deuteranopia). Add the line-over-band composite arm to `colorblind.test.tsx` when the line lands (scoped there).
- **CHECK:** confirm the engine emits the **per-year percentile envelope** the fan needs (`confidence.ts` has `terminalValuesReal` terminal-only + a `percentile()` helper; a per-year trajectory fan may be a D2/U6 engine add — verify, don't assume).
- **CSP-safe:** data-viz color rides **SVG presentation attributes** (`fill`/`stroke`), NOT inline `style`; `verify:csp` proves it when the band ships.

**The four-skill UI loadout is STANDING for EVERY UI session** (CLAUDE.md "UI design skills"): `compound-engineering:frontend-design` (the **Every** variant — NEVER the official tone-hazard one) + `emil-design-eng` + project `back-nine-design` + project `web-design-guidelines`. Precedence on conflict: back-nine-design > emil > frontend-design > web-design-guidelines.

## Build state — DONE (git log + docs/insights/ have the how)

- **P1 — the engine: COMPLETE, reviewed, pinned (CLOSED 2026-06-11).** U0 (scaffold/PWA/engine-purity lint/constants/CSP) · U1 (Monte Carlo core) · U2 (tax+accounts overlay) · U3 M1–M6 (healthcare: ACA pre-65 fixed-point, IRMAA 2yr-lag, HSA 4th bucket, cross-overlay) · U4 (encrypted local store + recovery/export/restore) · M6 (cross-overlay integration + the solver output contract) · Tracks A/B1/C1/C2/C3 (doc reconciliation + the accumulation fold + the date-search). **P1-EXIT pin pass** (`53943a2e`): 25 constants + 161 tickerBlend rows verified vs IRS/CMS/HHS/eCFR/SEC primaries; mortality = SSA TR2024 cohorts (male 1969 / female 1972; S(90|65) 0.3209/0.4348, couple 0.6162; `SURVIVAL_MAX_AGE` 119); bond gates = Damodaran proxy arms beside the Shiller spine. Insights 032–034.
- **P2·U5/D1 — the intake: BUILT + REVIEWED.** Built `a300acf7`→`9982fdad`; review fold `c4acd5bf`. The 10-lens review → 4-refuter verify → 11 fixes + 2 refuted (one was the orchestrator's own — insight 037).
- **SS benefit sub-engine — PURE CORE + INTEGRATION BUILT + BOTH REVIEWED.** Constants + pure core `cae139d0`→`7497ad6e`; pure-core review `15f4f63a` (claim-age domain guard, insight 039); integration `2ec80071` (pia/birthYear swap + `PersonInputsLegacy` fork + the seam: own + Method-C excess + §202 survivor + the 84-site migration; reduce-to-spine byte-identical); **integration-seam review + fix** (this session) — the §202 survivor base now realizes the deceased's claim age AT DEATH (`realizedClaimAgeAtDeath`), closing an optimistic DRC over-credit on death-before-claim paths (insight 040); 6 new seam goldens. **Intake UI polish deferred** (NEXT item 1).

## Load-bearing contracts (do NOT regress — all tested)

**Engine (P1):**
- **Single shared market draw / CRN** — all buckets share ONE market-return draw per year; per-bucket draws forbidden (breaks CRN + re-enables asset-location). Draw schedule = pure fn of path/horizon dims only. Stateless Box-Muller (no cached spare).
- **Reduce-to-spine byte-identity** — every overlay (tax/healthcare/earned-income/accumulation) reduces byte-identically (same seed) to the Trinity/Bengen decumulation when OFF. Golden cases never perturbed. Externally-derived fixtures (DND 012).
- **`validateParams` is the R19 gate** — the worker boundary is untyped (structured clone), so every incomputable input is rejected THERE. **Finiteness FIRST** — a NaN passes every relational/`??` guard (insights 008/010). `ENGINE_MAX_*` domain bounds (dollars ≤ 1e12 · moments ≤ 1.0 · horizon ≤ 120) close the float-overflow tail (insight 028). Non-integer seed rejected as indeterminate.
- **U2:** gross-up contraction k≈0.74 / `GROSS_UP_MAX_PASSES=128` (insights 006/007); every tax figure read from `@engine/constants` (one canonical year-keyed table — burned/057,061,063); per-person ledger sums to `buckets.pretax`.
- **U3:** ACA = an OUTER fixed point — bisection on a PIECEWISE-monotone residual; **segment the bracket at the 133%-FPL applicable-% discontinuity** and pick the cheapest feasible root (insight 013). IRMAA = a 2yr-LAGGED feed-forward (no fixed point), a constant addend to `net[t]` — record `irmaaMagi` (TAXABLE ss = AGI), NOT `acaMagi` (FULL ss); the seed is fail-loud-REQUIRED near-65; the surcharge is NEVER a solver search variable. Feed the MAGIs the SURFACED FLOORED locals from `solveGrossWithdrawal` — never recompute MAGI off a raw-gain ledger (a down-market negative gain → understated MAGI → sign-inversion). If a muni bucket is ever added, muni interest enters BOTH MAGIs AND §86 provisional income in ONE change.
- **U3·M5 (HSA):** hsa is MEDICAL-EARMARKED (type-level + general-depletion guard) — never a general drawdown source; the qualified cap NEVER includes the ACA premium and keys to the OWNER's age (rollover-aware); `oopMedical` CAP-ONLY; hsaSpend is a year-known CONSTANT, never an ACA-bisection search var; `hsaLive` = initial balance > 0 OR any year's inflow > 0 (insight 020); post-65 non-qualified spend NOT routed (a P4 last-resort draw adds the income inclusion in the same change).
- **U4 (vault):** the write gate is ONE predicate (derived DK ∧ current-or-just-minted passphraseWrap ∧ single-active-writer ∧ ¬locking); recovery-unlocked is unwritable until `setNewPassphrase` re-mints. Lock authority is SYNCHRONOUS — `lock()` bumps the epoch before any await, gen-checks after every await (insight 030); `firstSave` returns the phrase whenever the vault write committed (insight 031). "Wrapping" = AES-GCM `encrypt()` of raw DK bytes — NEVER `wrapKey()`; every key handle `extractable:false`. The floor (zxcvbn≥3 ∧ ≥12 code points) gates passphrase-SET only, never unlock. Every multi-record mutation is ONE IndexedDB tx; format/schemaVersion gates run BEFORE decrypt — a newer payload is `newer-version got:N`, NEVER "damaged". The codec proves SHAPE, R19 proves SEMANTICS (never duplicate a domain rule). Honest lock = reference-drop, never claim zeroization. Persisted "never-depleted" = a numeric sentinel (`-1`), never `Infinity`/`NaN` (DND 009).
- **M6 (solver output):** `totalTaxPaidReal` accrues AFTER the depletion check, ACCOUNTING ONLY (never a search var); `Distribution.taxAware` is PRESENCE-keyed on the overlay + every entry finite; the typed `SimInfeasible` sentinel is reachable ONLY through the overflow class; the faithful-collection fixture pins all six surfaces against direct per-path runs WITH a live hsa (insight 029 — an equality on a structurally-zero surface discriminates nothing).
- **C2 (accumulation):** byte-identity is PRESENCE-keyed (construct absent ⇒ byte-identical); the §2c scale reads `growthOnlyTotal`, never `totalValue(state)`; contributions credited END-OF-YEAR at face, a depleted year forfeits them; the §7 working-year clamp is death-AWARE and `ss` is never clamped; every `YearContribution` channel is per-person (insight 024); a depleted/$0-start with the construct present is rejected.

**D1 (the intake — tested; the 8 originals + the review fold):**
1. **Copy fence** (ESLint): no inline user-facing strings in src/ui+src/intake (incl. aria-label/placeholder/alt/title) — all route through `src/ui/copy.ts`. PROVEN non-vacuous by a planted-fail test (review T2).
2. **Render anchor + `validateParams` coupling:** intakeMap's builders return null EXACTLY while `missingRequiredFacts()` non-empty; no-missing ⟹ `validateParams` ACCEPTS (spine + every date candidate). Tested as a COUPLING, incl. the review's three new guards (stale-retired-contribution, $0-date-portfolio, present-birthYear/absent-currentAge).
3. **No-write-until-Save (HARD):** nothing reaches IndexedDB during intake (tested vs the REAL db.ts + a planted-write control).
4. **Epoch / stale-discard:** the seed mints ONCE; `setLatestEpoch(epoch)` is called BEFORE `runDateSearch(...,epoch)` AND is now **route-independent** (a spine recompute cancels an in-flight sweep too — review AT2); promise-identity discard; a superseded `cancelled` outcome **HOLDs**, never commits (review AC1 — committing blanks the strip).
5. **Spend period force-confirm:** an unconfirmed-default figure ≥ $8k (month-view) forces an explicit $/month-vs-$/year answer — **no upper bound** (a figure above the old $50k ceiling left under the default was the 12× misentry; review AB1). The advance() gate reads `model.getSnapshot().draft` (live store), not the render closure (insight 036). An explicit unit tap disarms it (correct-by-design).
6. **The magic moment crowns the FINAL tier:** the terminal advance delegates SOLELY to `onComplete` (the 16k final-tier recompute); a trailing provisional recompute would supersede it via epoch and render the uncrowned 2k tier (review R3).
7. **Answer strip:** reserves its box (`min-block-size` — insight 035; a live region above the controls is a layout-shift generator); carries a STABLE a11y label + `aria-live=polite` (review CLUSTER E).
8. **ACA quote escalator:** per-member premium split at today's ages, own-age escalation, staggered-exit summing; premium+slcsp STEPPED, oop NEVER. The entered premium is the **household's combined** marketplace figure (the escalator splits it — copy says so, review AB3). Un-fetchable at runtime (R36/CSP) — a named required input, never synthesized.
9. **Money input:** `type=text`+`inputmode` (never `type=number`); `parseMoney` rejects misgrouped commas / exponent / hex magnitude traps (review AB2); errors forgive on re-edit, not next blur (review DA1); Remove is two-tap (review DA4). The store→intake **layer inversion is now ESLint-enforced** (review A1 — CLAUDE.md claimed it; it wasn't).
10. **Entry budget** holds via the lazy intake chunk — re-run `verify:bundle` after ANY new import into App/ColdStart.

## Open Items (decide at the named unit)

- **U8 — ScenarioV3 (the obligation):** `ScenarioV3` is **already DEFINED** (`model.ts:846-870` + the `SCENARIO_V3_FIELDS` exhaustiveness guard) — health streams + accumulation fields are in the shape. Remaining = **the codec v3 arm** (`scenarioCodec.ts` imports only V2 today) + **`AnyScenario` union membership** (`model.ts:904` deliberately excludes V3) + **the Save ceremony** (`flow.tsx:143` `onComplete` is wired but `IntakeApp` passes no callback → nothing saves) + **load-on-return** (`App.tsx`/`IntakeApp.tsx` cold-start a fresh `memoryModel` every session). `firstSave()`/`save()` infra exists from U4. The codec already refuses `version > 2` calmly (`got:N`); the placeholder test (scenarioCodec.test.ts:92) already asserts the v3-refusal and **STAYS GREEN** when the arm lands (decode stops refusing) — it does NOT "flip". **Sequencing UNBLOCKED** (`2ec80071`): the SS field swap is DONE, so `ScenarioV3.people` (`PersonInputsV3 extends PersonInputs`) now carries `pia` + `birthYear` — U8 persists the settled shape, no v3→v4 migration debt. The codec's v3 arm adds a SEPARATE `checkPersonV3` (`needFinite(v,'pia')` + `needInteger(v,'birthYear')`) — NEVER mutate `checkPerson` (it validates the frozen `PersonInputsLegacy` v1/v2 shape; a forward note sits in `scenarioCodec.ts`).
- **U8 copy — the GCM ambiguity hedge:** `wrong-recovery-phrase` is cryptographically indistinguishable from a bit-rotted recoveryWrap with the CORRECT phrase — the calm restore copy must hedge BOTH ways ("that phrase doesn't match this backup — or the file is damaged"). Documented at `RestoreResult`.
- **U8 PWA-update wiring:** the update handler must re-check `isWriteInFlight()` immediately before `skipWaiting` (the write tail is covered AT CALL TIME only; insight 030's Also-Applies); defer `skipWaiting`+reload across a Save until the no-write-in-flight signal clears (U8 owns this integration test).
- **P3 display:** surface `TaxAwareResult.totalNetPremiumReal` + `totalMedicareCostReal` + the per-year `overCliff`/`belowFloor` disclosure flags to `SimOutput`/`Distribution`/the wire before the first healthcare-cost consumer. Surface the survivor cliff-drop ($84,600→$62,600), the stranded-HSA depletion, and the tax-off-toggle paradox (all correct + conservative but surprising — a P4 ranking must know).
- **The catch-up step-down disclosure RENDER** (`firstStepDownYear` + `slots.stepDownNote` computed, slot unrendered) lands with its first real surface (D2/U6) — keep deferred; don't ship a half-surface.
- **U14 — oracle-before-solver:** the optimality oracle must exist + pass BEFORE U15 can recommend. RESIDUAL: the planted "candidate that throws" oracle case (public inputs can't reach `SimInfeasible` in the gated domain — the oracle harness mints one). The U14 solver-token directional clause reduces to methodology.ts's 3 holdouts (validationMarket/productionMarket/survivorSpendingRatio — no government primary) + the **ε** calibration (the survival-equivalent band, calibrated against the oracle).
- **`seedB` held-out construction — DEFERRED to U14:** pin SplitMix64-high-32 *or* SHA-256(seedA) mod 2³² + a statistical-decorrelation bar + the deterministic B-family.
- **Survivor SS before own claim — ✅ RESOLVED** (`2ec80071`): the §202 survivor benefit now pays from age 60 (lock-flat, RIB-LIM, DRC flow-through), closing the early-widowhood gap. REMAINING P4 item = the survivor **two-stage claim OPTIMIZER** (survivor-first→own-at-70, the deemed-filing-exempt lever) — deferred because the engine optimizes NO claim ages yet (a lever with nothing to pull it). The shipped default is "claim as soon as eligible," one-signed conservative under the lock-flat reading.
- **Overlay hot-loop perf — DEFERRED to a measured pass** (when the P4 solver makes `simulate` hot). Memoize `living` by alive-bitmask + pre-size the per-path arrays (both byte-identity-safe). Also: `survivorBenefitAnnual` is recomputed every post-death path-year with t-invariant args (byte-identical; memoize per-path on the first death — the SS-seam-review-040 P3 advisory, no wrong dollar). **LANDMINE — do NOT "optimize" the double `allocateWithdrawal`** (the last-pass alloc lags the converged gross by one iteration; reusing it breaks reduce-to-spine).
- **WASM** is the only path to a TRUE cross-engine bit-identical headline. When it lands: add `'wasm-unsafe-eval'` to CSP `script-src`. Trusted Types is a planned U-later hardening (breaks `new Worker(new URL())` without a TrustedScriptURL policy — insight 002).
- **`deploy-ek.yml` is a GHOST** (projects/exploding-kittens gone) — delete whenever.
- **The predicate-hoist (D1 review G, deferred):** intakeMap + questions re-declare the route/age gate predicates; aligned today but a drift surface. Hoist to one module + add a missingRequiredFacts↔intakeSteps reachability test when convenient (P3).

## Landmines

- **SS seam (`2ec80071`) — do NOT regress:** the §202 survivor branch reads `params.people[d/s]` (pia/claimAge/birthYear/currentAge), NOT just `offsets` — a seam-unit test with synthetic `PersonOffsets` + a mismatched `params.people` silently gets `ss=0` (the index guard fails); a real §202 test needs an ALIGNED 2-person `params.people`. The Method-C **excess END-gate IS the all-alive branch** (excess lives ONLY there; once `!allAlive` the survivor branch owns SS — never add excess into the survivor path, that double-counts). `survivorStartAge = max(60, currentAge + deathOffset)` is **LOCKED at the death** (computed from `deathOffsets[d]`, not `currentAge+t`) — recomputing it per-year from the survivor's current age would ramp the factor = optimistic = the sin. The deceased's **CLAIM AGE is REALIZED at death** (`realizedClaimAgeAtDeath` caps the planned claim at the deceased's age-at-death, floored at ⌊FRA⌋) — passing the planned claim verbatim credits DRCs a worker who died before claiming never earned (insight 040, the optimistic survivor-floor sin); a discriminating seam test must drive a **death-before-claim** path AND a **nonzero spousal excess** (every pre-fix fixture used an already-claimed, excess-0 couple). `birthYear` is **REQUIRED** on `PersonInputs` (the FRA lookup runs at pia=0; `validateParams` gates it int [1900,2200]) — do NOT default it (burned/062). **Two birthYear sources coexist** by design: `PersonInputs.birthYear` (SS FRA) vs `OverlayPerson.birthYear = startCalendarYear − currentAge` (RMD bands) — don't collapse them. `PersonInputsLegacy` is the FROZEN v1/v2 person shape (`checkPerson` validates it, stays `socialSecurityReal`); U8 adds a SEPARATE `checkPersonV3`, never mutates `checkPerson`.
- **Cardinal:** *"it's just for friends" NEVER softens validation* — calm-but-wrong is the sin; the bar RISES for a recommender. The N=1 cold-read judges *tone*, never *correctness*.
- **Federal-data freshness is a BUILD-time gate, never runtime fetch** (breaks engine purity + CSP `connect-src 'self'` + offline + determinism). `reVerifyEveryBuild` + `verify:aca`'s 30-day staleness + the annual pin pass. The ACA legislative entry can flip the whole pre-65 model.
- **Cross-engine headline is NOT bit-identical in plain-TS** — `confidence.ts` quantizes the headline to a coarse grid BEFORE the band-edge decision (the screenshot-reproduction guard). `survivors/paths` is integer-exact but a path within transcendental-rounding-noise of depletion can flip the count ±1 across engines.
- **MC-vs-history is TWO-SIDED (Kitces):** the MC sits below history at 4% but crosses ABOVE at 5% — never assert a blanket "MC < historical".
- **U1 calibration is DIRECTIONAL, not pinned** (Shiller-government-bond calibrated). Both bond-series gates cleared via the Damodaran proxy re-scope; the MC's `validationMarket` summary moments stay directional until a deliberate re-derive-from-committed-series pass (optional; noted at `methodology.ts` pinTo).
- **CSP ships via HTTP response headers** (`vercel.json`), not a meta tag; `style-src 'self'` (no unsafe-inline, no nonce) — prefer stylesheet-class-driven CSS custom props over per-element inline `style={{}}` for dynamic values. motion@12 layout-animation injects a `<style>` → needs `<MotionConfig nonce>` when it lands. Self-host all fonts (count against the 300 KiB entry budget).
- **`/ultramode-code-review` is the standing cadence** at every unit boundary (always-on lenses + conditionals-by-diff + ≥1 adversary, escalated to a diverse panel for risk; adversarial-verify EVERY acted-on finding against source — incl. your OWN findings, insight 037; fix+gate; `/distill`). Don't run the turnkey 11-persona `ce:review` per milestone (half noise, no verify stage). `/code-review ultra` (billed cloud) stays available for a deeper pass.
- **Read `docs/insights/` before any unit** — the engine-purity lint is airtight (keep it); don't compute golden fixtures via the engine (DND 012); finiteness before relational (insight 010).
