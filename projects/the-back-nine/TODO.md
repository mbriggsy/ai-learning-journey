# The Back Nine — TODO

> Actionable next-actions only. **Where we are** ("you are here") lives in [`docs/roadmap.md`](docs/roadmap.md) — the maintained per-unit status table. **What we're building + why** is [`docs/README.md`](docs/README.md). No session history here; git log has it.

## Next, in priority order

1. **U7 — the confidence-statement surface (IN PROGRESS; building via Path B = the band on real data first).** Done this session: the **engine per-year band fan** (opt-in `simulate(params, seed, {bandFan:true})` → `distribution.bandFan`; `BandFan`/`BandFanYear` in `src/shared/model.ts`; living-cohort + $0-ruin + `cohortFraction` semantics; byte-identical to a fan-off run); the **producer** `resolveBandData(fan, outcomeState, {formatDollar, annotations?, callouts?})` in `src/viz/bandData.ts` (resample → fixed 49-pt lattice, `dollarMax ≥ max(p90)`, `isFixedLattice` throw, `cohortFraction` carried per `BandSample`) — **the deferred U6-review producer-seam guards are now WIRED here**; and the **non-color verdict signals** `src/ui/verdictSignal.tsx` (six silhouettes + `VerdictIcon`, decorative-by-default). **Remaining U7 build, in order:**
   - **(a)(b)(c) — ✓ DONE** (commits ccf2410f "U7 surface" + f7413daf "U7 foundation"; verified 2026-06-24 against source, gates green). `src/ui/outcomeStates.ts` exists (`Record<OutcomeState,…>` exhaustive — compile-error on a gap; re-derives no thresholds; tested). `src/ui/copy.ts` carries the 5 outcome WORDS (`outcomeOnTrack`…`outcomeAlreadyFailing`; indeterminate has none) **and** the verdict-grammar slot fns (`verdictRoomClause`/`verdictTrimClause`/`verdictHoldClause` keyed off `DollarAdjustment.direction`, + `verdictSurvivorStepDown` — $ drop, single-filer, NO "widow's penalty"). `src/ui/copyGuard.ts` (+ 297-line test) ships all five gates, the `FIELD_OP_ALLOWLIST` landmine class, `staticDisclosures` proven OUT, enumerate-`copy`-all-pass.
   - **(d) — ✓ BUILT + Chromium-screenshot-verified 2026-06-25 (the N=1 cold-read still owed).** `src/ui/ConfidenceStatement.tsx` + `src/ui/styles/confidence.css` (+ `__tests__/ConfidenceStatement.test.tsx`, 9 tests). Verdict-first: glyph + verdict WORD (uniform `--ink` — no good/bad hue) + the "X of 10 futures your plan covers" reading + the direction-keyed magnitude clause; the band is the on-demand `ConfidenceBandPanel` drawer fed by `resolveBandData`. Modes: reading (6 states — over-funded reads "more than 9 of 10" via the ceiling clamp; indeterminate = range copy + ellipsis, no word) · pending · compute-error. `@starting-style` entrance reveal (numbers static, no odometer), reduced-motion drops movement, `focusHeading` on an optional `focusSignal`. New copy keys `confidenceRegionLabel`/`confidenceCoverageCaption` (verdict-scoped) + `band*` chrome (all pass copyGuard); new ui formatter `src/ui/money.ts`. **NOT wired into the live intake yet (by design):** the running app still renders the D1 `AnswerStrip`; swapping `ConfidenceStatement` in + routing the band fan through `engineClient`/`memoryModel`, and deciding which lead shows, is a **D2 + U6-closeout** step best done with Briggsy awake to cold-read and direct D2. The `focusHeading` import from `@intake/a11y` is lint-clean (no `src/ui/**` import-ban; `a11y.ts:2` pre-declares the reuse).
   - **(e) — the survivor readout. (e1) engine distribution + (e1b) income step-down + (e1c) survivor reading are ALL ✓ BUILT & adversarially verified (2026-06-27).** `simulate`'s `survivorConditioned` surface emits the equal-weight survival fraction (statistic A) **and** `incomeStepDownMonthlyReal` (the $/month income cliff — gross pre-tax non-portfolio income drop, steady-state-anchored counterfactual, median, floored ≥0); `confidence.ts` `buildSurvivorReading` emits the "X of 10 as the survivor" + outcome word (`SimulationResult.survivorReading?`). Externally-derived + reduce-to-spine + byte-identity tested; a 4-lens adversarial workflow caught + fixed a P1 understatement (an fd-anchored counterfactual went NEGATIVE for pre-claim deaths — the §202 widow benefit front-running an unclaimed retirement benefit; the steady-state anchor fixes it, regression-guarded). Design + locked decisions + the open e2 cold-read questions: [docs/plans/features/u7-survivor-readout.md §6](docs/plans/features/u7-survivor-readout.md). **Remaining: (e2) `src/ui/SurvivorReadout.tsx` ONLY** — eye-oracle, DEFERRED to a Briggsy cold-read (NOT engine-blocked anymore; it consumes `survivorReading` + `incomeStepDownMonthlyReal`). Two copy questions ride into e2's cold-read (the `verdictSurvivorStepDown` tax-clause em-dash + the SS-only attribution — §6). Cardinal-sin-dangerous (a calm-but-wrong survivor verdict is the exact sin).
   - **(f) — ✓ BUILT + Chromium-screenshot-verified 2026-06-25.** `src/ui/preview/U7Preview.tsx` (+ `fixtures.ts` + `preview.css`), reached at **`?preview`** (DEV-only branch in `main.tsx`, dead-code-eliminated from prod — bundle still 197.8 KiB). Renders all six states + pending/compute-error + the survivor-deferred note, driven by honest representative fans (over-funded climbs and never touches $0; already-failing collapses to the $0 floor). **Every state + the enlarge modal were screenshot-read through Chromium** — the six verdict glyphs rendered for the FIRST time (designed blind) and read as distinct silhouettes; the inline bands are correct. (Survivor readout is the deferred placeholder — its engine surface e1/e1b/e1c is now built; only the e2 UI remains, item e.)
   - **N=1 laptop cold-read ✓ CLEARED 2026-06-27** — all six states + pending/error/enlarge-modal: glyphs read pairwise-distinct, tone honest, band honest, modal fits. The over-funded reading changed to the proportion **"better than 9 in 10"** (the integer "9 of 10" snapped to "10 of 10" on the cold-read). **`/ultramode-code-review` ✓ DONE** (8 lenses, 0 P0/0 P1; mitigations landed — the producer-seam $0-fan fail-loud + the cohortFraction seam guard + the over-funded ceiling single-source + the tautological-test pins; [insight 044](docs/insights/044-a-can-never-happen-comment-is-a-claim-about-the-gate-not-a-fact.md)). **The U7 SURFACE is cold-read + review cleared.** The ONLY remaining U7 build is **item (e2) — the survivor readout UI — eye-oracle, deferred to a Briggsy cold-read** (its engine surface e1/e1b/e1c is built + adversarially verified; above). To re-view: `pnpm dev` → `http://localhost:5173/?preview`. Visual direction is RATIFIED — "The Clubhouse Ledger" (`src/ui/styles/tokens.css`). (`compound-engineering:frontend-design` skill won't load post-reload — name glitch; not blocking since direction's ratified. Do NOT fall back to the official `frontend-design` plugin.)

2. **Phase-3 render + the U6-render closeout** (`src/viz/ConfidenceBand.tsx` `ResolvedFan`).
   - **✓ FIXED 2026-06-25 (option a) — the median trace truncation at the enlarge-modal scale.** Dropped the median's `pathLength: 0→1` draw (it fought `non-scaling-stroke` and under-covered the line at the larger scale); kept the opacity fade, so the full median now renders at every scale. `ConfidenceBand.tsx:8-11` contract comment updated; `ConfidenceBand.test.tsx` had no pathLength assertion (only `d`-geometry + element-count), so it stayed green.
   - Treat a `dollarMax`/`horizonYears` change across recomputes as a **re-draw, not a cross-scale morph** (still owed from the U6-review); and drive **thin-late-year de-emphasis** off the per-sample `cohortFraction` so a band narrowing because couples died never reads as rising certainty. *(The `cohortFraction` is now finiteness+range-guarded at the `resolveBandData` seam — [insight 044] — so the de-emphasis render consumes a validated signal; the render itself is still owed.)* The remaining ~8 advisory U6-review P3s (the inert `outcomeState` on `ResolvedBandData` — now comment-marked a forward-carry; the modal reduced-motion symmetry test) stay low-priority — [insight 043](docs/insights/043-a-domain-guard-loud-on-one-degenerate-input-fails-calm-on-its-sibling.md).

3. **D2 — the state-adaptive first answer + two-pane laptop layout.** Date-first for not-yet-retired, spine-first for already-retired; one intake flow, two leads. The showcase surface (laptop is primary). Composes over the U6 band + U7.
   - **U7-review obligations that land HERE (the live-wiring unit):**
     - **$0-portfolio households:** `resolveBandData` now FAILS LOUD on an all-$0 fan — a VALID Social-Security-funded $0-portfolio decumulation household ([insight 044]). D2 must screen `initialPortfolio === 0` and render the verdict **without** a portfolio band (nothing to plot but the $0 floor; an over-funded $0 household needs a cold-read on whether to show a band at all).
     - **Plan-horizon annotation:** the real engine's fan can end **before** `maxHorizonYears` (the band stops at the last living-cohort year). The caller must align the "Plan horizon" x-annotation to the fan's **actual last year**, not `maxHorizon` (the `resolveBandData` seam is correct; this is a wiring detail).
     - **Provisional eyebrow reserved slot (insight 035):** when D2 wires the live provisional→final toggle, `.cs-provisional` must reserve its box (it sits above the focusable band button). Deferred from U7 on purpose — fixing it earlier would alter the just-cold-read-approved static layout.
     - **Already-failing magnitude clause — BRIGGSY'S COLD-READ CALL:** off-track and already-failing share `verdictTrimClause` ("About $X a month less would bring it onto steadier ground"). The review flagged (0.45 conf) that for a 0-of-10 `rethink-inputs` plan this could read as a falsely-actionable "cut this and you're OK." It CLEARED the 2026-06-27 cold-read — so it's not changed. Re-cold-read the **already-failing** case specifically asking *"does this number read as 'cut this and you're fine'?"*; if yes, add a dedicated already-failing clause (a "what would have to change" framing, not a single sufficient-sounding trim figure).

4. **U8 — save/load (the first encrypted Save).** `ScenarioV3` is defined; remaining = the codec v3 arm (a *separate* `checkPersonV3`, never mutate the frozen `checkPerson`), `AnyScenario` membership, the user-facing **first-Save ceremony** (passphrase-strength gate `zxcvbn-ts` ≥3 ∧ len ≥12 + recovery-phrase display + mandatory export), and load-on-return. NOTE: the encrypted-vault IndexedDB writer (`writeVault`/`rewriteModel` in `store/db.ts`) already exists (the Act-1 encrypted store) and is wired into `backup.ts`/`session.ts` — U8 is the *flow* + the v3 writer, NOT a new persistence layer. Fold in the portfolio-holdings model field ([`docs/decisions/portfolio-holdings.md`](docs/decisions/portfolio-holdings.md)) on the same persisted shape — no v3→v4 migration.

5. **Single-user "just me" mode** (deferred). A real people-of-one path — NOT "zeros for the spouse" (a phantom zero-spouse still carries a mortality curve and warps joint-survivor + the MFJ→single transition).

Then Act 3 (Controls, U9–U13) → Act 4 (Solver & Recommendation, U14–U17). See [`docs/roadmap.md`](docs/roadmap.md).

## Standing cadences
- `/ultramode-code-review` at every unit boundary; the four-skill UI loadout before ANY user-facing surface (CLAUDE.md "UI design skills").
- `/brief` (read `docs/insights/`) before a unit; `/distill` after.
- **Delegated build:** use **native Agent Teams** for live-steer, **eye-oracle** units (Briggsy steers mid-build) and the **Workflow** tool for fire-and-forget, **test-oracle** fan-out. The durable laws live in memory `feedback-delegated-build-laws` (decide-before-dispatch · loop=unit-of-trust · independent-verify-by-a-different-agent · task-list-canonical · coordinator-restraint · eye-vs-test-oracle). *(The `loop-engineer` + `swarm` skill wrappers were archived 2026-06-24 — thin wrappers over the now-native capabilities; don't reach for them.)*

## Verify-owed
- The OOP-medical hint figure (`src/intake/referenceData.ts` → `OOP_MEDICAL_TYPICAL_HOUSEHOLD`) is grounded-search-sourced, **not** primary-table-verified (`directionalUntilPinned`). BLS bot-walls `curl` — ask Briggsy to pull the CE "Age of reference person" table to pin it cell-by-cell. Conservative-by-design while it stays directional.
