# The Back Nine — TODO

> Actionable items only. Live plan: `docs/plans/back-nine-mvp/roadmap.md` (4-phase spine). Product: `docs/plans/direction-reset-2026-06-04.md` (north-star) + `docs/brainstorms/the-back-nine-requirements.md` (v2). Landmines + solver-blocking gates live in the roadmap's "Risks" + "Validation Gates". Project insights: `docs/insights/`. No session history here — git log has it.

## Current State
**P1·U1 (Monte Carlo engine core) SHIPPED + CI green on Linux (2026-06-06).** Pure, deterministic `(params, seed)` engine: vendored `mulberry32` + stateless Box-Muller + exact simple→log moment conversion (`rng.ts`); the single shared per-year update (`decumulation.ts`, contract #3, used by both backtest and MC); Trinity rolling-window backtest on **real 1925–1995 Shiller data** vendored from cFIREsim-open (`historical.ts` + `reference/shillerSeries.ts`); sex-specific cohort longevity with per-path survivor identity (`longevity.ts` + `reference/mortality.ts`); 4 CRN-neutral drawdown policies (`sequencing.ts`); the MC spine — dimension-only CRN draw schedule + earned-income bridge + SS step-down + R19 gate (`simulate.ts`); pure distribution→reading with coarse-grid quantization + 10/10 clamp (`confidence.ts`); typed-array worker transfer with a structural main↔worker split (`engineProtocol.ts`/`engineWire.ts`/`engine.worker.ts`). **Verified: typecheck clean · 113 tests pass (15 files) · initial JS 195.4 KiB ≤ 300 KiB · CI green on Linux · seen in-browser (real engine through the worker → "9 of 10 · over-funded").** A 10-skeptic adversarial review folded 3 R19 gaps in `validateParams`.

**P1·U0 (foundation) SHIPPED earlier** — scaffold, PWA, engine-purity lint, constants module, ACA re-verify gate, strict CSP, full CI. 5 U0 insights in `docs/insights/`.

**P1·U2 tax constants SOURCED** (the 4 throw-on-read gaps, `constants/tax.ts`): 2026 single ordinary brackets, single age-65 addition ($2,050 — distinct from the $6k senior bonus), cap-gains 0/15/20% breakpoints, and the full post-2022 Uniform Lifetime Table (49 rows). Each grounded + **adversarially verified to the dollar** against IRS Rev. Proc. 2025-32 / eCFR §1.401(a)(9)-9 (independent PDF/reg parse), marked `directionalUntilPinned` until the P1 pin gate. The `taxOverlay.ts` build is now unblocked; the **Joint-Life/Last-Survivor Table II grid is the one remaining tax constant** (see Open Items).

**P1·U2 M1 (overlay seam) SHIPPED + CI green (commit `b50a8c39`).** `taxOverlay.ts` → `runTaxAwareDecumulation` tracks per-bucket balances (taxable/pretax/roth) as an auxiliary ledger while advancing the authoritative TOTAL through the **same `stepYear` the spine uses** — so the total trajectory is **byte-identical** to the validated spine under the EXHAUSTIVE OFF condition (proven with exact `toBe` equality across 2 scenarios × 3 single-pool positions × 4 policies). Built **standalone** — NOT yet wired into `simulate.ts` (like `sequencing.ts` was in U1), so the prod bundle is unchanged. `grossUpForYear()` is the inert placeholder where M3+ tax math lands.

**P1·U2 M2 (RMD forced distribution) SHIPPED (commit `b5a54b33`).** SECURE-2.0 RMD as a **pre-tax→taxable ledger relocation**: birth-year band (72/73/75 via `rmdStartAge`) → Uniform Lifetime divisor → the forced excess beyond spending relocates to taxable. `selectRmdDivisor(ownerAge, spouseAge?)` is the marked seam stubbing to ULT; the >10yr-younger-spouse **JLLS grid stays an untouched throw-on-read gap** (Open Items / M6). **Corrected the locked plan (`phase-1-foundation.md` lines 172 & 186):** an active RMD with tax OFF is **TOTAL-NEUTRAL, not byte-identity-breaking** — under contract #2 pre-tax and taxable grow identically, so the relocation can't move the total; `terminalReal`/`depletionYear` come from `stepYear` on the RMD-untouched gross withdrawal. Only the M3 tax makes it bite; RMD-inert stays in the OFF condition as a clean-fixture guarantee. **A 7-lens adversarial workflow found ZERO implementation defects** (6/7 lenses clean incl. a meta-check that steelmanned the original plan claim); the 7th's test-coverage gaps were folded — a 2-year **basis-recurrence** fixture (`taxable/total = 1/d73 + (1−1/d73)/d74`, externally derived from published divisors, matched to 12 digits), the 72-band threshold, the spouse-ignored stub, depletion-through-RMD, and proportional+roth isolation. **Verified: typecheck clean · now 157 tests total (16 files) · lint clean · bundle 195.4 KiB.**

**P1·U2 M3 (ordinary-income tax) SHIPPED (commit `71ed4f07`).** Ordinary tax on the MFJ/single bracket schedule, with the year's cash **grossed up to net `spending` after the tax that withdrawal triggers** — a bounded per-year **fixed point** (`solveGrossWithdrawal`: gross = net + tax, tax on `max(alloc.pretax, rmd)`, iterated to convergence; a geometric contraction that **throws** rather than ship an unconverged tax, burned/062). Pure helpers (progressive brackets + deduction stack: standard + age-65 addition × 65+ filers + OBBBA senior bonus w/ MAGI phase-out) read every figure from `@engine/constants`. **Scope:** Roth + taxable-basis withdrawals are NOT ordinary income here (cap-gains = M5); MAGI = ordinary income (SS = M4); filing fixed MFJ (survivor switch = M6). **This is where the RMD BITES** — the tax leaves the portfolio, so a taxed run ends BELOW the spine. Config reshaped: a shared `Household {startCalendarYear, filing, owner, spouse?}` on the ON variant (tax needs ages+filing even when RMD is off); bare `{taxEnabled:false, rmdEnabled:false}` stays the OFF anchor. **Golden fixtures (DND/012) hand-derived by 3 independent computers + a hand-calc, agreeing to the cent:** `ordinaryIncomeTax` of (150k,mfj,2)=11,974 · (250k,mfj,2,phasing out)=35,294 · (80k,single,1)=7,065. **An 8-lens adversarial workflow found ZERO implementation defects** (7/8 clean incl. fixed-point, deduction stack, brackets, reduce-to-spine, constants, + a scope meta-check); the 8th's coverage gaps folded — the gross-up under **realistic coupling** (proportional multi-bucket, ordinary income a strict fraction of gross, independently re-solved & matched to 8 digits), the phase-out driven through the loop, tax-on depletion, a single-filer end-to-end, a bracket-edge fixture. **Verified: typecheck clean · now 171 tests total (16 files) · lint clean · bundle 195.4 KiB.**

**Plan set remains LOCKED** (roadmap + 4 phase docs). Superseded `docs/plans/mvp-confidence-spine/` = mined history — do NOT `/ce:work` it.

## Next Steps (priority order)
1. **P1·U2 — the tax-and-accounts overlay (IN PROGRESS, built in milestones; M1 seam DONE; M2 RMD DONE; M3 ordinary tax DONE).** `src/engine/taxOverlay.ts`. Per `phase-1-foundation.md` (Unit 2). **Anchor invariant for EVERY milestone:** an overlay-ON path must still reduce byte-identically to the spine under the EXHAUSTIVE OFF condition (buckets collapsed **AND** conversion=0 **AND** tax off **AND** RMD-inert). Remaining milestones, in order:
   - **M2 — RMD forced distribution — DONE (commit `b5a54b33`).** Birth-year band + ULT divisor + pre-tax→taxable relocation; `selectRmdDivisor` seam stubs to ULT (JLLS deferred). Total-neutral with tax off (the corrected line-186 claim).
   - **M3 — ordinary-income tax — DONE (commit `71ed4f07`).** Gross-up fixed point (`solveGrossWithdrawal`) + pure tax helpers (`ordinaryIncomeTax`, exported); `Household` config reshape; golden fixtures + 8-lens adversarial review folded. The RMD now bites (taxed run ends below the spine). `grossUpForYear` is gone — the fixed point IS the seam; M4 extends `solveGrossWithdrawal`.
   - **M4 — SS provisional-income fixed-point (NEXT).** Fold the Social-Security provisional-income layer INTO the existing `solveGrossWithdrawal` fixed point: iterate provisional→taxable-SS (the Pub 915 worksheet via `ssProvisionalThresholds`, frozen/un-indexed)→ordinary tax→gross-up to convergence, so taxable-SS enters both the ordinary-income base AND MAGI (which then re-feeds the senior-bonus phase-out). Pinned pass count; **no in-range default** for an unconverged value (fail loud, burned/062). Externally-derived fixture: a known SS + other-income year → known taxable-SS + incremental tax (DND/012, never via the engine's own formula). The SS step-down at the first death already rides U1's survivor boundary.
   - **M5 — Roth conversion + cap-gains/QD stacking:** conversion = pretax→roth taxed as ordinary income (RMD-first legality); cap-gains stacked on ordinary income via `capitalGainsBreakpoints`.
   - **M6 — MFJ→single survivor switch + `simulate.ts` integration.** Filing flips at the first-death boundary (rides U1's survivor boundary — NO new boundary). Wire the overlay into `simulate.ts` (insert between `netWithdrawalForYear`→`runDecumulation`); extend `model.ts` to the schemaVersion-2 bucket/birth-year/filing shape (none exist yet — schemaVersion still literal `1`); wire `bracket-fill`'s real ceiling (`sequencing.ts:85-89`, currently falls back to `pre-tax-first`). The directional tax-math fixtures + the full reduce-to-spine-through-`simulate` byte-identity land here; golden test with a born-pre-1960 spouse.
2. **P1-EXIT dataset pins (directional→exact; parallelizable, must clear before the PHASE closes).** Three named gates flip U1's directional fixtures to pinned-exact:
   - **SSA `table4c7`** cohort curves → replace `reference/mortality.ts` (currently a Gompertz fit to grounded anchors) 1:1 with a committed table4c7 snapshot (SSA bot-blocks curl — manual fetch + commit).
   - **Trinity long-term-CORPORATE** bond series → `shillerSeries.ts` is GOVERNMENT-bond yield-as-return; pin to flip 50/50 from the govt 100% toward the corporate 95%.
   - **Ibbotson intermediate-GOVERNMENT** total-return series → pin Bengen's 4.15% to bit-exact.
   Each fixture stays marked directional in code until its gate clears. Name who/what clears each.

## Open Items (decide at the named unit)
- **`seedB` held-out construction — DEFERRED to U14** (not U1). U1 ships single-seed CRN via `buildDraws(seed, paths, maxHorizon, peopleCount)` — one `mulberry32` consumed in dimension-only fixed order (all market normals, then longevity uniforms), so two candidates under one seed draw byte-identically. When U14's held-out grade lands, pin the `seedB` construction (SplitMix64-high-32 *or* SHA-256(seedA) mod 2³² — pin one) + a statistical-decorrelation acceptance bar + the deterministic `seedB[0..m-1]` B-family (re-derivable from the one persisted `seedB`).
- **`market.space`/`returnsAreReal` are spine-REJECTED, not implemented.** The U1 spine models SIMPLE-space + REAL moments ONLY; `validateParams` rejects `space:'log'` and `returnsAreReal:false` as indeterminate (the type still advertises them, the worker boundary is untyped). Implementing log-space / nominal-deflation is a SCOPE EXPANSION needing new externally-derived golden fixtures (DND/012) — never a guard-only flip.
- **JLLS Table II grid — the one remaining tax-constant gap** (`constants/tax.ts` → `jointLifeLastSurvivorTable`, throws on read). The 4 named U2 gaps are sourced + verified (Current State). What remains: transcribe the ~3,000-cell **Joint-Life & Last-Survivor grid** (Pub 590-B Table II) verbatim + a golden test against the **eCFR §1.401(a)(9)-9(d)** reg as the independent transcription oracle (DND/012 — NEVER compute the cells from the engine's own actuarial formula). Mechanics are already encoded in the entry's note (>10yr-younger sole-spouse switch = gap≥11; symmetric grid; annual re-entry, not subtract-one; anchors owner75/spouse64=25.3, owner76/spouse60=28.2, consistency cell(75,65)=24.6=ULT-75). Needed for the age-gapped-couple RMD (Table III overstates forced income for a >10yr-younger sole spouse → can invert a conversion ranking); U2's ULT-path golden tests can land first. Restrict to the reachable owner-72..120 × younger-spouse rectangle, exploit symmetry, clamp 120, finite 1-decimals (DND/009).
- **Pin gate (P1-exit, parallelizable):** the 4 newly-sourced tax figures + the pre-existing MFJ figures stay `directionalUntilPinned: true` — flip them together (to `false`) only at the deliberate P1-exit pin pass that confirms against the IRS primaries. The solver token (U14) requires `directionalUntilPinned === false` on every rec-relevant figure, so this is load-bearing; don't flip piecemeal.
- **Plan-citation cleanup (fold when next touching the phase-1 doc):** the doc over-attributes 3 items to the research — the two-sided low-withdrawal MC caveat + log-σ-vs-simple-σ aren't in §Strand 4; "transcendentals differ across V8/JSC/SpiderMonkey" isn't in §Strand 2. The substance is now CONFIRMED in code (the MC reproduces the two-sided crossover; the σ²/2 drag is exact; quantization is enforced) — just soften the citations.
- **Exact ε** for the survival-equivalent band — calibrated against the U14 oracle (a gate; the oracle-cleared token is withheld until ε is calibrated).
- **U4 KDF-location spike:** main-thread + a rendered "unlocking…" pending state vs a dedicated crypto worker — decided by measured jank on the reference device.
- **WASM** compute-budget threshold + mid-tier reference device (instrument-first). When WASM lands: add `'wasm-unsafe-eval'` to CSP `script-src`. WASM is ALSO the only path to a TRUE cross-engine bit-identical headline (see the cross-engine landmine).
- **CI housekeeping (dated):** `verify-the-back-nine.yml` uses Node-20 actions (`actions/checkout@v4`, `actions/setup-node@v4`, `pnpm/action-setup@v4`). GitHub forces Node-24 default **2026-06-16** and removes Node-20 **2026-09-16** — bump these action majors before then (a non-blocking deprecation warning today, not a failure).

## Landmines
- **Cardinal:** *"it's just for friends" must NEVER soften validation* — calm-but-wrong is the sin; the bar RISES for a recommender. The N=1 cold-read judges *tone*, never *correctness*.
- **U1 calibration is DIRECTIONAL, not pinned.** The MC's absolute numbers are Shiller-GOVERNMENT-bond calibrated (50/50 MC ≈ 95% vs the govt 100% historical) — NOT the Trinity-CORPORATE 95%. The 3 P1-exit pins flip them. Don't read the directional bands as exact.
- **MC-vs-history is TWO-SIDED (Kitces):** the MC sits below history at 4% (i.i.d. tail penalty) but crosses ABOVE at 5% (overstates sustainable income in the 70–90% range). Never assert a blanket "MC < historical" — only in the stress region (`simulate.test.ts`).
- **Cross-engine headline is NOT bit-identical in plain-TS.** `survivors/paths` is integer-exact, but a path within transcendental-rounding-noise of depletion can flip the COUNT by ±1 across engines. Quantization to a 1% grid REDUCES (does not eliminate) the headline-flip risk; the absolute guarantee is the WASM trigger (`confidence.ts` header).
- **`validateParams` is the engine's R19 gate** — the worker boundary is untyped (structured clone), so every incomputable input (mean ≤ -1, |ρ| > 1, NaN moments, unsupported space/real) must be rejected THERE, not assumed away by the type. The adversarial review caught 3 such escapes; keep the gate tight when adding params.
- **`App.tsx` SMOKE_PARAMS is a scaffold liveness demo** — P2·U7 replaces the App body with the real first-answer surface; don't build on the smoke readout.
- **Oracle-before-solver:** U14's optimality oracle must exist + pass BEFORE U15 can recommend (harness-minted opaque token; compile error to skip).
- **Read `docs/insights/` before U2:** engine-purity lint is airtight (keep it); don't compute golden fixtures via the engine (DND/012). Full Risks set in the roadmap.
