# The Back Nine — TODO

> Actionable items only. The live plan is `docs/plans/back-nine-mvp/roadmap.md` (the 4-phase spine). Product definition: `docs/plans/direction-reset-2026-06-04.md` (north-star) + `docs/brainstorms/the-back-nine-requirements.md` (v2). Landmines + solver-blocking exit gates live in the roadmap's "Risks" + "Validation Gates" sections. No session history here.

## Current State
**Weighted deepen DONE + folded clean (2026-06-05).** The roadmap + all 4 phase docs are written in `docs/plans/back-nine-mvp/` (U0–U17). The weighted full-broad deepen ran via a 29-agent workflow (7 domain researchers vs IRS/CMS/OWASP primaries · 9 units adversarial-deepen→verify · 3 seam checks · coherence pass) and **all verified findings are folded in clean** (plain body content, no amendment headers) across the 4 phase docs + roadmap, plus **2 source-of-truth research-doc fixes** (senior-deduction MFJ full-phaseout $350k both-65+; IRMAA Part B top $689.90). Units contiguous U0–U17; zero deepening-drift residue. The `deepened:` / `doc-reviewed:` stamps stay **BLANK** until BOTH the deepen AND the document-review are folded — **document-review is still pending**. Superseded `docs/plans/mvp-confidence-spine/` stays as mined history.

## Next Steps (priority order — the next session)
1. **document-review** (the adversarial/persona pass — feasibility, scope-guardian, security-lens, product-lens, coherence, adversarial) on the FULL deepened set. Fold findings **clean** — plain body content, **NO "tracked amendment" headers / cascade ledger** ([[feedback-deepening-drift-anti-pattern]]). Use Sequential Thinking for synthesis; verify critical fixes against source ([[deepen-plan-lessons]], [[feedback-sequential-thinking-always]]).
2. **Set the roadmap's `deepened:` (2026-06-05) + `doc-reviewed:` stamps** once the document-review is folded — both passes complete only then.
3. **Then `/ce:work` foundation-first** (run `/brief` first — the hook gates `ce:work`, not `ce:plan`/document-review). Start at P1·U0 (scaffold) → U1 (engine).

> The deepen's new locked decisions live in the docs + [[project-the-back-nine]]: survivor-spending=sticky-essentials (Briggsy's call), seedB=hash(seedA), ε split (never select survival-equivalence on B), oracle=constant-marginal-rate-conditioned, IRMAA ×enrolled-count, HSA qualified-medical-only, §1014 step-up disclosed for leave-more, RMD forced-floor + non-convertible.

## Open Items (deferred-in-doc — decide at `/ce:work`, owned by the named unit)
- **mulberry32 seed decorrelation (seedB derived from seedA, not consecutive) + positional CRN** — the reframed U1 spike (the 2^32 period is adequate, not the constraint; counter-based draw optional since the absolute-year-indexed matrix already gives positional CRN). Decided at implementation.
- **Exact ε** for the survival-equivalent band (P4·U14 oracle calibration, directional-until-pinned); **exact passphrase-strength threshold + library pin** (P2·U8).
- *(Resolved in-doc: the "never depleted" sentinel is decided in P1·U4 — a tagged-union discriminant / out-of-domain integer, never Infinity/NaN/null. The canonical year-keyed constants module is introduced in P1·U0 and every dated constant routes through it.)*

## Landmines
The full set lives in the roadmap ("Risks & Dependencies" + "Validation Gates") and the north-star's "Carried landmines." The single most important: **"it's just for friends" must NEVER soften validation** — calm-but-wrong is the cardinal sin and the bar RISES for a recommender. And: **the solver optimality oracle (U14) must exist and pass BEFORE the solver (U15) is allowed to recommend** — the N=1 cold-read judges tone, not correctness.
