# The Back Nine — TODO

> Actionable next-actions only. **Where we are** ("you are here") lives in [`docs/roadmap.md`](docs/roadmap.md) — the maintained per-unit status table. **What we're building + why** is [`docs/README.md`](docs/README.md). No session history here; git log has it.

> **Dev seeds are LIVE** (`?seed=retired` = all-retired on-track spine band; `?seed=date` = still-working fuck-off-date band; `?seed=borderline` = **borderline retired ("On the line, 7 of 10") whose band descends to $0 — the two-pane honesty cold-read**; `?seed=dateborder` = borderline date with a $0 tail, but long-horizon-squashed; **`?seed=dip` = THE HARD-GATE SEED — the real non-monotone ladder (dips at 0-2, crown at 5) + an applied Roth conversion, the U10 cold-read surface**; `?seed=order` = the custom-drawdown round-trip seed — 3 split buckets, policy `custom` + order `['roth','pretax','taxable']`, the only seed driving the U10 biconditional through the codec; **`?seed=date65` = the all-65+ STILL-WORKING couple (66/65) — the date route pricing Medicare with NO false "unpriced" note, the insight-080 regression witness live** (Medicare pricing unit, 2026-07-10); **the STATE faces (the seed increment, 2026-07-16): `?seed=nc` = the NC-priced flagship (3.99% flat BITES — the state-absent twin's on-track 0.8555 drops to BORDERLINE 0.838, engine-pinned) · `?seed=pa` = the PA "usually a small piece" (NOT byte-identical: PA taxes the realized gains on the reinvested-RMD taxable bucket at 3.07%; verdict unmoved) · `?seed=fl` = the $0-constitutional affirmation (byte-identical, still PRICED) · `?seed=elsewhere` = the ANSWERED-but-unpriced face (monolith verbatim + byte-identity — the reduce-to-spine membership witness) · `?seed=datenc` = the date-route NC witness (the second producer, `dateStatePriced`)**). **The SOLVE faces: `?seed=surplus` = the over-funded ACTIVE recommendation witness (U16 — the delta-as-hero surplus lockup; a real ~6min full-precision solve; now ALSO the delta-qualifier face: "$280,000 more" beside "In the typical future the edge is closer to about $190,000.") · `?seed=steer` (2026-07-23) = the NO-PRETAX REFUSAL witness (Roth+brokerage couple, engine-pinned on-track, overlay-present pretax-0 — invite → GoalPicker → the calm typed-refusal note, NO solve; the `blocked{no-pretax}` face's only live drive).** Jump straight to a worded result + band to cold-read any change. DEV-only, DCE'd from prod (`src/ui/devSeeds.ts`). **`?vault=<key>` (e.g. `?vault=retired`) PLANTS an encrypted vault from that seed + drops on the Unlock screen with the passphrase PRE-FILLED — one click opens it, no re-driving intake + the Save ceremony. The param STRIPS ITSELF once the plant lands (history.replaceState), so a plain refresh probes the REAL vault like prod — re-planting is an explicit re-entry of the `?vault` URL, never a refresh side effect (the plant clears the vault first; leaving it armed silently reverted a saved edit — Briggsy, 2026-07-02). DCE-proven (grep dist/ for the dev passphrase = 0).** `plantDevVault` is memoized so StrictMode's double-invoke plants once (a concurrent second plant races the session epoch → spurious write-failed). **`?vault=stale` (U13) = the AGED vault** — the retired household as if saved ~2 years ago under older tax/health vintages: the ONLY live drive of the re-entry staleness surface (every organic save is same-day fresh). **`?vault=statestale` (the seed increment, 2026-07-16) = the aged-STATE-profile plant** — the NC household as if saved earlier THIS year under an older NC rate profile, via its OWN light doctor (`doctorStateStaleVault`: savedAt −150d + the state stamp alone; the FULL doctor's −2y anchor would trip the engine's priced-state year bound, simulate.ts:641 → R19 indeterminate): the `stalenessStateTax` gate note fires in ISOLATION, then the borderline NC-clause hero. **`?vault=datearrived` (U17 §S6, 2026-07-27) = THE ARRIVED HOUSEHOLD** — the `dip` date couple whose plan was BUILT 6 years ago, so the lifestyle crown (5) now sits BEHIND the elapsed window: the ONLY live route to the hero's arrived arm (`dateInYearsPast`, "that year has already come and gone"), and the surface the four parked aged-surface tone calls ride. Its doctor (`doctorArrivedVault`) ages `birthYear` ALONGSIDE `startCalendarYear` to hold the `currentAge === startCalendarYear − birthYear` model invariant (`model.ts:98`) — measured byte-identical crowns either way — and `savedAt` stays RECENT on purpose, so every vintage is legitimately fresh and the BUILD clock is the frame's only aged signal. **It lights ONE arrived arm, not two:** the floor crowns at 0 and `floorLineText` short-circuits offset 0 before the three-way split (`FuckOffDate.tsx:197`), so `dateFloorCoveredPast` still belongs to `?vault=datestale` alone. It is also the first live surface where §S2.1's work-stops WITHDRAWAL fires (crown behind Today), pinned in `verify:fit`.

## Next, in priority order

⏭️ **THE LIVE QUEUE — 2026-08-01, THE QUEUE-VERIFICATION SESSION. Ten pilot-owned queue items were adversarially verified against source BEFORE any code was written (20 agents, verify → independent-refute, every cited line opened). The base-rate law held AGAIN: not one of the ten was executable exactly as filed. Two shipped, three entries below are corrected in place, one is STRUCK as false, and two forks are routed to Briggsy.**

**✓ SHIPPED THIS SESSION:**
- **`b0a52a6e` — THE HAWK-VETOED SURPLUS ABSOLUTE WALKED PAST THE GATE IN ITS CONTRACTED FORM.** `copyGuard.ts`'s certainty clause was uncontracted-only, so *"You are safe either way."* was caught and **`"You're safe either way."` — the exact string the 2026-07-18 council vetoed, and the form a writer actually types — scored ZERO hits across all 48 false-certainty patterns.** Proven before the fix: the literal was planted in the `mustCatch` corpus first and the run red by name, then the class was widened (`'?re|'?d be`). Scanned against every runtime copy string before landing — zero hits. **⚠️ AND THE DOC HALF MATTERED MORE THAN THE CODE HALF: `docs/product.md` still PRESCRIBED the vetoed phrase in TWO places** — §6's pivot entry and **requirement R21 itself** — with no veto annotation, against a council the log records as ratified-and-executed. A builder greping product.md for the surplus-pivot wording would have authored the vetoed line and been red by a gate whose reason lived nowhere near it. Both now name the ratified exit (delta-as-hero, shipped in U16); R21's substance is untouched. **⚠️ THE QUEUE'S OWN CLAIM AT `:30` THAT `recommendationView.test.ts:197` IS A DEAD ASSERTION IS FALSE — do not delete it.** `:196`'s `toBe` calls the SAME slot function on both sides, so it pins the WIRING and cannot see a reword of the slot TEMPLATE; `:197` was the only live guard on that template and now carries the reason it is not redundant.
- **`f88fe30e` — THE ASSUMPTION PANEL NAMED THE ACA QUOTE PAIR FOR AN UNKNOWN-AGE HOUSEHOLD AND HID ITS EDITOR** (an R7 completeness break). `missingRequiredFacts` required the pair for pre-65 **or unknown** age; the panel gated the editing row on the narrower known-and-under-65. **The route is LIVE, not synthetic** — unknown age is the fresh-draft default, the guided walk's birth-year field writes `undefined` through, and both sanity rules guard on `!== undefined`, so nothing refuses it; resolve → Assumptions → "edit in the walk-through" → blank a birth year → return lands `inputs-incomplete`, the exact arm that renders the missing-fact line. Fixed with ONE exported predicate replacing three re-typed copies. Mutation-proven: the old predicate reds the new arm on the RENDERED ROW while the `missingRequiredFacts` assertions stay green, so the arm discriminates the panel and not the map. **Four corrections to the filed prescription, all from opening the line** — the prescribed assertion was not executable (the missing-fact line truncates to three names), the arm needed an explicit `inputs-incomplete` snapshot (the harness default is `idle`, which renders no missing line at all) and a real-producer `missing` prop (it defaults to `[]`, so the obvious version was vacuous), `intakeMap`'s comment claimed unknown ages are *"not required"* while heading the block that requires them, and the old *"the engine's priced domain"* phrasing must NOT be carried onto the widened predicate (`healthcarePriced` is known-ages-only BY DESIGN).
- **`<this commit>` — TWO WRITTEN FALSE CLAIMS ABOUT SS ENFORCEMENT, FOUND BY THE CENSUS, CORRECTED.** `constants.shape.test.ts` asserted the flag with the failure message *"the unenacted 50→33% phase-down must be caught at build"* — **nothing catches it at build; the test passed while its own message lied** — and `docs/decisions/ss-computation.md:80` stated *"the constant must catch enactment at build time, not drift silently"* as a shipped ruling. Both now say what is true and carry the gap.

- **`b23a6c20` — THE ACA PERCENTAGE TABLES RODE THE FRESHNESS RECORD BY PROXY.** Full entry in the `socialSecurity` census item below; the headline is that a re-indexed Rev. Proc. could ship under a green `verify:aca` indefinitely, and the figures written into the record were **externally cross-checked cell-for-cell**, with the un-opened primary PDF disclosed rather than implied.

**STATE — 2026-08-01: 3202 tests / 163 files** (+8) · typecheck 0 · lint 0 · doc-stats OK on BOTH tracked surfaces **plus `roadmap.md:101`'s "Now …" prose claim, which the gate structurally cannot read and which this session moved by hand** (the queue's own rot landmine). `verify:fit` / `verify:csp` / `verify:aca` / `verify:state-tax` NOT re-run locally (no code touched their surfaces); they ride CI. **✓ CI GREEN BY EXPLICIT ID, CONFIRMED IN-SESSION (polled to `completed`, never read off a watch exit code): `f88fe30e`/30704202572 · `011858c1`/30704382573.** *(`b0a52a6e` was pushed in the same batch as `f88fe30e` and has no separate run by construction — the run on `f88fe30e` covers both trees.)* **Twelve for twelve across this queue's tracked history, no red anywhere.** *(Only the commit carrying THIS line is unverified, by construction — read it as "verified through", never "HEAD is".)*

⏭️ **THE PRIOR LIVE QUEUE — 2026-07-31 LATE, THE RECORD-CARD SESSION (the flake session's record is kept below).**

**✓ CI IS GREEN THROUGH HEAD, BY EXPLICIT ID — the prior queue's "⚑ check these first" is DISCHARGED, and so is this one's.** Verified by polling `gh run view <id> --json status,conclusion` to `completed`, never off a watch exit code (that landmine fires in BOTH directions). `48e48570`/**30660623330** · `87361040`/**30664264585** · `76076788`/**30668197798** · `532cad82`/**30678900747** (the record card's in-frame line, item 1f half 1) · `a3c6e640`/**30679241168** · `8df242a0`/**30679511142** (the ACA drift seam bound) · `e7d2ee78`/**30680704783** (the datesplit crowns pinned) · `292cf9bd`/**30681007801** (the rec/recold door walk). **Nine for nine, no red anywhere** — the two-red-commits surprise that opened the flake session did not repeat. *(Only the commit carrying THIS line is unverified, by construction — read it as "verified through", never "HEAD is".)*

**✓ SHIPPED 2026-08-01 (`8df242a0`, CI 30679511142) — THE ACA DRIFT SEAM IS BOUND. The entry is kept below for its reasoning, because the mechanism it describes is the reason the bind is load-bearing and the next re-verifier must understand it.** ⏰ **The dated obligation it was written against is UNCHANGED and is now the nearest thing on this queue: ACA reds 2026-08-25** (24 days out at the time of writing) — the bind means a re-verifier who updates only the JSON now fails loudly instead of silently shipping a stale runtime clause. **⚑⚑ `socialSecurity.ts:174` — THE CENSUS IS DONE (2026-08-01) AND THE FORK IS NOW BRIGGSY'S, WITH A RECOMMENDATION. The two false claims this gap had already grown are FIXED; the gate itself is unbuilt pending his ruling.**
- **THE FULL `reVerifyEveryBuild` CENSUS of `src/engine/constants/` — five live entries, and the picture is not what the queue assumed:** `health.ts:41` `acaEnhancedSubsidyStatus` **FULLY GATED** (CI + runtime + the record↔constant bind shipped `8df242a0`) · `stateTax.ts:82` `ncRateSchedule` **FULLY GATED** (CI + the certification-pinned token) · **`socialSecurity.ts:174` `spousalRate` — ZERO GATES, the only entry with none** · ✓ **THE SECOND GAP THIS CENSUS FOUND IS CLOSED — SHIPPED 2026-08-01 (`b23a6c20`).** `health.ts:71` `acaApplicablePercentage` + `:102` `acaApplicablePercentageEnhanced` were gated by **PROXY ONLY** — riding `aca-last-verified.json`'s clock while `AcaRecord` had no `attests` field, where `StateTaxRecord` forces one under its own rule *"a hollow record is not a re-verify."* The record re-verified a legislative STATUS STRING and nothing checked the band table the engine prices every pre-65 Marketplace household with. **⏰ It was ANNUAL drift, not hypothetical: the reverted table comes from a Rev. Proc. re-issued every year and clause (ii) indexing applies to it** (the enhanced table's own note says indexing does NOT apply *there* — which is what identifies the base table as the indexed one). **Now: `attests` on both tables + a hollow-record guard in `checkAcaStatus` + two BIND arms deriving the figures from the constants and comparing them to the record's hand-typed transcription, mutation-proven in BOTH directions.** ⚠️ **THE LAYER DISTINCTION MATTERS AND THE MUTANT PROVED IT: with the shipped ceiling re-indexed 9.96 → 10.12 and the record untouched, `pnpm verify:aca` still prints OK** — the script cannot see the constant; the bind arm in `pnpm test` is what reds. Do not describe `verify:aca` as the thing that catches table drift. ⚠️ **THE ONE FAILURE MODE, named in `howToClear` step 6: re-type the attestation FROM the Rev. Proc., NEVER from `health.ts` — copying it from the constant makes the bind circular and defeats the gate.** *(Residual, deliberately not done: `AcaRecord` still has no top-level `primarySources` array where `StateTaxRecord` requires one. Each attested table now names its own `source`, which covers the load-bearing case; a top-level array would duplicate `pinTo`/`discriminatingProof`. Revisit only if a re-verify wants a machine-checked source list.)*
- **THE CADENCE QUESTION IS ANSWERED — it is not a ruling, it is a fact the module already states.** `socialSecurity.ts:6-9`: these are **STATUTE**, not year-keyed COLA figures — "stable until Congress acts". Annual-indexation drift risk is **ZERO**; the only risk is legislative and slow (a scored-but-UNENACTED 50%→33% phase-down over ~16 years). A 30-day ACA cadence here would be absurd. **Annual is the defensible ceiling.**
- **⚑ THE PRECEDENT THE QUEUE MISSED, and it is the right-shaped one:** `src/engine/constants/__tests__/medicareTrend.reverify.tripwire.test.ts` — a dated wall-clock re-verify TEST needing **no JSON record, no script, no `package.json` row, no CI edit**; it rides `pnpm test`. Its own header draws exactly the distinction this item was said to need a ruling on: *"A dated wall-clock re-verify gate, NOT `reVerifyEveryBuild`: a KNOWN annual calendar event … not per-build legislative volatility (the class `verify:aca` guards)."*
- **⚑ THE FORK IS BRIGGSY'S — RECOMMENDATION: CI-ONLY, IN TRIPWIRE FORM. Do NOT add a runtime withhold. Three verified reasons:** (1) **BLAST RADIUS IS CATEGORICALLY DIFFERENT** — `consumedConstants.ts:127-129` pulls the whole `socialSecurity.*` family whenever any person has `pia > 0`, i.e. **essentially every household**, where ACA's clause fires only for pre-65 Marketplace runs and NC's for one state. A stale-STAMP withhold would silence the recommendation **for everyone** with the law unchanged — alarm-when-fine at maximum scale (insight 101), and the cardinal rule cuts both ways: refusing to speak because a calendar lapsed is its own dishonesty. (2) **THE RUNTIME MIRROR IS NOT CHEAP** — ACA's clause reads its date from INSIDE the constant's value (`oracleToken.ts:163`), but `spousalRate.value` is the bare number `0.5` (consumed at `socialSecurityBenefit.ts:186`, pinned `toBe(0.5)`), so a runtime clause needs a NEW date carrier: a value-shape change that breaks both, or a new optional field on `Sourced` — **no precedent either way.** (3) **THE TRIGGER IS NOT A CLOCK** — enactment would be loud, public and years-phased; what a stale check actually risks is overstating benefit income, real but slow and human-detectable. **A runtime clause stays fully additive later, so the recommended branch forecloses nothing.** ⏱️ Recommended branch ≈1 hour of pilot work; the other branch ≈4× and touches the three gate-list doc surfaces whose "all six `verify:*` run in CI" claim just closed on 2026-07-31.
- **⚠️ THE FALSE-CLAIM TRAP FOR WHOEVER BUILDS IT: a dated tripwire catches a MISSED RE-VERIFY, it does NOT detect an enactment.** Any comment, test title or doc line saying the gate "catches enactment at build time" is the same defect in new paint. Word it as *"forces a human re-verify at least annually."* ✓ **The two instances of exactly that falsehood are already corrected** (`constants.shape.test.ts`'s failure message, which passed while lying, and `ss-computation.md:80`, which stated it as a shipped ruling).
- **Two mechanism notes so nobody re-derives them if the runtime branch is chosen:** a new `WithheldReason` kind does **NOT** break typecheck (`recommendationView.ts:263` has a `default` arm failing closed to `copy.recHoldGeneric`), and a plain `recHoldSs*` string is **not a slot**, so it never reaches `copyGuard.test.ts:292`'s exhaustiveness — only slot FUNCTIONS do. It would still owe the `recHold*` control-scoped sweep.

**THE ORIGINAL FILING, kept for its mechanism:** `verifiedOn: '2026-07-26'` is hand-typed in **two independent places** — `aca-last-verified.json:2` and `src/engine/constants/health.ts:35` (plus prose at `health.ts:34`) — and the 30-day window in **three** (`json:5`, `solver.ts:70`'s `solverAcaFreshnessWindowDays = sourced(30,…)`, and the `?? 30` fallback at `verify-aca-status.ts:39`/`:65`). **NOTHING BINDS ANY PAIR** — `scripts/__tests__/verify-aca-status.test.ts:5` uses its OWN fixture (`'2026-06-04'`), never the shipped record. **THE TWO COPIES FEED DIFFERENT CONSUMERS, WHICH IS WHAT MAKES IT SILENT:** CI's `verify:aca` reads the JSON; the RUNTIME withhold (`oracleToken.ts:179`, `acaCheckAgeDays > solverAcaFreshnessWindowDays.value`) reads `health.ts`. **So a re-verifier who updates only the JSON ships green CI, and a month later the product silently withholds the recommendation from every pre-65 Marketplace household with every gate green and nothing naming the cause.** ⏰ **This is dated, not theoretical: ACA reds 2026-08-25, so the record gets touched within 24 days.** ⚠️ **THE TRAP: a new binding arm takes the suite to 3189 and reds `verify:doc-stats` unless README + roadmap are re-stamped in the SAME commit** (`verify-doc-stats.ts` counts the live suite and runs BEFORE `pnpm test` in CI). **Ride-along candidate, same class:** `socialSecurity.ts:174` carries `reVerifyEveryBuild: true` with NO record and NO gate at all.

**STATE — 2026-08-01 AT SQUEAKY, EVERY FIGURE RE-RUN BY HAND ON A CLEAN TREE (nothing copied):** **3194 tests / 163 files** · **bundle 259.7 KiB ≤ 300, measured on a FRESH `pnpm build`** (insight 057: the gate reads `dist/` WITHOUT rebuilding — a stale dist is a false green, and this has bitten twice) · typecheck **0** · lint **0** · `verify:doc-stats` OK at 3194/163 on BOTH tracked surfaces · `verify:aca` OK (reverted, verified 2026-07-26, window 30d) · `verify:state-tax` OK (NC 2026-09-01, PA + FL 2027-07-15). **⚠️ `verify:fit` and `verify:csp` were NOT re-run locally this pass** — they need a dev server + real Chromium and no code changed after `292cf9bd`; they are green via **CI 30681007801 on HEAD**, which runs all six `verify:*` scripts. Stated rather than claimed, per "if you can't verify it, don't claim it."
- **⚑ THE COUNT MOVED 3188 → 3194 AND THE PROSE STAMP WAS THE THING THAT ROTTED, NOT THE DOCS.** README + roadmap were already correct at 3194 (re-stamped by `8df242a0`/`e7d2ee78`, which added the arms); only this queue's STATE line still said 3188. **The gate caught nothing because the gate does not read this file** — `verify:doc-stats` tracks README + roadmap only. Exactly the landmine already filed one section down: *a number that lives only in prose rots silently.* Re-derive it at every squeaky; never carry it forward.

**WHY THIS SESSION HAPPENED: the prior queue's first instruction was "⚑ CHECK CI ON `8d4d4e58` AND `d2f4782c` FIRST." Both were RED**, on TWO DIFFERENT nondeterministic arms, and the squeaky commit above them was green by luck. Nothing in the repo said so.

- **✓ `92c18a59` — the no-auto-write sweep stopped racing a chunk resolution.** Run 30599862965 red the U17 §S5 control at **63 turns against a 60-turn sweep** — the sensor working exactly as designed, announcing the sweep beside it had gone vacuous. **The filed reason the fix was "unavailable" was FALSE in four places** ("warming the session would create the database"): importing `vaultSession` resolves a MODULE and opens nothing (`vaultSession.ts:19-20` bare bindings, open in `getDb()` `:23-25`). Fix = **denomination, not a number**: resolve the chunk in `beforeEach`. Measured **7/8/9 turns → 1**; margin ~7× → 60×. FLUSH_ROUNDS untouched.
- **✓ `707b75ae` — THE SURVIVOR-DOOR FLAKE WAS A MOCK BYPASS, NOT A TIMEOUT.** Five-plus sightings, three prescriptions, all three adjusted a clock. `vi.mock('../RecoveryFlow')` was BYPASSED and App mounted the **REAL** component, which has no `data-testid`, so the wait could never succeed. App.tsx imported each lazy chunk from TWO sites; Vite gives a module ONE mutable callstack array (`module-runner.js:1214-1218`); Vitest push/splices the mocked id around an await (`startVitestModuleRunner…js:393-403`, its OWN line-395 warning names the case); a second arrival trips `isSelfImport` → `_vitest_original` (`:560-563`); `React.lazy` caches it forever. **Fix: one memoized loader per chunk**, `.catch` clearing the slot so a failed warm still leaves the click a retry (`ErrorBoundary.tsx:2-8`'s offline-survivor case).
- **✓ `edd4cf91` — the store's no-auto-save law concluded from silence it never earned.** The chain lands in **1 turn**; the old `flush()` was 1 turn ⇒ **zero margin**, and THREE of five conclusions spent none. Proven blind: with a real auto-save planted in `memoryModel.update`, **checkpoint 1 passes while the write is in flight**. Now carries a MEASURED CONTROL (TIMING) beside the retitled DETECTION control.
- **✓ `b743ecc6`** — the `assumptionPanel.test.tsx` path was cited in a case Linux does not have. **✓ `200128b5`** — three doc surfaces listed the gates and all three omitted `verify:state-tax`; README's block is headed *"all run in CI"* while `README.md:78` already named the gate, so the file contradicted itself. **✓ `8556960e`** — the `datesplit` crowns were wrong in SIX places, one labelled "Engine-proven".

**⚑ THE PROCESS FINDING THAT OUTRANKS ALL OF THEM — THIRD CONFIRMATION, NOW A LAW.** A 5-prescription verification fleet returned **ONE executable**. Combined with the two prior audits (6-of-11 and 3-of-5 wrong), **the base rate for a filed prescription in this queue is roughly 25-40% executable as written.** The failures are never sloppiness — they are anchors that drifted, mechanisms the code does not have, and edits that would write NEW false claims. **Never execute a filed prescription without opening every line it cites. Budget verification time as if the prescription were unwritten.**

**⚑ FOUR FILED PRESCRIPTIONS WERE VERIFIED AND FOUND NOT EXECUTABLE — corrected here so nobody runs them as written:**
- **The `"safe either way"` copyGuard ban — the anchor is wrong and the diagnosis is overstated.** `copyGuard.test.ts:440` is `] as const`, closing a `deltaSlots` array of slot KEY NAMES in a require-hedge test — a phrase there fails typecheck or is a silent no-op. The real anchor is the `mustCatch` corpus at **`:516-577`**, where the veto's other half already sits at `:526`. And "nothing bans it" is half false: **`copyGuard.ts:174` already catches the UNCONTRACTED form** (`are safe`); the hole is the CONTRACTED `you're safe either way` — which is exactly the string `docs/product.md:102` records as vetoed. **The class fix (recommended over the token fix): widen `:174` to `/\b(is|are|'?re|will be|'?d be|stays?|remains?) (secure|safe|locked)\b/i`** — scanned against all runtime copy, ZERO hits. **⚠️ ORDERING TRAP:** landing the widened `:174` FIRST makes the phrase-literal arm vacuous (it stays green with the literal deleted). Land the literal, prove its arms red, THEN widen. Also: `recommendationView.test.ts:197` is a DEAD assertion — `:196`'s `toBe` subsumes it, so the veto's other half was never really held there either.
- **The insight-086 comment fixes** — retargets are right but the package writes a wrong line number as it lands (edit 2 shifts `deriveSeedB` and edit 3 cites its pre-shift line), and its "last surviving pointer" rationale is stale (S5 shipped; `savedRecommendationMint.ts` is live at `IntakeApp.tsx:428` and stores no seed). Also FALSE as filed: "no shape test reads these files as TEXT" — `constants.shape.test.ts:772` walks ALL of `src/` via `walkTs(SRC)`. The edits survive it, but the stated reason does not.
- **The TODO grep-citation fix** — the underlying finding holds (the real counts are "full lifestyle" 2, "whole budget" 0, "all-in" 3, **all comments and test titles, zero user-facing copy**), but the prescribed replacement plants a self-contradiction INSIDE the line it edits: it asserts the band now names both levels while item (b) forty words later still calls the chart essentials-only. `cold-read-log.md:29`'s version was always correct — it scoped the grep to the captured landing copy; the TODO transcription dropped the scope.
- **The devSeeds tier fix** — executed, but NOT as filed: the prescription would have written new false provenance claims. Shipped with measured numbers only (`8556960e`).

⏭️ **THE PRIOR LIVE QUEUE (2026-07-27 late — U17 S0→S5 SHIPPED; the S5 RESIDUE SWEPT; **S6's BUILD IS DONE**, its cold-read chairing is what remains; S7 DEFERRED BY BRIGGSY'S RULING).**

**✓ U17 S6 — THE BUILD LANDED, four commits: `7e113b6e` (the `?vault=datearrived` plant + its five arms) · `58076ba6` (the aged band's first real-browser x-axis arm + the record's first trip through disk) · `c55913b0` (the Caddie walk's chartless-arm fix) · `65b306e7` (the App.test flake re-fix). 3174 tests / 163 files · verify:fit 52/52 · bundle 259.1 KiB on a FRESH build · typecheck/lint/doc-stats clean. CI green by explicit id on `7e113b6e`/30309791057 and `c55913b0`/30311388677.**
- **THE PLANT.** Base `dip`, aging depth 6 — the first live route to the hero's arrived arm (`dateInYearsPast`). **TWO corrections to the filed probe, both from opening source:** it lights **ONE** arrived arm, not two (floor crown 0 short-circuits at `FuckOffDate.tsx:197`, so `dateFloorCoveredPast` stays `datestale`'s); and aging `startCalendarYear` alone breaks the documented `currentAge === startCalendarYear − birthYear` invariant (`model.ts:98`), so `doctorArrivedVault` ages `birthYear` in step — measured byte-identical crowns either way.
- **THE THREE PRE-EXISTING GAPS CLOSED ALONGSIDE:** the `stale` plant's missing FAST engine-acceptance pin (which surfaced the `doctorStaleVault` defect now filed in UNFINISHED FIXES) · the saved recommendation's first real crypto/IndexedDB round-trip (the S3 gap) · the aged band's x-axis, which had **zero** e2e coverage.
- **✓ THE WALK RAN 12/12 AND THE PANEL IS CHAIRED — six cards + tape row filed in `docs/caddie/cold-read-log.md:23-51` (`aa9b2ef9`).** *(Corrected 2026-07-30: this line used to end "the cards are not written yet", contradicting `:29` fifteen lines below, which already named `aa9b2ef9` as "the S6 cards".)* **⚠️ THE BUNDLE `temp/caddie/2026-07-27-22-37-39/` NO LONGER EXISTS — squeaky cleared `temp/`.** Every card's rendered-verbatim quote is now unreproducible from the repo; where a claim was re-checked this session it was re-derived from its PRODUCER in source instead (and matched). **Do not send a builder looking for that bundle.**
- **⚑ FOR HIS EYE, FROM THE WALK — NOT A DEFECT, A RHYTHM CALL.** On `seed:datemixed` (essentials dated, full-lifestyle date NOT on the board) **all three lever doors land the chartless `no-anchor` arm** — "Change your withdrawal order", "Try a Roth conversion", "See your health cost picture" — each speaking `copy.leverPreviewNoDate` ("This comparison anchors to your work-optional date, so it needs one on the board first. Applying a change still updates the answer above."). Every line is individually honest; **three polite refusals in a row is a rhythm question no test can answer.** Captured at both viewports under the `-preview-no-anchor` suffix.
- **WHY THAT FACE HAD NEVER BEEN COLD-READ, AND IT IS A SHAPE WORTH REMEMBERING:** `driveLeverPreview` demanded a TwoFutures chart with no arm for the structurally chartless state, so the walk failed red on `datemixed` at both viewports — on correct product behaviour. **The face was never walked because the walk crashed on it, and the crash went unnoticed because the face was never walked.** A blind spot that hides its own cause. Fixed in `c55913b0` with insight 029's teeth intact (reaching NEITHER terminal state still fails red).

**✓ THE RESIDUE SWEEP — 2026-07-27, four commits, every gate green by the pilot's own hand: `d5d77191` (084's Caddie caps) · `ee7d0307` (three mis-cited pointers) · `b985d0c3` (the two surviving mutants) · `f74065cc` (the phone CLS + the phone joins the fit loop). 3166 tests / 163 files · verify:fit 51/51 · bundle 259.1 KiB on a FRESH build · typecheck/lint/doc-stats/aca clean.** It opened with a **22-agent queue audit, every claim adversarially refuted** — and the headline is the process one: **of 11 claims, ZERO were substantively false, but SIX of eleven PRESCRIPTIONS were wrong**, three of them in ways that would have failed on unmutated source or made things worse (the date-route mutant's filed recipe asserts a spy is uncalled when the hydrate effect calls it unconditionally; the phone-CLS fix named a tier that does not exist in a mobile-first codebase; the Q7a rider's obvious fix manufactures "a difference of about $0" on a real $18,000 delta). **`TODO.md`'s own law says Unfinished Fixes must be prescriptions rather than diagnoses — this sweep says a prescription earns the same source-verification a diagnosis does, and does not inherit trust from the diagnosis above it.**

**⚑ WHAT THE AUDIT CHANGED FOR S6/S7 — read before starting either:**
- **✓ S6'S PLANT IS CONSTRUCTIBLE — PROBED 2026-07-27, and the blocker in the source was a SCOPED claim that read as universal.** `devSeeds.ts` said the arrived hero "is NOT coherently mintable" (needs elapsed ≥ ≈8y vs savedAt's 2020 codec floor ≈6.5y). That is true of **`datesplit` only**, the base it was written against. **Crowns measured through the REAL date search (provisional tier): `dip` lifestyle=5 · `date` 9 · `datesplit` 10 · `dateborder` 10 · `date65`/`datenc` 0 · `datemixed` no-date-in-window.**
  - **THE ANSWER: base `dip`, aging depth 6.** Its lifestyle crown holds at **5 across every depth probed (0/3/5/6/7/8)** — it does NOT chase its own tail the way an ages-shift would — so elapsed 6 > 5 lands `dateInYearsPast`, and the search still returns a real `dates` outcome (engine-accepted). Floor crowns at 0, so `dateFloorCoveredPast` lights too: both arms arrived on one plant.
  - **The savedAt floor is NOT the bound it looked like.** Depth 6 clears 2020, and per §S0 the clocks are independent by design — `startCalendarYear` is written once at BUILD and survives every re-save, `savedAt` is the LAST save — so "built 6y ago, saved since" needs no fudging. It is also a genuine exercise of the two-clock split S0 shipped.
  - **The base MUST be stateless, and that is the real bound the spec was reaching for.** `datenc` rejects at any depth ≥1 — *"startCalendarYear precedes the priced state rate schedule"* (`simulate.ts:640-643`). The spec's mandated `earliestPricedRateYear` probe is **vacuous on a stateless base** (the gate is `isPricedState`-gated); the honest probe is the date search itself.
  - **⚑ ONE CALL FOR THE BUILD, not a blocker:** `dip` is THE HARD-GATE SEED (the non-monotone ladder + an applied Roth conversion, U10's cold-read surface), so an arrived plant on it carries the dip too. Richer surface, or a confounded cold read? Decide before minting; a purpose-built stateless base with a small lifestyle crown is the alternative.
- **Q7a's gating premise is FALSE.** The spec ships it gated on the two dollar dialects "never co-rendering." They already do, twice: inside `ariaSummary` as ONE string (`copy.ts:2194-2195`), and **visibly** — `RecommendationSurface.tsx:431` prints the grouped hero while `:465` prints `skew.medianQuote` through `formatAbsoluteDollar` (`recommendationView.ts:547`), which diverge above $1M. **And the obvious fix is a trap:** deriving the delta from ROUNDED endpoints re-creates the absurdity `recommendationView.ts:366-379` exists to prevent (`deltaCollapsesToZero` tests the RAW delta), and the naive `rendered_B − rendered_A === rendered_delta` test passes it vacuously (0 === 0).
- **Q7b has no spec.** Its entire body is one line ("Reorder only"), no document names what moves, and it is NOT reorder-only: `no-pretax` is structurally unreachable pre-pick at TWO independent sites (`solveDispatch.ts:62-63`, `memoryModel.ts:692-695`), and the rider would delete the drive path `vertical-fit.spec.ts:353` depends on. **Defer, or amend the spec first — it is not buildable as filed.**

**STATE — 2026-07-30/31, EVERY FIGURE RE-RUN BY HAND AT SQUEAKY (nothing copied):** **3187 tests / 163 files** · **verify:fit 52/52** · **bundle 259.8 KiB ≤ 300 on a FRESH `pnpm build`** (the insight-057 landmine: the gate reads `dist/` without rebuilding — this figure was taken after a real build, and it MOVED from 259.1 on the copy added this session) · typecheck / lint / doc-stats / aca / state-tax all clean. The session's arc, all pushed: `e4d858b6` (queue audit) → `d8f35d8c` (Roth door) → `72d638d9` (datemixed refusal) → `d5e7b466` (arrived premise) → `bd94d78e` (queue) → `7e131330` (record plants join the door walk) → `f1e31826` (band caption layer 1 + the council digest row) → `8d4d4e58` (**THE FLIP**) → `d2f4782c` (both spending levels). **CI verified GREEN by explicit id through `f1e31826`; `8d4d4e58` and `d2f4782c` were pushed after that check and are UNVERIFIED here by construction — ⚑ CHECK THEM FIRST NEXT SESSION.**

**✓ RESOLVED 2026-07-31 BY MEASUREMENT (`8556960e`) — AND THE RISK INVERTS. Both numbers were right, at DIFFERENT TIERS.** Measured through the real search: provisional (2,000 paths) = floor 2 `confirmed-date` / lifestyle 10 **`window-edge-unconfirmed`**; final (16,000) = floor 2 / lifestyle **9 `confirmed-date`**. The live surface runs FINAL (`IntakeApp.tsx:278`, `Result.tsx:275`/`:289`; counts pinned `dateSearch.ts:79`), so 2026+9 = the rendered "around 2035" — **and the band owes NO edge hedge on the shipped surface**, the opposite of what the note below feared. **THE REAL FINDING IS A TRANSIENT NOBODY HAS COLD-READ:** during entry the provisional tier genuinely returns `window-edge-unconfirmed`, so the hedge ("That sits at the edge of what this window can confirm", `copy.ts:2245`) **RENDERS AND THEN WITHDRAWS** when final lands. That is a live cold-read surface and belongs on his list. Six stale copies of the old crown pair were swept in the same commit — one of them labelled "Engine-proven". *(The original note is kept below for its reasoning.)*

**⚑ THE ORIGINAL NOTE, KEPT (2026-07-31):** `devSeeds.ts:1403` records *"`datesplit` crowns lifestyle at 10"* and the S6 scout repeated it, but the LIVE surface renders *"about 9 years out — around 2035"* with the work-stops marker at ages 67/68 (= 58+9). The rendered household is self-consistent, so this is a stale/mis-measured COMMENT rather than an engine defect — **but it matters for the next build**, because a crown at 10 would be `window-edge-unconfirmed` (`decideTrack:338`, the weakest-evidence crown the rule can emit) while 9 is a confirmed date, and that changes whether the band owes the edge hedge. **Re-measure through the real search before trusting either number.**

**PRIOR STATE — 2026-07-27 LATE (kept for its stamp trail):** **3174 tests / 163 files** · **verify:fit 52/52** · **bundle 259.1 KiB ≤ 300 on a FRESH `pnpm build`** (the insight-057 landmine: the gate reads `dist/` without rebuilding) · typecheck 0 errors · lint / doc-stats clean. The session's arc, **all CI-green by explicit id where the run completed**: `7e113b6e`/**30309791057** (the arrived plant) → `58076ba6`/**30310885497 = FAILURE**, the App.test flake — diagnosed and re-fixed, see the wrong-clock entry below → `c55913b0`/**30311388677** (the Caddie chartless-arm fix) → `65b306e7`/**30311949599** (the flake re-fix, GREEN) → `2e10af91` (queue) → `b449bc09` (insight 106) → `ed6432cd` (datearrived joins the door walk) → `9249e4d4` (the Roth prescription) → `aa9b2ef9` (the S6 cards + tape row). *(This line names the commits it was written IN, so it lags HEAD by construction — a property, not drift. Read it as "verified through", never "HEAD is".)* **⚑ CHECK CI ON THE SQUEAKY COMMIT ITSELF next session — it is pushed after this line is written and is therefore unverified here by construction.**

**PRIOR STATE (2026-07-26, kept for its stamp trail):** the 2026-07-26 arc is `65710561` (ACA re-verify) → `c02e3f53` (the health sheet's calendar) → `61e1add2` (this queue), **all three green by explicit id: 30206572897 · 30207087996 · 30207250072** — and the two docs commits that followed are green too: `2fb6cd80`/**30207535152** and `693c9425`/**30209087002** (HEAD). *(This line names the commits it was written IN, so it lags HEAD by construction — that lag is a property, not drift. Read it as "verified through", never as "HEAD is". It was **two** behind when re-audited 2026-07-26.)* Earlier ids: 30163571502 (S3), 30170575831 (S4, after its own 30169928294 = FAILURE — the App-probe mock leak fixed in `bd4ebcf8`), 30136387827 (S1), 30137920900/30137942030 (S2). **Every figure re-run by hand 2026-07-26, after S5 step 14:** **3164 tests / 163 files** (3070/159 before S5's steps 1–5; 3114/161 after step 5; the +1 over the 3163 stamped on 2026-07-26 is the record card's re-homing, net of the arms that moved out of `RecommendationSurface.test.tsx` — README + roadmap already carried 3164 and `verify:doc-stats` is OK against the live suite) · **fit 50/50 — ALL green, no `test.fail()` left in the harness** (was 48/48 = 47 green + 1 declared `test.fail()`, the filed record-card fold breach; Briggsy's 2026-07-27 placement ruling closed it and the annotation was removed as it was written to demand — the arm now asserts the law positively on BOTH plants, `?vault=rec` and `?vault=recold`. The +2: that split, plus a new arm pinning the beat-beside-the-record grid seats — a state no harness can DRIVE, since a live solve is 80–200s, so the beat is injected) · bundle **259.1 KiB** ≤ 300 (measured on a FRESH build — see the landmine) · typecheck/lint/aca clean. **`verify:doc-stats` was RED before step 14** (README + roadmap still claimed 3114/161 while steps 11–13's two new test files had already landed) — both surfaces re-stamped in that step. **S5's build order is [`docs/plans/features/act4-u17-s5-execution-plan.md`](docs/plans/features/act4-u17-s5-execution-plan.md)** — 14 steps, derived from a six-seam scout and rebuilt TWICE under adversarial review (v1 drew 5 P0 / 5 P1 / 9 P2 across three lenses; v2 drew 2 P0 / 8 P1 / 9 P2 across four, all four returning `holds=false`). Steps 1–5 landed as `173c4997` (substrate + the doc sweep), `c68afc5e` (the mint + the save-view + the CI gate), `15c5d64c` (the copy register). **`verify:doc-stats` JOINED CI in `c68afc5e`** — it had been local-only, which is exactly how `173c4997` shipped 2 tests of drift through a green run. U17's roadmap row is **in-flight** (five of the spec's **eight** stages landed — S0–S4 of S0–S7; the old "of seven" mixed two framings, S0 being substrate). **All five build stamps are now IN the spec** — S0/S3/S4 were retro-stamped 2026-07-26 after an audit found the TODO claiming stamps that only S1 and S2 had. The roadmap's U17 row was re-stamped in the same pass (it still read "S0–S2 SHIPPED", and per project precedence the roadmap wins on build status — so it was the surface that had to be right).

**✓ ACA RE-VERIFIED 2026-07-26 (`65710561`) — ⏰ MAIN GOES RED **2026-08-25 (UTC)**, and the LAST FULLY-SAFE DAY IS **2026-08-24**. *(CORRECTED 2026-07-30 — this line previously said "RED on 2026-08-26", one day late, in the UNSAFE direction. Computed, not recalled: `verify-aca-status.ts:39-43` is a strict `ageDays > (maxAgeDays ?? 30)` against `Date.parse('2026-07-26')` = UTC midnight, so the threshold is crossed at `2026-08-25T00:00:01Z` — the whole of the 25th is red but its first second. **For Briggsy in US Eastern that is 2026-08-24 20:00 local**, so a local `pnpm verify:aca` fails on the EVENING OF THE 24TH. A pilot who trusted the old sentence and scheduled the 25th walked into a red main.)* The record lives at the **repo ROOT** (`./aca-last-verified.json`), NOT `src/engine/constants/` — `CLAUDE.md` says otherwise and is wrong (`verify-aca-status.ts:49` is `join(process.cwd(), …)`). The REAL flip window is 2026-12-04.**

**⏰ CORRECTED 2026-07-31 — THE NEARER GATE IS ACA, NOT NC. THIS LINE SAID THE OPPOSITE AND WAS FALSE BY SEVEN DAYS.** Computed by hand from the records, not recalled: ACA reds **2026-08-25** (`aca-last-verified.json` `verifiedOn: 2026-07-26` + `maxAgeDays: 30`, against the strict `ageDays > window` at `verify-aca-status.ts:39-41`); NC reds **2026-09-01** (`state-tax-nc-last-verified.json` `nextDue`, against `nowMs > dueMs` at `verify-state-tax.ts:98`). **ACA is SEVEN DAYS NEARER and always was** — NC could only lead if ACA's `verifiedOn` were after 2026-08-02, which it has never been. A pilot who trusted the old sentence scheduled the wrong record first and walked main into red. PA and FL are `2027-07-15`, no near-term exposure.
- **⚑ NEITHER GATE IS MERELY CI, AND THE QUEUE NAMED ONLY THE CI HALF.** Both have a SECOND, runtime enforcement layer that withholds the recommendation from real households. **ACA:** `oracleToken.ts:178-198` — `acaCheckOverdue` compares `acaEnhancedSubsidyStatus.value.verifiedOn` against `solverAcaFreshnessWindowDays`, and `evaluateAcaFreshnessClause` returns `{kind:'aca-unverified'}`, surfaced at `recommendationView.ts:258`. `solver.ts:67` names it outright: *"one calendar, two enforcement layers."* So a stale ACA record does not just red main — it withholds the recommendation from **every pre-65 Marketplace household**. **NC:** the withhold rides `ncRateSchedule.directionalUntilPinned` (`stateTax.ts:72`/`:77`) into `oracleToken.ts:98-103`, is **LIVE TODAY at zero days out, and NO `nextDue` bump clears it.** ⚠️ **The trap:** bump the NC date, watch CI go green, and believe NC households were unblocked. They were not. Only the ~Aug-2026 certification pinning the rate clears that token — Briggsy's own state.
- **⚠️ UNGUARDED DRIFT SEAM (found 2026-07-31, unfiled before):** the ACA `verifiedOn` is hand-typed in **two** places (`aca-last-verified.json:2` and `health.ts:35`, with a third prose instance at `health.ts:34`), and the 30-day window in **three** (`json:5`, `solver.ts:70`, and the `?? 30` fallback at `verify-aca-status.ts:39`/`:65`). **No test binds any pair.** A re-verify touching only the JSON ships green CI over a stale product-side clause; a record that loses `maxAgeDays` still passes at 30 days. ⚠️ Adding a binding arm reds `verify:doc-stats` unless README + roadmap are re-stamped in the SAME commit (`verify-doc-stats.ts` counts the live suite and runs BEFORE `pnpm test` in CI). *(~~And `verify:state-tax` is missing from BOTH doc tables… the docs under-claim it.~~ **✓ CLOSED 2026-07-31.** All THREE surfaces now name it — `README.md`'s gate block, `docs/roadmap.md`'s gate table, and `CLAUDE.md`'s Commands table, which also gained the `verify:csp` row it was missing. **The README omission was the worst of the three**: its block is headed *"Quality gates (all run in CI):"*, which asserts completeness — and `README.md:78`'s own narrative already listed "state-tax-freshness" among the gates, so the file contradicted itself. Verified at execution: all SIX `verify:*` scripts in `package.json:18-23` really do run in CI, in order, at `.github/workflows/verify-the-back-nine.yml` **in the MONOREPO ROOT** — the project has no `.github/` of its own, and the workflow says so at its own `:11`. There are zero local-only gates today.)* Status stays `reverted`, now over-determined by primary sources rather than a news read. **The re-verify found a FABRICATED CITATION IN OUR OWN RECORD:** the 2026-07-03 entry confirmed itself against "IRS FS-2026-19", which is a 404 (the 2026 fact-sheet series runs FS-2026-01..12) — an invented document *lineage*, not a wrong number, so `statusConfirmed:true` was partly resting on nothing and the next reader would have assumed link rot. Struck, with the strike recorded in the file so it cannot return. Three more substantive corrections landed in the record: the old proof (9.96%) was **non-discriminating** (it is published identically in BOTH regimes — DND-012's own failure mode inside the record that cites DND-012; the discriminating fact is the table's SHAPE — no row above 400%, floor 2.10% not 0.00%); the Senate has **never voted** on H.R. 1834 (S 3385's cloture failure PREDATES it by four weeks — parked-and-available, not spent); and retroactivity is **drafted in**, with a condition we had omitted entirely (§36B(c)(2)(A) pays only for months a premium was actually PAID — a household our own honest "coverage costs more now" message pushes off the Marketplace recovers NOTHING). **`aca-last-verified.json`'s `howToClear` now carries the five-step chain that works** — follow it, and never write a citation you have not opened. Forward clock lives in that file: FY2026 reconciliation is spent (P.L. 119-98, clean); FY2027 (H.Con.Res. 113) instructs no tax-writing committee, so §36B is not in the 51-vote lane as passed; the December 4 CR expiry is where a PTC extension actually gets traded.

**⚑ STILL OPEN from the re-verify (filed, not done):** the uncapped excess-APTC clawback (P.L. 119-21 §71305 struck §36B(f)(2)(B) for TY2026+) **— ⚠️ THE FILING BLAME WAS WRONG, corrected 2026-07-26.** It is recorded at `aca-last-verified.json:21` under the key **`adjacentButSharp`**, whose own last line already rules *"Do not file this as merely 'adjacent'"* — **the record is right; the gap is downstream.** (a) **Nothing consumes the field:** `verify:aca`'s `AcaRecord` interface (`scripts/verify-aca-status.ts:18-25`) does not list it, so it is inert prose the gate never reads — insight 074's class. (b) **The copy gap is NARROWER than "a near-cliff downside line", which ALREADY SHIPS** — the "Roth conversions in those years" fact card prices a converted dollar as tax PLUS the burned discount, with the odds of tipping past the line beside it. What is genuinely absent is different in kind: the **mis-estimated-year REPAYMENT** exposure — with the ceiling struck, a household that under-estimates income owes the whole excess back, and **neither pending bill restores the cap**. Decide whether the conversion guidance owes THAT line. *(Statutory clauses here are UNVERIFIABLE from this repo — they rest on the record's own authority, and this same file carried a fabricated citation one cycle ago.)* Also unguarded: **`socialSecurity.ts:174` carries `reVerifyEveryBuild: true` with NO last-verified record and NO CI gate at all** — a re-verify flag with zero enforcement.

**✓ THE U17 PRE-BUILD COUNCIL — RATIFIED (wf_f4ced3c8-2f6, 23 agents, ZERO crashes, 8/10 high, `action: execute`; `docs/council-log.md` top row carries the full rulings).** The executable shape is **`docs/plans/features/act4-u17-saved-recommendation-build-spec.md`** — it SUPERSEDES plan 4's §U17 where they conflict. **HAWK VETO fired + HONORED into the shape (three arms):** no future-tense named marker left of Today · no wall-time re-base of the Roth start on EITHER side (persisted or presentation-only) · no re-basing the band's percentile GEOMETRY (it redraws resolved outcomes as today's known balance). **THE RED TEAM BROKE FOUR RULINGS, every seat conceded IN SOURCE:** (a) `solverRunFingerprint.ts:31-34` EXCLUDES `consumedConstantEntries` — **constant-blind by design** — so a bare fingerprint match would re-present a saved conversion under superseded brackets *with a proof attached*; the no-clock-fired + `SOLVER_CODE_VERSION` conjuncts are now BINDING. (b) the clock finding (below). (c) `recommendationView.ts:436-448`'s deliberate dialect split would ship per-bar figures that subtract to something other than the delta hero → **Q7a is a GATED rider**. (d) five staleness clocks are bare vintage compares → naming them to an unexposed household is alarm-when-fine (insight 101 inverted). **The chair's law worth keeping: _a test that pins a defect is the defect's second copy, not evidence_** — `bandAnnotations.test.ts:170-184` pinned the work-stops placement as intended, and the spec REWRITES it rather than deferring to it.

**✓ U17 S0 — SHIPPED (`102416af`), the aged surface's two-clock lie closed.** The band called the BUILD year "Your save": `elapsedPlanYears` derived from `startCalendarYear` (written ONCE at `memoryModel.ts:522`, untouched by every re-save) while the label claimed the save — false for any re-saver, and contradicting the fresh "Saved to this device" badge on the same screen. Four separate comments already forbade it; one surface did it anyway. **Shipped:** `offsetHasPassed(planOffsetYears, yearsSincePlanBuilt)` exported ONCE from `curveMarks.ts` (strict `<`) with **THREE** consumers bound + source-bind-pinned — the ladder filter, the hero crown-arrived withdraw, and `dateTradeoff.ts`'s offer filter (**a third re-typing the spec had not enumerated**); the field renamed `yearsSincePlanBuilt` everywhere with zero orphans; `bandClockSavedLabel` → `bandClockBuiltLabel` naming the build year outright, with BOTH renderers moved in the same change (the fan AND `twoFuturesChrome.ts` — insight 086); `planClockSeam.test.ts` is the re-saver witness the suite could never have had (every aged fixture ages both clocks in lockstep — `doctorStaleVault` moves `savedAt` −760d AND `startCalendarYear` −2y deliberately, so the bug living in their *difference* was structurally invisible; insight 073's class). **THE VERIFIER FAILED THE FIRST CUT AND WAS RIGHT TWICE — both fixed by the pilot's own hand, both mutants re-planted red and restored with Edit:** (1) the seam test had **COPIED** the mint into its own helper under a comment claiming it matched `Result.tsx` — planting `+3` in the component left **1096 tests green**; fixed with the exported `planClockAnchor` **PLUS** a source-bind arm, because calling the shared function from a test proves the FUNCTION, never that the COMPONENT still calls it (insights 032 + 081); (2) the low-end clamp was unpinned — `years <= 0` → `=== 0` stayed green because a negative INTEGER is nulled downstream by the `yearsSinceBuilt > 0` gate, so the arm never witnessed the clamp it named (insight 029); only a **non-integer negative** can tell the implementations apart.

**✓ U17 S1 — SHIPPED (`e4754134`, CI 30136387827).** The Roth echo speaks the CALENDAR YEAR on read AND write: `rothPlanStartFor` (bandAnnotations.ts) is the ONE start derivation (rides `offsetHasPassed`; both echo sites + the lever's applied re-seed consume it, source-bind-pinned in planClockSeam.test.ts); the lever's start field asks "Starting in which year" (commit converts year→offset against the build year — nothing persisted changes shape) and a past year refuses ALOUD via the R19 grammar with the earliest startable year QUOTED (`errRothStartPast` joined `SlottedErrorKey`); the fresh default seeds the WALL year; tense picked by `passed` ("started in 2025" mid-flight — never future-tense about a passed date). "Suppress when unanchored" satisfied STRUCTURALLY (savedAnchor REQUIRED on both components; unanchored unrepresentable). The model.ts "t=0=today"/"0=this year" comment roots swept (087). 3 mutants red→reverted; the tense-swap mutant caught an 081 tautology in my own first echo test (toBe against the mutated slot's own output) — literal tense regexes now pin it.

**✓ U17 S2 — SHIPPED WHOLE (`5300f155` + cleanup `0db6689c`, CI 30137920900/30137942030).** All five arms + the §S2.5 ruling, dated stamp in the spec: (1) a PASSED crowned offset **withdraws at the array** (`offsetHasPassed(crown, clock) || crown === clock` — the equality arm is Today's own column, the fresh offset-0 precedent generalized; fresh derivation byte-identical); the defect-pinning test REWRITTEN carrying the spec + council row. (2) the elapsed segment **demotes under a second STATIC luminance mask** nested in the cohort-fade group (`ElapsedDimGroup`; `elapsedFadeStops` + `ELAPSED_DIM=0.45` in bandGeometry beside their cohort siblings; `elapsedYearsWithin` in bandData is the ONE numeric domain gate — ui's `planClockWithin` delegates; fresh DOM byte-identical, no wrapper at all). (3) the aged fan ships **ONLY beside the premise line + the RENDERED inline re-confirm control** (insight 100; the fod-retry pattern — the first block-CTA cut breached the 791 fold by 5px, MEASURED at his real viewport; the inline control + tightened premise land at **41.7px slack**; laptop tier uses the WCAG 24px min-height, phones keep 44); no `onReconfirm` ⇒ the fan withdraws structurally in BOTH heroes' resolved memos; residual spoken UNDETERMINED. (4) **§S2.5 RULED (pilot, dated):** the arrived idiom is the STRICT three-way split — `dateInYearsPast` retexted "that year has already come and gone", NEW `dateInYearsNow` carries the old "about now" text at its one true boundary (agreeing with the ladder's "stopping today" crown), floor mirrors (`dateFloorCoveredNow`/`Past`); hero + floor consume `offsetHasPassed`, and the reentry.test arrived pins were rewritten to the strict contract. (5) on a SPLIT the marker names WHICH date (`bandClockWorkStopsSplitLabel` "Essentials date" + a11y sentence; split-ness via `composeDateSplit` — the renderer's own producer). Ride-alongs: `BandSavedAnchor → BandPlanClockAnchor` across all 11 sites (the S0 filing closed); verify:fit grew premise/control/mask pins on BOTH aged-vault arms (42/42). 4 mutant families red→reverted. **LIVE-VERIFIED in real Chromium at 1536×791:** the stale spine frame (premise + dim + re-based axis in-frame, disclaimer at 749) and the datestale date frame (anchored hero 2033 · the floor's NOW arm · work-stops correctly absent · premise + dim).

**✓ U17 S3 — SHIPPED (`374299c9`, CI 30163571502).** Q3's trichotomy + Q5's record, plus both carried persistence prescriptions. **THE RECORD CARRIES ITS OWN ERA** — the ruling the spec was silent on and the reason the stage is correct: `scenarioFromDraft:56-60` re-mints all five vintage stamps at EVERY save, so a plan re-saved after a bracket change reads "no clock fired" while its remembered verdict was priced under superseded rules (the constant-blind fingerprint hole one level up). `src/store/savedRecommendation.ts` snapshots the era and runs the **same** `deriveStaleness` against THAT — one comparator, two subjects, overlay typed `Required<Pick<ScenarioV3, keyof SavedRecommendationEraV3>>` so dropping a key is TS2741, not a green suite. All five era fields **REQUIRED** (the scenario's absent-is-quiet contract has a real population — a pre-U13 vault predates the stamps; a record born at U17 does not, so absence pinned `rulesMoved:false` forever = fail-OPEN). `!==` not `<` on the solver version (a NEWER-build record is equally un-re-presentable; solverCodeVersion.ts:25's `<` prediction swept). Verdict **enumerated, never a remembered dollar**: `gradeStatistic` NOT stored (`gradeAxisFor(goal,surplusRegime)` derives it; storing it made 8 of 12 impossible triples representable), `subTenthCollapse` biconditional on `grade`, `noDollarRegister` carries what no dollar-free projection could reconstruct. **The codec's first tolerated drop** (`delete` before the cast; only `Corrupt` caught) is now REPORTED via `ScenarioDecode.droppedAtoms`, and `scenarioIdentityKey` makes the dirty compare key-order-insensitive (arrays keep order) — retiring the false `resultSave.ts:13-14` comment by removing what it justified, not documenting the accident. **PROCESS:** 4 adversarial seats found 6 real defects in the first build (the fail-open era caught **independently by two**), a re-verify pair found the write-side dead-end; 12 mutant families red→restored with Edit.

**✓ U17 S4 — SHIPPED (`0c2fe6cb` + the flake fix `bd4ebcf8`, CI 30170575831).** **The stage's content turned out to be a SHIPPED defect, not a prospective register** — the scout proved the record card cannot render at all (nothing calls the trichotomy, no seed carries a record, no fit arm covers it), so authoring its copy now would be dead code. Meanwhile `reentryChrome` was pushing ONE healthcare line off the OR-collapse of all seven healthcare clocks: an all-65+ household takes `buildOverlay`'s Medicare-only branch (`intakeMap.ts:581-583`), ships NO `enrolledPremium`, and can never open the engine's ACA gate (`taxOverlay.ts:1696-1701`) — it prices **zero** ACA and was told "Health-coverage rules have been updated." And never only copy: `healthcare.moved` fed `rulesMoved` → BOTH hero echoes AND S3's conjunct 3, so the same falsehood would have demoted a still-valid saved recommendation. **The three-way now lands ON the existing two-predicate split:** exposed ⇒ NAMES itself + `rulesMoved`; not-exposed ⇒ **SILENT** (byte-identical recompute — both "rules changed" and "worth a look" are false); unattributable ⇒ AGGREGATE under one nameless sentence, `anyStale` only. Exposure is INJECTED from the run's own built params (`src/ui/stalenessExposure.ts` — six reads, six producers, never one derived from another; the store cannot import intake). **The state-tax clock's documented KNOWN QUIET LIMITATION is CLOSED** by exactly the mechanism its own comment prescribed. Ride-alongs: `stalenessHealthcare` split to `stalenessAca` + `stalenessMedicare` with every renderer re-pointed in the same commit (086); tax gated on `overlayBuilt`; contributions on non-empty streams; blend silent for provably-inert households; `part-b-trend`'s false "no exposure gate" comment swept; a source-bind arm on IntakeApp's shipped call; an exhaustive clock→family table; and the **`rulesMoved` ⟹ at-least-one-line invariant** that structurally kills the alarm-nothing-may-explain class. **THE PILOT'S OWN GOVERNING RULE WAS WRONG AND A VERIFIER CAUGHT IT** — see insight 103 and the landmine below.

✓ **U17 S5 — SHIPPED WHOLE (2026-07-26/27; all 14 steps of `docs/plans/features/act4-u17-s5-execution-plan.md`).** `173c4997` (substrate + the doc sweep) → `c68afc5e` (the mint + the anti-lie save view + **`verify:doc-stats` JOINED CI**) → `15c5d64c` (the copy register) → `ec87622d` (the gesture, the surface, the CSS) → `a7eaf074` (the batteries + the record plants) → `4753b08f` (the placement fix). **Green by explicit id: 30213988773 · 30215270749 · 30215902089 · 30220469229 · 30224254633 · 30276663987** — then `00502669` (the queue) and **`59b1bbf6`/30279014650, the CI-only flake fix** (see the landmine). **3164 tests / 163 files · verify:fit 50/50 · bundle 259.1 KiB · typecheck/lint/doc-stats/aca/csp clean.**
   - **THE PLAN WAS REBUILT TWICE UNDER ADVERSARIAL REVIEW BEFORE ANY CODE WAS WRITTEN** — v1 drew 5 P0 / 5 P1 / 9 P2 across three lenses; v2 drew 2 P0 / 8 P1 / 9 P2 across four, all four returning `holds=false`. Both v1 P0s were fatal-by-construction: the record card mounted where every superseded arm was unreachable, and a writability gate that killed the control on the no-vault path (every dev seed, the Caddie walk, the fit gate).
   - **TWELVE MUTANTS THAT SURVIVED REVIEW NOW ALL DIE** (replanted one at a time against the FULL suite, restored with Edit). The cardinal-sin pair: the `saving` arm rendering the saved badge, and the trichotomy reading the DRAFT instead of the DISK — the latter's failure output literally painted "It still matches your plan as it stands today" over a write that had just failed.
   - **A SHIPPED U16 ASSERTION WAS VACUOUS AND IS REPLACED:** `queryByRole('button', {name: /save/i})` could never have caught the control it guarded — `recommendSaveCta` is "Keep this strategy read" and contains no substring "save". Every save-control assert now pins the exact accessible name.
   - **THE REFUSAL COPY INVERTS THE ORIGINAL OBLIGATION, and the guard proves it.** Filed obligation 2 said a dropped record must report "we saved your plan, we could not save the recommendation" — but the gesture RETURNS before the store is touched, so on both routes nothing was written. That sentence is planted as a mutant and reds by name.
   - **THE CARD'S PLACEMENT IS BRIGGSY'S RULING (2026-07-26), ON MEASURED NUMBERS.** It sits BELOW the in-frame R13 disclaimer, joining the doors as a sanctioned below-fold casualty. Measured at his real 1536×791 @2.5 DPR: the caveat bottoms at **701px** on BOTH plants — 90px inside the frame, within 1px of the record-free twin — and the producible WORST case (a 279px three-clause card) moves it **zero pixels**. Content was never trimmed. **The re-hoist mutant reds on DOCUMENT ORDER and not on pixels** (the grid row is explicit), so the arm asserts `compareDocumentPosition` AND geometry — a geometry-only gate would wave through the CSS-only reorder that breaks focus order (WCAG 2.4.3). The `test.fail()` breach filing is RETIRED exactly as designed.
   - **THE REFUSAL CARD DELIBERATELY DID NOT MOVE** — it is the rendered outcome of a tap (insight 100), belongs beside its control, and cannot appear on an idle frame, so it never competes for the fold.

**⚑ NEXT, IN PRIORITY ORDER:**

0. **⚑⚑ THE ESSENTIALS MEDIAN LINE — DEMOTED 2026-07-31 FROM "THE ONE LIVE BUILD" TO A COLD-READ QUESTION. RECOMMENDATION: DO NOT BUILD AS SPECCED. Briggsy's call — he asked for it explicitly 2026-07-30 ("yes, draw the essentials line too"), so this is pushback on his own request, filed with the evidence.**
   - **⚑ THE DEMOTION'S THREE REASONS, each verified in source 2026-07-31 (read-only fleet, every anchor opened):**
   - **(1) THE LINE HAS NOTHING TO DRAW ON A CROWNED BAND — an engine proof, not taste.** `dateSearch.ts:522-527` establishes that a crowned band's `outcomeState` is in `{on-track, over-funded}`, and `:577` builds the band from it. On-track means the quantized lower bound cleared `BANDS.onTrack` — ≥85% of paths never deplete — which puts the 50th percentile squarely among the survivors. **So the full-budget median NEVER dives to $0 on any band the date route draws.** The decision-relevant moment a second line exists to show — the full median hitting the floor while essentials survives — is STRUCTURALLY UNREACHABLE on the crowned arm. What ships instead is a second line in the upper half of the plot, above a line that never fails, restating "less spending leaves more money."
   - **(2) THERE IS NO FOURTH VISUAL CHANNEL. The accent budget is worse than this item claimed.** Vermilion is spent in **THREE** places, not one: `OddsLadder.tsx:301`/`:303` (the crown ring + dot, pinned as crown identity by `colorblind.test.tsx:275-278`), `TwoFutures.tsx:328`/`:334`/`:482`, and `RecommendationViz.tsx:101`/`:148`. And blue is **BANNED** as an on-band line — `palette.ts:118-121` carries the MEASURED landmine: *"a series-blue line is lost in the dark half of the band under deuteranopia; the on-band line MUST be `--ink`"* — and ink is already the median. **Both chromatic accents AND the ink channel are spoken for.**
   - **(3) "NEARLY FREE" IS TRUE OF THE ENGINE AND FALSE OF THE VIZ.** Six structural changes: a second-series field on `ResolvedBandData` (`bandData.ts:146-176`); a new parameter + parallel resample in `resolveBandData` (`:314-338`, loop `:350-391`); the `dollarMax` overlay term (`:395`); widening `linePath`'s `key: 'p50'` string-literal type (`bandGeometry.ts:116-121`); a FOURTH legend row (`bandData.ts:127-129` + `BandLegend.tsx:31-39` both render exactly three); and a fourth `BandTooltipRow` figure (`:385-390`) or the line is silently un-scrubbable. **Plus two rendering landmines nobody had written down:** a dashed data line collides with the dashed gridlines already drawn at `ConfidenceBand.tsx:164` (`strokeDasharray="2 4"`), and the new path MUST ride inside BOTH the cohort-fade mask (`:249`) and `ElapsedDimGroup` (`:250`, `:286-291`) or it stays full-strength exactly where this product de-emphasises — an honesty regression — while the median is a `motion.path` (`:273-278`) whose plain-`<path>` sibling would pop while it morphs.
   - **⚑ THE ONE ARM WHERE IT WOULD EARN ITS KEEP — the dissent's shape, not the council's.** On the FLOOR-CROWNED fallback (`datemixed`, lifestyle never dates — `dateSearch.ts:529-532`) the band draws the ESSENTIALS world and the FULL-budget track is the one that fails. A lifestyle median over an essentials band would show a line that genuinely depletes — the informative crossing, in the direction that carries information. **If his eye still wants a line after seeing the shipped prose live, build it THERE and only there.**
   - **⚑ AND IT WOULD MAKE THE SHIP GATE HARDER TO PASS, NOT EASIER.** The gate asks *whose range is the shaded area?* Today: one band, one caption, one answer. Add a second series and a reader who could not previously name the band's owner now has two candidates to disambiguate by line style. **The gate exists to detect union-reading; a second line is the thing most likely to CAUSE it.** Building pre-emptively bets against the test.
   - **✓ ALL FOUR ENGINE CITATIONS RE-VERIFIED 2026-07-31, and the seam is GREEN-LIT.** `simulate.ts:1571-1573` is verbatim as filed; `bandData.ts:395` is fan-p90-only (an overlay term really is owed); `palette.ts:42-43` confirms only two chromatic accents; `OddsLadder.tsx:301`/`:303` confirm vermilion is spent on the crown ring and dot. **A SECOND SINK BREAKS NEITHER INVARIANT, with reasons:** *CRN* — the draw schedule is dimension-only (`simulate.ts:1301`), neither decumulation consumes draws (both are handed the SAME materialised `realStock`/`realBond` arrays built at `:1515-1516`), and the floor withdrawals are draw-free (same pure `cashTermsForYear` call, `:1517-1519`). *Reduce-to-spine* — the sink is push-only in both consumers (`decumulation.ts:155`, `taxOverlay.ts:1829`) with no read anywhere, and the spread stays presence-keyed at `:1655`/`:1672`.
   - **⚠️ BUILD DISCIPLINE IF IT SHIPS (four items, from the same verification):** (a) the fold at `simulate.ts:1781-1784` MUST be duplicated for the new array with the same `t < len ? v : 0` alive-but-broke padding — **the floor track depletes LATER, so its recorded length differs from the full track's**; (b) leave the `horizon <= 0` continue at `:1461-1483` untouched (that path joins no cohort); (c) the today-anchor is safe to share (`buildBandFan` `:1079-1111` anchors year 0 at `initialPortfolio` for both tracks); (d) **write the missing regression arm** — nothing in the suite currently pins fan-on ≡ fan-off with a budget present.
   - **⚑ A QUESTION FOR HIM THAT DID NOT EXIST WHEN THIS WAS FILED:** `d2f4782c` already shipped the essentials number **in prose**, quoting him directly and naming ~$74,000 vs ~$54,000. **Is the DRAWN line still owed now that the numbers landed?** Item 0's own ship gate — the five-face cold read asking *whose range is the shaded area?* — is the right instrument to ask it, and asking costs a walk rather than a build.
   - **THE ENGINE COST IS ZERO EXTRA SIMULATION — RE-CONFIRMED 2026-07-31 with every line opened, and it STANDS. One citation corrected.** The budget gate is **`simulate.ts:1380`** (`params.budget ? new Array(paths) : undefined`), whose own comment at `:1373-1379` says it is *"param-driven, not caller-opt-in"* — **NOT `:1498`, which is the derived `withdrawalsFloor` allocation off that gate.** The floor pass runs on `if (withdrawalsFloor)` (`:1663`, and `:1743` on the spine arm), `:1770-1773` harvests ONLY the depletion year + survivor tally, and `fanTrack` (`:1390`) decides only which pass receives the sink. **Both decumulations already execute for every budget-carrying household; on the lifestyle-crowned arm we compute the floor balances and discard them.**
   - **⚑ TWO BUILD FACTS THE FILING NEVER CARRIED, if it is ever built:** (a) **the fold padding must key to the NEW array's own `.length`, never `pathBalances.length`** — `runDecumulation` pushes at `decumulation.ts:155` BEFORE the `if (step.depleted) break` at `:156`, so a track's recorded length is `depletionYear + 1` when it depletes; the floor spends `cash.netFloor ≤ cash.net` so it depletes LATER or never, making its array **≥** the full track's, never shorter. (b) **Reduce to p50 IN-ENGINE, never ship a second fan across the wire** — `fanValuesByYear` is `maxHorizon × paths` (~720k numbers per track at the final tier) and `worker.test.ts:358-360` records that the fan crosses by **structured clone, not transfer**. A p50-only series is ~45 numbers and structurally cannot be mistaken for the vetoed second fan.
   - **⚑ A REAL COVERAGE GAP CONFIRMED AND WIDER THAN FILED — worth closing on its own, independent of whether the line ever ships.** `grep -n "budget" src/engine/__tests__/bandFan.test.ts` returns **ZERO** matches, and every other fan-on test was opened: `budgetTwoTrack.test.ts:567-592` has a budget but compares fan-vs-fan (never fan-on vs fan-off); `dateSearch.test.ts:643-664` is a real reduce-to-spine arm but its `coupleInput` (`:85-99`) carries no budget; `worker.test.ts:525` is explicitly *"budget-less input"*; `survivorConditioned.test.ts:146` and `roth.test.ts:67` likewise. **Nothing in the suite pins fan-on ≡ fan-off with a budget present.**
   - **THE EXACT SEAM:** `simulate.ts:1571-1573` is `const pathBalances = wantFan ? [] : undefined` routed to `fullSink` XOR `floorSink`. **The change is a SECOND array**, so both passes write. Sinks are write-only, presence-keyed, spread into the tax inputs (`:1655`, `:1672`) and never read back — so CRN and reduce-to-spine hold by the same four proofs that cover the existing sink (draws are dimension-only `:1301`; both passes already run; write-only; pinned by `bandFan.test.ts:229-244` asserting terminal/depletion/taxAware equality fan-on vs fan-off).
   - **ONLY THE MEDIAN IS NEEDED** — not a second fan. The veto is against a second percentile FAN and was re-affirmed on measurement; a single labeled line is a different object and the Hawk re-ranked to it in rebuttal.
   - **⚠️ THE UNRESOLVED BLOCKER IS NOT COLOR MATH, IT IS THE ACCENT BUDGET.** `palette.ts:42-43` carries TWO accents and **vermilion is already spent** on the odds ladder (`OddsLadder.tsx:301`/`:303`) one scroll down. The council's "vermilion, dashed, triangle" encoding therefore reuses one accent for two unrelated meanings on one page (crown vs essentials). **Decide this in writing before building** — insight 038's own remedy is "pick ink or the cross-hue accent", and ink is taken by the median line. Encoding-first is the way out: dashed + distinct marker + a direct end-of-line label naming the spending level does the work, and colour becomes the fifth channel rather than the first.
   - **BINDING:** composite CVD probe of the RENDERED pixel pairs (line over every band stop, over `--ink`, and over the blue median at the crossing) plus a planted-fail that MUST fail; `dollarMax` (`bandData.ts:395`, verified fan-p90-only) gains an overlay term — never clip, never truncate; re-measure the hero fan at 1536×791 (the dissent's live concern: an essentials median can exceed the lifestyle p90 and flatten the hero plume into a strip, which re-creates essentials-as-visual-hero in a new costume — **if it does, the dissent wins and floor-fan-plus-lifestyle-line ships instead**).
   - **THE COUNCIL'S OWN SHIP GATE, not optional:** a five-face Caddie cold read asking ONE question — *whose range is the shaded area?* If readers bind it to the UNION, the overlay DROPS and the minimalist's no-overlay ruling ships as written; the naming layer and the flip stand regardless.

1. **⚑⚑ THE S6 FIX LIST — THREE OF SIX SHIPPED 2026-07-30, ALL THREE COLD-READ IN A REAL BROWSER BEFORE AND AFTER. The three that remain are all BRIGGSY'S, not blocked on build.**
   - ✓ **(c) the Roth door** — `d8f35d8c`, CI 30571823021 green. Full entry in UNFINISHED FIXES, incl. the three wrong prescription steps caught before executing.
   - ✓ **(e) `datemixed`'s refusal, THE FALSEHOOD HALF** — `72d638d9`, CI 30575431614 green. **Re-driven live first, and the defect was SHARPER than filed: it is TWO falsehoods.** The landing headline already answers the question in the product's own words — *"No fuck-off date within the next 10 years — with what you've entered so far"* — so the refusal re-asked an answered question under a name the reader has never met; **and** "it needs one on the board first" is false on its face, because the board carries a plotted, labelled *"Essentials date"* (aria: ages 65 and 66) that the prose dates to 2033. Now: *"This comparison anchors to your fuck-off date, and this plan doesn't have one yet."* — the headline's own term, the true absence, and **6 chars shorter** (the panel-raw phone-clipping flag). The key name did NOT change: `leverPreviewNoDate` leads with `lever`; renaming it to carry `fuckoff`/`workoptional` trips `isVerdictKey`'s SUBSTRING net (`copyGuard.ts:72`) and silently promotes the line into free-numeral scope. **The walk's hardcoded literal is GONE, not moved** — `caddie-walk.spec.ts` now reads `copy.leverPreviewNoDate` directly, so the next reword cannot re-blind the only face that reaches this arm.
   - ✓ **(d) the arrived premise vs its own axis** — `d5e7b466`. **The unknown is the BALANCE, not the DATE**, exactly as the section's own comment always said ("only the fan's WIDTH is conservative, its LOCATION is unknown"). Both arms moved together. **⚠️ The obvious fix — dropping the Today tick — is the U13/§S0 defect in reverse and is forbidden by name at `vertical-fit.spec.ts:1565-1566`; the tick is right, the sentence was wrong.** A new arm was MANDATORY because every existing consumer renders THROUGH the slot and follows a reword silently in BOTH directions: reverting now reds the two new arms while FuckOffDate/ConfidenceStatement stay green — the demonstration that they never could have caught it. **Fold A/B'd at his real 1536×791: byte-identical geometry, the reword costs ZERO pixels.** ⚑ **A number worth carrying: the arrived plant's disclaimer clears the fold by only 4px** — far tighter than the 41.7px the queue cites for the stale spine. Pre-existing, not consumed by this change, but the next author editing that region has almost no room.
   - ⚑ **(a) the two-odds collision — NOT MINE.** The copy-only half (a visible budget referent on the ladder, whose only visible title is the budget-free "How your odds shift by when you stop") is buildable, but the audit's finding is that the real cure — naming the two SPENDING LEVELS — is Card 2's territory. **Same decision, so it should be ruled with Card 2, not ahead of it.**
   - ⚑ **(b) the money chart's essentials-world-under-a-lifestyle-headline — PARKED-FOR-BRIGGSY**, his framing fork, explicitly deferred to debate 2026-07-27.
   - ✓ **(f) CARDS 6–7, the record card — HALF (1) SHIPPED (`532cad82`).** The in-frame line is now **"It may no longer fit the two of you."** — a verb about the ADVICE, never about the display. `verify:fit` gained a `standingBottom <= frameHeight` arm on BOTH plants (measured 774px, 17px slack, one line; mutation-proven RED at 801px). **HALF (2) — naming the strategy — is STILL A BRIGGSY RULING, not a build:** it amends his own written ruling 3. Full entry + the four build traps in UNFINISHED FIXES.
   - ⚑ **THE RHYTHM HALF of (e) is untouched and is a TONE CALL:** three doors still refuse in a row on `datemixed`. Rewording does not resolve it — after a perfect sentence the household still opens three doors and is turned away three times.
2. **✓ U17 S6 IS COMPLETE — build, walk AND cards all landed 2026-07-27.** The panel ran (wf_efc3968f-cf5, **140 agents, zero crashes**) and its six cards + tape row are filed in `docs/caddie/cold-read-log.md` / `tape.md` (`aa9b2ef9`). **⚠️ ALL SIX HARD-FLAGGED — nothing shipped**, because three carry **calm-but-wrong BLOCKERS**, the one class the batched-oracle grant never covers. **They are PRE-EXISTING product defects, not S6 regressions** — the stage's real value was that its plant finally made these faces LOOKABLE. **THE NEXT BUILD IS THAT FIX LIST, not more U17.** In severity order: (a) **`datesplit` shows TWO ODDS FOR ONE MOMENT** — prose "about 9 of 10 odds" vs the ladder's "about 4 of 10 — below the line" for the same year, with ZERO on-surface text distinguishing essentials-priced prose from the full-budget ladder (verified: `grep -c` for "full lifestyle"/"whole budget"/"all-in" = 0); (b) **the money chart is the ESSENTIALS-ONLY world under a FULL-LIFESTYLE headline** on three faces — *Briggsy's framing fork, he asked to debate it, do NOT pilot-fix*; (c) **the Roth door** (own entry above, cleared-to-fix); (d) the arrived premise line contradicting its own labelled Today tick; (e) `datemixed`'s refusal denying a date the same screen plots, in a term the product never uses; **(f) ⚠️ ADDED 2026-07-30 — CARDS 6–7 WERE DROPPED FROM THIS LIST ENTIRELY.** The list ran (a)–(e) while the batch carded SEVEN faces: the S5 record card's first human-visible read is `cold-read-log.md:39`, HARD-FLAG, **HIGH/BLOCKER**, and it never reached the queue. Two defects: on `recold` the card says *"We can no longer show this as current"* — **a verb about SHOWING, not about the advice being WRONG**, so a household still executing the saved conversions reads the display as stale and the advice as standing, and **both of its causes sit below the 791 fold, leaving only the understating line in frame**; on `rec` the card asserts *"It still matches your plan as it stands today"* while **naming no strategy at all** — no order, no conversion, no amount, no saved date — asking the reader to accept a claim whose subject they must supply from memory (rule 38 detail-hunger, his standing lens since 2026-07-11). The log's own disposition line reads **"HARD-FLAG ×6 — none ships"**; the transcription to this queue lost a card on the way. ~~the missing `stale`-plant engine-acceptance pin~~ ✓ SHIPPED in `7e113b6e`. **The four parked aged-surface tone calls now have the panel's advisory answers on the card — incl. the work-stops SILENCE reading MISSING rather than restrained, with the note that §S2.1's veto forbade a FUTURE-TENSE marker left of Today and never required silence (a past-tense mark is available within the veto). His call.** **S7 is DEFERRED — Briggsy's ruling 2026-07-27:** close U17 at S6 and re-file Q7a as its own unit with a corrected gate (its filed gate manufactures "about $0" on a real $18,000 delta by subtracting ROUNDED endpoints; the honest one derives from RAW values and pins the rendered figures against them). Q7b stays parked until a document names what it reorders.
3. **⚑ NEW COLD-READ SURFACE, NEVER LOOKED AT: the `datesplit` tier transient. THE TRANSIENT IS REAL — and this entry's first draft got the surface, the string and the scope wrong. Corrected 2026-07-31 against source.**
   - **NOT "during entry."** The intake strip renders `answer.outcome.floor` ONLY (`AnswerStrip.tsx:143`) and `datesplit`'s floor is `confirmed-date` at 2 at BOTH tiers — nothing hedges during entry. It happens **on the RESULT screen after the reveal**: `revealAndSharpen` (`IntakeApp.tsx:275-279`) sets phase `'result'`, AWAITS a provisional commit, THEN dispatches final. So *"the live surface runs the final tier"* is misleading — provisional runs first **and it renders**.
   - **NOT `copy.ts:2245`.** That is the FLOOR hedge, gated on `floor.kind` (`dateSplit.ts:132`) — dead on this seed at both tiers. The string that actually renders is **`copy.dateWindowEdgeNote` (`copy.ts:391-392`) at `FuckOffDate.tsx:388-390`**, hero-keyed.
   - **UNDER-STATED: THREE things change at once** — the headline 10→9, the hedge withdraws, and a NEW tradeoff line APPEARS (`dateTradeoff.ts`). **With NO provisional label, because `answerView.ts` never sets `view.provisional`** — and `dateSearch.ts:29-32` states the very contract the shipped render breaks. ⚠️ The `.fod-provisional`/`.cs-provisional` eyebrow does **NOT** run today: `e2e/reviewSurface.ts:66-68` records that waiting on it was proven VACUOUS because those classes never render on the result hero; the only `provisional:` setter in `src/` is `U7Preview.tsx:67` (`false`). **Do not cite it as a shipped mechanism.**
   - **THE READ: a DEFECT, but not the filed one.** The number is never optimistic — `dateSearch.ts:31` says the provisional haircut *"errs later/conservative"*, so provisional shows a LATER date (10) than the truth (9). **The defect is REGISTER: a provisional reading rendered in the FINAL register with no marker.** Second-order and worth his eye: the withdrawal is **silent to AT** in a way it is not to sighted users — `FuckOffDate.tsx:280` announces only the new `heroLead`, so a screen-reader user is never told the caveat they just heard is now void.
   - ✓ **THE CROWNS ARE PINNED — MEASURED, NOT TRANSCRIBED (2026-07-31).** Both tiers now carry exact-value expectations in `devSeeds.test.ts`, and a NEW final-tier arm covers the tier the live surface actually renders (no arm had ever run `final` on this seed — every existing one ran `provisional` for suite speed, so the crowns a household reads were backed by a comment). **The prose figures were CORRECT and are now contract:** provisional floor **2** `confirmed-date` / lifestyle **10** `window-edge-unconfirmed`; final floor **2** `confirmed-date` / lifestyle **9** `confirmed-date`. Mutation-proven by swapping each tier's value for the other's — `expected 9 to be 10` and `expected 10 to be 9` both red, so **the transient itself is now a tested fact**, not a claim.
   - ⏭️ **WHAT REMAINS IS THE COLD READ, NOT A MEASUREMENT.** The hedge (`copy.dateWindowEdgeNote`, `FuckOffDate.tsx:388-390`) renders off the provisional reading and withdraws when final lands, with no provisional marker on the hero — a caveat that retracts itself. **Walk it and chair it;** the numbers underneath are now guarded, so a re-tune that collapses or widens the transient reds in the suite first.
4. **HIS EYE** — the stacked tape rows (07-15 → 07-23; `?seed=steer` + `?seed=surplus` the newest looks). The U17 ⚑ tone calls arrive LATER, at the `?vault=datearrived` walk: how hard the elapsed segment is de-emphasised (tint vs hatch vs rule) · the premise line's wording · the de-mudded arrived idiom.
5. The ~Aug-2026 **NC certification** checkpoint (triple-duty, unchanged) — ⚑ **and it is the ONLY thing that clears the NC recommendation withhold**, which is live TODAY and which no `nextDue` bump touches (see the corrected gate entry above).
6. **THE S5 RESIDUE — MOSTLY SWEPT 2026-07-27** (details + corrections in UNFINISHED FIXES below): ~~the phone CLS defect at 390×844~~ ✓ · ~~the two surviving mutants~~ ✓ · ~~the record plants missing from the Caddie route list~~ → **there is no route list; the keys walk unedited, just pass them as targets on the S6 walk** · **STILL OPEN: the post-ceremony landing announce**, which jsdom structurally cannot answer — **and the filed "80–200s solve blocks a real-browser check" premise is WRONG:** `caddie-walk.spec.ts:612-615` already drives a LIVE full-precision solve to the committed beat under a 720s budget (`:825`), so a Playwright arm can reach the real ceremony without any store-injection seam. A browser still cannot prove an AT SPEAKS it — that part stands, and needs a real NVDA/VoiceOver pass.

**⚑ UNFINISHED FIXES (prescriptions, not diagnoses):**

- **⚑⚑ THE RECORD CARD (Cards 6–7) — ✓ HALF (1) SHIPPED 2026-07-31 (`532cad82`); HALF (2) IS A BRIGGSY RULING AND STAYS OPEN.**
  - **✓ WHAT SHIPPED.** `copy.ts:1322` now reads **`'It may no longer fit the two of you.'`** The prescription below was verified line-by-line before executing and **every anchor held** — the register claim, the direct `copy` read, the five-arm fail-closed split, the gate scoping. Rare in this queue, and worth recording as the counter-example to the base-rate law. The defending comment was rewritten in the same edit (087) — left behind, it would have minted the showing-verb back.
  - **✓ THE FOLD IS NOW GUARDED AND MEASURED, not arithmetic.** `vertical-fit.spec.ts` asserts `standingBottom <= REAL.height` on BOTH plants, captures the sentence text so a red names the wording that broke it, and is mutation-proven RED at 801px with a two-line variant. Live at 1536×791: standing bottom **774px, 17px slack**; the causes sit at 806/835, below the fold by design.
  - **⚠️ TWO CORRECTIONS TO THE FILED PRESCRIPTION, both found by MEASURING what it asserted:** (1) it rejected `'It may no longer be the right read for the two of you.'` (54 chars) as *"one wrap from eating the fold slack"* — **false; that variant does not wrap and lands on the identical 774px.** It lost on TONE, not pixels. (2) the *"roughly 10px of slack"* figure was low — measured **17px**. **The real law is ONE LINE** (a second costs ~27px), never a character budget; the gate owns that number now instead of prose. **Do not re-reject a candidate on a guessed width.**
  - **⚑⚑ HALF (2) — NAMING THE STRATEGY — NOW HAS A SHARPER CASE THAN DETAIL-HUNGER, FROM THE 2026-07-31 DOOR WALK. STILL A BRIGGSY RULING; DO NOT PILOT-FIX.** The S6 panel flagged Card 6 for rule-38 detail-hunger ("names no strategy at all"). **The doors make it worse than unsatisfying — they make the sentence misreadable in the OPTIMISTIC direction.** On `?vault=rec` the card reads *"It still matches your plan as it stands today."* while **door 2, one flick down, labels the household's current order "A little from each — your current order"** (proportional). And the saved record's winner is **`taxable-first` with `noChange: false`** (`devSeeds.ts:1297-1311`) — i.e. the remembered advice was **switch to Brokerage first, and the household never did it.** "Still matches" is engine-true (it means the record is still RE-PRESENTABLE — fingerprint + solver version + era unmoved), but a household reads *"matches your plan"* as **"I am doing this"**, and the next door shows they are not. **This is the exact MIRROR of the superseded verb fixed in `532cad82`:** there, a claim about SHOWING read as advice-still-stands; here, a claim about RE-PRESENTABILITY reads as already-executed. Same family, opposite arm. **`recold` is clean on this axis** — "may no longer fit" makes no execution claim — so the defect is **HOLDS-ARM-SPECIFIC**, which no landing-only walk could have surfaced.
  - **⚠️ IF HALF (2) IS RULED IN, THIS CHANGES WHAT IT MUST SAY.** Naming the ACTION alone (policy · order · goal) is necessary but NOT sufficient — the card would then name "Brokerage first" beside a door labelling proportional as current, which states the contradiction rather than resolving it. **The honest shape is the action PLUS its execution status**, and the record cannot supply the latter: nothing in `SavedRecommendationV3` records whether the household ever acted. **Determine that before drafting copy** — the candidates are (a) compare `action.drawdownOrder`/`policy` against the LIVE draft's own policy at render time (derivable today, both sides present), or (b) keep the card silent on execution and drop "matches your plan" for a re-presentability word that cannot be read as an action claim. **(a) is a new render-time comparison, not a copy edit.**
  - ~~*(the half-(1) prescription — what renders, the register finding, the gate trace, the comment sweep, the fold arithmetic)*~~ **STRUCK: it executed, and its reasoning now lives where it belongs — in the defending comment at `copy.ts:1310-1332`, which is the only copy of it that cannot rot away from the string it defends.**
  - **⚠️ STILL BINDING FOR ANY FUTURE TOUCH OF THIS CARD — do NOT bring the causes into frame.** The only mechanism is re-hoisting the card above `<Disclaimer inFrame/>`, which re-acquires the 67–161px breach and **retires Briggsy's 2026-07-26 placement ruling**; `vertical-fit.spec.ts:1793-1796` already watches that mutant red. Reword the in-frame line or move the SUBJECT into the heading — never re-order the card.
  - **⚑ HALF (2) — NAMING THE STRATEGY — IS A RULING, NOT A BUILD.** It requires amending Briggsy's written ruling 3 (`act4-u17-s5-execution-plan.md:19-21`, *"the card is MINIMAL… does not quote the remembered grade/verdict enums"*). Proposed line: **the ACTION is named (policy · custom order · goal), the VERDICT stays silent (grade, `noChange`, `surplusRegime`, `noDollarRegister`).** **What the record CAN honestly carry** (`model.ts:1828-1962`): `action.policy` (five shipped labels, `copy.ts:778-790`), `action.drawdownOrder` (codec-guaranteed a full 3-permutation, `scenarioCodec.ts:487-500`; labels `:796-798`), `goal` (`:1407`/`:1410`), and the mint year. **What it CANNOT: any conversion** — `SavedRecommendationActionV3` has **no conversion field**, deliberately (`model.ts:1880-1886`), and the live ranking is sequencing-only. **So the chaired defect is answerable ~60%, and that is by design, not a gap.** Dollars are structurally banned (`model.ts:1815-1819`).
  - **⚠️ IF HALF (2) IS EVER BUILT, FOUR TRAPS:** (a) `vertical-fit.spec.ts:2054` selects `p.rec-note__line` with `toHaveCount(1)` — new siblings MUST carry distinct classes or it reds for the wrong reason; (b) `RecommendationSurface.test.tsx:846-859` **stays green while its TITLE becomes false** (it bans the raw `taxable-first`, and the card would render `Brokerage first`) — retitle it, do not just let it pass; (c) `copyGuard.test.ts:292`'s `SLOT_RENDER` exhaustiveness means new slots need samples **in the same commit** or the TEST FILE fails to typecheck; (d) the heading must stay a **noun phrase** — `model.ts:1931-1934` forbids re-telling a `noChange` record as a switch, so `You chose to move to…` would be a lie on every stay-put record.
  - **FILE SEPARATELY, found while writing this — BOTH VERIFIED 2026-08-01; (i) CONFIRMED AND UNDERSTATED, (ii) STRUCK AS FALSE:**
  - (i) ✓ **CONFIRMED, and worse than filed.** `recommendationView.ts:170` declares `winnerStrategyKey: CopyKey`, `:225-231` is the `WINNER_STRATEGY_KEY` map, `:410` computes it — and a repo-wide grep returns **exactly three hits**: those two plus ONE unit-test assertion (`recommendationView.test.ts:183`). **So it is not unreferenced dead code — it is consumed BY TESTS ONLY, a value that cannot reach a screen.** Understated because **the beat names NO strategy anywhere at all**. ⚑ Whether it SHOULD is coupled to the record card's half (2) — **Briggsy's ruling, same decision, rule them together.** The mechanical finding (a computed field with no render consumer) is separable and is his to route.
  - (ii) ✗ **STRUCK 2026-08-01 — THE PREMISE IS FALSE. `Saved in 2026.` does not render in the same calendar year.** `recommendationSaveView.ts:287` computes `const aged = epochDayToCalendarYear(todayEpochDay) - mintYear >= 1` and `:313` sets `savedIn: aged ? text.savedIn(mintYear) : undefined` — so a same-year record shows **nothing**, which is the behaviour the entry was asking for. It is **pinned**, not accidental: `recommendationSaveView.test.ts:501-504` asserts `toBeUndefined()` for the same-year case and `:505-510` pins the calendar-TURN boundary. The sub-claim that `RECORD_AGED_DAYS = 400` leaves the same-year case unexercised is therefore also wrong — the *plant* does not exercise it, but the *unit arms* do, deliberately.
- ~~**⚑⚑ THE ROTH DOOR CALLS THE HOUSEHOLD'S OWN EXECUTED CONVERSION A TYPO**~~ ✓ **SHIPPED 2026-07-30 (`d8f35d8c`, CI 30571823021 GREEN).** Verified in real Chromium on `?vault=datearrived` at 1536×791, not only in jsdom: zero alerts, `aria-invalid` absent, no dangling `err-` reference, the true note rendering "…started in 2024", the announce true on tap, and door 3 + door 5 finally agreeing on the year in the same tense. Five mutants planted one at a time, all red, all restored with Edit — the one that earned its keep is the `aria-invalid` ungate, which reds ONLY the AT assertion (a text-only test ships that bug); and the over-suppression mutant reds THREE arms including two pre-existing §S1 ones, independently proving the fresh no-applied-plan path is untouched. **THE FILED PRESCRIPTION BELOW IS KEPT FOR ITS REASONING, AND THREE OF ITS STEPS WERE WRONG — see the five corrections at the end of the entry; they were caught by verifying BEFORE executing, which is the whole point of insight 105.** *(Still filed, deliberately NOT built: editing a MID-FLIGHT conversion — changing the remaining years — is the engine-semantics question §S1 named at `RothLever.tsx:48-49`. Briggsy's call, its own unit. The new copy is careful not to pre-empt it: it never says "under way", because a passed START does not prove a running schedule — a 4-year plan begun in 2024 is finished by 2029.)*
  - **⚑ ONE TONE CALL LEFT ON THIS FACE, ROUTED TO THE ORACLE NOT PILOT-FIXED:** the static field help "The calendar year the conversions begin — this year or later" now sits directly above the new note. Both sentences are true; together they read slightly at odds. That is a rhythm judgment the Caddie panel should chair, not something to silently reword.
  - **THE ORIGINAL FILING, for its reasoning:**
  - **WHAT RENDERS (verified verbatim, `temp/caddie/2026-07-27-22-37-39/vault-datearrived/real/door-3-revisit-your-roth-conversion/aria.yaml`):** the applied plan pre-fills (`34,000` / start `2024` / `4` years), a `role=alert` fires ON OPEN — *"That year has already passed — this plan can start the conversions in 2026 or later."* — and **`button "Add this to my plan" [disabled]`**. Meanwhile door 5 states the SAME plan as live fact: *"Converting ~$34,000 a year for 4 years, started in 2024."* Two surfaces, one household, opposite claims about one year.
  - **MECHANISM:** `RothLever.tsx:114-124` re-seeds the APPLIED plan on open, mapping its offset back to a calendar year (`:120`); `:133` then runs `startYearPassed` on that seeded plan with **no distinction between a year the user TYPED and the household's own executed history**. U17 §S1 shipped the refusal (correct for authoring) and its own comment at `:48-49` already filed the gap — *"Re-anchoring the engine's own conversion semantics (what a mid-flight past start MEANS) is FILED, not built here"* — but the refusal it shipped makes that filed case a visibly broken door TODAY.
  - **REACHABILITY IS DATED, NOT HYPOTHETICAL:** per the `⏰ 2027-01-01` landmine every 2026-built plan flips to `yearsSincePlanBuilt = 1` with zero elapsed days, so any real household with a conversion starting in their build year hits this on **2027-01-01**.
  - **⚠️ THE OBVIOUS FIX IS A TRAP — do NOT just suppress the alert.** `complete()` (`:53-58`) ALSO returns null on a past start (`:56`), and that is what disables Apply. Delete the error alone and the reader gets a dead primary control with **no rendered reason** — insight 100's exact violation, trading a wrong sentence for a silent dead end.
  - **THE PRESCRIPTION:** (1) derive `appliedStartYear = applied && rothPlanStartFor(savedAnchor, applied.startYearOffset).year`; (2) gate the FieldError on `startPast && plan.startYear !== appliedStartYear` — the refusal then fires only on a year the reader actually typed; (3) render a NEW true slot in its place when the past start IS the applied history — it must state the conversion is already under way, name its start year from the anchor (never re-typed), and **promise only what exists**: Apply stays disabled by `complete()`, so the sentence must point at `leverRothRemove` ("Take the conversion back out") and must NOT invite editing amount/length (insight 100 — the amount is editable in the DOM but cannot commit while the start is past). (4) Editing a MID-FLIGHT conversion (change the remaining years) stays FILED — it is the engine-semantics question §S1 named, Briggsy's call, its own unit.
  - **SCOPE:** user-facing money surface ⇒ the four-skill UI loadout, a copyGuard slot, a mutant-proven arm on BOTH branches (typed-past-year still refuses; applied-history does not), and `pnpm verify:fit` re-run.
  - **⚠️ FIVE CORRECTIONS TO THE PRESCRIPTION ABOVE — VERIFIED IN SOURCE 2026-07-30, THREE OF THEM SUBSTANTIVE. The DIAGNOSIS survived three adversarial refuters unscratched (0/3); the PRESCRIPTION did not. Insight 105 firing on our own filing, one session later.**
    1. **THE FILE IS `src/intake/RothLever.tsx`, NOT `src/ui/`.** There is no `RothLever` under `src/ui`. (The S6 card's `source binds` line carries the wrong path; this queue's bare `RothLever.tsx` is merely ambiguous.)
    2. **`copyGuard.ts:63-65` IS THE WRONG CITATION AND THE CLAIM BUILT ON IT IS FALSE.** `:62-65` is `VERDICT_KEY_PREFIXES`; the control rules are `CONTROL_KEY_PREFIXES` at `:102-116` with `isControlKey` at `:128-130`. **`leverRoth*` is NOT control-scoped** — `isControlKey` is a LEAD-prefix `startsWith`, and `leverRothX` leads with `lever`, matching neither prefix set nor `isVerdictKey`'s `/verdict|readout|headline|fuckoff|workoptional/` net. So "require-hedge swept" is **backwards: the hedge the filing assumed was enforced is UNENFORCED.** The repo already pins this exact trap for `assumptionRothName` at `copyGuard.test.ts:711`. **The naming choice silently flips the gate set** — a key named `roth*` IS control-scoped and DOES fire require-hedge. Pick deliberately. Any new SLOT must also join `SLOT_RENDER` or the exhaustiveness arm at `copyGuard.test.ts:435-437` reds.
    3. **"APPLY STAYS DISABLED BY `complete()`" IS FALSE, AND THE FALSE HALF IS THE DANGEROUS ONE.** `:236` sets **`aria-disabled`, never the DOM `disabled` attribute** — the button stays clickable, and `:238-241` announces `copy.leverPreviewPending` = *"Working out both futures…"* — **a false progress claim on a face where nothing will ever be worked out.** (The captured `[disabled]` was Playwright rendering `aria-disabled`; the observation was right, the mechanism word was not.) So the prescribed new copy would truthfully point at Remove while the button beside it lies on click. **That announce needs its own branch in the same unit** — otherwise the fix trades a wrong sentence for a wrong sentence one tap away.
    4. **THE GATE MUST COVER `invalid={startPast}` AT `:191`, WHICH THE PRESCRIPTION NEVER MENTIONS.** Gate only the `FieldError` (`:194-200`) and the field keeps `aria-invalid="true"` **plus an `aria-describedby` pointing at a node that no longer renders** (`fields.tsx:88-93`, `:156-157`) — a screen-reader reader is still told their own executed year is invalid, and gains a dangling ARIA reference. **Lift it to one shared `const typedPastYear = startPast && plan.startYear !== appliedStartYear` and feed BOTH.** *(The gate logic itself was traced through five branches — typed-different-past-year, typed-back-to-applied, no-applied-plan, future-applied-start — and is sound in all five.)*
    5. **TWO TESTS WILL GO RED AND THE FILING NAMES NEITHER.** (i) `src/intake/__tests__/rothLever.test.tsx:465-480` drives exactly this case and asserts `getByRole('alert')` at `:477` — **its title and its `:474-475` comment both encode the OLD ruling**, so a mechanical `toBeNull()` swap leaves a lying test name; this is where the demanded both-branches mutant arm lands. (ii) **A SILENT TRIPWIRE:** `src/ui/__tests__/planClockSeam.test.ts:228-230` source-binds the LITERAL text of `RothLever.tsx:120` with `/rothPlanStartFor\(savedAnchor, applied\.startYearOffset\)\.year/` — the natural hoist-to-a-const implementation **deletes that substring and reds the arm, and the failure will read as a source-bind violation with no apparent connection to this fix.** Keep `:120` literally intact and derive `appliedStartYear` as a second call, or update the regex consciously.
    - **ALSO IN SCOPE, DECIDE EXPLICITLY:** on the mid-flight face `complete()` returns null so `:145` calls `run(null, …)` — **the two-futures comparison is withdrawn entirely and the door shows no preview at all.** Worth a ruling even if the answer is "stays with the filed §S1 re-anchoring unit".
- **⚑ `doctorStaleVault` BREAKS A DOCUMENTED MODEL INVARIANT, AND IT COSTS `?vault=stale` ITS VERDICT — diagnosis COMPLETE, but the fix needs a RULING first, and both obvious repairs are MEASURED DEAD ENDS.** Found building S6's arrived plant (2026-07-27), pinned as found in `devSeeds.test.ts` (the new `'stale' is ENGINE-ACCEPTED on the SPINE path` arm) so any future repair must change that line consciously.
  - **THE DEFECT.** `doctorStaleVault` (`devSeeds.ts:1027`) ages `startCalendarYear` −2 and touches neither `birthYear` nor `currentAge`. `PersonInputs` (`model.ts:98`) documents `currentAge === startCalendarYear − birthYear` as a model invariant — and **nothing enforces it**: not the codec (`scenarioCodec.ts` gives `startCalendarYear` only a `needInteger`), not `draftFromScenario` (passes people through verbatim), not the sanity layer (which the JSDoc says "owns" it — it does not). So the engine's TWO birth-year reads fork silently: the SS sub-engine's stated `p.birthYear` vs the tax overlay's derived `startCalendarYear − currentAge` (`simulate.ts:1346`).
  - **THE COST, MEASURED.** On the `stale` plant (base `retired`, stated 1960/1961) the derived pair becomes **1958/1959**, straddling the SECURE-2.0 RMD band edge (`bornThrough: 1959 → 73` vs `null → 75`) for BOTH people — RMDs forced two years early. **`?vault=stale` resolves `borderline`; fresh `retired` resolves `on-track`.** The fresh scenario's derived pair equals its stated pair exactly, confirming the invariant is real and organically held. The fit gate never saw it: `assertResolvedSpine` only needs a resolved run, and borderline is resolved.
  - **DEAD END 1 (measured, do NOT re-probe):** age `currentAge` by the depth instead → **`buildSpineParams` returns `null` outright.** A `'retired'` person owes `retirementAge <= currentAge` (`sanity.ts:11-14`) and the seed is retirementAge 65 against a doctored currentAge 64.
  - **DEAD END 2 (measured, do NOT re-probe):** age `birthYear` by the depth (the lever `doctorArrivedVault` uses successfully) → moves this seed's **FRA** band: 1960 is the FIRST year of the `bornThrough: null` 67y0m band, and 1958 reads 66y8m (`socialSecurity.ts` `fullRetirementAge`). Different SS benefit on a fixture the landmines document as KNIFE-EDGE.
  - **WHY THERE IS NO THIRD LEVER:** the `retired` seed is all-65+ BY DESIGN, so its people are born 1960/1961 — and the SECURE-2.0 and FRA band edges both sit at exactly 1959/1960. **Any −2 aging of an all-65+ 2026 household crosses both.** The break is structural to "age this particular plant two years", not a slip.
  - **SO THE RULING COMES FIRST (Briggsy's or a council's), and it is a question about MEANING, not code:** should `?vault=stale` render the verdict of the household as ANCHORED AT ITS BUILD YEAR (today's behaviour — arguably honest, since the app genuinely recomputes on the saved `startCalendarYear`), or should the aged plant hold its FRESH verdict so the staleness surface is the only thing that changed? **Only after that ruling is a prescription writable**, and it will be a re-tune of a knife-edge fixture (the `KNIFE-EDGE DEV SEEDS` landmine governs: re-tune the household knob, never loosen the pin). `datestale` (base `datesplit`) rides the same doctor and must be re-measured in the same pass — its crown-window pins are the tell.
- **THE S5 RESIDUE (filed 2026-07-27; items 1–2 CLOSED the same day, 3 reframed):**
  1. ~~**PHONE CLS at 390×844 — the save tap collapses the slot 18.13px.**~~ ✓ **CLOSED 2026-07-27 (`f74065cc`).** The repair ran DOWNWARD, because the filed prescription had the tier backwards: this codebase is **mobile-first** — there is no phone breakpoint, the base rule IS the phone and `@media (min-width: 68rem)` is the laptop tier, so `min-block-size: 8rem` was the *phone's* rule the laptop merely inherited. A "`@media` bump at the phone tier" would have had nothing to bump. Base is now **10rem**, laptop narrows to its measured 8rem. **The number was re-measured across the sub-laptop range first: 390×844 is the GOVERNING width at 146.13px (4L hint) and 320×844 is slightly SHORTER at 145.25px** — narrower is not monotonically taller, since the one-line arms rewrap at 320. Hint untrimmed. **The phone JOINED the fit reservation loop** (50 → 51 arms), so a reword that outgrows 10rem now reds in CI instead of on a real phone.
  2. ~~**TWO MUTANTS SURVIVE the full suite.**~~ ✓ **CLOSED 2026-07-27 (`b985d0c3`)** — both planted, proven RED, restored with Edit (`git diff --stat src/ui/IntakeApp.tsx` empty). (a) The date-route arm carries its CONTROL in the same `it(` one variable apart, because the memo has five conjuncts and only one is under test. **The finding that makes the mutant dangerous rather than redundant: `Result.tsx:476` gates the recommendation SURFACE off on a date route, but `SavedRecordCard` mounts OUTSIDE that gate at `:508`** — Result's own suppression does not cover the card, so the memo's conjunct is the only thing standing there. (b) No existing arm could discriminate the mint basis (both sources are equal by construction on a healthy frame), so the new arm FORCES them apart with a sentinel. **RIDE-ALONG, and it bit mid-work: `mockClear()` does not undo a `mockReturnValue`** — the diverged spy leaked forward and took two later arms with it, the same asymmetry that cost S4 a red CI run. `beforeEach` now re-seeds the implementation to the real producer.
  3. **⚠️ RE-OPENED 2026-07-30 — THE 07-27 "AUDIT" THAT CLOSED THIS WAS ITSELF WRONG, AND IT CLOSED A LIVE COVERAGE GAP.** The 07-27 entry claimed *"there IS no route list… only `intake` has a key whitelist"*. **False.** `e2e/caddie-walk.spec.ts:877-883` gates the DOOR walk on an explicit key allowlist — `target.key === 'stale' || 'datestale' || 'statestale' || 'datearrived'` — and **`rec` / `recold` are not on it.** The repo's own commit `ed6432cd` says so in as many words (*"The vault door walk is an explicit allowlist… `datearrived` shipped without joining it, so the six-face bundle captured its unlock/gate/verdict and none of its five sheets"*) — i.e. **the exact defect this entry declared impossible had already been diagnosed and fixed for a different key, in a commit landed the same day.** *(What WAS right: `CADDIE_TARGETS` is env-driven with no landing-route list, so the landing/gate/verdict arc is key-agnostic. The audit generalized from the landing walk to the DOOR walk and never opened `:877`.)*
     - **THE LIVE GAP: `?vault=rec` and `?vault=recold`'s door sheets have NEVER been captured by any oracle.** S6 walked their landings only — which is precisely why Cards 6–7 could judge the record card's *verb* and nothing behind its doors.
     - ~~**THE FIX IS TWO KEYS ON ONE LINE:** add `'rec'` and `'recold'` to the `:877-883` allowlist~~ **✓ THE CODE HALF SHIPPED in `7e131330`** — the allowlist now reads `'stale' || 'datestale' || 'statestale' || 'datearrived' || 'rec' || 'recold'` and lives at **`e2e/caddie-walk.spec.ts:899-905`** (the queue's `:877-883` anchor is stale; ⚠️ the drift was caused by `72d638d9`, **not** by `7e131330`'s own comment insertion — do not re-file that causal claim). **✓ THE RE-WALK RAN 2026-08-01 (`292cf9bd`, CI 30681007801) — 4/4 green, both plants × both viewports. THE ITEM IS CLOSED.** Its card is **Card 8** in [`docs/caddie/cold-read-log.md`](docs/caddie/cold-read-log.md) — **transcribed there BEFORE the squeaky that cleared its bundle**, deliberately, because the 2026-07-27 batch lost its rendered verbatims to exactly that deletion. **Both open questions are now MEASURED, not guessed:** `recold`'s door set is **IDENTICAL** to `rec`'s (the feared reseat from `confidence.css:460`/`:473` did not bear out), and it is **FOUR** doors — split · withdrawal-order · roth · assumptions, **no health door** — so the old "five door sheets" figure was wrong and is retired. **⚑ AND THE WALK FOUND A NEW BLOCKER on the arm nobody was watching:** on `?vault=rec` the card's *"It still matches your plan as it stands today"* reads as ALREADY-EXECUTED while door 2 shows the household's order is still proportional — the exact **mirror** of the verb fixed in `532cad82`, and `recold` is clean on that axis, so one fix could never have caught both. It routes to the record card's half (2), **Briggsy's ruling.**
     - **THE LESSON, and it is the session's second copy of it:** the 07-27 audit read `:100-103`, found no list, and generalized to the whole file. **A grep that finds no list is not proof there is no list** — and this is the same "a source read is not a cold read" failure the S6 chair recorded against itself on Card 5, one layer down.
     - ✓ **THE RE-WALK RAN 2026-07-31 — 4/4 green, bundle `temp/caddie/2026-08-01-02-52-21/`** (both plants × both viewports; ⚠️ squeaky clears `temp/`, so re-walk rather than hunt for it). **TWO MEASURED ANSWERS TO QUESTIONS THIS ENTRY LEFT OPEN:** (1) **`recold`'s door set is IDENTICAL to `rec`'s** — the entry warned not to assume parity; measured, they match exactly. (2) **It is FOUR doors, not five** — the unverified "five door sheets" figure is wrong in the safe direction: `door-1-split` · `door-2-withdrawal-order` (+preview) · `door-3-roth` (+preview) · `door-4-assumptions`. **No health door on this base** (`healthPriced`, `Result.tsx:562`). **The walk's real catch is filed against half (2) above — the HOLDS card reads as an execution claim the door beneath it refutes.**
     - ⏭️ **STILL OWED ON THIS BUNDLE: the FORMAL PANEL CHAIRING.** What ran was a PILOT read of the captures, which is exactly the thing the S6 chair recorded against itself on Card 5 — **a source read is not a cold read.** The pilot read covered the record card and the two lever previews; **doors 1 and 4, the CVD arms, the `sr-only` channel and the phone rhythm are UNREAD by any oracle.** Chair the bundle (or re-walk and chair) before treating these faces as cleared.
  4. **The post-ceremony landing announce is UNPROVEN and jsdom cannot answer it.** On the CEREMONY route (R1's no-vault path) IntakeApp's `phase === 'save'` branch returns before Result renders, so the region node is BRAND NEW on remount and the announce lands in the same commit as its own insertion — the burden/045 may-not-fire shape. The unit arm proves the string is WRITTEN, never that an AT SPEAKS it. **Needs a real NVDA/VoiceOver check on the real ceremony route.** If it does not fire, seed the region empty for one frame before consuming `landed` — do NOT change the flow. **⚠️ CORRECTED 2026-07-27: do NOT build a store-injection test seam for this.** The reasoning that a live solve is too slow to drive is false — **`e2e/caddie-walk.spec.ts:612-615` already drives a LIVE full-precision solve to the committed beat in real Chromium under a 720s budget (`:825`, recorded ~90s for `nc` to ~4-7min for `surplus`)**. The 80–200s figure is a constraint of the FIT harness only (`vertical-fit.spec.ts:632-633`), which a new spec does not inherit. So the MECHANISM half (a MutationObserver arm proving the region mutates AFTER its own insertion) is reachable today with `walkSolve`'s polled pattern — adding an injection seam to a correctness-critical app on a false premise is a real cost. Only the "does an AT actually announce it" half needs a human with a screen reader.
  5. **`confidence.css`'s `:not(.rec-aside)` exclusion is now INERT** (the record card left its reach; a refusal can only exist on a frame that already carries a beat, so the switch fires either way). The prose was rewritten to describe what it actually protects. **Retiring the clause altogether is a judgment call, not a defect** — leave it unless a reader is misled by it.
- ~~`BandSavedAnchor` rename~~ ✓ CLOSED in S2 · ~~the arrived arm's non-strict compare~~ ✓ CLOSED by the S2.5 ruling · ~~`resultSave.ts:13-14`'s false mechanism + the key-order arm~~ ✓ CLOSED in S3 (`scenarioIdentityKey` removed the dependency; two more false comments in the same file swept with it).
- ~~**THE FOUR S5 MINT OBLIGATIONS**~~ ✓ **ALL FOUR CLOSED — struck 2026-07-30 after a source audit; this was the single largest dead block in the queue.** The heading's own premise ("none is closeable before the writer exists") expired when S5 shipped the writer on 2026-07-26/27, and nobody came back to strike it. Evidence, each opened: **(1)** `savedRecommendationMint.ts:141` `const v = validateSavedRecommendation(record)`, source-bind-pinned at `savedRecommendationMint.test.ts:488`. **(2)** RESOLVED BY INVERSION and the queue already says so at the S5 entry above — `IntakeApp.tsx:448` refuses on `!probe.ready || probe.droppedAtoms.length > 0` BEFORE the store is touched, so the filed sentence ("we saved your plan, we could not save the recommendation") is **forbidden**, not owed. **(3)** `savedRecommendationMint.ts:132-133` injects `noDollarRegister` under the comment "The INJECTED register, never re-derived". **(4)** the fingerprint bind shipped as an S5 test with its negative twin (`savedRecommendationMint.test.ts:23-25`). *(Kept as one struck line rather than deleted: the four obligations are the reason the S5 mint is trustworthy, and a future reader who finds them missing would re-derive them.)*
- ~~*(the four, retained for their reasoning:)*~~
  1. **Call `validateSavedRecommendation(record)` (scenarioCodec.ts) BEFORE the record touches the draft.** That is the root fix that makes the write-side drop unreachable; without it, a bad mint saves the plan with the record silently gone and only `SaveReady.droppedAtoms` knows.
  2. **A non-empty `droppedAtoms` at the gesture must REFUSE ALOUD** ("we saved your plan, we could not save the recommendation") — insight 100: the gesture promised an affordance, so it owes a rendered outcome. It must never report a saved recommendation.
  3. **`noDollarRegister` MUST be copied from the composed view's own `noDollar`** (`recommendationView.ts:377` = `isNoChange || !winnerDisplaysAhead || deltaCollapsesToZero`), **never re-derived record-side** — only the first disjunct is reconstructible from the record, and insight 081 says a re-derivation forks at the producer's first early return. Get this wrong and the card re-tells a refusal-to-claim as an active switch.
  4. **`fingerprint` has NO type bind and cannot get one** — `solverRunFingerprint.ts:61` is `export type SolverRunFingerprint = string`, a bare alias, so persisting a `candidateId` or a seed would compile, lint, and leave every trichotomy arm green (reading `inputs-changed` forever). **The bind must be an S5 TEST:** mint from a real `solverRunFingerprint(...)` call → encode → decode → trichotomy reads `current`.
- **The era's `appDefaultVersion` arm has no BEHAVIOURAL witness and cannot until a second app-default era ships.** The era map is **`ERAS`** — module-local, NOT exported, and **NOT named `APP_DEFAULT_ERAS`** (that name survives only in comments; corrected 2026-07-26). It holds exactly one entry `'p2-d1'` (`appDefaults.ts:35-37`) and it IS `CURRENT_APP_DEFAULT_VERSION` (`:41`), so `appDefaultMoved` is false on both sides of any assertion (`staleness.ts:417-422` skips on same-version and reads `era === undefined` on any other). It is pinned STRUCTURALLY (the `Required<Pick<…>>` overlay ⇒ TS2741) and **`savedRecommendation.test.ts:202-222`** (the appDefault-specific half at `:213-222`) says so honestly. **When a second era ships, add the behavioural arm** — do not assume the structural pin covers it.
- **Two S3 gaps that are correct-by-inspection but UNPINNED:** (a) `scenarioCodec.ts`'s `if (!(e instanceof Corrupt)) throw e` re-throw is unexercised — `decodeScenario` builds its input from `JSON.parse`, so there is no seam to inject a `TypeError` from inside a validator; ~~(b) no record has been through the REAL crypto/IndexedDB round-trip~~ ✓ **(b) CLOSED in `58076ba6`** — `vaultRoundTrip.test.ts` gained two arms over a `doctorRecordHolds` scenario: the record survives `firstSave → loadVault → unlock` byte-for-byte (asserted on the RECORD FIRST and by name, so the codec's one tolerated drop reports itself as "the record vanished" rather than an opaque whole-object diff), plus a field-sensitivity companion proving a moved `mintedAt` really travels. Mutation-proven by forcing the tolerated drop: both new arms red, the two pre-existing record-free arms green. **(a) remains open and is still seam-less.**
- ~~`verify:fit` not re-run for S3~~ ✓ CLOSED — run at the head AND tail of S4, 42/42 both times.
- **⚠️ THE INTERMITTENT FULL-SUITE FAILURE WAS **NOT** CLOSED — IT RECURRED THE SAME DAY, AND THE FILED PRESCRIPTION BUMPED THE WRONG CLOCK. RE-FIXED 2026-07-27 (`c55913b0`+); the paragraph below is kept because its DIAGNOSIS was right and only its prescription was wrong.**
  - **THE RECURRENCE:** CI run **30310885497** (commit `58076ba6`), same file and same arm — but the failure MODE differed: not `Test timed out in 5000ms` but **`TestingLibraryElementError: Unable to find an element by: [data-testid="recovery-flow"]` at 5033ms**. That difference is the whole tell, and the filed line *"if a red returns after this, it is a genuine hang — treat it as one"* would have sent the next reader hunting a hang that does not exist.
  - **WHY THE FIX COULD NEVER HAVE WORKED:** it raised **vitest's per-test `testTimeout` 5s → 20s** (`vite.config.ts:97-98` — verified applied, not merely filed). But the arm that fails carries its **OWN INNER BUDGET**: `App.test.tsx` `findByTestId('recovery-flow', …, { timeout: 5_000 })`. **An outer per-test budget cannot rescue an inner wait that has already given up** — the test died at 5033ms, ~5000ms of it that inner `findBy`. The `testTimeout` bump remains correct and stays; it simply governs a different clock than the one that fires here.
  - **THE REAL MECHANISM:** `RecoveryFlow` is reached through **`React.lazy`**, so the forgot-door click starts a dynamic import and the wait races module resolution + a Suspense flush against a wall clock. Under a contended parallel run the EVENT LOOP is starved — the import is not slow. The budget had ALREADY been raised once for this (1s → 5s, 2026-07-18) and blew through the raise, which is the macrotask-budget landmine's exact prediction: *a bigger number relocates the failure to the next unlucky runner.*
  - **THE FIX IS DETERMINISM, NOT A NUMBER:** `await import('../RecoveryFlow')` BEFORE the click. The module is `vi.mock`'d already, so resolving it up front removes module resolution from the timed path entirely; what remains is a render flush, and the surviving budget is an explicit **hang guard**.
  - **⚑ HONEST LIMIT — this is NOT proven closed, and must not be marked so again.** The contention that triggers it is not reproducible locally (the arm passes on Windows every time, including before this change). What IS proven: the previous prescription targeted a clock that cannot fire first. **Watch runs on `App.test.tsx`; if it reds again, the remaining suspect is event-loop starvation itself, and the answer is the landmine's measured-cost pattern (poll to a multiple, assert the MEASURED cost), never another raise.** Possible aggravator worth noting: the same commit added two `vaultRoundTrip` arms that each drive real PBKDF2-600k, raising parallel-run CPU load — plausible contributor, not a proven cause (this flake has four documented sightings predating it, one on a docs-only commit).
- **THE ORIGINAL (2026-07-27) DIAGNOSIS, KEPT — it was right:** Four sightings, never named, because every prescription said "capture it next time" and every re-run destroyed the evidence. **CI run 30285582342 caught it on a TODO-ONLY commit:** `src/ui/__tests__/App.test.tsx` > the survivor-door re-entry arm, **`Error: Test timed out in 5000ms`** — not an assertion failure, exactly as the direction predicted, but **NOT the Monte-Carlo battery the filing expected.** That arm is a jsdom test whose only cost is driving PBKDF2-600k twice through a real recovery flow (2.99s locally for the whole file, zero explicit timeouts). Correlating all four: heavy engine tests that pass in isolation · two different MC goldens (longevity, gradeCalibration leave-more) · a lone unnamed red · this. **Nothing links them but CPU contention in a 3164-test parallel run against vitest's 5000ms DEFAULT, which this repo had never overridden** (`vite.config.ts`'s whole `test` block was one `exclude`). **FIX: `testTimeout: 20_000` + `slowTestThreshold: 3_000` in `vite.config.ts`** — a hang guard, not a performance budget. This is deliberately NOT the "blanket bump" the old prescription forbade: that clause was CONDITIONED on the culprit being the MC battery, where a per-file timeout is right and those files already declare 120s-900s explicitly. A test slow for a REAL reason still owes its own `{ timeout }`; the threshold keeps the signal in the reporter instead of hiding it behind a pass. **If a red returns after this, it is a genuine hang — treat it as one.**
- **The date-route ACA clock over-alarms on a work-to-65+ crown (deliberate, bounded, documented).** `exposureForDraft` reads the PRE-CROWN base overlay, which is the right producer at the unlock gate (no crown exists yet) — but `rulesMoved` travels to `FuckOffDate.tsx:393`'s standing echo beside the LANDED crown, where a crown ≥2 years out can price zero ACA years. Chosen over the alternative, which was a **silent stale on the enhanced-subsidy flip** — the one event `verify:aca` gates every build on — for the pre-65 marketplace planner. **⚠️ THE FILED FIX IS WRONG — DO NOT EXECUTE IT AS WRITTEN (corrected 2026-07-26).** Both this note and `stalenessExposure.ts:103-105` said "gate the echo on `acaPricedForRun(d, crownedOffset)`". That would be a **silencing** bug: `stalenessNote` is a bare OR-collapsed boolean (`IntakeApp.tsx:394` reads `reentry?.rulesMoved`, and `rulesMoved` = appDefault ∨ tax ∨ stateTax ∨ movedClocks ∨ contribution) with **no clock attribution**, so gating it on ACA pricing would ALSO suppress the echo for a household whose **federal-tax or Medicare** clock moved — trading an over-alarm for a silent stale on the more consequential clock. That is insight 103's exact trap, in the exact file that documents insight 103. **The real fix needs crown-aware exposure re-derivation (or per-clock attribution carried to the echo), not just the crown threaded through.** `acaPricedForRun` does already exist and is exported (`intakeMap.ts:896`) — the predicate was never the missing piece.
- **⚠️ THE STATE-TAX AGGREGATE ARM — DIAGNOSIS HOLDS, PRESCRIPTION DOES NOT. CORRECTED 2026-08-01 (every one of its six anchors opened; all six were right, and the MECHANISM was still wrong). DO NOT BUILD AS FILED — it is council-shaped, and it is parked behind a trigger.**
  - **THE DEFECT IS REAL.** `pricedState: PricedState | undefined` collapses "the run prices no state" (silent, correct) into "we cannot tell WHICH profile to compare" (should aggregate), so an unbuildable draft leaves the state clock silent on real drift — the residual of the shape S4 closed for tax.
  - **⚑ THE FILED FIX IS A NO-OP FOR ITS OWN TARGET CASE.** `nameable` (`staleness.ts:431`) only pushes when it is CALLED, and **every** consumer calls it as `stampMoved && nameable(clock, read)` (`:449`, `:595`). For the state clock `stampMoved` is a compare keyed by `stateProfileKey(pricedState)` — which **requires** `pricedState`, and `pricedState` is `undefined` in exactly the unknown case (`stalenessExposure.ts:138`). So the compare short-circuits false, `nameable` is never reached, and **adding the sixth `ExposureRead` + the union member changes ZERO behaviour.** Closing it needs a SECOND, roster-wide predicate for the unknown arm (`PRICED_STATES.some((s) => saved[key(s)] !== current[key(s)])`), which needs a new import at `staleness.ts:148` — **not "the existing idiom".**
  - **⚑ "AND AN AGGREGATE COPY STRING" IS FALSE — a new slot would be DEAD CODE.** `copy.stalenessReferenceTables` (`copy.ts:1236`) already exists and `reentryChrome.ts:111` pushes it ONCE for the whole bucket. `copy.ts:1229-1235` documents that the sentence was deliberately written NUMBER-NEUTRAL *"so it stays honest as the set changes"* — it was built to absorb new members. A per-clock push also collides on the React key (`reentryChrome.ts:108-110` warns of this).
  - **⚑ "THE ONLY CLOCK THAT BYPASSES `nameable`/`bucketFor`" IS OVERSTATED.** `placeHealth` (`staleness.ts:501-506`) bypasses both too, hand-rolling `some`/`every` over a family array. The accurate claim is narrower: **the state clock is the only clock whose exposure is not expressed as an `ExposureRead` at all.** *(Anchor nuance: a rewrite starts at `:470`, not `:472` — the block's two `const` reads are at `:470-471`.)*
  - **⚑ THE UNNAMED DESIGN HAZARD, and it is the actual fork:** a 7th field `stateTax: ExposureRead` beside `pricedState` creates **two bits that must agree** (`'priced'` iff `pricedState !== undefined`) with nothing enforcing it — a representable contradiction of exactly the shape `stalenessExposure.ts:22-44` forbids. `staleness.test.ts:108-111`'s `pricing()` helper overrides only `pricedState`, so **all eleven of its call sites would silently produce `{pricedState: undefined, stateTax: 'priced'}`.** Widening the ONE field to `PricedState | 'unknown' | undefined` makes the contradiction **unrepresentable** — that is the choice to put to a council, alongside "may a roster-wide OR fire the nameless line for a household whose own state did not move?"
  - **PARKED BEHIND A STANDING TRIGGER, not scheduled:** reachability was verified three ways and holds — the unknown arm fires only via `missingRequiredFacts(d).length > 0`, and a vault can only be written after a resolved answer, so it is **UNREACHABLE in-build**. Cross-build the class is real and has a shipped precedent (`intakeMap.ts:122-123` once added a required fact a pre-C3 date vault would lack) — **but `staleness.ts:57-59` records the installed base as "~zero (pre-launch, dev plants only)".** ⏰ **Fire this when EITHER is true: the first real installed vault exists, or something is added to `missingRequiredFacts`.** *(This session's own `anyPre65OrUnknown` change did NOT widen that set — it widened a predicate already gating it.)*
- **Two S4 sentences have NO live cold-read path** — the nameless aggregate and the reworded contribution line. Every `?vault` plant's base seed carries per-account manual blends (so the blend clock is correctly silenced on all of them), and no doctor ages `dateVintage.contributionYear`. **Minting either means touching a plant's accounts or adding a doctor, which would disturb the engine-proven outcome pins those plants carry — S6's call, not a silent gap.** The contributing date seeds are `date` (stillWorking), `date65` (stillWorkingAllMedicare), **`dip`** (dateDipSeed — **the registry key is `dip`, NOT `datedip`**) and **`datenc`** (dateNcSeed, which spreads stillWorkingAllMedicare's contributing accounts) — **NOT `datesplit`** (verified: its base carries no `annualContribution`). *(Corrected 2026-07-26: the prior list both mis-keyed `dip` and omitted `datenc`. ~~The same wrong list is written at `src/ui/devSeeds.ts:1139-1140` — fix it there too.~~ **✓ ALREADY CLOSED — verified 2026-07-27 by reading the file, not the pointer.** There is exactly ONE copy of that list in `devSeeds.ts` and it is CORRECT: `:1430-1435` names `date`/`date65`/`dip`/`datenc`, spells out that the registry key is `dip` and NOT `datedip`, and records why `datesplit` is excluded. The source fix rode `ee7d0307`; only this queue entry lagged, and its `:1139-1140` cite now lands on the record-plants banner — a stale line number outliving the defect it named.)* **Mechanism caveat for whoever mints the aggregate:** do not model it as "only an `'unknown'` read reaches the nameless line." `bucketFor('priced')` returns `'named'` but the blend push is gated on `!== 'silent'`, so **both `'priced'` and `'unknown'` push it — only `'unpriced'` is silent** — and blend is never nameable (no per-row comparison exists to attribute it). A household that DOES read the ticker table therefore lands on the nameless aggregate on a blend re-date.
- ~~**The `stale` plant has no FAST engine-acceptance pin**~~ ✓ **CLOSED 2026-07-27 (`7e113b6e`)** — the arm now runs hydrate → `buildSpineParams` → `validateParams` → `runEngine` → a real `outcomeState` on the SPINE path, in under a second. **AND IT IMMEDIATELY EARNED ITS KEEP: it caught that `?vault=stale` resolves BORDERLINE where fresh `retired` resolves ON-TRACK** — the `doctorStaleVault` invariant break now filed at the top of this section. The value is pinned AS FOUND, so any repair must change that line consciously. *(Historical wording below, kept because its correction of the earlier overstatement was itself right.)* (**wording CORRECTED 2026-07-26 — the earlier flat "NO engine-acceptance pin" was wrong and would have sent a builder hunting a hole that isn't there**). Two witnesses DO exist: `e2e/vertical-fit.spec.ts:951-1020` drives `?vault=stale` through a real unlock and asserts `assertResolvedSpine`, which needs `.confidence-reveal[data-twopane]` — emitted only on a resolved run, never on R19 indeterminate; and `src/ui/__tests__/devSeeds.test.ts:202-228` (**path corrected — the test file lives under `__tests__/`**) runs the SAME DOCTOR (`doctorStaleVault`) through `runDateSearch`, which calls `validateParams` internally (a differently-named wrapper is why a file-scoped grep missed it) — **⚠️ but over a DIFFERENT BASE (`datesplit`, the `datestale` plant), so it witnesses the DOCTOR, not the `stale` plant's household. Corrected 2026-07-26: `retired` is all-retired and can never enter `runDateSearch` at all (`dateSearch.ts:389` refuses an all-retired household), so the `stale` plant has exactly ONE engine-acceptance witness today and it is the 90-second Chromium arm.** (`doctorStaleVault` has **four** call sites in that file — `:207`, `:561`, `:590`, `:739` — not two; `:561`/`:590` are the lane/fail-loud arms and assert no engine.) **The real gap is the missing FAST unit arm on the SPINE path** — hydrate → `buildSpineParams` → `validateParams` → real `outcomeState`, modelled on **`devSeeds.test.ts:517-536`** (corrected 2026-07-27 from `:505-524` — see the S6 queue item above for why every prior citation of this arm was wrong). `devSeeds.test.ts:727-728` states the PRINCIPLE the missing arm would serve ("fails HERE (fast) instead of only in the 90-second Chromium run") — **but it is the rationale for the note-count reader arm that shipped at `:735`, NOT a filed request for an engine-acceptance arm (corrected 2026-07-26). The engine-acceptance gap is unfiled in the test file; only this TODO names it.** S6.
- **`docs/roadmap.md:100` still records "2880 tests / 155 files"** inside the U16 ship-battery paragraph — left deliberately as a dated record of what was green at that ship. The gate reads the "N tests across M files" form at `README.md:78` and `roadmap.md:167`, both current at **3070 / 159**. **Leave line 100; do not "fix" it into drift.** **The nearest hijack hazard is NOT line 100 — it is `roadmap.md:98`** (`+124 tests across 10 new files`), which is ALREADY in the `across` form and escapes the gate's regex only because the word **`new`** sits between the digit and `files`. `extractClaim` matches FIRST-ONLY (no `g` flag), and 98 precedes 167 — so a harmless "tighten the prose" edit deleting one adjective would silently hijack the roadmap check onto a frozen historical number. The symptom is a loud red, and the danger is the well-meaning fix: overwriting the frozen record with the live count AND leaving 167 permanently unguarded.

**⚑ RESCUED FROM THE COLLAPSED ARCHIVE (2026-07-26) — prescriptions, not diagnoses:**

- **TWO FALSE SOURCE COMMENTS — comment-only, no behaviour or test moves** (086):
  - (a) `src/shared/model.ts:2176` — `partBTrendVintage`'s *"…so no exposure gate"* was already swept from its twin at `src/store/staleness.ts:550-553` (U17 §S4). **Delete that clause**, and qualify `:2175`'s "every vault saved under the old table" → **"every vault whose run PRICED MEDICARE"**: the schedule is built only under `healthcareEnabled && config.taxEnabled` (`src/engine/taxOverlay.ts:1110-1111`; pinned `staleness.test.ts:354-381`).
  - (b) Kill the **"the record stores the seed"** falsehood before S5 mints (no type bind can refuse a seed field). `heldOutSeed.ts:13-14`: seedA is `ScenarioV3.seed` (`solveDispatch.ts:64`), and `SavedRecommendationV3` (`model.ts:1802-1842`) carries **NO seed** — it is identified by `fingerprint`. Rewrite `4-recommendation.md:306` → "re-solves on the scenario's persisted `seed` (= seedA), `seedB` re-derived via `deriveSeedB`, and the record's `goal`."

- **PLAN 4 POINTS BUILDERS AT DEAD COPY — pointer/header fixes only; do NOT rewrite the body** (`4-recommendation.md:17` makes that deliberate; grep premise *phrases*, not numbers — insight 018):
  - (a) **The binding DEAD-COPY list is stale by exactly +10 in BOTH copies** — verified: `:222`/`:224` are blank, `:235` IS the tax-blind→tax-aware reframe, `:249` the tax-aware-baseline scenario, `:253` the surplus+no-change compose. In `act4-u16-recommendation-surface-build-spec.md:374-379` — `:222`→**`:232`**, `:224`→**`:234`**, `:225`→**`:235`**, `:239`→**`:249`**, `:243`→**`:253`**; **item 6's `:52` is correct — do not move it.** `4-recommendation.md:211-213` repeats the same numbers: ":222/:224/:243"→":232/:234/:253", ":225/:239"→":235/:249". Same pass: give every entry its **PHRASE** as primary key, the line as a hint only.
  - (b) ~~**Nothing bans "safe either way"**~~ ✓ **SHIPPED 2026-08-01 (`b0a52a6e`) — but NOT as filed.** The token add at `:190` was the wrong fix and `copyGuard.test.ts:440` was the wrong anchor (it is `] as const`); the CLASS widening of `:174` shipped instead, with both arms pinned in the `mustCatch` corpus and the red observed before the fix. **⚠️ Its claim that `recommendationView.test.ts:197` is the sole/dead witness was FALSE and that assertion was KEPT** — see the corrected entry at the head of this queue. `docs/product.md`'s two prescriptions of the vetoed phrase were fixed in the same commit.
  - (d) **⚠️ TWO MORE DEFECTS IN THIS ITEM, found 2026-08-01 while verifying (a):** (i) **the U16 spec MISQUOTES contract #6** — `act4-u16-recommendation-surface-build-spec.md:380` renders it *"you're safe either way; **it** keeps more from the IRS"* while `4-recommendation.md:52` actually reads *"…**this** keeps more from the IRS"*. Under (a)'s own new law (*the quoted PHRASE is the primary key, the line is a hint*) that entry's key greps to **zero hits** — so the one item (a) blesses as "correct, do not move" is the one the new key silently breaks. **Fix the quote in the same pass as (a), or (a) lands a key that cannot be found.** (ii) **`4-recommendation.md:256` carries an UNLISTED "safe either way"** — neither dead-copy list names it; both stop at `:253`.
  - (c) `4-recommendation.md:13` → **"U14–U16 SHIPPED; U17 in-flight (S0–S4 landed, S5 next)"**. Insert a precedence blockquote between `:268`/`:270` mirroring U16's `:207-215`, naming `act4-u17-saved-recommendation-build-spec.md` as superseding, carrying a DEAD-PREMISE list: `:272`/`:284` (re-present-under-saved-vintage — refuted by `staleness.ts:17-21`), `:280`+`:290` (schemaVersion-3 bump — the record is additive-within-v3), `:286` (tax-blind spine + Act-3 drift confirm).

- ~~**The Q4d k=10 SE-multiple has NO lower-edge witness.**~~ **✗ STRUCK 2026-07-31 — THE PREMISE IS FALSE. DO NOT BUILD THIS. It was sized "large"; it is instead a queue entry to delete.** The filing's load-bearing claim was that `gradeCalibration.test.ts:252-269` is *"green for any k ≥ 3.3"*, i.e. that k is unwitnessed below 8. **k is ALREADY BRACKETED BEHAVIOURALLY, by two arms in that same file:** `member(30, 2_000)` at `:128` carries margin 0.015 / se 0.002719 = **ratio 5.52**, and `:131` asserts `demotionFired` **true** — which reds for any k ≤ 5.5; `member(120, 2_000)` at `:139` carries margin 0.06 / se 0.005311 = **ratio 11.30**, and `:141` asserts `demotionFired` **false** — which reds for any k ≥ 11.3. **The suite pins k to ≈(5.5, 11.3).** The filed arm would have added nothing.
  - **AND THE PRESCRIBED ASSERTION HAD NO BEHAVIOURAL CONTENT.** `gradeCalibration.ts:210` fires on `some(m.margin < k*m.se)` — the **MINIMUM** ratio decides — while `solver.ts:95-96` calibrates k on the class **MAX** (≈8.1). Predicate and rationale key on opposite ends, so the filed `Math.min(...) > 8` demanded all five 112k members inside `(8, 8.1]`, and the `demotionFired` lines are mutation-blind at k=8 and k=10 alike. A ~900s real-engine arm buying nothing the tree already has.
  - **TWO RESIDUES WORTH KEEPING, neither requiring the build. ⚠️ BOTH CITATIONS BELOW WERE WRONG — corrected 2026-08-01 against source.**
  - (a) **the two prose records of the SAME 2026-07-19 calibration CONTRADICT each other, and the contradiction is REAL** — **`src/engine/constants/solver.ts:93`** (⚠️ **NOT `src/engine/solver.ts`, which does not exist**; the line numbers 92/93/95-96 are correct against the real path) says per-member SE ≈ 0.00037–0.00051 (implying 124k ratios 4.1–5.5) while `gradeCalibration.test.ts:255` says those ratios are ≤3.2 (implying SE ≈ 0.00066–0.00088). Neither is pinned.
    - **⚑ THE LIKELY ROOT CAUSE IS A UNIT MISLABEL, NOT A BAD MEASUREMENT — and it decides how to word the fix.** The constants side closes internally to three decimals (0.0041 ÷ 8.1 = 0.000506 ≈ its stated 0.00051; 4.15 × 1.96 = 8.13); the test side does not. Read `:255`'s "≤ 3.2" as **margin/BAND** rather than margin/SE and the two reconcile (3.2 × 1.96 = 6.27). ⚠️ **Do NOT write this up as corroborated to two decimals** — the tempting cross-check (dividing the CONTROL mean advantage by an SE-of-mean taken from `act4-u16-recommendation-surface-build-spec.md:110-113`) is invalid: that 0.000122 sits inside the **BOOTSTRAP** parenthetical (`stress-near-tie-inversion.ts:43` prints it as `bootstrapAdvantageSeOfMean`, and the script emits no control SE at all), and bootstrap rep-to-rep variance carries block-resample shape variance on top of path noise. It is an UPPER bound, not an independent estimate. The mislabel hypothesis survives directionally; the precision does not.
    - **The cheap close still stands: `:252-269` ALREADY runs the 124k 5-member family at 16k paths — emit its five `(margin, se)` pairs and reconcile the two comments at zero extra compute.** ⚠️ It is a `900_000`ms-ceiling arm, so "zero extra compute" means *no new run*, not *fast*.
  - (b) **⚠️ THE GREP CLAIM IS LITERALLY FALSE TODAY.** The 112k world has never been run and no fixture exists — but `grep -rn 112_000` now hits **only `TODO.md`** (this entry). The cited `solver.ts:92` hit is gone. Still fine, since nothing depends on it — but do not use that grep as evidence of anything.

- **AssumptionPanel hides the ACA-quote row for an UNKNOWN-AGE household while `missingRequiredFacts` names it** — a required fact with no editor. Collapse three re-typed predicates to one: (1) `intakeMap.ts:87-88` → export `anyPre65OrUnknown` (`currentAge === undefined || currentAge < 65`); (2) `:162` → `if (anyPre65OrUnknown(d)) {`; (3) `AssumptionPanel.tsx:280` — delete the local `anyPre65`, add `const showHealthQuote = anyPre65OrUnknown(draft)`, `:641` → `{showHealthQuote && (`, retext the `:640` comment; (4) delete the third copy at `questions.tsx:1141-1142` and import instead; (5) arm beside `assumptionPanel.test.tsx:84-104` (**camelCase — corrected 2026-07-31; the tracked path is lowercase-`a` and the uppercase spelling does not exist on Linux CI**) with `currentAge`/`birthYear: undefined`, asserting both the missing-fact line and the `seat="health-quote"` row, mutation-proven by reverting `:280`. **Caveat — determine first, do NOT let it block the fix:** the state is reachable only through the `inputs-incomplete`/`fallback` arms (`Result.tsx:280`, `:540-541`) — confirm whether any intake path commits a blank birth-year at all.

- **⚑ THE BUDGET STICKY DISCLOSURE — `budgetModel.ts:22`'s "(flagged in TODO)" points HERE. Ship it; do NOT re-seed.** `freshLine` (`BudgetBuilder.tsx:82-88`, `src/intake/`) seeds `other`+`essentials`, the survivor-STICKY pick; reseeding to `food` would call a survival cost scalable — the optimistic direction the cardinal rule forbids. Add `budgetStickyEssentialsNote` beside `budgetAnchorRampNote` (`copy.ts:181-182`); it **must NOT contain the substring "survivor"** (`copy.ts:1000` — the copyGuard net catastrophe-gates any key that does); content per `isSurvivorSticky` (`budgetModel.ts:31-36`). Render as a second `budget-readout__line--muted` after the ramp-note block (`BudgetBuilder.tsx:358-360`); swap `budgetModel.ts:22`'s "(flagged in TODO)" for the key name; re-run `pnpm verify:fit`. **Ships EITHER WAY, in the same commit:** re-date `model.ts:554` → "may join additively in any later unit (U11 shipped without it)". The scoped OOP-only `healthcare` category itself is **PARKED — his eye**, not this entry.

- **R27 `dateinvert` — the seed is now OWED (U10 fired the trigger); two comments still say the opposite. ⚠️ EVERY ANCHOR IN THIS ENTRY HAD DRIFTED — re-verified and re-anchored 2026-08-01. SPLIT IT: (a)+(b) are a cheap comment fix, (c) is an open-ended hunt.**
  - **✓ THE STALE-COMMENT HALF IS TRUE AND CHEAP.** U10 has definitively shipped (`rothConversion` + `drawdownOrder` on `ScenarioDraft` at `memoryModel.ts:165-166`; `expandRothConversion` wired at `intakeMap.ts:547-548`, explicitly Y-INVARIANT on the date route; a live roth-first seed already exists at `devSeeds.ts:661-662`), so both "UNREACHABLE in v1" / "reactivates with U10" comments are genuinely stale.
  - **⚠️ THE CORRECTED ANCHORS (the filed ones are ALL wrong — `devSeeds.ts` has drifted ~+21 lines, the test ~+75):** the R27 note is at **`devSeeds.ts:475-486`**, NOT `:454-465` (`:454-455` are `bline('housing'…)`/`bline('utilities'…)`). The stale test comment is at **`devSeeds.test.ts:308-309`**, NOT `:230-233` (which is unrelated final-tier prose). The registry insertion point is **`:950`/`:951`**, not `:929`/`:930`. The inversion render is **`FuckOffDate.tsx:399`**, not `:370`. The binding prerequisites are **`council-log.md:32`**, not `:31` (which is the 2026-07-08 laptop-fit row). **And the file PATH is wrong: `src/ui/devSeeds.test.ts` does not exist — it is `src/ui/__tests__/devSeeds.test.ts`.**
  - **⚑ (c) IS NOT A "MINT", IT IS A SEARCH — size L, and it should not be started casually.** The filing states the mechanism as DERIVED; it is not. Landing the FLOOR track under 100% FPL while lifestyle stays subsidized is a **dip-class tuning hunt** over household parameters, each probe costing a real date search. **Prerequisites BINDING** (`council-log.md:32`): derive EXTERNALLY (DND 012), engine-verify before any cold-read, extend the CVD probe for a new cue. **Filed, never faked.**
  - **RECOMMENDED SPLIT: land (a)+(b) as a standalone comment-truth commit whenever anything else touches `devSeeds.ts`** — it costs minutes and kills two false comments — **and leave (c) parked until its own session.** The budget's gated state-adaptive second beat names this as its engine gate — no restatement there.


**LANDMINES (2026-07-31 late, the record-card session):**
- **⚑⚑ `sed -i` IN GIT-BASH REWRITES THE WHOLE FILE'S LINE ENDINGS CRLF → LF, AND THIS REPO IS CRLF WITH `core.autocrlf=false`.** A mutant planted with `sed -i` and restored by `cp` is byte-clean; one whose restore never runs leaves the file LF, and `git diff --stat` then reads **1210 insertions / 1158 deletions on a 52-line change** — the real edit buried in whole-file churn, and the history rewritten if committed. **Plant mutants with Edit, or verify `git diff --stat` shows only the lines you touched before committing.** Recovery is `sed -i 's/$/\r/'` on the LF file (safe only at zero existing CRs — check first).
- **⚠️ A MUTANT SCRIPT THAT TIMES OUT LEAVES ITS LAST MUTANT PLANTED.** A 3-mutant loop hit the 10-minute tool cap mid-run, and the `cp` restore after the final one never executed — the tree carried `toBe(3)` on a crown pin. **Always re-read the mutated line after a batch, never trust the script's own restore step to have run.** Budget one mutant per invocation when each run costs minutes.
- **⚠️ `grep -c $'\r' "$f"` SILENTLY MISREPORTS INSIDE A `for` LOOP** — the `$'…'` did not expand, leaving an EMPTY pattern that matches every line, so every file reported its TOTAL line count as its CRLF count. It produced two confident, wrong readings in opposite directions before `cat -A` settled it. **For a line-ending question, use `cat -A` on one line, or `cmp`. Never a bare grep count.**

**LANDMINES (2026-07-31, the flake session):**
- **⚑⚑ WHEN THREE PRESCRIPTIONS IN A ROW ALL ADJUST A CLOCK, STOP ADJUSTING CLOCKS.** The survivor-door flake took 1s → 5s → 20s across months. None could have worked: the arm was never slow, it was mounting the WRONG COMPONENT. **A budget that keeps expiring on work that should take milliseconds is evidence the wait is IMPOSSIBLE, not slow.** Ask what would have to be true for the wait to succeed at all.
- **A FIX THAT RAISES A BOUND CAN DESTROY THE EVIDENCE THAT WOULD HAVE SOLVED IT.** Raising the inner `findBy` budget to 20s made it EQUAL to `testTimeout`, so the outer clock always fired first and the DOM dump was suppressed. Run **30310885497** — the ONE red whose inner budget was smaller than the outer — printed the real component's markup in plain text and sat unread for four days, because the runs either side of it looked like identical bare timeouts. **An inner wait must stay strictly below its outer budget or it can never report.**
- **TWO `import()` CALL SITES FOR ONE `vi.mock`'d MODULE IS A SILENT REAL-MODULE BYPASS.** Vite gives a module one mutable callstack array; Vitest push/splices the mocked id around an await and serves an overlapping second import the REAL file (`isSelfImport` → `_vitest_original`). Vitest's own source carries the warning. **Memoize every lazy loader to one in-flight `import()`** — and clear the slot on rejection, or you delete the retry the ErrorBoundary path depends on.
- **NEVER MEASURE THE TREE WHILE AN AGENT FLEET IS WORKING IN IT.** A `verify:doc-stats` RED and two bogus counts (3192/165, 3193/166) were my own agents writing scratch test files mid-measurement; the truth was 3187/163 throughout. Read-only fan-out is safe; interleaving reads with edits is not. **Also: a green full suite under heavy CPU contention is NOT a repro attempt** — both flakes stayed green locally under 10+ agents. Deterministic probes found both.
- **A NUMBER THAT LIVES ONLY IN PROSE ROTS SILENTLY, AND A "PROVEN" LABEL DOES NOT PRESERVE IT.** Six copies of the `datesplit` crowns were wrong — one labelled "Engine-proven" — and survived 22 engine commits because the tests assert the ORDERING and a RANGE, never a value. **If a figure matters, put it in an expectation or expect it to drift.**
- **QUOTE A DEV-SEED CROWN WITH ITS TIER OR YOU WILL FILE THE WRONG ONE.** provisional = 2,000 paths, final = 16,000 (`dateSearch.ts:79`), and they crown DIFFERENT offsets with DIFFERENT confidence kinds. The live surface runs final.

**LANDMINES (prior arc):**
- **A MACROTASK-TURN BUDGET CALIBRATED ON WINDOWS IS A RACE ON CI, AND THE ARM IT BREAKS IS USUALLY THE NON-VACUITY CONTROL** (2026-07-27, run 30277757106 RED → `59b1bbf6`/30279014650 green). `recSaveNoAutoWrite`'s tap arm measured **7-9 turns** locally against a **30**-turn `settle()` budget — 3× margin — and ubuntu still saw **zero** mints inside it, because under the 3163-test parallel run those turns are starved by CPU contention (the same shape already filed against the heavy engine battery). **The trap is that the arm which fails is the CONTROL, not the sweep** — the negative arms stay green by construction, so a starved run makes the whole no-auto-write law silently vacuous and only the control says so. **DO NOT just raise the constant** — the wait is inherently racy and a bigger number relocates the failure to the next unlucky runner; at 150 the lifecycle arm blew vitest's 5s default outright (every round pays a full `act()`, ~8ms). **The pattern that works:** the positive arm POLLS to a multiple of the budget, then asserts its MEASURED cost `<= FLUSH_ROUNDS` — which converts the sweep's soundness from an assumption into a per-run measurement — plus an explicit `{ timeout }` on every settle-heavy arm. **⚠️ THAT FIX DID NOT HOLD, AND THE PRE-WARM REJECTION BELOW WAS FALSE — corrected 2026-07-31.** Run **30599862965** red the same control again at **63 turns against the raised 60**. The struck sentence read *"Pre-warming the module is NOT available here: each arm asserts an empty IndexedDB as its first line, so warming the session would create the database."* Importing `vaultSession` resolves a MODULE and opens nothing — `vaultSession.ts:19-20` are bare uninitialized bindings, the open is in `getDb()` (`:23-25`), and `db.ts`/`session.ts`/`backup.ts` run no executable statement at module scope. The test file already relied on this: it imports `openVaultDb` statically and its planted control asserts an EMPTY database list *before* calling it. **THE REAL FIX: resolve the chunk in `beforeEach`** so the wall-clock stage leaves the timed window — measured serially on Windows, **7/8/9 turns → 1**. The poll and the `<= FLUSH_ROUNDS` certificate STAY. **The durable lesson is denomination, not contention:** the budget counted macrotask turns while the dominant stage was wall-clock, so no constant was ever portable — and "CPU starvation" is demoted to a contributing cause, since the CI control arm is 293ms total with no per-turn instrumentation and cannot discriminate.
- **A CITATION YOU HAVE NOT OPENED IS A LIABILITY, AND IT OUTLIVES THE GATE BUILT TO CATCH IT.** `aca-last-verified.json` confirmed itself against "IRS FS-2026-19" — a 404. The fabrication was not a wrong digit but an entire document lineage ("updated as X on DATE"), specific and plausibly formatted, so the next re-verifier fails to find it, concludes link rot, and moves on. It survived a 30-day freshness gate indefinitely while propping up `statusConfirmed: true`. **Every citation in a dated record must be RETRIEVED and OPENED before it is written down**, and a re-verify must re-open them, not re-read the summary.
- **`HTTP 200` from govinfo is NOT proof a document exists** — it serves a "Page Not Found" *body* under a 200 for missing public laws and bill variants. Identity-check the body (I walked into this confirming the 2026 public-law ceiling and only caught it by reading the first 150 chars). The same class: `BILLSTATUS` bulk XML goes stale (16 days, in-window) and its `latestAction` element misreports — read the `actions` list, and prefer the live page.
- **REBUILD BEFORE YOU QUOTE THE BUNDLE NUMBER — insight 057 bit a SECOND time, at squeaky.** The existing landmine ("`verify:bundle` reads `dist/` WITHOUT rebuilding") was already filed and we still shipped a TODO claiming "every figure re-run" with a **256.5 KiB** measured against a two-day-old `dist/`; CI's fresh build said **256.6**. Knowing the gate is stale-prone is not the same as running `pnpm build` first. The sentinel is `dist/`'s mtime vs the last source commit.
- **`mockClear()` does NOT drain a `mockResolvedValueOnce` queue — `mockReset()` does.** This cost S4 a red CI run (30169928294) that was green twice on Windows. It composes with any consumer that awaits a dynamic import before calling the mock (`App.tsx:91`): the call can land after its own test tore down, stealing the NEXT test's one-shot, which then falls through to the default. **Prefer a whole-test `mockResolvedValue` + a `mockReset` + re-seeded default at the boundary; a one-shot queue is a leak waiting for a slower machine.** Both halves are now pinned by their own red-proven arms in `App.test.tsx`.
- **A rule written to kill an OVER-ALARM must be checked in the SILENCING direction.** S4's first governing rule ("no run-layer reader ⇒ aggregate") would have shipped a silent stale on the 2028 IRMAA re-index while fixing a false alarm. The safe-looking direction was the dangerous one. See insight 103.
- **A contract is only as true as its PREMISE, and a premise does not travel with the words.** S3's first build copied the scenario's "absent stamp = not-comparable, quiet" contract onto the record. On the scenario it is right (a pre-U13 vault genuinely predates the stamps); on a record born at U17 the population is empty, so the identical rule fails OPEN — `rulesMoved:false` forever. **When transplanting a rule between types, state its premise out loud and check it holds at the destination.**
- **A copy of a producer's arithmetic under a comment claiming it matches IS NOT A PIN.** This cost S0 a failed verification — and S3's first build put another one in `draftFromScenario.test.ts` under a title naming IntakeApp's real reconstruction. When binding a seam, export the producer and add a source-bind arm on the CONSUMER — both halves, always.
- **A fail-loud fix can be worse than the failure.** S3's write-side drop first returned `ready:false`, which routes to `deriveResultSave`'s `{kind:'none'}` — the save CTA, badge and aged-balances clause vanish with no message, over a defect in OUR code. **Before making something refuse, trace the refusal to what the user actually SEES**; `SaveReady.detail` has no reader.
- **An assertion can pass through a mechanism other than the one it names.** A negative-integer clock was nulled by a downstream gate, so the clamp test never exercised the clamp. Choose the input that ROUTES DIFFERENTLY under the mutant.
- **The clamp asymmetry is a PILOT RULING, dated in the spec — do not "correct" it.** High end throws (no honest picture to re-base); low end clamps (a device clock behind the build year is a broken clock, not a household state; crashing a reader over their own system settings buys no honesty).
- **Capture failure NAMES on every full-suite run — never `| tail`.** The S0-era "2 failures, names not captured" mystery RESOLVED itself in S2: piping the suite through `tail -5` ate the names AGAIN, and the re-run showed 2 REAL failures (the reentry.test arrived-arm pins carrying the old non-strict contract §S2.5 changed — rewritten, not patched). The App.test.tsx:121 jsdom navigation WARNING is a separate known shape and still prints on green runs.
- Workflow agents leave the tree dirty by design here — **never `git checkout --` to revert a planted mutant on a dirty tree; Edit only.**

---

## Prior queues (U8 → U16) — collapsed 2026-07-26

> **The history is in `git log` and in [`docs/roadmap.md`](docs/roadmap.md)'s per-unit You-Are-Here table.**
> Roughly 137 KB of closed prior-queue narrative used to live here — 72% of this file, and 56,910
> characters of it on a SINGLE line that no grep, diff or review could read with context.
>
> **It was not inert.** A 195-agent sweep before the cut found **105 obligations still open**
> inside it, each adjudicated against source: a per-path engine rebuild costing a run 32,000 redundant
> schedule builds, a dev seed whose trigger fired at U10 and was never minted, a Part-B re-verify
> tripwire dated 2027-09-01, and two dozen tone calls that had never reached the human eye. They are
> merged into the four sections below. **Nothing else in the archive carried a forward obligation.**
>
> **The lesson, which is why this pointer exists rather than another archive:** a work queue that
> carries its own history buries live work where no tool can find it. Line 3 of this file already said
> "no session history here" — the file violated its own first sentence at 72% of its byte weight, and
> the archive is exactly where every stale item hid through repeated audits.

## Parked (Briggsy's cold-read)

> **Batched-oracle note (2026-07-11):** these pre-grant parked tone reads are no longer per-surface gates — they ride his any-time reads and the Briggsy-bar gauntlet. Kept as the audit map of what his eye hasn't graded yet.
- **THE SURVIVOR FOLD — soft revisit (2026-07-02, "fine — may revisit later"):** the statement-shaped rework CLEARED, but his read was luke-warm, not a rave. If his eye returns: the fold's presentation (`survivor.css` `.survivor-fold*`) + the body words (`survivorReadoutEyebrow` / `survivorReadoutCoverage` / `verdictSurvivorStepDown` — all still craftsman's-lead first drafts). LANDMINE unchanged: gate-7 (≤1 subordinate count on the first frame) is test-pinned in `ConfidenceStatement.test.tsx` — any rework keeps it.
- **DESIGN REVIEW — TONE COLD-READS OWED (2026-06-29; `docs/council-log.md` ⚑).** The design council ruled each item's SUBSTANCE already won (full 0..10 ladder axis, show the survivor reading, no axis cap) — only the **tone** read is yours: **CF3** two-pane lower-left whitespace (calm or dead? worst on the DATE route with the ladder closed); **CF4** the coincident "7 of 10" (verdict + survivor echo — the survivor reading STAYS, cutting it would be calm-but-wrong); **CF6 ✓ RESOLVED** (2026-06-29, Briggsy-approved) — shipped a y-axis "X of 10" odds scale (`OddsLadder.tsx` `LadderYAxis`, anchors at rungs 3/5/7, capped below the ceiling) so each dot's height reads off the axis; chose this over per-dot labels (full "X of 10" collides at ~44u dot spacing; bare numbers break the ceiling clamp + confuse with the year axis) and over a line/% (false precision + the headline frame). Live-verified; **CF5** the $20M squash tone (axis cap settled "don't build"); **CF2** band per-year $ range in the a11y tree (enhancement, not a blocker — the AT-reader parity item).
- **THE TWO-PANE (D2d) — FIRST-FRAME TONE COLD-READ ✓ CLEARED (2026-06-29; the council's binding ship gate, now closed).** Briggsy's N=1 verdict on `?seed=borderline` (laptop, wide): **"calm, not an ambush — ship the two-pane."** The two-pane is the **unconditional shipped path** — the single-column-richer fallback (`--text-hero` headline + band-only `--content-wide`) is OFF THE TABLE. The deferred `--text-hero` verdict bump was NOT requested (layout-alone dominance reads calm); the band-at-478 + bottom whitespace read as calm subordinate (accepted within the "calm" verdict). Remaining two-pane items are only the optional design-review nits (CF4 doubled "7 of 10") — available if his eye returns, not blocking.
- **TWO DISCOVERED v1 FACTS (not bugs, surfaced building the seeds):** (a) the **non-monotone ACA-cliff "doesn't hold" dip is UNREACHABLE in v1** — 16 candidates swept, all monotone: proportional drawdown + no conversions never straddles the 400%-FPL cliff non-monotonically (the cliff only bites if the crowned date is pre-65; younger households that get there can't crown a marginal date). Needs Act 3's conversion controls to manufacture, so D2c's never-seen-live dip stays parked. **Now a formal HARD pre-Act-3 gate** (council 2026-06-29): before the U10 Roth-conversion lever ships, drive a real non-monotone seed through the LIVE ladder render + N=1 cold-read — filed at the top of `docs/plans/3-controls.md` + `docs/council-log.md`. "Tests green" ≠ "the warning was seen to draw." (b) the **date route's $0 ruin tail is long-horizon-SQUASHED** (`?seed=dateborder`: ~$6M p90 plume flattens the tail) — the spine `?seed=borderline` is the clean ruin-tail render. This is the same `LONG-HORIZON BAND-SCALE SQUASH` parked item, now seen on the date route.
- **THE TWO-PANE HONESTY-FLOOR e2e — ✓ SHIPPED inside `verify:fit` (2026-07-08):** the `.band-droppable-label`-visible-at-1088px assertion + the 3 legend rows ride the vertical-fit gate's honesty-floor arm (mutation-proven: the ≤260px container query widened to 600px went red).
- **THE CURVE-DRAWER (D2c) — TONE COLD-READ OWED (2026-06-29):** built + verified live; needs Briggsy's eye on the real render (`?seed=date`, open the "How your odds shift by when you stop" drawer). **Three things to judge:** (1) **tone/layout** — the full 0–10 ladder leaves the bottom ~60% empty (council-mandated no-truncation, but sparse), and below-bar dots carry NO numeric odds (only the crown shows "9 of 10"; others read by position + a11y). Are per-dot odds labels / a leaner rung window wanted? (2) **the DIP not seen live** — the `?seed=date` curve is monotone (household both ≥64, no pre-65 ACA cliff), so the ACA-cliff "doesn't hold" rendering is unit-tested but UNSEEN. *Prescription if wanted: add a pre-65 near-cliff non-monotone dev seed to `src/ui/devSeeds.ts` so the dip cold-reads live.* (3) **the no-date plotted curve** — still DEFERRED behind the Hawk veto (a worded "how close" line ships instead); returns only behind a no-pickable-dot planted-fail + this cold-read (`docs/council-log.md` ⚑).
- **THE SCRUB READOUT'S DEAD-COHORT NOTE — WORDING (council-resolved 2026-06-28 — see `docs/council-log.md`):** `copy.bandReadoutThinNote` is now **"Too few couples to show a range."** (was "Few couples reach these years"). Council drafted + measured the line; Briggsy cold-read-picked it. It names the honest WHY (sample too thin to quote, *not* "you're broke"), is **width-verified** in real Source Sans 3 (164px of ~200px usable — the rejected 'live this long' consensus variant overflowed at 230px), and is now **catastrophe-gated** (`isMortalityKey`, `copyGuard.ts`). FAST-FOLLOW ✓ SHIPPED (2026-07-09 late, commits 3131a887 + the 6c9fed39 race fix): `e2e/design-tokens.spec.ts` measures the LIVE catalog string (+ the widest static label) in the shipped face against `READOUT_W − 2×READOUT_PAD_X` (the pad moved to `bandGeometry.ts` for the source bind, insight 032); the probe WAITS for the lazy IntakeApp chunk's stylesheet rule before measuring (the CI race, insight 070); mutant-proven both directions (a longer catalog string red; a nonsense awaited rule times out red).
- **AT-READER PORTFOLIO-RANGE PARITY — ✓ SHIPPED + VERIFIED LIVE (C2, council 2026-06-29, 8/10, `docs/council-log.md`).** A screen-reader user now hears the band's portfolio **dollar range** as ONE static `.sr-only` sentence (the pointer scrub stays aria-hidden). Layering: pure `selectAtRangeColumn` + `AT_RANGE_COHORT_MIN` (`src/viz/bandGeometry.ts`, planted-fail tested) → `composeBandAtRange` (`src/ui/bandPanelChrome.ts`, single-sources the SAME resampled `tooltipRows` the scrub shows) → `atRangeSentence` prop rendered in `ConfidenceBandPanel` (string-free viz) → wired into BOTH routes (`ConfidenceStatement`/`FuckOffDate`, `useMemo([resolved])` so it re-renders with the band on a scale re-key — insight 047). Column = deepest INTERIOR lattice point with cohort ≥ `AT_RANGE_COHORT_MIN`=0.75 (never the survivor-dominated gate edge, never the horizon; withdraws to silence if none qualify). Survivor-neutral anchor (years-from-now), range-first/median-subordinate, **$0-spoken-as-depletion** in THREE shapes (normal range / ruin / total-depletion "gone"). Verified live (real Chromium): `?seed=retired` normal, `?seed=borderline`+`?seed=date` ruin, `?seed=failing` gone — exactly ONE node each. **Margin (0.75) + the three sentences ✓ APPROVED 2026-06-29** (tweakable later — change `AT_RANGE_COHORT_MIN` for the year, the `bandAtRange*` slots for the words).
- **THE LONG-HORIZON BAND-SCALE SQUASH (council-resolved 2026-06-28, conf 8/10 — see `docs/council-log.md`):** ~$20M p90 squashes the median on the date route's ~61y horizon. **CAP LEVER = DON'T BUILD.** The council reversed a unanimous open-to-cap after *measuring*: on the real ~61y route the median lifts only ~9px→19px in a 342px plot (intrinsic ~19:1 living-cohort range; log forbidden by the floor law), the cap silently rescales the shipped spine band 34–50% (both bands share `resolveBandData`, `answerView.ts:65/96`), its no-op guard is vacuous (`linearFan` bottoms at cohortFraction 0.6, never crosses the 0.5 fade — the insight-048 untested-gate trap), and it manufactures a *mirror* false belief (p75–p90 pancaked flat against the ceiling = false hard wall, worst for the color-blind reader). The axis-scale lever is the wrong tool. **Gate institutionalized: no band-scale change ships on an unmeasured readability claim.** ⚑ STILL YOURS: (a) tone cold-read on the real render — is the honest-as-is squash tolerable, or serve first-glance comprehension via a NON-axis aid (make the scrub readout primary/discoverable + a worded deep-tail note reusing `bandReadoutThinNote`)? (b) [VERIFIED 2026-06-28 — not a bug] the ~26% swing is the **expected** provisional(2k)→final(16k) path-count tier change (finer p90 tail); the UI re-draws not morphs (`FuckOffDate.tsx:152`); the 88.88M/65.42M figures are ungrounded (appear nowhere in code). **Latent U8 note:** `seed` is persisted but `tier` is NOT, so a completed date-route scenario could reload at the coarser provisional axis — handle at Save/reload.
- **Spine deriver near-Today tick skip (advisory, review 44f8b0c0):** `deriveDateBandAnnotations` drops a decade tick within the pad of Today; `deriveSpineBandAnnotations` does NOT, so an EARLY-retiree spine band (retired at 58/59) stacks an age-60 tick on Today. Cosmetic (the value is correct). Apply the same skip for consistency, or accept — Briggsy's eye.
- **The work-income field meaning (C3) — ✓ RESOLVED → Option B, SHIPPED + SIMPLIFIED 2026-06-30 (council 8/10 + Briggsy's cold-read, `docs/council-log.md`).** The field means FULL working-year income = pay PLUS working-year investment income; modeled R40 streams (pension/rental/annuity/alimony) stay OUT (KTD-9). The live `model.ts`⟷`copy.ts` contradiction is closed, the optimistic "usually the same as your pay" nudge killed, the "wages-only" comments disambiguated (engine-sense = exclude MODELED streams), the `?seed=dateborder` attribution landmine fixed, KTD-9 record reconciled, source-bound copy regression tests added.
  - **The intake does NOT re-ask pay (simplified 2026-06-30, Briggsy's cold-read).** It was first a two-input split (pay + investment); Briggsy's N=1 read flagged the duplicate pay question as confusing — and the engine can't model a time-varying salary anyway, nor do we ask the quit date (it's the swept output). So: the IRMAA override is `earnedIncomeReal + investment`, derived at `intakeMap.buildDateInput`; the step asks ONE field per working spouse — `workingYearInvestmentByPerson` (the genuinely-new, cardinal-sin-relevant fact — a blank can't become a silent $0, `intakeMap.test` guard). `workingYearWagesByPerson` was removed. The steady-pay simplification (a bonus/RSU spike near 65) is DISCLOSED on the step (conservative-or-disclose). This reversed the council's separate-pay-field call on Briggsy's comprehension signal; the look-back IRMAA-seed fields stay single (a filed return's MAGI is a reported total).
  - **(b) the new wording ✓ APPROVED (2026-06-30 — Briggsy: "wording is great").** C3 is fully closed. Tweak path if his eye returns: the `workIncomeIntro` / `workInvestmentLabel` / `workInvestmentHelp` / `workIncomeDisclosure` slots.

- **⚑ FUTURE — designer pass (Briggsy 2026-06-30, non-blocking): per-spouse help text repeats in `Paired` steps.** On two-person steps the per-field help paragraph renders identically under both "You" and "Your spouse" (the work-income "Investment income on top" help; and similarly the SS / salary steps). Not wrong, just visually repetitive. Have a designer weigh in on hoisting shared per-field help to a single step-level line vs the current per-field repeat — a `Paired`-component pattern decision (`src/intake/questions.tsx`), not C3-specific.

**⚑ RESCUED FROM THE COLLAPSED ARCHIVE (2026-07-26):**

- **HIS EYE — THE STACKED TAPE ROWS, `07-08 → 07-23`** — empty verdict columns at `docs/caddie/tape.md` `:29`/`:30` · `:34`–`:37` · `:39`–`:43` (`:43` — CF3 tone pilot-cleared calm 07-10, reversible at his glance, row stays UNSCORED). Laptop **1536×791**; phone needs `pnpm dev:phone`. **A pilot clearance never scores a row** (`:21-22`); U13 card #2 CLOSED (`cold-read-log.md:265-267`) — **do not re-park**.
  - (a) Back-fill a **07-08 row after `tape.md:44`** (`?seed=health`, +17px; `?seed=dip`'s Roth-door TwoFutures scrub); (b) add a **Supersession note at `tape.md:46`** — U17 reworded that frame post-filing (`copy.ts:1160-1167`); `:56-58` scores what he saw.
  - (c) **Pending CHARACTER** and (d) **delta-as-hero WORDING** — `?seed=nc` (~90s) then `?seed=surplus` (~6min): the `.solve-pending` breathe + "this can take a few minutes", then the "$280,000 more" hero beside the re-referented `recSkewMedian` line; scores `:30`+`:29`. Tempo = `tokens.css:153`+`base.css:183`, **never per-surface.**
  - (e) **`spendHelp`, TWICE:** "Somewhere else" ⇒ `copy.ts:113` (state bill **INSIDE**); NC/PA/FL ⇒ `:127` (**OPPOSITE**) — `caddie-walk.spec.ts:737-738` picks NC, so captures since 07-15 are priced-only.
  - (f) Verbatims **also score the Opus-vs-Sonnet diff** (`:29`): flip the five perception seats to `'sonnet'` ONLY on a clean scored diff (`caddie.js:85-95`) — the `false-pass` hunter (`:95`) and every refuter stay Opus, **never flipped on cost grounds.**

- **THE AGED SURFACE — FOUR tone calls at `?vault=datearrived`; his eye due BEFORE 2027-01-01, not "later"** (S6 mints the plant; `AGED_PLANTS` **`devSeeds.ts:1470`** — **re-corrected 2026-07-31 from `:1352`, which was itself ~118 lines stale**; it holds **SIX** keys today, **not five** — the old count omitted `datearrived: { base: 'dip', doctor: doctorArrivedVault }` at **`:1493`**, in a bullet that is itself about the datearrived plant: `stale`/`datestale` (`doctorStaleVault`), `statestale` (`doctorStateStaleVault`), `rec`/`recold` (`doctorRecordHolds`/`doctorRecordSuperseded`), `datearrived` (`doctorArrivedVault`); the entry contract is `interface AgedPlant` at **`:1465`** = `{ base: DevSeedKey, doctor }`): elapsed-segment de-emphasis · premise wording · the arrived idiom · **the WITHDRAWN work-stops marker** — S2.1 killed "Work stops" before "Today" (`bandAnnotations.ts:342-343`, honored hawk veto), so the call is now the *silence* — honest, or missing?

- **BLOCKED, NOT DROPPED — the no-dollar compose face has no live witness (do NOT read its tone from source).** `noDollar` (`recommendationView.ts:377`) puts `recComposeAlready` (`copy.ts:1325`) **beside the confident grade word** (`:395`). **The hunt IS the refusal record — do NOT re-run the same grid** (`hunt-no-dollar-witness.ts`): **re-entry needs a REGIME change, not resolution** — at `tieTolerance: 0` the crown is seed-A survival alone. Constructible later (`tieTolerance`>0, date-route parity, `payload.noChange` `:341`) ⇒ mint it beside `steer:` + a `devSeeds.test.ts` arm. Unfunded ⇒ **permanently synthetic**; then **HIS call** — gate `word` (`:395`) on `noDollar`, strike "the compose state's 'already' relief" from spec `:396`.

- **U17 — the fresh SPLIT face has never been cold-read.** Add **`seed:datesplit`** (`caddie-walk.spec.ts:45`) to the S6–S7 walk beside the aged seeds. Two ⚑ questions: (a) `bandClockWorkStopsSplitLabel: 'Essentials date'` (`copy.ts:511`, drawn `bandAnnotations.ts:350`) — does naming it close the fan-vs-hero coupling (`cold-read-log.md:380-391`, disposition `:425` HARD-FLAG/PARKED), or does he still bind it to the headline? (b) `bandFollowsFloorNote` (`copy.ts:472`) — "opaque to a stranger", the disclosure that chair-kill rests on.

- **⚑ ARMED FLIP — the F3 legend dissent (O14).** `stateResidenceLegend` (`copy.ts:94`) is tense-neutral while the heading route-swaps; **a Caddie panel's "honest and plain, not cold" is NOT the verdict** (`cold-read-log.md:83`). **If he reads it FLAT the flip is four edits, not a word swap:** `stateResidenceLegendRetired`; a `legendKey?` prop through `SegmentedControl` (`questions.tsx:559` hardcodes it); `stateStepRetired` (`:592`) must override `render`, not just `headingKey`; the `AssumptionPanel.tsx:509-513` seat off `anyWorking(draft)`; pin both (`questions.test.tsx:540`). **If unremarked:** strike `copy.ts:90-93`, score `tape.md:34`.

- **O15 — the 'elsewhere' uncapped grade. FRAMING FORK, HIS/council call, NEVER a copy fix.** Answered-but-unpriced reads the IDENTICAL uncapped "On track — 9 of 10" (`cold-read-log.md:107`; `tape.md:35`). **His read:** `?seed=elsewhere` beside `?seed=fl` — is the same 9-of-10 honest here? **HOLD ⇒ ratified; CAP ⇒ a NEW grade seam, own council** (`stateTaxDisclosure.ts:33` byte-identical; never fires for FL). **STANDING TRIGGER — until it fires, do not re-raise:** `PRICED_STATES` grows (`constants/stateTax.ts:50`) or the picker stops collapsing `{SC,GA,DE}`.

- **STANDING PARKS.** (a) **O16 tape-watch, every walk:** `verdictMedicareResidual` (`copy.ts:941-942`) is frozen claim-free while `spendHelp` (`:113`) says "keep that bill inside this figure"; flips ONLY if a reader reads that as an invite to double-count — the rider then lands **atomically with the E13 Lead referent**, **do NOT half-patch one home.** (b) **9-of-10 vs eight-in-ten** (`copy.ts:1605` beside `:461`/`:495`): Council Q1 **RATIFY** binds the 07-10 chair-kill (`council-log.md:18`) — **no unification, no de-collide; do NOT "fix" this in any copy/rule-36 pass.**

- **THE FIDUCIARY'S CURRENT-LAW-AS-WRITTEN CAVEAT — his yes/no, never a rider; unanswered since 2026-07-09** (`council-log.md:27` + `medicare-pricing-build-spec.md:82`). Owed: *"Scheduled expirations like the 2025–2028 senior deduction are modeled as expiring — if extended, your later-year picture improves."* Flip condition verbatim: *"flips on the hawk ruling the OBBBA-2025 vintage label insufficient."* **If YES:** `assumptionTaxLaw{Name,Value}` beside `assumptionIntro` (`copy.ts:1006`), copyGuard-checked (`copyGuard.ts:63-65`), **never re-typing "2025–2028"** (`tax.ts:141-145`); `'tax-law'` through `assumptionRegistry.ts:37-58`/`:158`/`:170` in one move or `tsc` reds; ship **hawk-gated, four-skill UI loadout, its own Caddie walk, never stapled to another pass.** **If NO-GO, close BOTH filings in the same pass.**

- **THE PHONE-RHYTHM PASS — STANDING, OWED, never graded** (`pnpm dev:phone`, 390×844@3). **The question is fold ORDER and rhythm, never fit — the phone scrolls by design.** (a) `?seed=elsewhere`, the unpriced disclosure below the fold (O15); (b) `?seed=date`, the primary CTA below the fold (`cold-read-log.md:768-769`); tweak surface = `result.css:175`/`:195` + the phone branches (`fuckOffDate.css`/`confidence.css`). **Do NOT carry the old "trim the date route at 1366×768" half — superseded** by the ORDER contract (`vertical-fit.spec.ts:695-730`).

- **TWO NEVER-GRADED FACES — fold into the S6 aged-seed walk, never standalone.** (a) **`?seed=datemixed`, never cold-read by any oracle** (`devSeeds.test.ts:230-231` — floor dated, lifestyle `no-date-in-window`): append to `CADDIE_TARGETS` (`caddie-walk.spec.ts:91`), chair the card. (b) **The save-ceremony register** (`copy.ts:584-586`, `:591`, `:684-686`) — a wording call touches those slots alone; drive `?vault=retired` → unlock → edit a figure (View-only needs a SECOND tab).

- **THE LADDER — a HIS-CALL fork, no prescription writable until he rules.** `ladderXAxis` (`copy.ts:483`) + `ladderOffsetTick` (`:1642`) speak years-from-now; every other chart speaks the fan's ages (`bandAnnotations.ts:212`). Fork = ages-tick vs keep-as-is; if ages, `oddsLadderChrome.ts:23-24` → `bandClockAges`, moving `twoFuturesChrome.ts:205`'s consumer in the SAME commit (086).

- **U16 §Q5's NC contract is 1-of-3 delivered and the docs claim otherwise — HIS call.** A `certification-pinnable` reason (`constants/stateTax.ts:72,77`) blocks the WHOLE token (`oracleToken.ts:98-103` → `:327-329` → `heldView`) — NC gets no rec; `recHoldCoupling` rides only `withheldConversionView` (`recommendationView.ts:601-614`), levers (`solve.ts:243-250`) Medicare-trend-only — **dormant**. **Recommend (B):** stamp the U16 spec `:215-228` with a dated deviation and correct `roadmap.md:100`'s "coupling caveat" — it rides the dormant conversion-only render, never NC. **(A)** (state reasons into `enumerateWithheldConversionLevers`) **must NOT ship without proving sequencing rank-invariance across the NC 3.49%/3.99% pair**; **the hawk veto already forbade the uncertified 3.49%**.

- **⚑ THE BUDGET HEALTHCARE-CATEGORY FORK — a one-way door the batched-oracle grant does NOT auto-clear.** `council-log.md:34`'s fiduciary dissent (a scoped OOP-only `healthcare` category off `oopMedicalAnnual`) was never graded. Adopt, or keep it compile-injected and invisible? If adopt, insight 055 gates it — re-derive the sticky map × degenerate scope BEFORE the enum grows, then add it to `isSurvivorSticky` (`budgetModel.ts:33`) + the label map (`BudgetLineItem.tsx:36-41`). The `model.ts:554` re-date + sticky disclosure ship **either way** — UNFINISHED FIXES owns those.

## Deferred (engineering, not cold-read)
- **THE RICHER MARKET DRAW — block-bootstrap + stochastic correlated inflation (`docs/decisions/market-model.md` §7).** The spine is i.i.d. parametric lognormal with a fixed deflator — optimistic on sequence/inflation *regime* risk, the one axis the cardinal rule swears off (the record names each caveat's error direction: §3 no autocorrelation, §4 the US-survivor rail that doesn't bite at the 85% on-track line, §5 inert inflation `stdDev`). **Build when any §7 trigger fires:** (1) the headline's *number* (not just direction) becomes load-bearing at the 85% line; (2) a non-survivor/global series is added to the historical anchor; (3) nominal/inflation display ships (the inert `methodology.ts:36` `stdDev` goes live). **Prescription:** (a) add a block-bootstrap-of-the-real-series and/or regime-switching draw mode in `src/engine/simulate.ts`/`rng.ts` behind the existing `(params, seed)` purity contract — **the CRN dimension-only draw order + single-shared-draw invariant DO NOT move** (`docs/architecture.md`); (b) add stochastic real-return-correlated inflation as a THIRD market factor in `buildDraws` (a new normal per path-year), wired through the deflator the overlay + SS COLA read; (c) **externally-derived fixtures for both** (DND 012 — block-bootstrap success rates + inflation-correlation moments derived by an independent path, committed, proven by hide-the-artifact clean-clone AJS 008; never the engine validating itself); (d) re-scope the MC-below-historical assertion (`simulate.test.ts:66`) to state explicitly which percentile band it constrains, so the over-funded-region rail can't be mistaken for a decision-boundary guarantee. Until then the honest contract is the as-built model in the record.
- **Scrub-interaction e2e (advisory, review 99dc2508):** the scrubber's *render-time* honesty gate is now unit-guarded (insight 048), but the POINTER glue — variant gating (drawer ignores touch / enlarged drag-scrubs), the 047 remount-clears-`idx`, the today-column (idx 0) surviving the `!== null` checks — is unreachable in jsdom (`getScreenCTM` returns null) so it has no regression guard (verified live, not pinned). If wanted: a Playwright e2e (the `scripts/serve-dist` + `csp.spec.ts` harness exists) that scrubs a thinned year (note replaces dollars), a full year (dollars shown), and asserts scrub state resets on a scale-change remount without closing an open modal. Logic is sound + seen-working; this is belt-and-suspenders.
- **DateBand → move `band` INTO the crowned track variants (U9):** today `band?` hangs at the run-level 'dates' outcome and carries its own `offsetYears` (self-describing — done). Once U9 splits floor/lifestyle into two offsets, move `band` onto the crowned `DateTrackOutcome` variant so present-iff-crowned is unrepresentable-when-violated and each band ties to its own track. (Advisory from review 44f8b0c0.)

**⚑ RESCUED FROM THE COLLAPSED ARCHIVE (2026-07-26):**

- **U15/MEDICARE-TREND — three fixes riding the next `healthOverlay.ts`/`taxOverlay.ts` commit, never standalone.** (a) hoist `partBPricingByT` out of `simulate.ts:1445`'s loop (`taxOverlay.ts:1110`) → optional `TaxYearInputs` (`:220`), **length-vs-horizon throw, no module memo**; sweep `:901`'s false "bound once per run"; **unmeasurable ⇒ WONTFIX**. (b) `healthOverlay.ts:589`'s Part-D walk → `:568`. (c) finiteness guards **ABOVE** `:699`. **Re-open `wf_05ad8c04-f86`'s journal first, enumerate the five unnamed fixes here; gone ⇒ the six are the whole set, strike the gap.**

- **SOLVE LANE — ONE trigger: a profile proving the committed-first spine lane STARVES; do NOT re-raise "waits on the profile".** (a) Cancel is dark — `engineProtocol.ts:262` passes no `shouldAbort`; **the landed commit-epoch half is not coverage; decide first or it's vacuous:** `ShouldAbort` is SYNCHRONOUS (`cancel.ts:29`) ⇒ go async. (b) Interactive tier **DEFERRED ON THE RECORD** (`act4-u16-…:302-342`); revival needs a design cutting DISPLAY latency with no down-sampling of any displayed figure, then re-clearing `fallback.ts:42,63` (**a FLOOR, not a pruning proof**).

- **DATE-ROUTE RECOMMEND-SECOND PARITY (+ Q1 + heir bracket). TRIGGER: council/his eye ruling the SHAPE** = the still-working solve base (crowned candidate, crowned offset). Point `solveDispatch.ts:67-68` at `buildDateInput(draft)?.params`, **delete BOTH `!isDateRoute` gates TOGETHER (`Result.tsx:316,405`)**, and fill `recommendationView.ts:412`'s `undefined` Q1 `survivalContext` from the rendered `ConfidenceStatementView` **BY REFERENCE, never re-authored** (`:172-175`). **HEIR BRACKET = HIS/council call: does 0.24 stay, does the R7 editable home ship;** if pinned, only `solver.ts:126-133` changes.

- **3rd-GOAL UNIT. TRIGGER: `live-bigger-now` joining `RECOMMENDATION_GOALS` (`model.ts:270`)** — same commit rules the downgrade: `scenarioCodec.ts:828-829`'s `needVocab` throws `Corrupt` on the WHOLE vault — keep the loud refusal (reasoned into `:823-827`) or take U17's tolerated-drop precedent (`:832+`). **Either way rewrite `scenarioCodec.test.ts:684`, never delete it.**

- **PER-ARM FLOOR VERDICT — never ruled; `engineWire.ts:241-244`'s floor TRACK unread. TRIGGER: a budgeted-arm surface asks. DECIDE FIRST: do budgeted arms speak a floor verdict, or is it the spine's `floorRelief` alone?** If yes, **do NOT patch the packer** (`:239-240`): export `buildFloorReading` (`confidence.ts:192`), add `floorReadingB?: Headline` to `SolveArm` (`solve.ts:98-111`), carry it **VERBATIM** to the UI.

- **THE DETAIL-DOOR ERA — its OWN post-U16 unit + walk, NEVER a rider; rider and gate-behind-door REJECTED with cause, don't re-litigate** (`state-tax-build-spec.md:236`, §5 `:187-189`). **TRIGGER: his eye on a row, or the dollar door asked.** Rows: PA's no-dollar line (`copy.ts:972`), the $280k row, unglossed NIIT + plan-cost-sharing (`copy.ts:844,846,894,900`), O14(d), a `'medicare-trend'` seat + disclosure (`assumptionRegistry.ts:37-201`), **O10's single-filer BRACKET** (`copy.ts:1809`) — **DETERMINE FIRST:** a `buildSurvivorReading` field (`confidence.ts:169`) or a static disclosure, **never a UI-side re-derivation**. **Register it in `docs/roadmap.md`'s You-Are-Here table** (`act4-u17-…:491`). **REWORD GUARD: `copy.ts:972`'s record is `devSeeds.ts:866-874` + `devSeeds.test.ts:380-392`; never restore the refuted byte-identity guess.**

- **O-LANE COPY MECHANICS — each rides its surface's next touch.** **O1 DECIDE THE FORK**: `confidence.ts:236` → `state === 'already-failing' || gap >= T`, **T = HIS card-#1 read off a real drive, never invented.** **O2** `copy.ts:1777-1778` → `…more a month than your plan spends today.`, threading `unpricedDisclosureActive` onto `VerdictDisplay` (`verdictSentence.ts:27-32`) — **both consumers, ONE commit**. ⚠️ **Never net the dollar in-engine** (`confidence.ts:214-217`); **`medicareUnpriced` is a DEAD SYMBOL — gate off `showMedicarePricedNote` (`healthSheetChrome.ts:336-341`), never off ages. FRAMING GATE: disclosed-out vs inline-caveat is PARKED for Briggsy.** **O4** Escape-focus arms (`controlSheet.tsx:115-121`, `BudgetBuilder.tsx:185`). **COLLISION:** bind bare "eight in ten" (`copy.ts:461,495,1587-1590`) to the band; keep the `:1452` allowlist.

- **CADDIE INCREMENT-4 RESIDUALS — MOVE THEM OUT OF `TODO.md`** into `## Post-gauntlet obligations` at `.claude/skills/caddie/SKILL.md:213`, carrying **Briggsy 2026-07-11, "evaluate effectiveness, then maybe globally deploy"** verbatim. **TRIGGER: the first gauntlet's wave scored — unfired; do NOT run early.** Then score every covered row (FALSE-PASS ⇒ family sweep), compute the four rates, judge saturation (minting = chasing, flattening = converged); **only if it graded well** mint `~/.claude/skills/caddie-builder/`, PATTERN only — **HARD LAW: corpus and harness NEVER transfer; port only `[briggsy-general]` rules. Never manufacture a consult-mode question; never score the two pre-law rows.** ~~Land 084's schema caps in `caddie.js` + `caddie-ab.js` **BEFORE** this walk.~~ ✓ DONE 2026-07-27 (`d5d77191`) — see the AGENT FANOUTS landmine for what the caps are and why they could not ride `COMMON`.

- **⚑ GATED — the budget's state-adaptive second beat** (`council-log.md:31`), at `Result.tsx:453-457`. **TRIGGER: the R27 `dateinvert` mint (UNFINISHED FIXES owns that engine gate — do not restate it here), THEN his cold-read. BINDING — the hawk's hard veto: strictly DIRECTION-NEUTRAL, never "the essentials date comes forward"/"the dates split apart"** (enforcement lives once, on the budget-axis LANDMINE). **REJECTED, don't re-propose: (B) the synthetic 2-line budget; (C)'s mandatory step (R5 never-gate).**

- **THREE COPY OBLIGATIONS.** (a) `copy.ts:1065-1066`'s "Research" claim + `sanity.ts:266-273`'s `≤ 0`-only gate: **one `search_with_grounding` + `curl` at Blanchett (`docs/research/engine-validation-and-tax.md:183`); unretrievable ⇒ drop "Research" as a lineage claim and record the refusal there** — never an invented lineage; warn tier **on his ask only**. (b) `copy.ts:764`'s "three accounts" promise to one-account households — **filter `SequencingControl.tsx:181-207` at render, or reword count-free.** (c) `answerProvisionalTag` (`copy.ts:378`) lacks a return variant — **RE-PRICE against U13 re-entry** (`IntakeApp.tsx:78`→`Result:394`). **DETERMINE FIRST: intake-side strip, or Result-only?** If Result-only: `answerProvisionalTagReturn` at `ConfidenceStatement.tsx:357`/`FuckOffDate.tsx:343` off a `returning` prop mirroring `stalenessNote`; check `copyGuard.ts:63-65,102-116`.

- **TWO UNPINNED GATES.** (a) ⏰ **DATED, before 2027-01-01:** `tax.ts:28`'s `TAX_YEAR = 2026` has no `verify:tax` ⇒ **2027-01-01 ships 2026 brackets green** — add `taxVintage.reverify.tripwire.test.ts` on the `irmaaTopTierReindex.tripwire.test.ts:27-43` idiom (`wallYear <= TAX_YEAR`); **determine first: IRMAA class or ACA class?** (b) Phone ReEntry has no contract — pin ORDER, not the CTA-in-frame bound (`vertical-fit.spec.ts:967-970`); amend `reviewSurface.ts:25`. **FORK FOR BRIGGSY: accept-and-record instead?**

- **TWO AssumptionPanel OBLIGATIONS. TRIGGER: next AssumptionPanel/AnswerStrip touch.** (a) Truer-picture fires on a COUNT drop only (`:271`); `xOfTenClamp` caps at 9 ⇒ over-funded→on-track dark — OR in a `wordFell` off `STATE_OPTIMISM_RANK`. **Predicate ships now, copy does not** — `assumptionTruerPicture` is false on a word-only drop and carries his 2026-07-10 clearance; **decide: one sentence for both axes, or a second slot (recommended).** (b) Export `sequencingDoorReachable`/`rothDoorReachable` from `intakeMap.ts:659` for `Result.tsx:379,397,458,465` — **⚠️ never fold in `AnswerStrip.tsx:97`, the $0-portfolio trust guard** — and hoist `compactMissingFactNames` out of `AnswerStrip.tsx:39-42,272-277`.

- **FIVE SMALL DEFERRALS, each on its OWN trigger.** (a) **`crownFor`** — if `gradeCalibration.test.ts:298/310/335/350` no longer need `namedDriverProbe`'s optional `crownFor` (`gradeCalibration.ts:355`), make it required and **delete the silent fallback `:364-372`**. *Fires on a second caller or calibration-arm edit.* (b) **ACA/door households never hear extras are held flat** (twin does, `copy.ts:969`) — widen `composeControlHealthOmissionsNote` (`stateTaxDisclosure.ts:90-92`) with an `extrasPriced` arg + NEW key, **never rewording `copy.ts:893-894,899-900`** (`stateTaxDisclosure.test.ts:133` pins byte-identity); gate `HealthcareSheet.tsx:212` on `extrasLines !== null`. **Own touch, own walk — never a rider.** (c) **$1k shadow-drag straddles the 133%-FPL jump** (`magiLandscape.ts:275`'s `STEP = 1_000`) — export `applicablePctDiscontinuityFractions` (`healthOverlay.ts:232`), clamp below the next jump (`−0.01` load-bearing); **not the 400% cliff** (`healthSheetChrome.ts:178`). *Fires only on a sub-$1,000 readout figure.* (d) **Single-user "just me"** — engine accepts 1..2 (`simulate.ts:544/549`): widen `memoryModel.ts:203,518`, narrow `draftFromScenario.ts:47-53`'s `!== 2` to `1 | 2` (**never coerce**), take `bandAnnotations.ts:253-258,314-321` + `copy.ts:1535,1575` off the age pair, add a solo plant, re-point `:17`. (e) **The "70 / 69" leader** (`ConfidenceBand.tsx:364`) — paint-only: `.band-annotation-leader` to `ROW_1 + dy - 10` (`band.css:62`), re-run `verify:fit`; **not the "anchor-shift" rewrite** (`bandGeometry.test.ts:208-281`).

## Landmines (carry forward)
- **`?vault` / unlock / save need a SECURE CONTEXT (`crypto.subtle`) — a plain-HTTP LAN-IP origin has NO WebCrypto** (caught live on Briggsy's phone 2026-07-10: `http://192.168.x.x:5173/?vault=…` dies at PBKDF2 `importKey` and the app SILENTLY renders footer-only; `localhost` is exempt, so laptop dev never hits it; `?seed=` routes touch no crypto and work anywhere). **Phone walks of vault surfaces use `pnpm dev:phone`** — https on port 5174 via mode-gated `@vitejs/plugin-basic-ssl` (self-signed: accept the phone's cert warning once). NEVER make dev https by default — the fit/caddie/CSP harnesses probe `http://127.0.0.1` webServer URLs. Prod (Vercel) is https; the silent footer-only no-crypto failure shape is a filed check only if self-hosting ever matters (the architecture §10 caveat family).
- **CHECK CI AFTER EVERY PUSH — a green local Windows run is not proof (burned/055 bit for real 2026-07-08):** main sat RED for two pushes unseen (a 5.7s engine test vs vitest's 5000ms default on ubuntu; green locally every time — squeaky ends the session before CI finishes). `gh run watch` after pushing, or at minimum eyeball `gh run list` next session-start. A vitest test whose LOCAL runtime nears ~3.5s needs an explicit `{ timeout }` (CI is ~1.4× slower; the dateSearch file shows the idiom).
- **Briggsy's REAL laptop viewport is 1536×791 CSS px @ devicePixelRatio 2.5** (screen 1536×960 logical; Chrome's UI eats ~121px) — measured IN HIS OWN BROWSER via claude-in-chrome, 2026-07-08. The old "~1871×917 logical (2x scaling)" from the 11d54dd4 screenshot analysis is **WRONG** — never tune a fit against it again. Playwright fit checks: use 1536×791 + the 1280×800 tier.
- **The one-frame fit law lives at `tokens.css --laptop-fit-height` (800px) — with the degrade contract** (doors are the ONLY sanctioned below-fold casualty; verdict/Medicare/band/R13-disclaimer protected — the Hawk's veto, council 2026-07-08). The R13 disclaimer is TWO mounts, ONE visible (in-frame at laptop, trailing below 68rem), mirror-pinned by `main.result[data-inframe-disclaimer]` — any new writer of Result's `computing` must keep the attribute mirroring the actions row or a zero/double-disclaimer frame becomes representable (`resultDisclaimer.test.tsx` pins the DOM order). Enforcement is REAL-BROWSER only (vertical fit is reflow-dominated — a token-sum arithmetic gate would go green while the frame overflows); the Playwright gate is LIVE — `pnpm verify:fit`, CI-wired (shipped 2026-07-08, item 5). Its final-frame synchronization rides `main.result[data-answer-tier="final"]` (memoryModel stamps the committing recompute tier on `ModelAnswer` — REQUIRED field, the compiler holds the door on new commit sites).
- **The DATE route scrolls ~470px at laptop BY DESIGN** (both graphs stacked — content-necessary; Briggsy-accepted 2026-07-08). Its honesty contract is ORDER: graphs → disclaimer → doors, doors always last.
- **`verify:bundle` reads `dist/` WITHOUT rebuilding** — a stale dist is a false green (caught live 2026-07-02: it reported the pre-U9b 221.8 KiB until `pnpm build` ran). Always `pnpm build` first ([insight 057](docs/insights/057-a-verify-gate-reading-a-prebuilt-artifact-verifies-the-previous-build.md)).
- **A reconciliation invariant has ≥ N writers for N inputs** — `annualSpendingReal` == Σ(budget lines@0) + `oopMedicalAnnual`; BOTH the builder Apply AND the oop-medical step re-reconcile at the call site (the store is @budget-banned). Any NEW writer of `oopMedicalAnnual` or of a year-0 line must re-reconcile atomically or the engine backstop demotes the answer to indeterminate ([insight 058](docs/insights/058-a-reconciliation-invariant-with-many-input-writers-is-not-closed-by-fencing-one.md)).
- **The BudgetBuilder lives in `src/intake/` (not `src/ui/` as the plan doc guessed)** — it's a form over the intake field primitives, and ui→intake is the established import direction (Result mounts it like AnswerStrip). The plan's file list is superseded on this point; the ultramode review should judge the layering call on its merits.
- **The `?preview` harness does NOT exercise the live deriver** — it renders fixtures with hardcoded annotations. The decade-age ticks + the date "work stops" marker only show on a **real drive / a dev seed** (`?seed=…`), never in `?preview`.
- **The SURVIVOR READOUT is LIVE ✓** (2026-06-28 — was dormant; wired via the `bandFan` mirror). `SurvivorReadout` now mounts below the spine band on the already-retired route; `survivorConditioned` + `survivorReading` cross the wire presence-keyed. Spine-only by design — the date route renders a timing claim, not a joint-solvency verdict (the survivor obligation re-attaches if D2c surfaces a post-stop confidence verdict).
- ONE shared market draw/year (CRN); a fan-on run is byte-identical to fan-off on the goldens (now proven for the DATE candidate config too — `dateSearch.test.ts` reduce-to-spine). Stateless Box-Muller. Externally-derived fixtures (DND 012). The band renderer + producer are DONE — feed them, do not redesign.

**⚑ RESCUED FROM THE COLLAPSED ARCHIVE (2026-07-26):**

- **⏰ 2027-09-01 — DATED CI RED BY DESIGN.** `medicareTrend.reverify.tripwire.test.ts:32` pins edition **2026**. **Never move the date — re-pin the data** at the ~June-2027 Trustees release, ONE commit, in order: primary tables only (`medicare-cost-trend.md`); `health.ts:206`→`2027` + body `:209-232`, direction **not one-signed**; `:233` mint `'medicare-trend-2027a'` — `part-b-trend` then fires on every old-table vault, **BY DESIGN, do NOT suppress**; re-derive the shape pin; **LAST** the tripwire `:32`, moving it first disarms the guard.
- **⏰ 2027-01-01 — the aged frame stops being a dev plant.** `planClockAnchor` (`bandAnnotations.ts:96-101`) is a calendar-year difference off `startCalendarYear` (`memoryModel.ts:515`): **every 2026-built plan flips to `yearsSincePlanBuilt = 1` with zero elapsed days**, arming every aged arm for real households (`curveMarks.ts:124`/`:137`). The deadline owns **the PARKED aged-surface tone calls (`?vault=datearrived`)** — his eye due BEFORE 2027-01-01, not "later." Repair `roadmap.md:89` + `cold-read-log.md:377` (both file it at the collapsed "TODO item 0"; its ladder claim is refuted).
- **KNIFE-EDGE DEV SEEDS RE-SINK ON ANY COST-CONSTANT CHANGE — re-tune the household knob, NEVER loosen the pin.** `retired` (1.055M IRA): **+40k sits ON the 0.85 edge**, **+60k kills the `nc` crossing** (`devSeeds.ts:56-59`). `nc` holds BORDERLINE **0.8425** under the twin's **0.8585**, both from `devSeeds.ts:857-858`; **never edit source to match the queue**. `dip` (700k/299k) works **OFF-RAY (+42/+17)** — the proportional ray is EMPTY (`:521-525`).
- **TWO COPY-COVERAGE TRAPS.** (a) **The ACA adverse ceiling is FIXTURE-PROVEN ONLY — a walk's silence is NOT coverage:** `XOFTEN_CEILING_ADVERSE` (`copy.ts:1469`) needs `worstOfTen >= 10`, which no dev seed reaches. (b) **"around ~" is a CLEARED 3-slot idiom — flagging the double-approximation is a KNOWN FALSE POSITIVE; do not re-litigate.** `acaCostNet`/`subsidyRegimeCostDelta`/`subsidyRegimeCostEven` — **cite by SLOT NAME, never line**; `cold-read-log.md:65`/`:93`'s `1568/1630/1635` cites are stale, **strike them**.
- **TWO DELIBERATE DUPLICATIONS — do not collapse either.** (a) **DO NOT MERGE the two `RecommendationVizProps` builders** (`recommendationView.ts:426-450` vs `runnerUpVizFor` `:568-597`): different second operands (`:377` vs `:573`/`:579`); `deltaLabel` `:435` is hero-source-bound, `gap` `:575` binds to nothing. Correct `:147`; pin both directions (`recommendationView.test.ts:394`/`:527`). (b) **The two `mean()`s are a DELIBERATE fork, chair-killed at review** — `objectiveHeadline.ts:29` stays simulate-free, `evaluate.ts:74` is the oracle's independent reference. **The dedupe is available and wrong.**
- **THE RECOMMEND-SECOND SURFACE — two standing hazards.** (a) A **beat-carrying frame SCROLLS BY DESIGN**, the exception to the doors-only one-frame law: `confidence.css:399-407` gives a real beat a full-width r5 row; substitute contract = the date-route ORDER idiom (`vertical-fit.spec.ts:305-354`). **Do NOT hoist the disclaimer back into r4**; **do NOT generalize past frames whose `:has()` fired**. (b) The dormant `survivalContext` must LIGHT in the same commit as the date-route parity: `Result.tsx:406` passes `spineConfidence` into `RecommendedBeat` (`RecommendationSurface.tsx:170+`), source-bind-armed **on the COMPONENT**. **DO NOT close this by deleting the unused field.**
- **AGENT FANOUTS — WEDGE, THEN LOST BINS.** Playwright-MCP agents **wedge silently**: **a load-bearing browser drive runs from the MAIN loop, never a subagent.** **Every finding path must end in a NAMED BIN**: a journal's last entry is a **VOTE**, not a merged result. ~~**084's caps are UNAPPLIED**~~ ✓ **LANDED 2026-07-27 (`d5d77191`), both files in one commit, schemas byte-identical.** All four schemas capped (`maxItems`/`maxLength` sized so a maximal LEGAL payload fits under the truncation threshold), `findings` moved ahead of `observations` in **both `required` AND `properties`** (the emission order is what survives a cut — `required` alone would not have done it), and overflow now degrades LOUDLY: past the cap a seat names the number it dropped. **The correction worth keeping: the caps could NOT ride `COMMON`** — it reaches only five of the seven schema-emitting seats, missing both refuters and the first-look seat, i.e. exactly the casualties 084 names. A separate `SIZE_LAW` const is interpolated into all seven; a size law is output FORMAT, not product context, so it is safe even in the no-context first-look charter. **Do NOT mark `council.js` done**: bin unseated openers (`:454`/`:506`) as `abstainedSeats[]`, keep the hawk guard `:474-480`.
- **The budget axis copy is DIRECTION-NEUTRAL BY VETO and only a comment enforces it** (`copy.ts:142-148`; `council-log.md:31` HARD VETO). `budgetCta` (`:149`), `budgetSheetTitle` (`:151`), `budgetSheetIntro` (`:152-153`) name the AXIS only, never sooner/safer/sharper — the R27 inversion is reachable (`dateFloorInversionNote` `:407-408` ships the opposite). **Fix (the ONE enforcement home; the gated budget beat and R27 mint cite here):** a literal-regex arm beside `copyGuard.test.ts:130-136` asserting all three match `/sooner|earlier|later|faster|safer|sharper|comes? forward/i` zero times. **Do NOT add `'budget'` to `CONTROL_KEY_PREFIXES` (`copyGuard.ts:102-116`)** — that arms require-hedge, not direction.
- **TWO UNGATED INVARIANTS.** (a) **The R7 registry gate is TOP-LEVEL-ONLY** — a nested field compiles with no disposition and no seat (`assumptionRegistry.ts:75`); ship the GATE-SCOPE header warning now. **Do NOT half-patch with a runtime seat assertion and no compile gate** — omission must be a `tsc` failure. (b) **The clock is ONE local-calendar chain — never re-derive a wall year, never go UTC:** `currentEpochDay()` (`scenarioFromDraft.ts:62-65`) subtracts `getTimezoneOffset()` on purpose; UTC expires budget windows early (`staleness.ts:601-610`). **Deleting the offset term leaves the suite green** — add a source-bind arm to `planClockSeam.test.ts`, revert with **Edit, never `git checkout --`**.
- **THE FIRST REAL INSTALLED BASE ARMS TWO SIBLING BLIND SPOTS — one mint closes both; do not add a clock** (`staleness.ts:53-62`). **Trigger: the first real vault.** (a) A pre-unit vault's `annualSpendingReal` still bakes in Part B/IRMAA — pessimistic-safe, but **no code may ever rewrite the stored figure**. (b) Engine-domain changes have no clock: an all-65+ vault recomputes LOWER, stamps equal. **Both:** mint a Q7 era key in `ERAS` (`appDefaults.ts:35-37`, add-only, never edit `'p2-d1'`) + re-point `:41`, **WITH** a discriminator on `AppDefaultEra` (`:31-33`) and an arm at `:417-422` skipping `staleness.ts:422`'s ratio conjunct.
- **THE STATE ROSTER GROWS ONE STATE AT A TIME — the 50-state brush is REJECTED WITH CAUSE, never revive without a council** (`architecture.md:170`) — **do NOT re-paste that prose into TODO; architecture wins on invariants.** **Do NOT half-add a state:** `PRICED_STATES` (`constants/stateTax.ts:50`), the `StateTaxVintageV3` field + stamp (`:391-397`), the picker arm (`questions.tsx:536-541`), the year through `stateStandardDeductionFor`. **Close the real gap now** — append to the JSDoc ending at `types.ts:198`, the ONLY graduated-state entry site: (1) re-derive k (`taxOverlay.ts:467-478`; new straddle channel, backstop `:507`); (2) the ceiling needs a STATE rail (`taxOverlay.ts:1465-1503`); (3) the not-yet-state-optimized caveat becomes MANDATORY (`stateTaxDisclosure.ts:18-21`).
- **LAYOUT — ONE SHELL, ONE UNGATED BUDGET.** (a) **A new sheet joins the family via `src/intake/sheetShell.css` — never by copying shell rules**: mount `<ControlSheet>` (`controlSheet.tsx:22`) or add your selector to the groups at `:18-19`, `:29-30`, `:65-66`, `:73-74`. (b) **The echo whitespace budget is UNGATED above 840px — the remedy is SPLIT, do not half-apply it: do NOT add `and (max-height: 840px)`** to `confidence.css:291-302` or the round-5 pair `:348-353` until a fit arm covers the untested 841–916px band — add 1536×841 to `e2e/reviewSurface.ts` + a `?vault=stale` arm (`vertical-fit.spec.ts:948-1082`). Rhythm-vs-fit there is **HIS call** — **do not raise it unprompted.**
- **Two U8 backup-door omissions are DELIBERATE — read the comment before "fixing" either.** (a) `ExportConfirm.tsx:82-85`: the CLICK records the sentinel (`:86`) even if the OS write fails — **do not trust the click less without a real completion signal.** (b) `BackupStep.tsx:11-15`: no `beforeunload`, no `holdUpdateApply` (unlike `SaveFlow.tsx:100`) — the vault is already durable; **do not copy SaveFlow's hold.** **Do not restate the rationale beyond this pointer.**
- **`gh run watch --exit-status | tail` returns the PIPE's exit code**, so a RED run reads as success — **watch UNPIPED, and confirm by explicit `gh run view <id>` regardless.** **AND IT FAILS THE OTHER WAY TOO (2026-07-27, run 30283045719): `gh run watch --exit-status` can exit NON-ZERO while the run is still `in_progress`**, so a healthy build reports as RED — that run finished `completed success` minutes later with `pnpm test` still mid-flight when the watch bailed. A false RED costs exactly what a missed RED does: it corrupts the "by explicit run id" law and invites a pointless bug-hunt. **THE ONLY RELIABLE READ IS A POLL ON THE STATE, NEVER THE WATCH'S EXIT CODE:** loop `gh run view <id> --json status,conclusion` until `status == "completed"`, THEN read `conclusion`. Treat `gh run watch` as a convenience for waiting, never as the verdict.

## Standing cadences
- `/ultramode-code-review` at every unit boundary; the four-skill UI loadout before ANY user-facing surface (CLAUDE.md "UI design skills").
- `/brief` (read `docs/insights/`) before a unit; `/distill` after.
- **Delegated build:** **native Agent Teams** for live-steer **eye-oracle** units; the **Workflow** tool for fire-and-forget **test-oracle** fan-out. Durable laws in memory `feedback-delegated-build-laws`.

## Verify-owed
- The OOP-medical figures (`src/intake/referenceData.ts` → `OOP_MEDICAL_TYPICAL_HOUSEHOLD`) are grounded-search-sourced, **not** primary-table-verified (`directionalUntilPinned`). BLS bot-walls `curl` — ask Briggsy to pull the CE "Age of reference person" table to pin them cell-by-cell.
