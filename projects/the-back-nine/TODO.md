# The Back Nine — TODO

> Actionable next-actions only. **Where we are** ("you are here") lives in [`docs/roadmap.md`](docs/roadmap.md) — the maintained per-unit status table. **What we're building + why** is [`docs/README.md`](docs/README.md). No session history here; git log has it.

## Next, in priority order

1. **R40 U4 — intake UX (U1+U2+U3 DONE & committed; U4 is next).** Decisions: [`docs/decisions/other-income-r40.md`](docs/decisions/other-income-r40.md) (nine KTDs); narrative: [`docs/plans/2-first-answer.md`](docs/plans/2-first-answer.md) R40 § Unit 4. **U1** (types, `772ca158`), **U2** (`compileIncomeStreams` + goldens, `7a87c009`), and **U3** (the atomic engine integration — streams threaded through seams 1/2 + the KTD-9 engine half) are landed, all adversarially reviewed. Build U4 per the plan-doc Unit 4 spec: `OtherIncomeEntry.tsx`, the opt-in expander, the no-safe-default survivor-%/alimony-date fields in-form, the "already receiving" toggle, the plain-language widow's-numbers row summary, and the neutral "not saved yet" affordance. **Load the four-skill UI loadout first** (back-nine-design > frontend-design > emil-design-eng > web-design-guidelines). Re-run `pnpm verify:bundle` after (income intake rides the lazy intake chunk).
   - **⚠ LANDMINE — the deferred KTD-9 copy half is owed by U4 (un-skip a tripwire).** U3 shipped only the *engine* half of KTD-9 (the engine owns each modeled stream's IRMAA-MAGI in all years). The *copy* half is THIS unit's: **(a)** re-spec the working-year override (`workingYearIrmaaMagiByPerson` → `irmaaMagiOverride`, built in `src/engine/healthcareStreams.ts`) as the **wages / non-modeled-MAGI component only**; **(b)** **invert the intake copy** — `src/ui/copy.ts` `workIncomeLabel` + `workIncomeHelp` currently say "the recent return's *whole* income" → must become "your working-year income **excluding** anything you entered as a retirement income stream." Until both land, an already-receiving taxable stream (a pension/rental paying in a §7-clamped working year) is **double-counted** in IRMAA-MAGI: the whole-income override carries it once, the engine's `ongoingTaxableIrmaaOnly` feed adds it again. Direction: over-states IRMAA/cost — **conservative, never optimistic** (why it's safe to ship deferred). Pinned by a skip-marked tripwire: **`it.skip('KTD-9 tripwire (copy half deferred to U4): the WHOLE-income override double-counts an already-receiving pension in IRMAA-MAGI')`** in `src/engine/__tests__/taxOverlay.test.ts` (the R40 · KTD-9 describe block). **After (a)+(b): drop the `.skip` — the tripwire then PASSES** (the stream counted once). Full context: KTD-9 + its SPLIT-DELIVERY STATUS note in the decision doc.

2. **U8 — save/load (brought forward).** `ScenarioV3` is defined; remaining = the codec v3 arm (a *separate* `checkPersonV3`, never mutate the frozen `checkPerson`), `AnyScenario` membership, the user-facing **first-Save ceremony** (passphrase-strength gate `zxcvbn-ts` ≥3 ∧ len ≥12 + recovery-phrase display + mandatory export), and load-on-return. NOTE: the encrypted-vault IndexedDB writer (`writeVault`/`rewriteModel` in `store/db.ts`) **already shipped with U4** and is wired into `backup.ts`/`session.ts` — U8 is the *flow* + the v3 writer, NOT a new persistence layer. Fold in the portfolio-holdings model field ([`docs/decisions/portfolio-holdings.md`](docs/decisions/portfolio-holdings.md)) on the same persisted shape — no v3→v4 migration.

3. **The first visible answer surface.** Two-pane laptop mockup (Briggsy's eye first — don't commit blind) → the U6 confidence-band render → U7 confidence statement + D2 state-adaptive surface. Load the four-skill UI loadout first. Detail: [`docs/plans/2-first-answer.md`](docs/plans/2-first-answer.md).

4. **Single-user "just me" mode** (deferred). A real people-of-one path — NOT "zeros for the spouse" (a phantom zero-spouse still carries a mortality curve and warps joint-survivor + the MFJ→single transition).

Then Act 3 (Controls, U9–U13) → Act 4 (Solver & Recommendation, U14–U17). See [`docs/roadmap.md`](docs/roadmap.md).

## Standing cadences
- `/ultramode-code-review` at every unit boundary; the four-skill UI loadout before ANY user-facing surface (CLAUDE.md "UI design skills").
- `/brief` (read `docs/insights/`) before a unit; `/distill` after.

## Verify-owed
- The OOP-medical hint figure (`src/intake/referenceData.ts` → `OOP_MEDICAL_TYPICAL_HOUSEHOLD`) is grounded-search-sourced, **not** primary-table-verified (`directionalUntilPinned`). BLS bot-walls `curl` — ask Briggsy to pull the CE "Age of reference person" table to pin it cell-by-cell. Conservative-by-design while it stays directional.
