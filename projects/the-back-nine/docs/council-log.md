# Council Log — the digest

Every Council of Elders verdict lands here, newest first. This is Briggsy's **after-the-fact review surface** under maximum-autonomy: scan it to see what the council decided and acted on without stopping you.

- **⚑** = executed at `yours-to-close` (a taste / direction call acted on at high confidence) — the ones you're most likely to want to eyeball or reverse.
- **action**: `executed` (council resolved it, pilot acted) · `surfaced` (brought to Briggsy — low confidence, veto, or hard-stop).
- Reverse anything: it's all reversible by design (hard-stops never auto-execute).

How it's run: `/council <issue>`, or the pilot auto-convenes whenever it would otherwise park a non-trivial judgment call. Mechanism: `.claude/skills/council/` (playbook + 9 elder charters) → `.claude/workflows/council.js` (debate engine).

| Date | Issue | Tier | Recommendation | Conf. | Action |
|---|---|---|---|---|---|
| 2026-09-05 | **What yields when the room runs out on the 320 reflow arm** — three exhibits, one mechanism (rem-fixed HTML chart text inside fraction-authored room): the band's scrub readout (13 px of every line past its own border box on the 238 px host, past the plot at the flip column), the ladder crown's headroom, the ladder's left "on track" label column. Full council **wf_1b45326f-9e8**, 21 agents, ~28 min. | council-decided · ⚑ one taste call HELD | The 38% cap is ACQUITTED — scale-invariant (0.380h under the 0.400h half-plot at every width); seven seats indicted it on circular arithmetic, all withdrew. **LAW → architecture §12: the room is not the ink.** A fraction-authored room bounds a BOX and is silent about the INK; contain the ink, never widen the room, never sanction a bound no browser has rendered; where containment and in-plot seating are jointly unsatisfiable the words LEAVE for flow reserved at their tallest, never grow in place. **SHIP NOW:** the crown's missing `.ladder-crown--side` rule + `?seed=atceiling` that renders it (the branch had NO CSS and no seed reached it); the INSTRUMENT — a classic-scrollbar 320 arm (WCAG 1.4.10's literal condition), 320 × root-20 arms that RENDER and REPORT the crown headroom / label clearance / readout ink as run annotations, the readout on the reader's-font arm, a seed guard on the readout sweep; a `fonts.ready` re-place in both chart-text layout hooks. **HOLD:** the readout's 320 shape (min-content · wrap · flow-row) and the ladder borrow's clearance — the 320 readout arms are DECLARED expected failures (`test.fail`) until the remedy + its oracles (ink-inside-its-own-box, crown-vs-marks, side-flip monotonicity) land together with plants. **REJECTED:** every geometry change (PLOT.top 64 ties by 0.17 px, 68 compresses every rung on every arm; PLOT.left breaks band/TF parity). Dissent (design-engineer + minimalist, craftsman, fiduciary): ship `min-width: min-content` on `.ct-readout` now — flips on two measurements (min-content constant across the 49 columns at every catalog composition; the 320 occlusion cold-read acceptable at ~16/49 rule-covered columns with a 62%×79% box). Honesty Hawk veto fired twice, HONORED. | 8/10 | executed — the ship-now list landed the same evening; the held 320 shape was RULED by his cold read 2026-09-06 (the readout crowded at 320 and at the reader's font, great in-plot on the 390 phone; the crown crowded beside its dot; the ladder column ok as rendered) and the remedy shipped the same day with its oracles — the measured readout seat (plot while the widest column fits beside a mid-plot rule, else a reserved flow row), the crown above-or-flow, the ladder column ACCEPTED and bounded; the dissent's min-content is dead; two residual forks (the phone's in-plot box, the rung-9 tell's dip into the plot) sit in the queue for his eye |
| 2026-09-05 | **The result charts’ TEXT layer — four svgs drawing words into a fixed 560-wide viewBox at width:100%, measured 6.9 CSS px on the phone / 8.0 at the 1088 floor / 10.0 on the 1536 window** (2 measurement fleets, 18 agents, the floor + mechanism + vertical strategy + gate design put to a full council; run wf_ecbe0ab2-7bb, 21 agents, UI elders self-seated) | council-decided (mechanism · vertical strategy · gate · both ride-alongs · the drop-guard retirement · the doc fix) + ⚑ yours-to-close (the three-register COLLAPSE amending the pilot’s "common factor" premise; the LOOK of HTML text over an svg plot on four charts) | **SHIP (C) GENERALIZED — "SVG draws, HTML writes."** Floor = `var(--text-xs)` (13 CSS px) on the smallest class, binding at PHONE; every `<text>` leaves the svg for an HTML layer positioned by viewBox FRACTION (React style-prop custom properties = CSSOM writes, proven under the served CSP); 20 ad-hoc px sizes collapse to three borrowed registers (xs / sm / lg — insight 082, never a novel size); the placer + `LABEL_CHAR_PX` + `READOUT_*` are DELETED, collisions resolved from MEASURED boxes; no viewBox grows — the band’s emptied 128u gutter hands height BACK to the fit law (BLOCKING PRECONDITION measured first: one row on every fit-law household, two on the scrolling date route, never three); the touch-pin bug and the dead `is-strong` selector ship in the same commit; the ≤260/280 drop guards retire; a real-Chromium gate asserts rendered px ≥ the token, bbox containment, non-overlap, nothing-named-hidden, text-follows-root-font, reduced-motion identity, with planted CLIP/SHRINK controls. **The red team broke the first consensus** (A-continuous, 8 of 9 openers): a 13px floor CLIPS the end-anchored `$2.25M` y-tick into `25M` — a plausible WRONG dollar on O3’s only decoder, with every proposed gate green — so the text moved rather than the floor dropping; three Honesty-Hawk vetoes fired (vs B, vs a LABEL_ROWS cap, vs any in-svg lift) and all three are DISCHARGED by the ship. Dissent (minimalist 0.78 + architect 0.84): keep y-ticks in svg with PLOT.left widened; flips only if the HTML block measured taller than the gutter — it did not. Filed separately: the band’s keyboard lockout (SC 2.1.1), TF’s touch-unreachable readout, prefers-contrast/forced-colors absent repo-wide. | 8/10 | **executed** — shipped the same day: `src/viz/chartText.tsx` + `.css`, all four charts rewritten, `e2e/chart-text.spec.ts` (19 arms green on the fit harness), `design-tokens.spec.ts` CSSOM probe, unit suites re-pointed, architecture §12, insights 115 + 116; witnessed at 1536×791 / 1088×800 / 390×844 on every chart |
| 2026-07-30 | **U9b Q3 RE-OPENED AND OVERTURNED — the date-route split render** (ratified 2026-07-02 at 8/10; re-convened on NEW field evidence Q3 could not have had). Briggsy RULED he wants both spending worlds on one chart; the council's job was HOW, honestly — or a rigorous demonstration it cannot be done. Full council **wf_457f0930-dba**, 21 agents, zero crashes. **THE FALSIFIED PREMISE:** Q3 justified its single FLOOR-crowned band on the grounds that a track-naming note would ensure "the range and the hero claim can never silently disagree." Verified in source and live: the note read *"This range follows the essentials-covered date"* — it named a DATE and never a SPENDING LEVEL, so the promise was never discharged (insight 100). U17 S6's cold read (panel wf_efc3968f-cf5, 140 agents) hard-flagged Cards 1+2 as calm-but-wrong BLOCKERS on exactly that composition: the reader binds a rising median and an $8M plume to their FULL life while the picture prices the ESSENTIALS one. | council-decided (the two-fan kill · the naming/ownership layer · the scoped O3 re-key · the dollarMax + composite-CVD gates · the first-year dollar law · Card 1's both-channel fix · build-gate-7 re-scope) + **⚑ yours-to-close** (the FLIP — which world owns the plume — and whether a dashed vermilion median reads calm at his real 1536×791) | **SHIP: one fan, one median line, ONE offset — and the fan follows the world the headline claims. NOT two fans (veto honored).** **HONESTY-HAWK VETO FIRED on a SECOND PERCENTILE FAN** — and it killed the pilot's own working hypothesis, which is why this was convened rather than built. **Measured oklab-L delta 0.0000 at every p** on the only ramp clearing the numeric floor ⇒ a color-blind reader sees ONE undifferentiated mass and takes the UNION of two envelopes as one world's range — wider and safer than either world truly is. Two further independent kills, each sufficient: overlapping translucent fills INVERT the ink-density ordering contract (palette.ts:114-125 — the darkest region would mark a crossing, not a likelihood), and a second fan is structurally UNDEFINED on `datemixed`. The repo had already ruled it ("two overlapping percentile fans are mud", TwoFutures.tsx:8-9); the red team conceded this half unbreakable. **The veto is HONORED, not live** — the ruling ships no second fan, anchors both series at ONE offset, and the Hawk re-ranked to a single fan + one labeled median in rebuttal. **THREE LAYERS:** (1) NAMING, universal across all five faces; (2) FLIP — fan/outcomeState/offsetYears/axis-marker re-key to the LIFESTYLE crown (floor-keyed plus a first-class no-lifestyle-date tell on `datemixed`); (3) OVERLAY — essentials median at that SAME offset via the existing `BandCallout` seam. Encoding: vermilion, dashed, triangle, non-scaling-stroke, direct end labels, composite CVD probe of RENDERED pixel pairs + a series-blue planted-fail that MUST fail. Dollars are FIRST-YEAR only, derived from the compiled arrays (the naive line-sum is a trap — OOP medical is netted per 2026-07-02 build-gate 1), behind a planted-fail on the $46k sum. Card 1 is NOT cured by the flip and ships in the same commit. **This council LIFTS O3** (copy.ts:1698) for a scoped `bandAtRange` re-key — one sentence, never a second. | 8/10 | **executed (partial, in progress)** — **LAYER 1 words-only half SHIPPED 2026-07-30**: `bandFollowsFloorNote` → `bandPricesEssentialsNote`, now *"This range prices your essentials only, not your full budget."* — the falsehood dies today while the DOLLARS deliberately wait for their planted-fail rather than being typed in haste into the very surface built to cure calm-but-wrong. Live-verified at `?seed=datesplit`; the axis keeps its labelled "Essentials date" so nothing is lost. 3181 tests / 163 files · verify:fit 52/52. **⚑ REMAINING, FLAGGED FOR HIS EYE: the FLIP + the overlay + Card 1** — four elders dissent (design-engineer 8, fiduciary-advisor 0.82, motion-designer 0.84 would KEEP the floor fan and overlay the LIFESTYLE median, since at a fixed offset the floor fan dominates at every percentile by an externally-derivable theorem — budgetTwoTrack.test.ts:546 — so that overlay is bounded INSIDE the fan, needs no dollarMax term, and is defined on every face; the minimalist, at 0.86 the highest confidence in the room, would ship NO overlay at all). **SHIP GATE (the council's own, not optional): a five-face Caddie cold read asking one question — whose range is the shaded area? If readers bind it to the UNION, the overlay drops and the minimalist's no-overlay ruling ships; naming + flip stand regardless.** WHAT WOULD FLIP IT: if the 1536×791 render under the flip shows the essentials overlay raising `dollarMax` enough to compress the hero fan into a strip, floor-fan-plus-lifestyle-line wins outright — a flattened hero plume re-creates essentials-as-visual-hero in a new costume. |
| 2026-07-24 | **U17 PRE-BUILD — stale-saved-recommendation handling** (the LAST Act-4 unit; the six riders U16's council hard-named + 2 proposed increments). Full council **wf_f4ced3c8-2f6**, 23 agents, ZERO crashes. **HAWK VETO fired + HONORED (scoped, three arms):** (1) a future-tense named marker printed LEFT of Today on an aged vault reads "our work-optional moment has already arrived" — optimistic, disclosed nowhere; (2) "my conversions begin in 2 years" while the engine converts at plan-year 2, already past; (3) re-basing the band's percentile GEOMETRY to wall time fabricates a starting point by redrawing already-resolved outcomes as today's known balance. The veto is honored INTO the shape — **this verdict CHANGES `bandAnnotations.test.ts:170-184`** (the test that pinned the work-stops placement as intended) rather than deferring to it; *a test that pins a defect is the defect's second copy, not evidence*. **THE RED TEAM BROKE FOUR RULINGS, every seat conceded IN SOURCE:** (a) `solverRunFingerprint.ts:31-34` EXCLUDES `consumedConstantEntries` — constant-blind by design — so a bare fingerprint match would re-present a saved conversion under superseded brackets *with a proof attached* (the no-clock-fired + code-version conjuncts are now binding); (b) `elapsedPlanYears` derives from `startCalendarYear` (`Result.tsx:131`) = since you BUILT the plan, which `staleness.ts:23-26` expressly forbids attributing to the save — the consensus's own history prose had committed that mislabel, and the value was unclamped above; (c) `recommendationView.ts:436-448`'s deliberate dialect split (humane `$X.XM` endpoints vs grouped-digit delta) would ship adjacent per-bar figures that subtract to something other than the hero — a NEW falsehood on the decision surface; (d) `staleness.ts:218-254` — five clocks are bare vintage compares, so naming e.g. `aca-status` at an all-65+ household is alarm-when-fine (insight 101 inverted). | **council-decided** (Q1 mechanics · Q2 both sides · Q3 conjuncts · Q4 exposure gate · Q5 record shape · Q6 · both Q7 gates — mechanical, reversible, source-forced) **+ ⚑ yours-to-close** (the aged-surface TONE: how strongly the elapsed segment is de-emphasised — tint vs hatch vs rule · the premise line's wording · the de-mudded arrived idiom) | **Q1 geometry contract (testable):** ONE exported arrived predicate, strict `planOffset < elapsed`, consumed by ladder AND band (source-bind test: one export, two call sites — kills the `FuckOffDate.tsx:296-298` / `curveMarks.ts:118` re-typing); no future-tense named marker renders left of Today — withdraw at the ARRAY (leaves the a11y tree too), never clamp; surviving markers speak ONE age base; the elapsed fan segment is de-emphasised by a STATIC mask nested in the cohort-fade group (`ConfidenceBand.tsx:199-226`) — **masked, never a re-trimmed `d`** (breaks the morph + vertex-snapped scrub) — non-colour channel PLUS a11y text; the aged fan ships ONLY with an adjacent premise line naming the balance vintage AND a RENDERED re-confirm control, residual disclosed **UNDETERMINED, never "conservative"** (no fold-legal premise line ⇒ the aged fan withdraws); `elapsedPlanYears` RENAMED to years-since-BUILT + prosed "since you built this plan" (balance vintage stays on `savedAt`), clamped to `[0, horizonYears)` and refusing aloud outside; on a split the crown names WHICH date. Plant a new stateless **`?vault=datearrived`**, constructibility-probed against `earliestPricedRateYear` (insight 085) — never a decreed aging depth. **Q2** refuse BOTH re-bases; speak the calendar year (`startCalendarYear + startYearOffset`) on READ **and** WRITE, refuse a past start, suppress when unanchored, rename `startYearsFromNow`; file the engine re-anchor. **Q3** re-present as current ONLY on fingerprint AND `SOLVER_CODE_VERSION` AND no-clock-fired; else the action-warning record + an invited re-open naming its cost. **Never auto-re-solve.** **Q4** a clock names itself only where the run was EXPOSED to it; ungateable clocks aggregate under one sentence true of every member. **Q5** identifying triple + fingerprint + code version + `savedAt` + an ENUMERATED era-stamped remembered verdict, rendered only inside the record card; atomic, presence-keyed, fail-closed but **NON-FATAL** (drop the atom, keep the model). **Q6** confirm the tape-watch. **Q7a** ADMIT **conditionally** — unify the dialect and DERIVE the displayed delta from the displayed endpoints, pinned `rendered_B − rendered_A === rendered_delta` across the roster with `ariaSummary` bound to the same; **gate fails ⇒ the rider does not ship**. **Q7b** admit (reorder only, existing strings verbatim, fold-measured). **DISSENT PRESERVED (red team; conceded by hawk/architect/craftsman/fiduciary, opposed by security on survivor-stranding — `writable()` is false for the recovery-unlocked session, `session.ts:305`):** fixing how the elapsed segment is DRAWN does not make the aged band honest — only its WIDTH is conservative, its LOCATION is undetermined and optimistic for any household that overspent. **Flip condition:** the `?vault=datearrived` walk showing a reader treat the aged fan as priced on today's balances DESPITE the premise line + re-confirm control, or the premise line failing the fold gate — either fires the withdrawal (aged fan disappears, re-confirm invite alone). Secondary (Q5): any read where a reader quotes the remembered figure as current drops the record to identity-triple-only. | **8/10 high** — nine seats converged *after* the red team landed six attacks and every seat conceded with source verification; off 9 because the fan's location residual is mitigated by copy whose sufficiency is unproven until the arrived-vault walk, and Q7a's arithmetic gate may not pass. | **executed** — spec written to this shape |
| 2026-07-22 | **U16 PRE-BUILD — the recommendation surface** (recommend-second · the surplus pivot under the runway veto · router/invalidation · fallback ladder · GoalPicker/affordance · the LIVE NC withheld render · skew + objective≡headline · the dead-premise reconcile · scope/phasing). Full council **wf_8d4c6f65-415**, 23 agents, 9 opener lenses + UI specialists (ONE crash: the security engineer's OPENING at the StructuredOutput cap — a named 019 abstention, the least load-bearing seat on this charter; its rebuttal completed and affirmatively found no crypto concern in scope). **HAWK VETO fired + HONORED into the shape (scoped):** "you're safe either way" AND "more than enough" never ship as minted rec-surface claims — a survival-ceiling absolute in the 70–90% band market-model §3–§6 names OPTIMISTIC; the sanctioned exit ships: **delta-as-hero**, survival context **SOURCE-BOUND to the spine's rendered confidence object by reference** (never a re-authored parity string — the U11 six-holes class killed structurally; require-hedge alone proves a word exists, not level parity). **THE RED TEAM'S ONE REAL HIT, ABSORBED:** the council had collapsed item-7's two sanctioned exits and assumed "richer-draw safely deferred" — the near-tie inversion stress test (does the difference-keyed grade invert a conversion ranking under a block-bootstrap draw at the 85% line?) is now RECORDED as a **U14/U15 grade-calibration gate** (spec §S0.2), with the post-trend-flip demotion re-cal (2026-07-19) as the U16-side valve; its Attack 2 was REFUTED as a hallucinated citation. RULINGS: **Q2** one worker + queue discipline, solve dispatches only after the spine commits; invalidation source-binds to `solverRunFingerprint` (never a bespoke epoch mirror); second worker + per-candidate abort deferred to a measured trigger. **Q3** knob-pin = TASK ZERO on the reference device, calibrated for RANK-STABILITY not latency; Phase-A full-precision-only under the shipped thinking-breathe (no spinner/%/count-up); the provisional heartbeat GATED on profile-proven winner-cannot-flip. **Q4** affordance in the doors DOM region, in-frame IF it measures at 1536×791@2.5dpr (measure, don't decree; spine content never demoted for it); GoalPicker joins sheetShell.css + reuses the ControlSheet focus contract. **Q5** the NC household (Briggsy's own, LIVE today) sees the REAL sequencing-only rec + the conversion withheld reason named TRUE adjacent (state, direction, ~August, refusing-to-guess register) + the **COUPLING caveat** ("may update once conversions certify" — the red team's Attack 4 concession); unclassified fails CLOSED; U16's first-solve register distinct from U17's. **Q6** the mean ranks AND displays; the skew disclosure QUOTES the median, adjacent AND inside the viz aria-label; percentile convention source-bound to confidence.ts; `assertObjectiveMatchesHeadline` pure + planted-mismatch-proven. **Q7** strike the dead tax-blind→tax-aware copy + both scenarios (DEAD-COPY list binding); baseline = a static nameplate; the A↔B residual never a rendered number. **Q8** staged independently Caddie-walkable sub-ships; the save gesture **ABSENT + a reserved layout slot** (the security seat decisive: `writable()` refuses in the survivor no-vault state — an inert "saved" is data loss at the widow-cliff; gesture + v3 write land together in U17); detail-door era its OWN post-U16 unit; R7 heir-bracket (editable) + SS-claim-age (disclosed) compile-enforced. FIX-ITS: copyGuard `CONTROL_KEY_PREFIXES` gains the recommend/grade prefixes (a live silent honesty hole — enum labels exempt) + colorblind.test.tsx closes its own U16 deferral with a planted-fail arm. **DISSENT PRESERVED** (hawk+fiduciary): the richer draw may be a U14 PREREQUISITE — flips if §S0.2 fires. Spec: `docs/plans/features/act4-u16-recommendation-surface-build-spec.md`. | council-decided | RATIFY the U16 charter; delta-as-hero is the render idiom; build staged S0→S5 | high 8/10 | executed (spec authored; build next) — ⚑ his eye: the pending-state character + the reframe tone (tape, not pre-confirm) |
| 2026-07-19 | **THE MEDICARE-COST-TREND SOURCING UNIT (pre-build)** — the shape ruling ((a) flat +1.4%/yr real · (b) two-regime escalator · (c) year-keyed V.E2 primary table) + the seven-question charter (IRMAA-surcharge scope · deflation · consumption atomicity · post-2035 tail · staleness vintage · seed drift · residual copy) over the research packet (`docs/research/medicare-cost-trend.md`, wf_933cef3b-16b). Full council **wf_c673339e-257**, 23 agents (ONE crash: the craftsman's OPENING died at the StructuredOutput cap — a named 019 abstention, least load-bearing seat on an engine charter; its rebuttal seat completed; the hawk sat both rounds). **HAWK VETO fired + HONORED (blocking):** BASE-ONLY scope (trend the Part B base, hold the IRMAA add-ons real-flat) mints the false belief that a modeled 2026–2035 IRMAA-step crossing reflects the Trustees' projected surcharge while the add-on sits frozen at 2026 — understating the crossing cost in the exact channel conversions pay through, flattering the recommended strategy; the charter ships the endorsed honest alternative. RULINGS: **DIRECTION** settled — real-flat and standalone (a) are OUT (the optimistic sin; research packet: falsified in every window). **SHAPE** — (c) LEADS but is RANK-FLIP-PROBE-GATED with the 091 mechanism-trace: smooth (b) vs jagged (c) on a conversion candidate parked at a near-decade IRMAA step, through the SHIPPED search→select path (095); a flip from the CUMULATIVE cliff trend confirms (c); a flip riding 2027's +3.25% policy-artifact low rules (b)'s smoothing (never anchor one year); no flip → (c) on constants discipline + the clean annual re-verify; (b) is the MANDATORY fallback if a year-keyed premium step breaks the ACA/IRMAA root-finders (013). **SCOPE** — full + DISAGGREGATED: Part B base trended; the Part B IRMAA surcharge DERIVED from the trended base via the statutory cost-share identity (NEVER a second re-typed V.E3 vector — V.E3 is the DND-009 cross-check only); Part D's surcharge on its OWN IRA-2022 6%-capped path tied to the Part D base, or held-and-DISCLOSED if unsourced (never silently frozen); the COMBINED surcharge tied out at 2026 with the tie-out planted RED first; the MAGI thresholds stay real-flat under the standing 2028 top-tier tripwire, disclosed. **DEFLATION** (if c) — store nominal verbatim + the sourced CPI path, deflate in-engine HORIZON-MATCHED (never the 2.4% ultimate on the near decade); DND-012 fixtures derive from the realized 2006–2026 real band + the published CAGRs, never the engine's own deflation. **CONSUMPTION** — both halves ATOMIC in ONE commit (074/081): the lying-mirror mutant planted RED first, then label flip + per-year pricing + BOTH solver seams (the tripwire's named pair) + the Q4d demotion re-cal from a recorded pre-commit probe on the deterministic post-flip world. **AGED-VAULT** (if c) — the validateParams year-domain twin (simulate.ts:640 precedent, insights 076/085) + a C0-continuous ~2075 tail splice: MANDATORY. **STALENESS** — a `medicareCostTrendVintage` stamp + the annual Trustees re-verify hook (verify:aca pattern); the first flip rides the era map. **COPY** — the three-string atomic reword + the 087 comment sweep; a SWAP never an ADD (no over-claim to fit, no content cut). DISSENT preserved (minimalist, red-team-reinforced): ship (b) as DEFAULT — expressed in real terms it dissolves the deflation sub-ruling entirely (no CPI table, no 2035 splice, no 013 discontinuity, no aged-vault domain re-audit) and (c)'s verbatim faithfulness is a fidelity the ranking never consumes; FLIP = the probe shows NO flip AND (c)'s machinery proves real standing cost — conversely a cumulative-cliff-traced flip kills the dissent. ⚑ for his eye: the residual-copy TONE (the exact "held flat" replacement wording) — ships PILOT-CLEARED under the batched-oracle law via the Caddie walk, his audit supersedes. | council-decided (copy tone ⚑ yours-to-close) | Shape (c) probe-gated w/ (b) fallback; scope full + disaggregated; both-halves atomic | 8/10 | executed — built same-day (ca41256f + the Part D pass). POST-VERDICT DATED NOTE (primary-source-wins): the Part D research returned SOURCEABLE (Trustees Tables V.E2/V.E4 verbatim, the §11201 2030 formula reset ≈3× per-tier jump), so the ruling's source-it arm shipped — with the guessed tied-to-the-Part-D-base MECHANISM corrected against the primary (per-tier ratios diverge across 2030; V.E4 consumed verbatim per tier, never a scalar or base-derived scale; the correction is dated in the build spec's Part D stamp). The shape probe CONFIRMED (c) — no flip, 013 never fired. |
| 2026-07-18 | **U15 — THE SOLVER CORE (pre-build)** — the six-question charter over the fold's forward-seams stamp (700319e8): objective wiring · the leave-more statistic · selection shrinkage · the four inherited seams · trend-unit ordering · scope trim. Full council **wf_45d9d56b-d1c**, 21 agents, zero crashes, hawk seated. **HAWK VETO fired + HONORED (blocking):** an un-fingerprinted token attests “THIS household's recommendation is validated” while proving only that SOME household passed (`mintedOver` binds no household — oracleToken.ts:288-295); U15 CLOSES both bindings — a COMPLETE run-derived household+candidate-set fingerprint (every ranking-affecting input: per-bucket balances, goal, heirBracket, spending, horizon, people, filing, dates, the enumerated candidate ids — with a moved-witness test) into `mintedOver` AND `RankingStabilityReport`, the solve entry refusing a mismatch. The red team's material hits — ALL verified + adopted: (1) the hawk's own consumedConstantEntries fix was HOUSEHOLD-BLIND (constants-only — two households in one state fingerprint identically; the hawk conceded + sharpened); (2) `caseLeaveMore` is DEGENERATE (r=0/deterministic/tieTolerance:0 → mean=median=every quantile, so a mean→robust switch passes `expectedRankingIds` byte-identically — typing not correctness, DND-012) → a dispersed-world hand-derived fixture is BLOCKING on Q2 either way; (3) shrink-then-argmax is a NEW unoracled selection surface → the insight-025 calibration case + a zero-shrinkage≡rankCandidates[0] identity test are BLOCKING. RULINGS: **Q1** CONSUME the harness scorer (scoreFromDistribution + rankCandidates + candidateTieBreak = the ONE scoring home; objective.ts a thin adapter; export the module-private tier2 so select.ts composes on the one orientation). **Q2** ONE statistic ranks AND displays (objective≡headline, locked) — attempt the dispersed-world fixture FIRST: tractable → downside-aware leave-more (median/low-quantile); intractable → mean-ranks + robust-grade with the mean's skew disclosed; grade = a separate per-path CRN axis over ranked vectors (093), finiteness-first. DISSENT preserved (architect; fiduciary leans same): keep the mean for ranking, robustness in the GRADE only — the dispersed fixture's tractability outcome IS the flip condition. **Q3** deterministic shrinkage of the seed-A advantage-over-the-conventional-prior toward zero, keyed to the ALREADY-BUILT CRN-difference SE (no second constant); survives→displaces, dominant survives unchanged; A-side only, pure seam (048); prior = conventional taxable-first, NEVER the user's custom. **Q4c** widen `solverCandidateId` with provenance (injective by construction — the `taxable-first:0` collision is MODAL: the enumerator and a userBaseline both mint it when the user holds the common default), SAME commit as the fingerprint; widen, never dedup. **Q4d** keep the fail-closed throw; the conversion demotion margin = a scale-free SE-multiple calibrated ONLY on a Medicare-bearing post-trend-flip world — stays uncalibrated/fail-closed through U15. **Q5** BUILD against the live fail-closed gate NOW (sequencing-only candidate sets mint; conversion-bearing sets stay trend-blocked by design); the trend unit + the post-flip margin calibration = a HARD pre-exposure blocker (co-runway preferred); every withheld conversion lever NAMED with its direction (092). **Q6** the goal field IN v1 (unset sentinel); cancel.ts SPLIT — the commit-epoch guard unconditional, mid-solve abort granularity gated on the profile's measurement; profile.ts IN as commit-after-core (the WASM port deferred, the fallback ladder ships); the TIER-LESS pending arm — every user-read state a structured flag+label; NO riders (051). | council-decided (⚑ yours-to-close sliver, shipped-at-high-confidence via this digest: the trend-unit CALENDAR — co-runway vs immediate-follow — and the profile-informed pending-state character) | **RATIFY-W/-AMENDMENTS — execute the amended shape** (the two red-team amendments PROMOTED to blocking: the dispersed-world fixture + the complete household+candidate-set fingerprint). | high 8/10 | executed (the build spec + the U15 build follow) |
| 2026-07-18 | **THE ACT-4 RUNWAY RECONCILIATION (pre-build)** — Briggsy's GO ("cleared for takeoff"); the month-stale plan (2026-06-18) reconciled against a codebase that shipped U9–U13 + Medicare pricing + senior-bonus + extras + state-tax since. Evidence: the 8-agent ground-truth audit **wf_1369c7e7-698** (the pinning census — 22 directional entries; the U17 rider sweep; the substrate inventory) + pilot source verifications. Full council **wf_d873be6e-5b2**, 21 agents, hawk seated. The red team's material hits — ALL verified + adopted: the asymmetric non-cancelling CRN residual (flatters conversion in the near-tie regime), the productionMarket permanent-flag contradiction (a perpetual block bricks Act-4; laundering the flag is the NC sin in mirror), gate-list-means-BLOCK for the Medicare-trend constant, the type-gate-proves-order-not-predicate hole (S0 must source-bind the mint predicate), and the clockless engine-domain inversion (the solver-code version stamp is a HARD requirement). | council-decided (⚑ yours-to-close riding to U16: the surplus-pivot RENDER framing, the GoalPicker framing, the invited affordance — digest items, U16-council-owned) | **RATIFY WITH AMENDMENTS — EXECUTE.** **Q1** the premise corrections adopted as dated supersessions (state roster-IN splits the NIIT+state pair — forced by D6's own falsifiable rule; the tax-blind-spine premise dead; Act-4 fields additive-in-v3, no bump; architecture §7.1 k/passes fixed). **Q2** the token = per-run-consumed-constants scoping split by directional KIND: certification-pinnable figures BLOCK the household until their dated event (NC blocks to ~Aug-2026, FL/PA mint free); methodology substrate (productionMarket/ε-context/survivorSpendingRatio) ships-disclosed difference-keyed, NEVER laundered to pinned; FRA = reachable-band pinning. **Q3** Reading B ships (CRN cancels the common-mode shape bias in pairwise differences) AMENDED: the conversion-flattering residual is NAMED and "just do it" DEMOTES on conversion near-ties, the shape note in the same DOM lockup. **Q4** U17 re-grounded (the plan's staleness "inversion" refuted against staleness.ts:17-21 — the spine already recomputes-under-current; what survives: the action-warning register + the live-clock-superset triggers + the net-new solver-code stamp + the savedRecommendation record; the six riders charter to U17's own council). **Q5** U14-first, one-unit-one-spec, the pinning pass as S0 — the executable shape is **`docs/plans/features/act4-u14-validation-harness-build-spec.md`**. **HAWK VETO (fired + honored, surgical):** the bald surplus-pivot absolute "you're safe either way" NEVER ships — §7 trigger-1 fires deterministically for that claim (settled correctness, never a U16 tone cold-read); exits = the delta-as-hero reframe or the richer-draw build. **Dissent preserved (hawk/fiduciary):** Reading A — the difference-keyed grade itself may over-claim at the 85% near-tie line; flip condition = a near-tie stress test showing the grade inverts a conversion ranking under the richer block-bootstrap draw. | high 8/10 | executed (supersession block folded into the plan; product.md D6 + IN/OUT amended; architecture §7.1 + roadmap re-stamped; the U14 spec authored; the build follows under the normal cadence) |
| 2026-07-18 | **THE RULE-36 OF/IN ODDS-DIALECT SWEEP** (filed 2026-07-11, triple-corroborated — the three-dialect mix on the maximal-sensitivity words). Grounding wf_f63a7407-36a (4 lenses: ZERO factual misdescriptions — the double-take is register-mix + adjacency, not a lie; the guard architecture mechanically forces the spelled band dialect; 200px readout width wall). Full council wf_67c72e89-e77, 23 agents, hawk seated. The red team's real hit: the Q3 remedy as a slot-lexeme swap was INSUFFICIENT (the ACA "about" is a template literal Q2's de-stack never reaches; the ceiling path was untested) — all ten elders conceded and sharpened; the tightened remedy is the ruling. | council-decided (the adverse-ceiling wording ships committed as "more than 9 in 10", the runner-up in dissent; his eye audits at the pre-walk per the batched-oracle law) | **Q1 RATIFY** — hero "N of 10" (survival) vs band "Eight in ten" (p10–p90 coverage) are two genuinely different measures (the 2026-07-10 chair-kill binds); no unification, no de-collide mandated. **Q2 FIX** — de-stack the "about better than 9 in 10" double hedge at ceiling via STATE-DERIVED flags (dateOddsText → withOddsAtCeiling; ladderMarkAria takes the mark's own atCeiling), never string-sniffing. **Q3 FIX — HAWK VETO fired + honored:** the unclamped ACA over-cliff count at ≥10 rendered the GOOD-NEWS "better than 9 in 10" on "the discount disappears entirely" — the calm-but-wrong shape on the one surface that must never soften; template edit on BOTH acaCostCliff slots to the valence-neutral XOFTEN_CEILING_ADVERSE ("more than 9 in 10") + the MANDATORY worstOfTen≥10 fixture through the real chrome; veto lifts only when both land. **Q4 SPLIT** — (a) fold AnswerStrip onto the composer's named reading seam (the U12 echo-desync's last un-folded sibling); (b) RATIFY the TwoFutures conservative count (folding the ceiling in would mint a fresh of/in collision inside one sentence); (c) kill the three false "composes naturally"/"top-of-scale" comments (insight 087) + sync the design-skill doc; (d) GATE rothDeltaEven on state-equality — equal clamped counts across a state move were rendering "doesn't look to change much" over a real improvement (the recommend-second suppression, red team #3); the new rothDeltaCountEven states the count fact, the state rider carries the move. DISSENT preserved: Minimalist + Design-Engineer ratify-both-counts on Q4 (flips if the AnswerStrip fold carried real regression risk — it didn't: word+count from shared sources, no clause ternary). | 8/10 | executed — a2360148 pass + 0e2667c6 (TwoTierHeadline joins the reading seam), CI green 29651890964; 2414 tests / 126 files (+8, +1); 5 planted mutants red→reverted; verify:fit 28/28 · csp 13 · bundle 249.7 KiB; pre-walk wf_81b4f14a-5b5 (23 agents): 3 cards PILOT-CLEARED, ZERO fixes — the ceiling voice read hedged-honest on every lens, the firmness-inversion texture chair-ruled as-designed, the one survivor chair-killed against source (the "around ~" 3-slot cleared idiom → stapled to the hedge-vocabulary family); the adverse ceiling stays fixture-proven only (no live seed reaches ≥0.95 over-cliff — coverage honesty on the card) |
| 2026-07-17 | **THE O16 RECONCILIATION-BRIDGE PASS — four forks over an engine-verified truth table** (the compliant-household bridge home; the grounding fleet's OWN find — the roth note's "pre-65 health-plan side effects — not counted" clause engine-proven FALSE for ACA-priced households, taxOverlay prices the conversion→MAGI→discount coupling in both preview arms; the O16(b) hedge question; the count-claim texture). Full council wf_fd7f75cb-916, 21 agents (one opener seat lost to the 084 StructuredOutput cap — 20/21, hawk seated + voting); grounding wf_b1a4d775-078 (4 agents, file:line truth table). The red team's real hit reframed Fork B: routing the ACA-priced converter to the All65 words would silently drop the COST-SHARING omission — one-way OPTIMISTIC on the conversion delta (CSR forfeiture past the 250%-FPL line, unpriced; the Roth and Health doors are independent). | council-decided (Fork B shape: narrowed plain residual + producer-keyed 3-state predicate; Fork A locus: panel-only, monolith FROZEN; Fork C ratify-no-change; Fork D-1 reword + D-2 freeze) + **⚑ yours-to-close (the exact wording — E13 surface-read; shipped at confidence, audited on the caddie tape)** | **HAWK VETO fired on bare option (i) + HONORED — the shipped shape is the hawk's own endorsed alternative.** Fork B: `acaPricedForRun` (producer's output; unknown-age/degenerate/Medicare-only/no-crown all conservatively FALSE) routes ACA-priced pre-65 households to the NARROWED residual naming "differences in plan cost-sharing" + affirming the discount coupling is in. Fork A: `assumptionStateUnsetNote`'s false "leave out" → conditional-on-the-reader's-action; the verdict monolith FROZEN (claim-free; **a11y dissent preserved: a compliant elsewhere household + SR users get no reconciliation — flips if a read shows invite-to-double-count; standing tape-watch**). Fork C: the neutral "could move this picture" hedge RATIFIED on a proven mixed-direction list (state tax+NIIT only worsen; top-ups+survivor recheck only HELP; cost-sharing+benchmark two-way — F4 direction-honesty applies only where direction is PROVEN). Fork D: "One modeling choice remains" → "A modeling choice:". **THE ARC (all same day): pass dc5cec9e · ultramode fold 6ae1c16a (the P1: the predicate re-keyed from buildDateInput's pre-sweep base overlay to buildControlPreviewParams — the crowned window-gated candidate, the run the note DESCRIBES; 2 refuted false alarms thrown out) · caddie pre-walk wf_7b0748e3-363 (33 agents, 3 cards PILOT-CLEARED, ONE chair fix: the governed face's budget twin bcbfae6e — "only if you gave it a budget line yourself — most budgets don't", the "all in" amplifier killed). 4 planted mutants red→reverted. The statestale contrast staple CLOSED by measurement (7.1:1 AAA).** | 8/10 | executed (2398 tests / 125 files · fit 28/28 · doc-stats synced · CI green by ids 29591184119 + 29593338758) |
| 2026-07-17 | **THE O14+O12+O9 COPY SWEEP — six design forks (F1–F6)** (the state-tax dialect sweep + the residual-density pass + the roth-omissions age-gating; the overnight arc under the batched-oracle grant, Briggsy asleep — convened so the pilot didn't park the forks). Full council wf_d3666133-c34, 21 agents; one opener seat lost to the insight-084 StructuredOutput cap (20/21 completed, hawk seated + voting; verdict integrity unaffected). The red team's two real hits reframed the case: F1's true defect is one lexeme with two referents (verdict "isn't counted" vs intake "keep that bill inside so it still counts"), and the copy.ts:114 comment claimed a drift-pin that did NOT exist. | council-decided (mechanics: the prerequisite pin, F4-must-change, no-fact-drop, F5-copy-only, F6ii appendix-split) + **⚑ yours-to-close (the exact replacement words — shipped at high confidence per the max-autonomy rule; runner-ups preserved in dissent)** | **All six executed.** F1: no five-frame unification (register variation legitimate — the rule-36 chair-kill precedent), but the verdict monolith joins the family's "priced" frame: "State income tax isn't priced yet. In a taxing state, that's a real yearly bill." (the counts/counted polysemy dead). F2: "priced" vocabulary KEPT (cold-read cleared). F3: tense-neutral legend "Your retirement state" — **DISSENT preserved (a11y-auditor + red team): route-swap it like qStateHeadingRetired; flips if his eye reads neutral as flat**. F4: the twice-refuter-confirmed rosier hedge reworded — "could sit tighter than shown" → "so their true cost could run higher than shown" (direction explicit; the 3 hedge siblings deliberately untouched — O9/086 orphan risk). F5: the ONE shared spend twin made tri-state true: "State income tax is priced by the tool too — leave it out of this figure, the same as the federal one." (the FL bill/tax presuppositions dropped; no new predicate). F6(i): **HAWK VETO fired + HONORED — zero facts dropped**; the double-em-dash three-fact chain reshaped into one-fact sentences, fit-gated and PASSED (verify:fit 28/28 incl. ?seed=pa at BOTH tiers). F6(ii): the date route promotes the on-typical extras appendix to its OWN `.fod-note` (scrolls by design); the spine keeps one block under the fold law. PREREQUISITE shipped FIRST: the real spend-twin drift-pin (copyGuard.test.ts) + the false comment corrected. O9 rode the touch: the pre-65 clause age-gated off for all-65+ households (`medicareOnlyPriced`, draft ages — unknown age conservatively keeps it; 4 static variants, each require-hedge-swept, the single-item arm reads "it" never "each"). Tape rows 2026-07-15/16 supersession-annotated BEFORE the rewords landed. | 8/10 | executed (2384 tests/125 files green · fit 28/28 · 3 planted mutants red→reverted · doc-stats synced; ultramode review + Caddie pre-walk follow in-arc) |
| 2026-07-15 | **THE STATE-TAX ENGINE UNIT — pre-build ratification** (TODO item 11; scope-parent wf_cc065e3b-bc1; trigger MET at filing — NC taxes Briggsy's IRA withdrawals + conversions; his full-decision-rights GO this morning). Full council (wf_d04148cb-1e5, 21 agents) over the 14-agent primary-source packet (wf_7bc1f76d-6cc: NC+PA deep + SC/GA/FL/DE characterization, per-figure adversarial second-source verification — 82 CONFIRMED / 71 PRIMARY-PINNED; NC's 2027+ schedule the ONE load-bearing UNRESOLVED). **Run record: the first pass lost 3 opening seats (hawk/fiduciary/a11y) to oversized StructuredOutput calls truncating after the first property — stopped, the SIZE LAW patched into council.js (efd8585e), resumed from cache; all nine seats voted.** | council-decided (all mechanics — EXECUTE) + **⚑ yours-to-close (FL-in-v1 · the affirmation wording · the picker's rendered feel — ship at high confidence; the Caddie pre-walk + his eye audit at product cadence)** | **EXECUTE the spec** (`docs/plans/features/state-tax-build-spec.md`), ONE commit: v1 prices **{NC, PA, FL}** (FL a SOURCED constitutional $0, never a silent default; SC/GA/DE deferred — an unbuilt state keeps the disclosed-out posture verbatim); the sentinel reconciliation (tax.ts:336-344 — its "neither sequencing nor conversion moves it" note is now FALSE) lands FIRST; state joins the gross-up as a 2nd addend (taxOverlay.ts:888) with the k re-derivation PROVEN in code (k_total ≈ 0.78 — NC/PA exempt SS so the state term rides without the ×1.85 torpedo; MAX_PASSES → ~192; the stress sweep re-RUN state-ON at the federal-worst small-net corner) but stays OUT of the bracket-fill rails while the roster is flat/zero (REOPENS with any graduated state, when the not-yet-state-optimized caveat becomes mandatory); **the HAWK VETO fired + honored: NC prices 3.99% FLAT all years, directionalUntilPinned — never the uncertified 3.49%/2027** (the codified cut is revenue-trigger-conditional on an ~Aug-2026 certification; the budget-deal schedule is unlocated at primary; verify:state-tax carries the checkpoint and self-corrects the instant either pins); NC conversions fully taxed / PA $0 at 59.5+ with the under-59.5 arm TAXED (conservative); the four "state isn't counted" monolithic strings SPLIT into independently-gated clause-parts dying on a producer's-output ROSTER-GATED statePriced — **the bare isDateRoute disjunct STRIPPED** (red-team hit: Medicare's version works only because dateSearch forces healthcare; an 'elsewhere' date-route household must never read a false "counted") — each gate mutant-proven RED; **reduce-to-spine +0 keyed on PRICED_STATES MEMBERSHIP, never truthiness** (red-team hit: a truthy 'elsewhere' string would compute-then-zero and break byte-identity); spendHelp goes state-aware with the state step BEFORE spendStep (the double-count inversion closed) and the U13 walk PROMPTS pre-unit vaults for the missing state; top-level `retirementState` + single-sourced STATE_ROSTER (explicit 'elsewhere') + StateTaxVintageV3 CONSUMED by staleness.ts; the NC survivor SD-halving ($25,500→$12,750) crossing-year external fixture MANDATORY; picker = the existing vertical SegmentedControl at 4 arms; display home = **option (b)** leaner v1 (R7 seat + OUTCOME-scoped affirm+residual SET — never an optimization claim, never affirm-alone; the dollar door stays its own later cold-read-bearing unit; (a)-as-rider and (c)-gate both REJECTED). **Red team: 3 cardinal-direction hits, ALL conceded in rebuttal, zero surviving; unanimous convergence.** **Dissent (preserved):** minimalist — NC+PA only, 3-arm picker; flip = FL's null module proves non-trivial or the 4th arm busts the calm/fit. | high 8/10 | **spec filed → BUILT + REVIEWED + PRE-WALKED all same day 2026-07-15** (c7e5936c the one-commit build · 388b8821 the 53-agent ultramode fold, zero false-alarm survivals · the Caddie pre-walk chaired 3 cards PILOT-CLEARED with two chair fixes live re-verified; 2354 tests / 125 files, every gate green, CI by explicit ids; O14 + the state-carrying seed increment filed; his eye rides the 2026-07-15 tape row) |
| 2026-07-11 | **THE ASK-FOR-MEDICARE-EXTRAS UNIT — pre-build ratification** (Briggsy's GO, same evening: "definite go" + the binding first-pass directive — "let them use national averages… valuable for a user's first pass thru and then refine in subsequent passes"; provenance = his three-round residual bounce → "why don't we just ask for it? correctness always out rules everything"). Full council (wf_efc6ece2-675, 17 agents, zero crashes). Amends wf_4c8cd836-b22's F3 scope by its author's own directive — EXTENDS the additive premise. | council-decided (all build mechanics — EXECUTE) + **⚑ yours-to-close (tone/copy only: the payment-fork wording, the MA-$0 fork line, the door-sheet disclosure, the bi-directional standing disclosure — the build produces them, then Caddie pre-walk + his eye score the rendered tape; not a pre-confirm)** | **EXECUTE the honest spec** (`docs/plans/features/medicare-extras-build-spec.md`): per-person PAIRED entry with a legible PAYMENT FORK (the MA-$0 line load-bearing; never a plan taxonomy); OPTIONAL-with-average — absent/skip funds the conservative-HIGH typical, NEVER a silent $0 (the mirror of oopMedical's pessimistic-safe absence); everyone route-priced incl. near-65; per-person persisted provenance stamps (never re-derived value==typical — the appDefaults inversion trap); anchor = a primary-sourced Medigap+PartD-regime figure, NEVER a population mean (the bimodal MA-$0 mass drags a mean optimistic). **The red team's TWO verified HIGH hits folded as HARD LOCKS:** (1) the engine funds a per-person HETEROGENEOUS VECTOR ending at each death — never enrolledCount×average (healthOverlay.ts:486 is count×uniform; count×avg reproduces the optimistic survivor under-charge; asymmetric-survivor DND-012 golden + planted-RED mutant); (2) a RENDERED disclosure home for BOTH populations — showMedicarePricedNote structurally suppresses the verdict residual for every pre-65-window household, so the door sheet carries its own legible line, with a FLOOR: no door home ⇒ door-household extras pricing GATES. Boundary flips atomically (fund-first if phased — the forward gap is double-omission/optimistic); the 058 writer enumeration is a ship gate; the residual moves as a set (Part D/Medigap clause dies · state-tax stays · real-flat expands). **The hawk fired a SCOPED veto naming SIX forbidden calm-but-wrong shapes — satisfied by this build (hawk conf 9); the six stay standing constraints.** **Dissent (preserved):** conservative-HIGH may read alarmist for the MA plurality; flip = evidence the fork doesn't steer them to $0 — fix STEERING, never the anchor. | high 8/10 | **spec filed same session** (`medicare-extras-build-spec.md`) → **EXECUTED + CLOSED (2026-07-12):** built 2026-07-11 late (503213f4 — the full F1–F5 shape, every hard lock held: the heterogeneous vector mutant-proven RED ×3, both disclosure homes, the fund-first flip, the 058 writer enumeration clean); the fresh ultramode review (wf_d875f061-f95 — 13 lenses + the O11 checker + 12 refuters, 27 agents, zero crashes): ONE confirmed finding FOLDED (the hero on-typical composer's untested arm selection → a 5-arm battery incl. the who-correctness mutant seen RED, the disclosure-view assembly single-sourced in intakeMap, the overlay's sibling-parity length backstop mutant-proven), 5 rejected real-but-immaterial on unanimous vote texture, **O11 CLOSED as RESOLVED** (oopHelp's blanket premiums line traced literally TRUE post-flip; sole latent nit = the rare purchased Part A, consistently carved out); the Caddie pre-walk (the codified `.claude/workflows/caddie.js` panel's FIRST run, wf_7a620d35-4ca, 57 agents; increment 4's `intake:fork` walk gave the fork its only capturable home): FIVE cards PILOT-CLEARED with two chair fixes live re-verified — the panel's unanswered-fork STANDING line (the rule-38 details home for the funded typical, no fabricated authorship) and the spend-step's keep-state-tax-inside instruction (the strip-it-nowhere optimistic read killed); NEW filings O12 (residual-density pass, needs its own fit round) + O13 (the medicare-block divider, measure-first). The ⚑ tone/copy slice rides the 2026-07-12 tape row awaiting his eye per the batched-oracle law. 2247 tests / 122 files, every gate green, CI by explicit run id. |
| 2026-07-11 | **The income-tax boundary of "Household spending, all in"** (fired live mid-cold-read — Briggsy: "why don't we count state tax as an expense?"; spendHelp named NO income-tax disposition at all, and a federal inclusion silently double-counts against the engine's own gross-up). Full council (wf_cc065e3b-bc1, 15 agents; one elder seat lost at the StructuredOutput cap, recorded). Options: A status-quo disclosed-out · B contain-by-instruction (the Part D/Medigap pattern) · C compute-per-state. | oracle-settled (FEDERAL named-excluded is mandatory — the double-count fence; state stays out of the engine this unit) + council-decided (reject B · spendHelp category line · residual magnitude reword · lever notes untouched · C committed as a filed engine unit) + **⚑ yours-to-close (A-over-B ran against his voiced instinct · the wordings · the roster input)** | **A MAGNITUDE-HONEST NOW + C COMMITTED; B REJECTED.** The red team broke a 5/6 B-consensus with four source-verified hits: the contained figure is a user's guess of an ENDOGENOUS quantity (real decumulation state tax is downstream of the recommendation the user hasn't seen); it is strategy-INVARIANT, so ranking — the tool's actual job — stays blind under B; `sequencingBaselineNote` ("same spending… the gap is the order itself") becomes false under B; `BUDGET_CATEGORIES` has no tax category, so a folded tax dollar inherits grocery/sticky/survivor scaling nobody chose. The hawk REDIRECTED his veto onto B **and onto soft-A** ("a little tighter" mislabels a recurring state-tax haircut as a rounding hedge — magnitude-honest wording required). **Dissent (advocate + architect, recorded):** B matches the user's voiced taxes-are-a-bill mental model; flip condition = Briggsy ruling his cold read a hard spec. | high 8/10 | **executed same session** — spendHelp gained the category fence ("Leave out income taxes too: the tool works out federal tax from your withdrawals itself, and state income tax isn't counted"); the residual reworded magnitude-honest ("a real yearly bill in a taxing state, not a rounding error" — the fit gate caught draft-1 standing too tall on the two tightest frames → compressed + re-proven 17/17; the spec's stale text pin updated). **BRIGGSY ENGAGED THE FLIP QUESTION LIVE** (the zero-vs-educated-guess challenge, the gas-bill analogy, the 50-state question) **and ratified A+C over B on the full trade-space ("that shape sounds right, file it")** — C scoped to the ROSTER footprint {NC, PA now · SC/GA/FL/DE named maybes}, state = intake best-guess + assumption-panel seat, ONE retirement state per run, outside-set households keep the disclosed-out posture, the broad-brush generic model REJECTED with cause (phantom tax on PA/GA/FL-class retirees + Roth-lever inversion risk). **Trigger MET at filing** (NC taxes IRA withdrawals; Briggsy lives there) → sequenced before real-money reliance. Filed: TODO item 11. |
| 2026-07-10 | **Pre-build ratification: the post-65 MEDICARE PRICING engine unit** (the council-REQUIRED U11 follow-up — base Part B + full IRMAA for households the ACA gate never covers). Full council (9 elders + red team, run wf_4c8cd836-b22, 23 agents), convened on a 6-reader recon sweep (wf_8bacb39a-59a) that reframed the unit: the pricing machinery + CMS-pinned constants ALREADY EXIST end-to-end (`medicareAnnualCost`, healthOverlay.ts:477-487) — the gap is ONE GATE (`acaTable !== undefined` doubles as the ACA and Medicare gate; intake enables healthcare only with a pre-65 member). Five forks ruled: gate seam · containment · scope · UI handoff · ship gates. | council-decided (engine + correctness mechanism, all five forks) + **⚑ yours-to-close (the fork-4 disclosure TONE only — the narrowed residual copy + the affirmative "Medicare is priced in" line; ships PILOT-CLEARED under the grant at high council confidence, Caddie pre-walk owed, his eye supersedes + scores the tape)** | **RATIFIED 9/10, hawk seated, NO veto — with the red team's verified mechanism hit baked in.** **F1 gate seam = WIDEN THE INTAKE GATE (1a):** a `medicareOnly` branch at intakeMap.ts:539-548 emits `healthcareEnabled:true` for the all-65+ household WITHOUT the ACA quote pair (ACA pricing self-skips on `pre65>0`); REJECTED the decoupled `medicareEnabled` toggle (forks four acaTable-gated sites into a drift surface); 1a discharges the insight-076 seed-coverage sweep STRUCTURALLY (the validation already nests in `if(o.healthcareEnabled)`). **F2 containment = ADDITIVE PREMISE RATIFIED** (entered spending EXCLUDES Medicare premiums; the tool adds Part B + IRMAA on top — the pinned engine premise taxOverlay.ts:303-308, the shipped oopHelp promise, the conservative side of insight 055; insight 024 forbids inferring from the lump). Riders in-pass: spendHelp's bare "insurance" (copy.ts:79-80) rewritten to the PRECISE boundary (tool adds Part B+IRMAA; user budgets Part D/Medigap inside spending); SS stays gross; installed-base double-count = re-entry-visible copy + a named follow-up, never silent migration. **F3 scope:** Part D base OUT-but-disclosed; hold-harmless OUT with NO constant (nominal provision in a real-dollar engine; IRMAA payers statutorily not held harmless; insight-074 trap); survivor SSA-44 recheck stays out (disclosed, pessimistic-safe); real-flat Part B KEPT but DISCLOSED (Tier-1 mandatory — the one residual leaning optimistic); the Medicare-cost-trend constant ELEVATED to a documented Act-4-solver-BLOCKING forward landmine; a dated **2028 IRMAA top-tier re-index TRIPWIRE test** (seniorBonusSunset precedent), not reVerifyEveryBuild. **F4 UI handoff = MINIMAL, RE-KEYED (the red team's consensus-breaking hit, chair-verified at source, 8/9 elders re-aligned):** `dateSearch.ts:222` forces `healthcareEnabled:true` on every date candidate, so a still-working all-65+ household ALREADY prices Medicare today while the age-keyed `medicareUnpriced` note tells them it isn't priced — a PRE-EXISTING LIVE false statement on the date route. RETIRE the age-based full-gap claim entirely (`verdictMedicareUnpriced` + `rothMedicareUnpricedNote`); the disclosure predicate MUST key off the run's ACTUAL route-aware pricing decision, NEVER ages (**the hawk's armed conditional veto**); REPLACE with a narrowed residual disclosure (Part D base / Medigap / purchased Part A / state tax / real-flat Part B — "could be a little tighter than shown", never the false "never up") + an AFFIRMATIVE "Medicare is priced in" line on the all-65+ verdict surface, the two shipping TOGETHER (hawk condition — the affirmative alone would be a new optimistic lie); door stays ACA-scoped (Medicare-only door rides a later cold-read-bearing surface unit); no grade cap found live. **F5 ship gates:** DND-012 hand-derived fixtures on the RUNTIME path incl. the t=lookback seed→history crossing, a tier-edge `<`/`<=` witness, and the survivor MFJ→single +2yr lagged filing flip; reduce-to-spine BOTH directions + a new Medicare-only OFF arm with presence companions; the 076/R19 planted-fail arm (absent-seed all-65+ → calm indeterminate, never silent $0, never mid-path throw); the date-route regression witness (mutate ages holding pricing → the note must not reappear); un-retuned dev-seed drift RECORDED before any re-tune (?seed=retired / ?vault=stale / ?vault=datestale — senior-bonus C3); verify:fit re-pinned honestly + a date-route all-65+ still-working arm; Caddie pre-walk BOTH routes before his read; ONE commit, no riders. **Dissent (recorded):** quote a representative dollar in the affirmative line (craftsman/fiduciary/advocate) vs qualitative naming (minimalist/security — spurious precision, healthReadout is spine-only); flip condition = a clean route-available figure that reads calm in the Caddie walk. Hawk's secondary: the date-route lie leans PESSIMISTIC (double-discount), the spine $0 gap is the OPTIMISTIC sin — name each with its true direction. Fiduciary current-law caveat stays SEPARATE (still filed for Briggsy). | high 9/10 | **executed same session (2026-07-10 night)** — ce:work build, serial fleet M1–M4 (gate/fixtures/UI/closeout), every hawk condition honored: the `medicareOnlyPriced` intake branch (ONE production seam — zero validation arms needed widening, the 076 discharge proved structural, mutant-red), the DND-012 battery (seed→history crossing at exactly t=2 · the $218k/+$1 tier edge · HSA cap-sum · containment no-false-fire · the 2028 tripwire; the survivor flip cited to its two shipped streamless fixtures, no brittle duplicate), the route-aware disclosure seam + affirmation/residual pair + spendHelp boundary + `?seed=date65`, verify:fit 17/17. **The ship surfaced ONE fork mid-build — the `?vault=stale` composite pushed the protected R13 disclaimer 41px past the fold — closed by EXECUTING the already-filed backup-door/disclaimer priority-inversion swap under the grant** (DOM-order move, door measured with a per-arm allowance; disclaimer back in-frame at 755; his eye owed on the card). Un-retuned drift RECORDED then purpose-restoring re-tunes (borderline IRA 640k · budget's own 600k — one knob provably can't serve both) + a new borderline state pin. Follow-ups filed: installed-base double-count (re-entry-visible copy, no silent migration), the ACA-sheet real-flat gap (own walk, never a rider). Ultramode review + Caddie pre-walk next; the tape scores his verbatims. |
| 2026-07-10 | **The AGED ODDS-LADDER re-base** (TODO item 0 — the two-time-bases family's THIRD sibling, HARD-FLAGGED by the Caddie panel's False-PASS Hunter: on an aged vault the hero re-derives to wall time while the ladder speaks save-relative offsets under "Years from now you stop" — the giant rosier hero over the honest contradicted ladder, on the magic moment). Full council + UI specialists (9 elders + red team, run wf_99415094-417, 23 agents). THREE ruled questions: Q1 past-rung treatment · Q2 crown-arrived edge · Q3 the U17 scope boundary on the card's four coupled findings. | council-decided (the design + every mechanical rail) + **⚑ yours-to-close (the rendered-surface sign-off: the DROP-vs-RELABEL empathy feel + the band↔ladder PICTURE coherence — Caddie pre-walks it, his eye closes the two-time-bases family)** | **Q1 DROP + RE-BASE (unanimous, red-team-conceded; ruled in on four independent lenses — honesty, verified geometry, rule 36, a11y):** passed stop-years drop AT THE MARK ARRAY (a past year is not a choice; its odds are counterfactuals against save-day balances — and the single-sourced aria/scrub sentence dies with the dot, never a CSS-hidden ghost); survivors re-base to years-from-today via a pure viz transform (`agedLadderMarks`: filter-BEFORE-geometry — a negative offset silently clamps to "today" at PLOT.left, a fresh lie; domain from the SURVIVORS; React keys on the DURABLE plan offset, insight 047; elapsed ≤ 0 returns the input by REFERENCE — fresh byte-identity as a rail). REJECTED: calendar-year ticks (a third chart dialect, corpus rule 36) and RELABEL-with-visible-odds (the advocate's veto — the "did I miss my 9-of-10 window?" scare). **Q2 WITHDRAW iff crown < elapsed** (strict: crown == elapsed keeps the "stopping today" crown, agreeing with the hero's arrived arm; one predicate covers filter + withdraw + fresh identity). **Q3:** park (a) the band's elapsed-segment projection, (b) work-stops-before-Today, (d) the floor's arrived-line mud for U17 — with a HARD-NAMED coupling: U17 decides ladder-withdraw + band work-stops geometry TOGETHER and plants an ARRIVED vault in its walk. **PULL (c) FORWARD as a shipping rail** (the red team's second-order catch, absorbed mid-council: dropping the visible contradiction makes a stale 9-of-10 MORE believed while the balances caveat lived only at the gate): the ladder caveat gains the aged-balances clause, keyed on elapsed ≥ 1 ALONE — it fires even when no rules-clock did — naming the SAVE YEAR (wall-time-stable), never a re-derived span. **SHIP-GATE:** the Caddie re-walks the re-based `?vault=datestale` (the band↔ladder PICTURE coherence is the red team's undischarged residual — if the walk condemns it, escalate THEN; do not pre-commit a fork) + a `?vault=datestale` verify:fit arm. **Dissent (red team):** DROP unifies two NUMBERS (hero = ladder crown) but the band's parked save-relative geometry may desync the PICTURE on the two stacked charts — flip condition: the walk's read. | high 8/10 | **executed same session (2026-07-10)** — built + mutation-proven (10 planted mutants red→reverted: boundary filter, re-base, identity short-circuit, withdraw boundary, caveat gate, the LIVE fit-arm pair, + the review-fold trio below); the fit gate grew the datestale arm (16/16 — the one-clock pin reads hero-N == crown-N structurally, surviving engine re-grades); the pilot's insight-078 same-meaning sweep folded TWO adjacent siblings the council never saw: the TRADEOFF line no longer OFFERS a passed stop-year (dateTradeoffPoint elapsed exclusion) and the no-date HOW-CLOSE line reads future offsets only (conservative); the Roth plan echo (save-relative "starting in about N years" + its WRITE-side twin, the sheet's startYearOffset field) FILED as the family's next sibling, U17-coupled, never half-patched. **The ship-gate RAN and BIT, twice:** (1) the 6-reader Caddie re-walk verified the fix (spouse: the ladder is now "the clearest thing on the page… agrees with the headline"; the Hunter's tradeoff lane CLEAN) but its False-PASS Hunter found the in-frame echo carrying only the reassuring half ("uses today's") over a below-fold balances caveat — `stalenessHeroNote` reworded to "Figured under today's rules, from the numbers you saved." (one line, both routes, fold law measured green); the band↔ladder residual did NOT condemn the fix — every tension anchor is a parked U17 facet, now corroborated ×5 readers (the urgency evidence rides U17's charter). (2) The 3-lens double-refuter diff review confirmed 2 of 3 findings (zero false-alarm survivals): the empty-filter how-close line could fabricate "0 of 10" on a vault older than the whole window (now SUPPRESSES — the suppress-don't-fabricate siblings' law) and the caveat's year source was the BUILD year, not the save year (`agedBalancesYearFor` now reads the persist machine's own saved scenario's `savedAt`, gated on the clean view — an edit or re-save kills the clause structurally; the hydrate seed now splices the DISK's savedAt, whose absence had silently killed the clause live: the datestale fit arm went red on exactly that and green after). His sign-off + the tape rows stay open. |
| 2026-07-10 | **O3 — AT text-path parity for the band chart** (the Caddie filing: legend text + the IQR "middle half" + the y-tick dollar ladder live inside the `role=img` subtree, absent from the a11y tree — is the C2 compact SR sentence honest parity, or must the SR path extend?) — light council (a11y-auditor + design-engineer + the hawk), run wf_27e87ceb-745. **The hawk-seat guard's FIRST LIVE FIRING:** the hawk OPENING crashed at the StructuredOutput cap again (the exact 2026-07-09 filed incident shape) — the new guard (shipped this session) retried the seat, the retry sat, and the council concluded on a REAL hawk position instead of a silent `fired:false` over an empty chair. | council-decided (the three exclusions — execute) + yours-to-close (the anchor-direction check) → **CLOSED same session by measurement** | **AS-DESIGNED, ratified in code** (bandPanelChrome.ts + copy.ts rationale comments): the sighted scrub itself quotes only p10/p50/p90 — no sighted reader ever receives an IQR figure, so the SR sentence is at PARITY with the sighted numeric channel (folding p25/p75 in would be over-parity); the y-ticks are a position→dollar decoder the SR reader is handed pre-decoded; the legend's two load-bearing tiers are verbatim in the sentence. Option B / a second SR sentence / an SR tick-ladder REJECTED. **The red team's real hit (the anchor: "deepest" is only cohort-monotone, not dollar-monotone — a pre-SS bridge trough could sit below it unheard) was MEASURED, not reasoned away — the low-edge probe ran on all three canonical fixtures and returned PASS ×3** (`anchorIsCleanMin: true` everywhere): date crown — AT y≈29 low $87k IS the couple-clean minimum; datesplit floor crown — AT y≈29 low $77k IS it; retired spine — AT y22.5 low $266k IS it. The feared earlier trough does not exist — the low edge worsens INTO depth; every lower interior low (incl. the date fixtures' $0 tails at y31–33, cohorts 0.57–0.68) sits in the survivor-dominated 0.5–0.75 band the **Briggsy-approved** `AT_RANGE_COHORT_MIN` margin deliberately excludes — a RULED exclusion, his cold-read-TUNABLE knob, now consciously on the record. FILED (low-priority, per the verdict): a null-anchor household's SR silence could wear a generic "too uncertain to quote" line. FILED (same doctrine, adjacent — pilot's note): the C2 sentence's "about N years out" speaks PLAN-time on an aged vault (~elapsed years long) — the one-time-base law's last un-anchored text; rides the next AT-copy touch. | 6/10 held → the probe closes the narrow ruling at high | executed |
| 2026-07-09 | **The senior-bonus SUNSET engine unit — pre-build ratification** (the U13 review-REQUIRED, tripwired follow-up: year-gate the OBBBA bonus to calendar 2025–2028; the pre-unit engine credited it in EVERY sim year — ~$12k/yr phantom MFJ deduction for 2029+, optimistic, the cardinal direction) — D1 API shape · D2 window home · D3 predicate · D4 DND-012 fixture policy · D5 seeds · D6 UI coupling · D7 ride-alongs · D8 no new disclosure (run wf_a5ccd576-048; full roster + rebuttal; 23 agents) | oracle-settled (the FIX DIRECTION — triple-compelled, not relitigated) + council-decided (the whole build shape, EXECUTE) + ⚑ yours-to-close (D5 seed-tone review — MOOT, zero drift; + whether the fiduciary's current-law caveat lands as a separate follow-up) | **RATIFY + BUILD as ONE commit with the red team's four hits as BINDING conditions.** C1 (the most dangerous): the DND-012 anchors as originally framed validated the WRONG primitive — hand-derive the out-of-window + crossing fixtures on the RUNTIME `ordinaryPlusCapitalGainsTax` with realizedGain>0 AND leftoverDeduction>0 (the gain-shelter path is byte-identical to the no-gain primitive ONLY at gain 0 — the no-gain anchor is structurally blind to the shelter channel), land the crossing on the statutory edge (a `<` vs `<=` off-by-one witness), and ADD a full-solver coupled crossing (low-ordinary/large-gain brokerage household through the gross-up + the bracket-fill ceiling→ledger feedback). C2: ADD `effectiveFrom: 2025` (absent — only sunsetAfter existed); the metadata-read home GATED by a mandatory fail-loud throwing guard + SYMMETRIC shape pins (the rmdStartAge "value-home precedent" was verified FALSE — its effectiveFrom has zero executable consumers); the literals never re-typed at the consumer. C3: the digest records each seed's UN-retuned drift BEFORE any re-tune (the re-tune must never blind the cold-read oracle); `?vault=stale`'s 2024 anchor legitimately prices no bonus — re-cold-read, don't "fix". C4: the readout threads calendarYear = startCalendarYear + the SAME yearsIn that derives count65, pinned by a READOUT crossing test. Settled: no real-dollar deflation (frozen-real, calendar-exact); `ordinaryIncomeTax` gains the param; D8 silence holds (074 — the fix makes the engine match the shipped silence); the tripwire retired in-commit. The red team's hardest attack (cliff inversion) REFUTED at source: both MAGIs exclude the below-the-line deduction, so no 400%-FPL/IRMAA inversion is reachable — the correction is uniformly conservative. **Dissent (fiduciary, advocate concurring):** a general current-law-as-written caveat IS owed in the assumptions registry; flips on the hawk ruling the OBBBA-2025 vintage label insufficient — filed for Briggsy, never a rider. **Seat incident (recorded):** the honesty hawk's OPENING crashed at the StructuredOutput retry cap and council.js `.filter(Boolean)` silently discarded it; its REBUTTAL seat ran fresh and voted RATIFY explicitly ("the fix kills a live calm-but-wrong optimistic error… no disclosure owed in-unit because the post-fix direction is conservative", conf 0.9) — the verdict's no-veto is grounded in that recorded vote, not an empty seat; the seat-crash hardening is FILED (insight 019). | 8/10 | **executed same session (2026-07-09)** — SHIPPED in one commit: the window consumed from the constant's metadata behind the fail-loud guard (+ symmetric shape pins), the calendar threaded through taxCore/magiLandscape/GrossUpContext/healthSheetChrome, all four conditions landed (the gain-shelter battery incl. the $840-through-the-shelter-alone fixture, both statutory edges, the full-solver step at exactly t=2029 — hand fixed-point algebra matched the engine to the sub-cent — the ceiling→ledger crossing, the 12¢→22¢ readout arm, PBT 2023–2036). Six moved fixtures hand re-derived (incl. the M6 cross-overlay golden: 1,397,277.51 → 1,395,389.58). **C3 record: every dev-seed engine-proven outcome pin GREEN UNCHANGED — zero state-level drift, NO re-tune performed.** The tripwire retired; 3 planted mutants red→reverted; typecheck+lint+build+bundle+fit+aca+doc-stats green. Docs: architecture §7.1/§8, staleness header, u13-build-spec supersession, README/roadmap counts. **The ultramode review RAN same session (wf_93b637f6-fcd — 6 value lenses + a 5-angle adversary panel, 2–3 refuters per finding, 23 agents, 0 crashes): 6 raw → 0 vote-confirmed, 5 advisory-immaterial; ONE 1–1 materiality tie hand-verified REAL + FOLDED** (validateParams `isFinite`→`isInteger` on `startCalendarYear` — the window guard's new mid-path throw vs the R19 calm-indeterminate contract; planted-fail arm inside the R19 battery, mutant-proven) — 2129 tests / 118 files. |
| 2026-07-09 | **U13 pre-build ratification (re-entry + per-surface staleness)** — SEVEN coupled calls after the 7-reader reality survey found the load-bearing conflict (the plan's "re-present under the SAVED fixture vintage, byte-identical" is architecturally impossible: the engine module-imports its dated constants — taxOverlay.ts:87 / healthOverlay.ts:37 — and only version STAMPS persist, so re-derivation always runs under CURRENT constants): Q1 byte-identity scoping · Q2 the wall-time anchor (none exists — ModelRecord={iv,ciphertext}) · Q3 the tax stamp (the plan's "stamp U10 writes" is FALSE — only an inert hardcoded 'OBBBA-2025' string) · Q4 applicable-% clock · Q5 the ReEntry mount + balance confirm · Q6 budget time-box scope · Q7 app-default immunity (run wf_a1ef7e25-64d; 23 agents; full roster + rebuttal) | council-decided (the honesty spine + 6 leans + Q7 rebuilt + 4 red-team constraints) + ⚑ yours-to-close (the staleness/confirm/recovery COPY tones — the Act-3 exit-condition cold-read, ships cold-read-ready) | **RATIFY the honesty spine and SIX leans; Q7 REBUILT; ship the corrected package.** **Q1 (oracle-settled):** byte-identical re-presentation is scoped to the NO-DRIFT case only; under drift the surface presents the CURRENT-vintage recompute WITH — never after — a calm "assumptions updated since your save" note; engine vintage-injection REJECTED, persisting constant sets REJECTED. **Q2:** `savedAt` ENCRYPTED in ScenarioV3 (additive-within-v3), finite integer epoch-DAY, codec RANGE-gated [~2020..2100] (046-class, not mere finiteness), stamped at the ATOMIC save-commit, EXCLUDED from the dirty/clean compare + round-trip guard via ONE shared normalizer (scenarioFromDraft's output is both the disk payload AND the dirty-compare operand — the clerk's determinism catch); absent savedAt (every legacy vault) SUPPRESSES the "~N years" claim, never fabricates. **Q3:** mint `TaxVintageV3` + `taxVintageStamp()` in tax.ts (mirror healthcareVintageStamp; TAX_YEAR + legal basis); scenarioFromDraft re-stamps ALL vintages fresh at save (058 — today only healthcare re-stamps); RMD-age (effectiveFrom 2033) + senior-bonus (sunsetAfter 2028) are DERIVED read-time communication notes against LIVE constants, never stamp-compares; the legacy opaque string stays (add-only). **Q4:** documented proxy (coverageYear + acaStatus + acaVerifiedOn + the reVerifyEveryBuild CI gate), no new field. **Q5:** IntakeApp-phase mount; the confirm is UNCONDITIONAL on every writable unlock, per-bucket incl. HSA + the SS fold-in, affirm-first read-back; changed split → the EXISTING intake walk-through, changed total → the U12 sharpen loop; read-only tab → view-only, no write affordance. **Q6:** budget time-box re-confirm fires on the already-retired spine route only ((wallYear − startCalendarYear) > endYear); the date route is documented-inert (its year-0 anchors to the FUTURE crowned work-stop). **Q7 REBUILT (the red team's most-dangerous catch, conceded across the roster):** mint CURRENT_APP_DEFAULT_VERSION, but the convener's "value≠current-default⇒immune" lean is INVERTED honesty — it silently withholds the "we updated our defaults" note from the took-the-default household after a conservative default move; detection = SAVED-ERA comparison via a small add-only version→default map keyed on the persisted appDefaultVersion (v1 seed: p2-d1→survivorSpendingRatio 0.75) — vacuously inert in v1, but the SHAPE locks correct for the U17-inheritable clock map; the "safe over-disclosure" framing is STRUCK from the record. **FOUR ADOPTED RED-TEAM CONSTRAINTS:** (a) drift booleans computed ONCE at unlock from the RAW-decoded model, carried as UI state, BEFORE draftFromScenario/scenarioFromDraft re-stamp (read the wrong object and the note either never fires or cries wolf); (b) the confirm GATES the reveal — it resolves BEFORE setPhase('result')/recompute, never verdict-then-ask; (c) the confirm is a PROMPT, never an attestation (no stored/displayed "confirmed-fresh on DATE" — R19 laundering); (d) the senior-bonus note quotes mfjBothSpouses65 = $350k, never the one-spouse $250k. **STRUCK from the fix list:** the ACA verifiedOn item — verified already fixed mid-council (62ebe5fa). **Render floors:** CSS-only crossfade (no framer-motion — the style-src trap), reduced-motion identity, never-color-alone info-silhouette distinct from the six verdict glyphs + text in the a11y tree, polite live region, tabular-nums on the year figures, no write-back into startCalendarYear/currentAge. **Dissent (minimalist, Q2):** drop savedAt, anchor every wall-clock on startCalendarYear — rebutted 7-elder (startCalendarYear = "since you BUILT the plan," mislabels every re-saver; only an in-ciphertext savedAt survives export-restore for the wiped-device survivor); flips only if re-save frequency proves ~zero AND the export-restore path is judged out of wall-clock scope. | 9/10 | **executed same session (2026-07-09)** — all three phases BUILT + LIVE-VERIFIED (commits 77ed4fa7 substrate · 1b1dd91f reader · 2a21b348 gate, each CI-watched by explicit run id): the gate PROVEN to hold both recompute tiers until the affirm (the gate-order pin), the savedAt normalizer proven live (an untouched hydrate reads clean), the stale surface walked end-to-end via the new `?vault=stale` aged-vault plant, `verify:fit` 14/14, 4 planted mutants red→reverted, 2108 tests / 118 files. **One integration ruling (the U12 builder-deviation precedent):** the confirm's change route converged to ONE affordance → the intake walk-through — the council's split-vs-total dual routing would force the USER to self-classify drift (the don't-make-users-think law), and both destinations are account edits at their entry point anyway; the sharpen loop stays one door away on the verdict. **One frame catch at live verify:** the two-line hero echo pushed the PROTECTED R13 disclaimer 13px below the one-frame fold — cut to one line at the measure (the full per-clock disclosure lives at the gate). ⚑ the copy tones ride the Act-3 exit-condition cold-read (TODO item 7). **The ultramode review RAN + FOLDED (2026-07-09 same day, wf_44cdf86d-b71 — 13 lenses incl. a 4-angle adversary panel, 44 agents, per-finding refuters; 31 raw → 18 confirmed → 8 root defects fixed, 9 advisories, 4 dropped): the Q3 senior-bonus "derived note" is SUPERSEDED-BY-EVIDENCE** — its ratified premise ("already deterministic inside the engine") is FALSE against source (the engine never reads sunsetAfter; the note asserted a re-pricing that never happens — calm-but-wrong optimistic, 4-lens convergence), so the sunset clock was REMOVED and the REQUIRED engine sunset unit filed + build-tripwired (red from 2028-01-01); the hero echo split onto rulesMoved (a lapsed budget window is a gate prompt, never "rules changed" over a byte-identical recompute); the contribution clock route-gated for retired households (+ route-true blend copy); the clock subsystem unified LOCAL-calendar (the Dec-31 UTC skew); the floor line anchored like the hero; the J1 survivor-door composition + the drifted-vault clean-badge law pinned; verify:fit gained the ?vault=stale arm (15/15). 7 mutants total red→reverted; 2120 tests / 119 files, every gate green. The dated supersession block lives in the build spec. |
| 2026-07-08 | **U12 pre-build ratification (the sharpen loop)** — eleven forks drafted AFTER a 7-reader ground-truth sweep: F1 market-moments editable? · F2 tax-payment-source · F3 the three non-parameter assumptions · F4 panel IA/door seat · F5 state-edge margin home · F6 sticky scoping + recompute policy · F7 the crossfade (grep=0 — Act 2 defined, never built) · F8 aleatory-floor posture · F9 cleared-required-fact→indeterminate · F10 four carry-ins · F11 R7 compile-enforced registry (run wf_dff75c2f-9e3; UI elders self-seated; 9 agents; the F8 probe study ran in parallel — 30 CRN-paired runs, two regimes) | council-decided (F2/F3/F5/F6/F7+CSP/F8-market-floor/F10/F11 + the F9 widening + F4 panel-fit arm + survivorSpendingRatio knob) + ⚑ yours-to-close (the F1 defer — it OVERRIDES a 5-1 elder majority; + the N=1 cold-read tones: crossfade, standing line, door label) | **RATIFY the spine; BLOCK the panel's fact-clearing editors behind two source-confirmed fixes; DEFER F1.** **HONESTY-HAWK VETO fired (NARROW) + the red team broke the 6-elder clean sweep:** F9 as drafted keys on `=== undefined` (intakeMap.ts:121) while validateParams accepts 0 (simulate.ts:507 `x >= 0`, no $0-spend guard analogous to the date route's $0-portfolio reject) — **a zeroed budget mints annualSpendingReal=0, clears every gate, and renders a confident "over-funded, 9 of 10" on a household spending nothing — a ROSIER lie than the held-stale verdict F9 was built to kill, reachable the instant the panel ships.** Required: the guard widens to NOT-VALIDLY-PRESENT (`!(spend > 0)` — NaN-safe, catches undefined/zero/garbage in one clause) + planted-fail tests on BOTH clear-to-undefined AND clear-to-0 + the engine-vs-store doctrine contradiction (memoryModel.ts:171-174 promises "the engine — never the UI — decides indeterminate"; buildParams returns null instead of the promised sentinels) resolved BEFORE build per Contradictions-Mean-STOP. **Resolution executed — and SHARPENED at build (the shipped shape): the build derivation proved pure engine-decides IMPOSSIBLE for intake-domain facts (an absent ACA pair builds params that validate fine — healthcare silently unpriced, optimistic — the exact reason the intake requires them), so `missingRequiredFacts` is the ONE authority (builders null exactly on it), the engine's R19 gate stays the decider for everything DISPATCHED, and post-first-resolve builder-null commits the new tier-less `inputs-incomplete` ModelAnswer arm — the store records the authority's verdict, fabricates nothing, and the fit gate's final-tier wait can never satisfy on it; pre-first-resolve byte-identical. The stale sentinel-promise comments rewritten, dated. Bonus kill en route: the old "$0 spend → honest 0-of-10" sanity pin was factually BACKWARDS and VACUOUS (its touchedAll never contained the spend field — insight 029).** **F1 DEFERRED to visible-only** (the red team proved the drafted σ-floor is one-sided, the mean-ceiling was a mid-council mint, and BOTH bound values are unsourced — burned/062/§8; the engine floors NEITHER: σ=0 and mean≤1 both pass validateParams) — the market model ships as a disclosed methodology block with provenance; **survivorSpendingRatio ships as the ONE real R7-editable methodology knob** (editable-READY: draft+persisted+sanity rule; its UI writer MUST run the intake sanity rule — validateParams allows up to 1e12); F1 flips back editable the moment a SOURCED two-sided bound (mean-ceiling + σ-floor + correlation sanity) is derived and cited with planted-fail range-gate tests. **F7 + hard CSP condition:** the crossfade is CSS-class transform/opacity ONLY — NO motion@12 layout-animation (injects an inline `<style>` that style-src 'self' blocks; dev/preview do NOT apply vercel.json headers = green-local/red-prod trap); verify:csp should exercise a verdict-change crossfade. **F8:** probe-first RATIFIED and the probe ANSWERED it — the per-edit width-delta heuristic is NOT honestly viable (width is a near-deterministic function of σ × wealth-to-spend ratio; a threshold fires on REGIME not edit-type: "floor" on almost every well-funded edit, almost never borderline — backwards); the standing line ships as a pure MARKET-FLOOR statement ("the range includes market luck we can't remove"), NEVER a "sharper inputs won't help" directive (same insight-025 defect); R8's "narrows on precision" has exactly ONE mechanical instance — the σ input, which is F1's edit (strengthens the F1 flip case). **F4** door-replace ratified (Assumptions takes the Review seat, the guided re-walk moves inside; count stays 5) + a NEW verify:fit arm measuring the PANEL's own vertical extent at 1536×791 ("count stays 5" proves the Result frame, not the panel). **F2/F3 oracle-settled** (insight 025 + the U10/U11 supersession precedent): tax-payment-source VISIBLE-disclosed with the engine slice FILED; longevity/survivor-SS/death-order = on-your-behalf disclosure rows, never fabricated knobs; 3-controls.md:238/251/254 superseded in-body, dated — mandatory. **F5** engine-side additive state-edge margin (0.98 is representable on the 0.01 grid — the red team's own quantization worry failed on inspection). **F6/F10/F11** ratified as drafted (red team conceded it could not break them). | 8/10 | **executed same session (2026-07-08 → 09 early)** — all three phases SHIPPED + live-verified (commits cd46a293 + bea7d65e, both CI-green): the veto arm PROVEN LIVE (spend→0 demotes to the calm incomplete strip, tier attr gone, hatch standing, recovery on re-entry); `verify:fit` grew the F4 panel-extent arm (14/14, mutant-proven); 16 planted mutants across the build all red-then-reverted; 2047 tests / 114 files. **Two builder deviations ratified at integration:** the hatch's compute-error arm (the old Review was unconditional there — dropping it would strand that household) and the crossfade's rendered-sentence swap key (an invisible $-step must never re-announce identical words). **One live-walk catch:** C1's sheet→sheet focus guard suppressed EVERY plain-close restore in real Chromium (onExitComplete fires while the dying dialog is still attached — jsdom detaches first, so the pin ran green); fixed by exempting the dying sheet's own subtree, both arms re-proven live. **OWED:** the ultramode review (next session — window budget; clean CI-green tree) + ⚑ the cold-read batch (panel/door-CTA/truer-picture/market-floor/crossfade tones — the bad-news transition is the formal Act-3 exit condition) + ⚑ **the F1 defer stays yours-to-review** (overrode a 5-1 majority; flip = sourced two-sided market-moment bounds, a bounded research task) — ✓ RESOLVED 2026-07-09: Briggsy KEPT the defer; the flip condition stays on the books, fired only on his ask. |
| 2026-07-08 | **The laptop Result frame no longer fits one frame** (Briggsy live, stations 2/3/4 of the U11 gate batch: "unnecessary scrolling on main page" — measured budget +101 / retired +60 / health +8 at the 800 tier; U10/U11 had legitimately grown the left column ~100-144px into a frame with ~6px slack) — rule the composition WITHOUT cutting content (run wf_a2d93977-960; UI elders self-seated; 21 agents; rebut:fiduciary-advisor died on a terminal API error — its OPENING was in the record and the final ruling was strictly MORE conservative than its own ask, so no resume) | council-decided (the density tier, the option kills, the fit-token unification, B(i) Medicare stays, the real-browser gate) + ⚑ yours-to-close (the residual ~70px — a TONE fork: terse door labels vs doors-below; plus the true-viewport confirmation) | **SPLIT.** The overflow is HONEST CONTENT doing its job — reclaim the frame from NEUTRAL WHITESPACE only, never the disclosures. **The decisive evidence was the design-engineer's LIVE browser measurement in the rebuttal round, which overturned the 8-elder opening consensus AND the convener's own lean:** Option A (full-width door shelf) is affirmatively NET-NEGATIVE (evocative sentence-labels can't collapse to one line even at content-wide; a shelf breaks the date route) — KILLED; Option C (disclaimer density) recovers 0px (the two-sentence R13 layout is deliberate) — KILLED; Option D (compact band tier) is INERT on the spine (the band is the non-governing column on every cold-read seed) — DEFERRED. SHIP: the short-laptop DENSITY tier (≥68rem ∧ ≤840px; whitespace one token step — result padding, two-pane row-gaps, stack gaps; the 917 showcase untouched; type NEVER moves); collapse the fit law to ONE token (`--laptop-fit-height` 800 — the source's own 768-vs-800 comment contradiction hid the regression; 768 formally retired) with the degrade contract AT the token (doors = the ONLY sanctioned below-fold casualty; verdict/Medicare/band/R13 protected); B(i) unanimous — the Medicare note KEEPS its veto-cleared verdict seat; enforcement must be a REAL-BROWSER Playwright height assertion (a token-sum arithmetic gate is reflow-blind and reproduces the silent regression). **HONESTY-HAWK VETO fired + HONORED:** the R13 disclaimer is the page-TRAILING element — the structural FIRST casualty of any overflow (reassuring verdict in-frame, "this can be wrong" scrolled out of sight); three elders who opened with "degrade the footer" all retracted. **Dissent (design-engineer + red-team #5 + minimalist):** honest content has OUTGROWN the fixed one-frame law — retire it for an explicit priority-order; flips if density + composition closes budget at 800 with the evocative labels intact (it did — see Action). | 8/10 | **executed same session** — density tier + fit-token + comment-contradiction fixes shipped and measured (−40px); his REAL viewport measured through his own Chrome via claude-in-chrome: **1536×791 @ 2.5×** (the "1871×917" screenshot-derived tuning target was WRONG — the council's ≤800 suspicion confirmed); ⚑ the tone fork presented with previews → **Briggsy picked "buttons drop below"** → the in-frame R13 disclaimer (two mounts, ONE visible, attribute-mirrored — a zero/double-disclaimer frame unrepresentable; DOM-order pin mutation-proven) + the doors as the grid's LAST full-width row on BOTH routes, commit 6f8305de. Measured after at his window: **budget 0 · retired 0 · health +17 (the 5-door second line only — the sanctioned casualty)**; phone byte-identical; the date route's content-necessary scroll keeps the ORDER contract (graphs → disclaimer → doors). The dissent's flip condition MET — the fixed-frame law holds this cycle. The real-browser vertical-fit gate is FILED as the next build (TODO item 5) — **✓ SHIPPED same day** (`pnpm verify:fit` + CI: 13 assertions over the mandated seed×viewport matrix on its own dev-server harness; 11 planted mutants all red; a 19-agent adversarial review confirmed ONE triple-lens finding — the spec's final-tier wait was vacuous — folded as the `ModelAnswer.tier` → `data-answer-tier` synchronization stamp, the dip arm's 6.8s→37.0s proving the wait went real). |
| 2026-07-03 | **The budget side-quest cold-read** (Briggsy, live, mid-walk: *"spending line by line — why does it feel like a side quest?"*) — diagnose the root cause + pick the remedy direction (run wf_67fa22e5-fbb; UI elders self-seated; 21 agents, 5 elders changed their minds post-rebuttal) | council-decided (diagnosis + remedy structure + the honesty gates) + ⚑ yours-to-close (the tone sign-off — does A + de-modalization clear the side-quest feel on your N=1 eye; every string overridable) | **Diagnosis settled across all 8 lenses: the side-quest feel is GRAMMAR (bookkeeping copy + world-dimming portal-modal choreography) + INVISIBLE REWARD — NOT placement** (your 2026-07-03 invite pull already proved placement alone insufficient; the budget is the ONLY list-builder in the product living in a modal behind a btn-quiet whisper while accounts/other-income build in-flow). **THREE-part composed remedy: (1) SHIPPED NOW — Remedy A**, reward-forward strictly DIRECTION-NEUTRAL copy: the invite carries the AXIS (`budgetCta` → "Split what must hold from what could give", measured single-line in the quiet row), title → "What must hold — and what could give", intro names what the split buys ("the answer takes each on its own terms") with NO promised direction, the reconcile-to-target audit line killed ("Lines to account for" → "About $Y a year goes into the lines below"), Cancel → "Not now"; reward copy ONLY in static strings, never the role=status readout (insight 035). **(2) COMMITTED NEXT (co-primary, its own tested change):** de-modalize the IN-FLOW mount — swap the spend-step body in place on the existing step-transition grammar (accounts/OtherIncomeEntry pattern, `--dur-step`, no bespoke duration); KEEP the Result mount modal (one component, two reveal shells — BudgetBuilder is already presentational over props); re-derive the insight-067 escape contract + real tests (054) + reduced-motion identity; byte-identity holds (the sheet writes nothing until a real Apply; commitBudgetPatch stays the sole writer). **(3) GATED (not this session):** D re-scoped to a state-adaptive second beat naming ONLY the axis/separation — **HONESTY-HAWK HARD VETO (conf 9, primary-source-verified) fired on directional-D as EVERY elder first drafted it** ("the essentials date comes forward / the two dates split apart"): the R27 100%-FPL/PTC inversion is REACHABLE post-U10-conversions (dateSplit.ts:25-29, :89-95) and the engine already ships `dateFloorInversionNote` saying the OPPOSITE — a directional promise at the ask manufactures the exact sin the module exists to prevent, on precisely the pre-65 cliff households U11 protects. Directional truth rides ONLY the existing gated post-split disclosures; prerequisite = derive the owed `dateinvert` seed EXTERNALLY (DND 012) + engine-verify BEFORE any cold-read + extend the CVD probe for any new visual cue. **REJECT B** (synthetic 2-line budget — fabricates the user's authorship of the split, inherits insight-055 sticky-pessimism AND the inversion, adds a second reconciliation writer) **and REJECT C's mandatory step** (R5 never-gate). **Dissent (minimalist, echoed by hawk on sequencing):** ship A ALONE and re-cold-read before spending the de-modalization refactor — flips if your A-alone read clears it; countered by motion-designer + red team (the portal-modal grammar is the loudest UNTESTED lever — position was already tested alone and failed). | 8/10 | **executed (parts 1+2, same session)** — Remedy A shipped + live-verified (copyGuard caught + killed a "no matter what" certainty idiom in my first intro draft; full-axis CTA measured single-line laptop + phone). **The minimalist's dissent RESOLVED AGAINST ship-A-alone by Briggsy's own read** (he walked A live: "still a side quest" — words on a whisper can't fix a whisper) → **de-modalization built + live-verified same session:** `BudgetBuilder` `variant: 'sheet' \| 'inline'` (one form core, two shells; Result keeps the modal — verified dialog/backdrop/scroll-lock/H2), the spend step consolidated to ONE inline mount (`SpendStepBody`) swapping the body in place on `--dur-step` `@starting-style`, edits survive Cancel-and-reopen, insight-067 focus contract (heading on open, live face's steer on close) — 4 new inline pins; 1934 tests / 110 files, all gates green (bundle 234.3 KiB). **One deviation from the letter of the verdict:** Cancel stays 'Cancel', NOT "Not now" — Briggsy's own 2026-07-03 cold-read had already renamed Not now → Cancel for honesty; the prior N=1 word outranks the council's 'neutral leave' note (briefly shipped, reverted same session). Gated-D + dateinvert filed in TODO. **⚑ tone sign-off ✓ CLEARED same day (2026-07-03, Briggsy live on the de-modalized room: "reads like a continuation")** — the yours-to-close residual is closed; the side-quest thread is resolved through part 2. **Ride-along finding (pre-existing, NOT mine — measured before/after):** the 56a852d5 one-frame laptop fit has regressed — `?seed=budget` 101px / `?seed=retired` 60px vertical overflow at 1280×800 (the U10/U11 doors + post-65 disclosure landed after the fit pass) — filed in TODO for the U12 sharpen loop. |
| 2026-07-03 | **U11 pre-build design ratification (healthcare surfaces)** — the four coupled decisions red-teamed before code (U9a precedent): Q1 enhanced-subsidy toggle placement · Q2 shadow-rate semantics (magiLandscape single-producer) · Q3 per-year healthReadout emission shape · Q4 bracket-fill cliff-aware ceiling + PICKABLE join (run wf_f8dae086-58f; design elders self-seated) | council-decided (all four + the veto-clearing mechanism + every engine/a11y/CSP/copy condition) + ⚑ yours-to-close (the shadow-rate/cliff RENDERING TONE only — the calm stepped readout, one tabular-nums anchor, cliff as a non-color shape break, NO gauge/dial; batched with the dip cold-read as the U11 SHIP gate, contract #8) | **RATIFY all four + BUILD.** **The 2+4 collision DISSOLVED (unanimous post-rebuttal):** magiLandscape emits ONE **UNBIASED** best-estimate skeleton MAGI feeding BOTH the ShadowRateReadout anchor AND the bracket-fill ceiling (room = rail − skeleton); the conservative "overstate proximity" honesty moves OFF the shared engine number into (a) hedge copy + a proximity range and (b) the EMPIRICAL `overCliffFraction` (measured through the real per-path fixed point); guard test = the crowned strategy's `bracketFillCeilings` byte-identical with/without the readout's display transform; reuse the engine's CEIL-quantized 400%-FPL compare (insight 012). **HONESTY-HAWK VETO fired + CLEARED (not overridden):** a fully-post-65 couple is priced **$0 Medicare (base Part B AND IRMAA) every year** — verified `intakeMap.ts:483` (healthcareOn needs a sub-65 member) → `taxOverlay.ts:1503` (the whole Medicare block gates on `acaTable !== undefined`) — silently inflating survival for the CORE already-retired demographic. Cleared by the ratified alternative: (i) the Healthcare door gates CATEGORICALLY on the engine's real priced domain (no hollow door); (ii) an ON-SURFACE predicate-driven disclosure on BOTH the confidence statement and the conversion recommendation for these households ("Medicare cost — base Part B plus the IRMAA surcharge a conversion can trip — is not yet modeled for your household"), live-verified to render (insight 048), text-reachable in the a11y tree; (iii) the confidence grade CAPPED for these households (never "robust — just do it"); (iv) the base-Part-B + full-IRMAA pricing path FILED as a REQUIRED scoped follow-up ENGINE unit (needs the irmaaMagiSeed validation regime — `taxOverlay.ts:1518-1527` throws unseeded — plus the Part-B containment question vs entered spending; never dragged into a surface unit, contract #1). **Decision conditions:** (Q1) toggle in the Result-door sheet; Apply STRIPS the key on revert (never writes `false`); single-key write fence + noWriteSeam test (058); a ColdStart RESTORE carrying enhanced=ON force-surfaces a persistent "hypothetical/expired regime, not current law" marker; the dated legislative note renders from CURRENT constants at read time (never the persisted stamp as truth — reVerifyEveryBuild); SLCSP stays a loud sentinel + look-it-up link. (Q3) healthReadout mirrors bandFan (opt-in, observe-only, presence-keyed byte-identity, dead-cohort truncation); `overCliffFraction` = fraction of LIVING paths, rendered "X in N" conditioned on cohortFraction and WITHDRAWN when thin (reuse isThinCohort); `irmaaSurchargeP50` plumbed SEPARATE from base Part B. (Q4) min(rails) derived ONCE in `buildOverlay` riding the shared overlayBase (NOT three parallel builders — the fork-trap); params-only/in-memory/never persisted (+Infinity nulls under IndexedDB, DND 009); the ACA rail gated on the same enhancedSubsidies flag; the user-facing option RENAMED to name the binding rail + show WHICH rail bound each year (label honesty); no explicit target-bracket picker in v1; deliberate cliff-CROSSING stays possible via the conversion lever (Act-4 solver freedom). **Cross-cutting:** cliff + >100% states render shape+word+magnitude (color redundant-only); copyGuard gains READOUT-specific prefixes (`shadowRate*`/`irmaaReadout*`/`acaCostReadout*`), NEVER bare `health` (it would sweep `healthQuoteHelp` intake copy); tabular-nums everywhere incl. SVG text; the toggle preview uses composeTwoFutures but HEADLINES the lifetime health-cost Σ delta (the regime's effect concentrates pre-65 and may barely move the portfolio median). **Dissent (fiduciary + minimalist):** price base Part B NOW (MAGI-independent, ~$4,400/yr/couple) rather than disclose a number you could make right — yields IF decoupling proves genuinely non-trivial (it is: base Part B is coupled to the acaTable gate at `taxOverlay.ts:1503`, a never-exercised path needing its own reduce-to-spine verification AND the containment question — do retirees' entered spending figures already include the premium SS deducts?). Red team's two consensus-breaking grounds both ANSWERED in rebuttal. | 8/10 | **execute** — ce-work builds U11 now from the ratified spec (tasks U11-1…7). ⚑ the shadow-rate/cliff tone cold-read rides the dip batch (SHIP gate, not build gate). The scoped post-65 Medicare pricing engine unit is FILED (TODO). |
| 2026-07-02 | **U9b design ratification (budget-builder UI half)** — the FIVE coupled judgment calls before ce-work builds: Q1 entry/placement · Q2 two-tier hero choreography · Q3 date-route split render · Q4 the spend question under a budget · Q5 the seed affordance (run wf_67d45818-f65; UI elders self-seated) | council-decided (all five structural rulings + seven build-gates) + ⚑ yours-to-close (the Q2 VERBAL composition/tone — relief-with-honesty vs two competing verdicts — a formal Act-3 exit condition via your N=1 cold-read, NOT pre-confirmed; the build ships the seam so both verbal arrangements are a copy.ts change apart) | **Execute all five.** **Q1** ONE door — a quiet Result-surface "break down your spending" affordance opening a portaled sheet (U7 progressive-disclosure inheritance; laptop = right panel); NEVER an intake gate (R8). **Q2** the FULL-LIFESTYLE verdict stays the hero (word+glyph+count+magnitude all full-track); the essentials floor is a SUBORDINATE relief line, **WORD + COUNT ONLY** (floorReading is a bare Headline — borrowing the full-track dollar would BE the insight-056 sin); degenerate collapses to the single-metric statement VERBATIM. **HONESTY-HAWK VETO fired on essentials-as-hero** (the scared spouse reads the easier floor number as "we're golden" — optimistic-undisclosed at the glance most readers stop at) — **HONORED by the lead pick**. **Q3** render BOTH tracks always (killing the shipped `answerView.ts:143` floor-only render — an essentials date presented AS the fuck-off date, the textbook sin and the unit's highest-value correction); the work-optional claim attaches ONLY to the lifestyle track; single FLOOR-crowned band + a track-naming note, NO second band (038's measured 0.024-deuter CVD lockout + the standing no-date-curve veto); lifestyle-not-yet = words + how-close; the R27 floor>lifestyle inversion rides an explicit plain-language subsidy-cause disclosure, never reordered. **Q4** the governed spend question renders READ-ONLY labeled text + a steer into the builder + a "back to a single number" escape (budget→undefined); REJECT proportional rescale (forges an unchosen allocation) and the live raw second-writer. **Q5** the STRUCTURAL SHELL — persist NO line, `budget` strictly undefined until first Apply; the scalar is a labeled anchor target; byte-identity by construction. **SEVEN BUILD-GATES pinned off the red team's three conceded hits:** (1) every lines-target NETS OOP medical (target = max(0, S−M); compileBudget re-injects M — a raw-S target commits S+M, the silent answer-jump) + a NEW medical-present fixture (ratio-1 byte-identity + ratio<1 conservative divergence — the degenerate golden tests M=undefined and cannot see it); (2) NEVER `budget: []` (truthy at intakeMap.ts:561, collapses spend to M); (3) focus stays FLOOR-keyed, lifestyle sharpens reach AT via a polite live region (047); (4) subordinates wrap in ONE col-1/row-2 container (the confidence.css grid collision); (5) recompute on deliberate commit only, reserved boxes (035/036); (6) every honesty decision a pure exported planted-fail helper (048); (7) ≤1 subordinate X-of-10 face on the first frame (the survivor folds to `<details>` when the relief holds the slot). Dev seeds mandated for all three floor≠lifestyle arms + a medical-present household (the R27 seed gated on real-engine reachability — document-or-fixture, never fake). **Dissent (Advocate, narrowed in rebuttal):** R2's integrated essentials-first SENTENCE may beat hero+subordinate-line — concedes the hero WORD is full-lifestyle; flips only the verbal arrangement, routed to your cold-read. Red team conceded the structure. | 8/10 | **execute** — built same-session (~70%), CLOSED next session (2026-07-02: component tests · dev seeds · live verify · ultramode · doc sync — 1629 tests / 92 files). **⚑ Q2 VERBAL cold-read ✓ CLEARED (2026-07-02, Briggsy live at `?seed=budget`): "reads good to me, honest relief, keep the stack" — the formal Act-3 exit condition is CLOSED, and the Advocate's integrated-sentence dissent is RESOLVED against (the hero + subordinate-line stack STAYS; the copy.ts seam remains if his eye ever returns).** Same read: the one-frame laptop fit "no scroll, love it!" and the reworked survivor fold (statement-shaped summary, commit 56a852d5) passed as "fine — may revisit later" (soft revisit parked in TODO). |
| 2026-07-02 | **U9a design ratification (budget-builder ENGINE half)** — ratify/correct the 4 open calls before the persisted shape ships to real vaults: persisted `budget?` line-item shape + v1 category/sticky set · `survivalFraction` semantics · the two-track evaluation shape · the DateBand crown (convened as Briggsy's proxy — he's at lunch, ce-work builds next) | council-decided (all engine mechanics + build-gates) + ⚑ yours-to-close (the v1 category ENUM + medical-fence — the one add-only vault one-way door) | **RATIFY-as-CORRECTED, execute now.** (2) `survivalFraction` STAYS full-track; the essentials floor arrives ONLY as presence-keyed `Distribution.floor?` + `SimulationResult.floorReading?` (flip-the-meaning rejected: silent reinterpretation, zero type protection). (3) ONE simulate run per candidate, TWO decumulation+overlay passes on the SAME realStock/realBond/deathOffsets (a tier is a different SPEND on the SAME paths, never a re-sim); compiled `{sticky[], scalableEssentials[], discretionary[]}` @shared construct from `src/budget/budgetToSpending.ts`; **r-selection realized PER-PATH in `cashTermsForYear`, NEVER baked into the profiles** (insight 040); the ~2× overlay cost is fiduciary-necessary (the three-link monotonicity proof requires per-track divergence a ratio-on-total shortcut cannot generate). (4) the single DateBand crowns off the **FLOOR** track with fan AND state BOTH riding the floor pass (all three fields one track ⇒ on-track-or-over-funded by construction); lifestyle "not yet" = a no-date track + words, never a hidden red band. (1) `budget?` additive-OPTIONAL within schemaVersion 3 (NO v4 — no migration machinery exists, live vaults on disk); `endYear`-ABSENT = lifelong (beats the plan's `endYear\|null` — DND/009 + optFinite precedent); household work-stop anchor (per-spouse windows deferred to U11). **HONESTY-HAWK VETO fired + ABSORBED:** the two silent-optimistic paths (survivor sticky essentials scaled by ratio-on-total; a budget omitting medical while the stale intake `oopMedicalAnnual` still sizes the HSA cap → portfolio relieved of medical it should bear → floor date crowned TOO EARLY) are both foreclosed — **medical FENCED out of the v1 enum** (plan:69 forbids a user-typed premium line), OOP medical compile-INJECTED into the sticky floor single-sourced from intake (containment by construction), plus a **fail-loud per-year guard** `floor-essentials(t) ≥ oopMedical[t]` replacing the silent fundingNeed clamp. **Corrected v1 categories:** housing(sticky) · utilities(sticky) · food · transportation · travel · gifts · other (`other`+essentials errs STICKY — conservative). **Build-gates:** degenerate single line maps to `scalableEssentials` + the reduce-to-spine golden asserts degenerate FLOOR==FULL both NON-ZERO on a SURVIVOR-PHASE path (insight 029); two INDEPENDENT DateTrackOutcome objects, alias only degenerate (insight 047); U9a asserts only REACHABLE monotonicity, cliff-inversion fixture DEFERRED to U10 w/ reactivation trigger (insight 025); no-t0-line budgets R19-warned, never silently $0; `needVocab` for category/tier; dateSearchProfile rebaselined (2 passes/candidate) in the SAME commit; `annualSpendingReal` reconciliation invariant == year-0 full total (incl. injected medical), atomically maintained + test-pinned. **U9b carry-forwards:** never present the floor date alone when lifestyle is later (`answerView.ts:143` discards `outcome.lifestyle` — live calm-but-wrong gap); second-band needs a non-color channel + CVD probe; ratio-on-total only ever as DISCLOSED presentation, never engine. Red team's consensus-breaking hit (category enums × medical treatment) ABSORBED in rebuttal — 9 lenses converged on the fence. | 8/10 | **execute** — ce-work builds U9a now from the ratified spec (`u9a-build-spec.md`, session scratchpad; run wf_f18bbb26-389). ⚑ **yours-to-review (non-blocking, add-only):** the v1 category enum + the medical-fence; **dissent (fiduciary):** include a `healthcare` category scoped OOP-only/non-premium, single-sourced to `oopMedicalAnnual` — additive-safe to adopt in U11 if your eye wants the visible line. |
| 2026-07-01 | **colaPct ceiling** — set a GROUNDED max (+ enforcement) for a user-enterable `fixed-pct` COLA rate, closing an optimistic never-deplete hole WITHOUT false-rejecting real COLAs (surfaced in the U8 ultramode review; the review deliberately did NOT guess the bound — insight 049) | council-decided (bound + form + floor + enforcement) + ⚑ yours-to-close (the intake teaching-copy WORDING only) | **`COLA_PCT_MAX=0.05`, `COLA_PCT_MIN=-1`, Form A (ONE raw-absolute cited constant in `src/shared/incomeBounds.ts`, read by BOTH gates, never re-typed — insight 020), BOTH gates HARD.** **FORM A over inflation-relative Form B** because the codec sits at the ESLint leaf-layer lock (can't import the engine's `inflationMean`) AND is byte-identical anyway (inflationMean is a fixed methodology constant, no override — intakeMap.ts:448). **NUMBER 0.05 not 0.06:** the red team proved the 1pp "float razor-edge" cushion is a PHANTOM — `parsePercent('5')===0.05` exactly (same IEEE-754 double as the literal; fields.tsx:172), so a real 5% annuity rider clears the INCLUSIVE `<=0.05` with zero risk → 0.05's false-reject set is EMPTY (3% pension, FERS≤CPI, SS via real-flat all pass), halving the residual and annihilating the 0.2–0.5 sin band (0.30→~11,000× real). The project's asymmetric cardinal rule (ties go tighter) fires on 0.05-vs-0.06 exactly as it killed 0.10. **THE FAN IS NOT A BACKSTOP** (the load-bearing insight): a fixed-pct stream compiles to a DETERMINISTIC SEEDLESS overlay (otherIncome.ts:51-56) that lifts every MC path's floor identically → the confidence fan NARROWS toward success, never widens to flag an over-optimistic in-range COLA → **the ceiling is the SOLE defense** (kills the fiduciary's "fan is the honest backstop", forces the tighter number). **FLOOR -1 allow [-1,0):** a decaying stream is conservative (non-sin); -1 is the math minimum turning an engine overlay crash into calm `corrupt` (insight 027). **BOTH gates HARD** (codec `corrupt` + intake refuse-to-commit at the same 0.05; a soft override buys calm at the cost of truth AND desyncs intake↔codec). **Hawk veto armed only vs `>=0.10` or soft-override-only — NEITHER chosen; Hawk ENDORSED 0.05-hard-on-both at conf 9.** Red team could not break the spine; the two hits (0.06→0.05, fan-not-a-backstop) were conceded UNANIMOUSLY by all 9 in rebuttal. | 9/10 | **executed + gated** (constant + `needColaRate` codec gate + `income-cola-pct-range` sanity rule + form refuse-to-commit + a11y invalid-wiring & the dangling-`aria-describedby` class repair + source-bind test `COLA_PCT_MAX>=inflationMean` + boundary-valid tests; 1387 tests / 75 files, typecheck+lint+bundle+doc-stats green; insight 050). ⚑ **yours-to-review:** the intake teaching-copy wording (the craftsman's lead SHIPPED — "Most cost-of-living raises run 2–3% a year… almost always a typo — please re-check that number"; tweak `errIncomeColaRange`). **Dissent (fiduciary + minimalist):** 0.05 could false-reject a bespoke contracted fixed-nominal escalator >5% IF the "~5% rider is the real max" grounding is soft — the SAFE/disclosed (pessimistic) direction, not the sin; flips (re-grounds UPWARD, never padded) only on a cited real >5% fixed-nominal product. |
| 2026-06-30 | **U8 decrypt-on-return — the HONEST FAILURE SURFACE** (3 coupled forks decided BEFORE wiring the UnlockScreen): **A** the `probeVault` `'damaged'` arm (wire a real restore-from-backup UI, or an honest deferred dead-end?); **B** the UpdateToast "defer skipWaiting across the WHOLE ceremony" goal is unbuildable as written (add a ceremony predicate, or redefine to "never skipWaiting mid-WRITE" + rule a mid-export reload benign?); **C** terminal/degraded states with NO copy (both-lost; read-only 2nd-tab open) | council-decided (all 3 fork CONTRACTS + build/ship gates) + ⚑ yours-to-close (the EXACT C(i)/C(ii) wording only) | **BUILD decrypt-on-return with all 3 forks resolved toward the HONEST surface. FORK A — WIRE the real restore-from-backup UI NOW (do NOT defer):** the restore promise is ALREADY LIVE in 3 shipped strings (`savedBody` copy.ts:428, `unlockDataDamaged` :446-447, `unlockWrongCredential` :444-445) and `restoreVault`/`clearVault` are built — a defer strands a real-money friend behind a promise made 3×. **2 SHIP GATES:** (1) add a restore-over-`kind:'damaged'` integration test FIRST — the survivor's literal path has ZERO coverage (every existing restore test wipes to no-vault first: backup.test.ts:72,95,120,129,141,164,177,210,222 — insight-048 trap on the last door); (2) the damaged arm calls `restoreVault` **DIRECTLY** — NEITHER a pre-`clearVault` NOR a refuse-over-damaged guard (`writeVault` clears in-transaction db.ts:197; `restoreVault` validates+decrypts before any write backup.ts:124-173; a pre-clearVault would DESTROY a recoverable damaged vault on a bad-file/wrong-word attempt = data-loss — **the architect's clearVault-first prescription is KILLED, resolved against him per the cardinal STOP-and-resolve rule**). Reuse seams (recovery-word raw single-field · `PassphraseStep` for the set-new step · the `unlockCopy` failure seam · `.save-step--pending` · the one SaveFlow announcer); only net-new primitive = a real labelled `<input type=file>` (never a pointer-only dropzone as the sole path). Restore copy keeps the R17 grade (consume-side of "the file + the word you kept", never "you're covered"). **FORK B — OPTION 2 (never skipWaiting mid-WRITE) as the data-safety floor, wired ASYNC** (`await whenNoWriteInFlight()` → **re-check** `isWriteInFlight()` → `updateServiceWorker(true)`; the current SYNC stub UpdateToast.tsx:15-32 tears). REJECT option-1's session ceremony predicate (unbuildable: `status()`=='unlocked' across the export window session.ts:355-365). The write-gate is BLIND to 2 windows → **extend coverage:** (a) the export step is a pure READ → defer the toast while it's mounted via SaveFlow's EXISTING `step==='export'` React signal (SaveFlow.tsx:79-90); (b) `restoreVault` writes via `serializeVaultWrite` DIRECTLY (backup.ts:183), bypassing `session.enqueueWrite` → route the restore commit through the session's write accounting (a thin `session.restore` bracketing `writesInFlight`) so a mid-restore reload can't silently drop the survivor's recovery. The export-step toast-gate is **MANDATORY** (beforeunload does NOT stop an intentional "Update now"). Fix the toast aria-live region (mount always-present-empty + toggle inner content — it currently mounts populated :26/35 → won't reliably announce; mirror SaveFlow.tsx:217) + reserve its box (insight 035). **FORK C — fill BOTH gaps in the PURE `unlockCopy` seam, planted-fail tested, never in JSX** (the `readOnly:true` success has no seam home — `describeUnlockFailure` types off `Extract<T,{ok:false}>` :25-26 → extend to the success arm). Direction council-decided; the WORDS are yours-to-close: **C(i)** both-lost = a calm always-present recover-surface line: the no-backdoor truth (no reset, not even us) **AND STEER TO THE EXPORT FILE**, NEVER "it's gone" (wrong-recovery is GCM-ambiguous backup.ts:47-51 — a rotted LOCAL wrap opened with the CORRECT word still restores from the file; no fake attempt-counter lockout — none exists session.ts:32-36); couples C(i) to Fork A. **C(ii)** read-only success = a PERSISTENT view-only banner, `role='status'` (not alert), icon+word+text in the a11y tree, says **RELOAD to make changes**, NEVER "close the other tab" (secondTab captured once at unlock, no re-probe session.ts:428 — the "close it there" draft is itself calm-but-wrong). **CROSS-CUTTING:** all strings via copy.ts/copyGuard; CSP-clean (no inline `style=`, no motion `layout` prop — the `<MotionConfig nonce>` landmine); reuse the ONE clear-after-announce live region; add a show/hide passphrase toggle (real `<button>`, `aria-pressed`) since the look-alike-letter hint copy.ts:444 is unactionable on a masked field; restore-pending reuses `.save-step--pending`. **Red team landed 6 real HITS, ALL how-level, all folded in as ship gates; `couldBreakConsensus=false`.** Minimalist opener DROPPED (StructuredOutput retry-cap — stochastic tool fumble; consensus 9-lens, unbroken; its scope-pole was voiced by proxy by architect+design-eng+security). | 9/10 | **execute** — build NEXT (correctness spine first: the restore-over-damaged test gate MUST land before the damaged arm ships); ⚑ the exact C(i)/C(ii) wording = your N=1 eye (ships against the committed direction, not pre-confirmed). **hardStop=false** (personal never-sold; UI wiring on a complete/tested backend; the one data-loss path — clearVault-first — is PREVENTED by this verdict). **Both honesty vetoes (Hawk + Advocate) fired on the REJECTED defer options → SATISFIED by shipping the honest path, not live against the rec.** **Dissent [security-engineer + fiduciary-advisor + craftsman]:** re-offer-export-on-return should be BUILT NOW, not disclosed-deferred (the toast-gate only closes the app-initiated "Update now" vector; a close-tab/navigate-away mid-export user still lands unlocked believing the off-device promise) — **flips to a U8 blocker IF** the close-tab vector proves common enough to be an ACTIVE lie (vs mere incompleteness the app never actively asserts). **Disclosed follow-ups (not U8 blockers):** (1) re-offer export on return when a committed vault has no recorded backup (needs a persisted backup-made sentinel — numeric, never Infinity/NaN per DND 009); (2) re-sync `architecture.md`'s stale BIP-39/HKDF-128 recovery description to the PBKDF2-600k user-passphrase reality. |
| 2026-06-30 | **U8 recovery-model rework** — the BIP-39 12-word recovery phrase is DOA (Briggsy cold-read); decide the replacement survivor recovery-credential MODEL (entropy-vs-memorability threat trade) | council-decided (model/mechanics/threat-ruling/R17-amend) + ⚑ yours-to-close (ceremony copy/tone) | **SHIP a memorable, floor-gated PBKDF2-600k USER-CHOSEN recovery credential wrapping the mandatory export — replacing HKDF-over-128-bit-phrase. Briggsy's lean (memorable 2nd passphrase) CONFIRMED, but the council did the honesty-hardening his lean alone would miss.** First **crypto council — the new permanent Security Engineer elder seated** (charter added to `council.js`; auto-seats on crypto/CSP councils, mirrors the 3 UI elders). **Build (council-decided, execute now):** (1) recoveryWrap → `deriveNewPassphraseKey`/PBKDF2-600k over a `FloorCheckedPassphrase` (reuse brand+floor, never raw-string derive on set-path); (2) **KEEP BOTH WRAPS ON-DEVICE** — architect's export-only refinement REJECTED (resurrects insight-031: abandon-export-after-commit ⇒ vault keyed only by daily passphrase = total loss); (3) export keeps carrying ONLY recoveryWrap (negative-pairing letter preserved — the salt PBKDF2 needs is already persisted, so NO envelope/formatVersion bump); (4) two-clause write-gate preserved (`passphraseWrapCurrent=false` until re-mint); (5) **MANDATORY: reject recovery==daily at mint** (necessary-not-sufficient vs correlation); (6) retire `recoveryPhrase.ts`+HKDF path+BIP-39 wordlist + `RecoveryPhraseDisplay`+`PhraseCapture`+`phraseCaptureChallenge`; firstSave stops returning a phrase; reuse `PassphraseStep` (no meter, name the failing floor clause as TEXT). **CRITICAL HONEST FRAMING (this is what SATISFIES the Hawk's veto, not optional polish):** do NOT mark R17 'closed/armed' on a 'shared' LABEL — "shared" is COPY, not enforced structure (one field; nothing enrolls/verifies the spouse). The survivor guarantee is the **FINDABLE ARTIFACT (the export file) + an ESTATE-HANDOFF instruction**, NOT verified memory; memorability primarily serves the PRIMARY's common-case re-entry. Three **substantive** disclosures (never a checkbox — already vetoed), existence council-decided / wording yours-to-close: (a) the cloud export is now your-recovery-word strength, **not 128-bit/unbreakable**; (b) store the file AND the word together where your spouse/executor will find them — "without both, no one can recover this, not even us"; (c) **steer AWAY from biographical/obituary-grade secrets** (anniversary/hometown/lake-house) — name the targeted family-insider threat; the word must be distinct-in-kind from the daily passphrase. **State-adaptive:** paired → shared memorable word; solo → estate handoff + sharper entropy disclosure. **R17 amend** (`product.md:146`): mechanism wording 'a client-generated recovery phrase' → 'a shared-household recovery credential + the export artifact'; INTENT untouched; STATUS re-graded to 'mechanism provided + estate-handoff scaffolded + disclosed-as-user-responsibility' (NOT survivor-recovery-guaranteed). **Hawk vetoed bare-A + the dishonest CLAIM, then CHANGED MIND to conf 8 holding the MODEL** ("the model clears my bar; the calm-but-wrong CLAIM and biographical COPY do not") — veto SATISFIED by the framing the verdict takes, never overruled. **Red team landed 4 real hits, all ABSORBED** (shared-is-copy-not-structure; negative-pairing-independence-reduced-to-correlation; memorability-serves-primary-not-survivor; zxcvbn-models-generic-not-targeted-guessing) and CONCEDED it could not overturn the headline. | 8/10 | **executed + VERIFIED LIVE** (date route, real Chromium): daily passphrase → recovery word (paired framing + anti-biographical steer) → equals-daily guard fires calm → securing → export with BOTH disclosures rendering → real encrypted blob (recoveryWrap only, no passphraseWrap) → "✓ saved" + "Saved to this device" badge; committed vault carries all 3 records w/ the PBKDF2 recoveryWrap. **Caught + fixed a live bug the tests couldn't see:** the recovery `PassphraseStep` was reused at the same tree position → internal state LEAKED (daily passphrase pre-filled the recovery field); fixed with distinct React `key`s, re-verified empty. 1364 tests, bundle 211.8 KiB, all gates green. ceremony copy/tone ✓ **APPROVED 2026-06-30** (Briggsy: "read is good to go") — recovery rework fully closed. **Dissent ([D], red team residual):** a system-generated STRONG credential keeps the cloud file 128-bit — flips if the new memorable ceremony STILL reads crypto-wallet on your N=1, OR the targeted-insider threat on a cloud-resident estate file is judged unacceptable for friends betting real money. **Clean-slate note:** an OLD HKDF-minted vault is un-openable by the new PBKDF2 recoveryUnlock; acceptable because U8 shipped 2026-06-30 with no real user data (personal pre-release; v3 treated un-shipped) — NO migration built. (Solo-household recovery intro variant has no dev seed to exercise live — single-user mode is deferred; it's a trivial intro string swap.) |
| 2026-06-30 | **U8 Save — what `retirementAge` does a STILL-WORKING person persist?** (schema requires it; the working draft leaves it `undefined` — the swept date is the answer) | council-decided (correctness/honesty) | **OPTION B (STRICT), NOT the convener's lean (A).** A still-working person genuinely HAS no retirement age, so the persisted model is honestly ABSENT-for-working — never a synthetic placeholder. **Honesty-Hawk VETO fired on bare-A**: storing `currentAge+1` is the masquerade `intakeMap.ts:411-415` forbids — and the cardinal-sin path is REAL in source (flip-to-retired `questions.tsx:181` does NOT clear `retirementAge`; `missingRequiredFacts` re-asks only on `undefined`; the stop-age field pre-fills `value={p.retirementAge}` — so a leaked placeholder silently becomes the user's "entered" age). **Red-team's decisive hit:** "Option B" splits into STRICT (reject-on-present) and a LENIENT (ignore-allow) form that re-opens A's masquerade under B's name — so the verdict MANDATES the strict form. EXECUTED: `PersonInputsV3` diverged to `Omit<PersonInputs,'retirementAge'> & { …; retirementAge?: number }` (engine `PersonInputs` stays required; placeholder stays PRIVATE to intakeMap); `checkPersonV3` reordered discriminant-first + a biconditional (`retired ⟹ needFinite`; `working ⟹ reject-on-present`, NOT the blanket `optFinite`); NO schemaVersion bump (un-shipped v3, read+write land atomically here — recorded as a divergence, not a relax-of-a-shipped-field). Three planted-fail codec arms (working-WITH→reject, retired-WITHOUT→reject, working-WITHOUT→accept) + the scenarioFromDraft todo flipped. **Reload byte-identity preserved** (buildPeople re-injects `currentAge+1` on the date route; the force-final reload recompute never reads the stored value). | 8/10 | **executed + VERIFIED LIVE** (the working/fuck-off-date household now saves end-to-end in real Chromium; 1413 tests, typecheck+lint green). **Dissent (guarded-A, the convener's lean):** store the inert placeholder — flips only if A were masquerade-safe without perpetual scaffolding (it isn't: strip-mapper unbuilt, flip-to-retired doesn't clear, no v3 shipped). |
| 2026-06-30 | **U8 first-Save ceremony — design the self-custody flow** (trigger · sequence · calm-vs-alarm · phrase-ack gate · mandatory-export · decrypt-on-return) | council-decided (build) + ⚑ yours-to-close (render tone + copy) | **BUILD U8 now (eye-oracle build).** Per fork — **(B trigger)** DELIBERATE "Keep this answer" Save, never auto-fire (auto-firing a security ritual onto magic-moment relief = the alarm-after-calm whiplash the tone law forbids); visually-dominant calm completion beat under the verdict, a11y-reachable "not saved yet — lives only in this tab" line + a `beforeunload` guard fired only when a completed-but-unsaved answer exists. **(C acknowledgment)** Re-type a 2–3-word POSITIONAL subset **dismissed-before-entry** (visible-phrase re-type is a tautology — proves reading, not storage; hidden-then-retype forces transcription from the user's own copy), framed an ENGAGEMENT NUDGE, never "verified/you're safe"; honest-limit line *"Only you can keep these safe — we can't check that you did. Without them, no one can recover your data, not even us."* **R17 MITIGATED, never CLOSED; checkbox-only VETOED.** **(A export)** stays BLOCKING (the click-through user IS the R17 victim) but satisfiable by ANY observable channel (download OR copy OR print/show-text) with a MANDATORY non-download fallback (`exportVault` returns a string — only the OS download can fail → iOS-PWA/storage-full would brick the un-skippable gate); **NEVER merge phrase+file** (`backup.ts` deliberately ships no `passphraseWrap` — co-locating holes the at-rest model). **(D reload)** FORCE `tier:'final'` recompute on decrypt-on-return behind ONE honest "restoring…" pending state; DON'T persist `tier`, DON'T persist the band (a cached band replays stale ACA law = calm-but-wrong); planted-fail reload-determinism guard. **5 a11y blockers** (phrase as ordered, character-reviewable AT-tree text, heap-dismiss only after capture; `aria-disabled`+announced reason, not native `disabled`; export operable via the fallback; NO strength meter — name which floor (score vs length) fails as text; pending state announced). **FLAG (verified):** plan says "commit LAST / export precedes it" but `exportVault` requires a committed vault (`backup.ts:69-71`) → honest sequence is firstSave (=commit, returns phrase) → capture → export reads the committed vault; firstSave IS the commit (no-persist-until-Save still honored — nothing hits IndexedDB during intake). **Hawk veto on checkbox-only HONORED** (the rec ships the endorsed hidden-then-retype). | 8/10 | **execute** — eye-oracle build NEXT; ⚑ render cold-read (tone of the 3 blocks + the copy) = your eye. **Dissent:** persist-the-band for instant byte-identical re-render (motion/craft/advocate) WINS IF measured 16k+KDF decrypt is genuinely multi-second AND a constants-version invalidation neutralizes the stale-ACA-law objection — a build-time measurement gate the rec already mandates. **Council bug fixed mid-run:** the honesty-hawk charter named a phantom `honestyRisk` field + omitted required `reasoning`/`confidence` → schema-loop; rewrote its OUTPUT line to the 5 schema fields (source + run-script), resumed from cache. |
| 2026-06-29 | **Work-income field meaning (C3)** — DECIDE wages-only (A) vs full working-year MAGI (B), don't re-surface | council-decided (meaning + copy + seed + test) + ⚑ yours-to-close (structural split scope · wording) | **OPTION B — the field means FULL working-year income = work pay PLUS working-year investment income (interest/dividends/cap-gains/K-1); modeled R40 streams (pension/rental/annuity/alimony) STILL EXCLUDED (KTD-9 no-double-count). Unanimous (9 elders + every rebuttal + the red team; red team does not resurrect A).** The engine is NEUTRAL A/B (one opaque per-person scalar, `healthcareStreams.ts:155-164`), so the CARDINAL RULE decides: A welds shut the ONLY channel for working-year investment income → under-states Medicare cost → rosier date = the sin. **EXECUTED:** killed the optimistic "usually the same as the pay you entered" nudge; additive/broadcast-honest help + neutral label (no "just what work pays"); rewrote the ambiguous "wages-only" dev comment + the `taxOverlay.test.ts` drift comment (engine-sense "wages-only" = exclude MODELED streams, NEVER exclude investment income — the load-bearing disambiguation); tightened the `model.ts` contract (dropped the C3-boundary hedge); reconciled `other-income-r40.md` KTD-9 (copy half landed + B); fixed the `?seed=dateborder` per-person attribution landmine (`[180k,0]`→`[95k,85k]`, household total preserved, engine byte-identical); source-bound copy regression test. **Honesty Hawk vetoed Option A — HONORED** (B IS the Hawk-endorsed alternative, so the veto mandates exactly what shipped). | 8/10 | **executed** (1375 tests, typecheck+lint+doc-stats green) · ⚑ the structural two-input split (I lean greenlight — Hawk + red team endorse; it's a scope call) + the exact wording = your cold-read |
| 2026-06-29 | **Already-failing magnitude clause** — does the shared `verdictTrimClause` read as falsely-actionable for a 0-of-10 plan? | council-decided (structure) + ⚑ yours-to-close (words) | **FORK already-failing to its OWN figure-LESS, lever-agnostic verdict clause** (`verdictRethinkClause` — "runs short from the start… more than trimming the budget") + reword the shared trim clause destination→**directional** ("would move it **toward** steadier ground" — fixes the same false-sufficiency on the off-track gradient the red team flagged). Engine fork via a 4th `'rethink'` `DollarAdjustment.direction` (insight-048 pure seam — decision stays in the pure engine, UI clause is a key-lookup). `?seed=failing` added + engine-run-pinned to actually land already-failing; planted-fail test (already-failing ⇒ rethink PRESENT, trim/room ABSENT); copyGuard catastrophe-net extended to the new clause. **Honesty veto on the status-quo clause HONORED** (the fix removes it; veto did not block). | 8/10 | **executed** (1359 tests, typecheck+lint+doc-stats green) · ⚑ word-pick is your live cold-read at `?seed=failing` |
| 2026-06-29 | **AT-reader portfolio-range parity** — give a screen-reader user the band's $ range? | council-decided (build) + ⚑ yours-to-close (margin value + words) | **BUILD it** as ONE static `.sr-only` node (scrub stays aria-hidden): year = deepest resampled lattice column **comfortably above** a NEW cited cleanliness-margin constant (> `COHORT_FADE.full`=0.5, never the gate edge, never the horizon; withdraw to the thin-note if none qualify); figures **single-sourced** via an index into the same resampled `tooltipRows` + `isThinCohort` gate (byte-identical to a scrubbable column); grammar names the quantity (savings $ today, not $/mo), survivor-neutral time anchor (never "couples"/both-alive), range-first/median-subordinate, **$0-spoken-as-ruin**; pure planted-fail-tested selector (`selectAtRangeColumn`, bandGeometry.ts) + composer (`composeBandAtRange`, bandPanelChrome.ts), re-rendered with the band (insight 047). **Hawk veto fired on the NAIVE shape (gate-edge survivor-dominated year as "your range") — HONORED** by the corrected shape. THREE wording shapes (normal range / `$0`-low ruin / total-depletion "gone" — the last added off the `?seed=failing` live cold-read). | 8/10 | **executed + VERIFIED LIVE** (real Chromium, all 4 seeds × both routes × 3 variants, exactly ONE `.sr-only` node each; 1374 tests, typecheck+lint+doc-stats green) · ⚑ the cleanliness-margin VALUE (drafted `AT_RANGE_COHORT_MIN`=0.75) + word-pick = your cold-read |
| 2026-06-29 | **Work-income "re-ask"** — pre-fill the IRMAA-MAGI field from the entered salary? | yours-to-close (split) | **SURFACED — STOP, don't seed (yet).** The field carries a **LIVE contradiction**: type contract (`model.ts:1056-59` — "working-year MAGI… NEVER a salary echo… enter K-1/investment income") vs shipped copy (`copy.ts:103-04` — "Usually the same as the pay you entered"). Red team **broke** the "salary over-states MAGI → conservative" premise two ways: (a) `earnedIncomeReal` is a SMOOTHED base (a bonus/RSU earner seeds BELOW true wage-MAGI); (b) working-year investment income is engine-unmodeled (R40 OUT-list) and reaches IRMAA-MAGI ONLY here. Either inverts → salary UNDER-states MAGI → under-states Medicare cost → rosier date = **the cardinal sin**. "Contradictions Mean STOP" (project law). Scoped Hawk veto fired. **EXECUTE-now regardless of scope:** kill the one-directional "usually the same" copy (a live optimistic nudge), require two-directional/additive framing, display-buffer-only seed (never commit-on-blur). | 8/10 | **surfaced** — your scope call (wages-only + disclose the excluded investment-income direction, **vs** full working-year MAGI). Dissent (fiduciary): a correctly-framed ADDITIVE display-buffer seed is strictly MORE protective than blank — don't over-defer |

## Council session 2026-06-29 (3 councils) — what's left
- **C1 wording ✓ APPROVED (2026-06-29):** the already-failing rethink line shipped as drafted; Briggsy cleared it ("wording is fine, tweak later"). Tweak path if his eye returns: the `verdictRethinkClause` slot (dissent preserved — fiduciary wanted a stark figure framed "wouldn't be enough on its own").
- **C2 margin + wording ✓ APPROVED (2026-06-29):** the AT-parity sentence (3 variants) + `AT_RANGE_COHORT_MIN`=0.75 cleared as drafted. Tweak path: the constant for the year, the `bandAtRange*` slots for the words.
- **C3 — RESOLVED → Option B + EXECUTED (2026-06-29, conf 8/10; re-convened with a sharpened "decide, don't punt" charge — the prior surface was over-deferral by its own fiduciary lens).** The field means FULL working-year income (pay + working-year investment income; modeled streams out). The live contradiction is closed (copy now matches the B contract), the optimistic nudge killed, the seed-attribution landmine fixed, regression-tested; the KTD-9 record reconciled. **(a) the two-input split was BUILT, then SIMPLIFIED (2026-06-30, Briggsy's cold-read — verified live, real Chromium).** His N=1 read flagged the duplicate pay question as confusing — and the engine can't model a time-varying salary, nor do we ask the quit date (it's the swept output) — so the pay re-ask was DROPPED. The IRMAA override is now `earnedIncomeReal + investment`, derived at `intakeMap.buildDateInput`; the step asks ONE field per working spouse, `workingYearInvestmentByPerson` (the cardinal-sin-relevant fact — a blank can't become a silent $0; `intakeMap.test` guard). `workingYearWagesByPerson` removed. The steady-pay simplification (a bonus/RSU spike near 65) is DISCLOSED on the step (conservative-or-disclose). This reversed the council's separate-pay-field call on Briggsy's comprehension signal; the look-back IRMAA-seed fields stay single (a filed return's MAGI is a reported historical total). **(b)** the new **wording** ✓ APPROVED (2026-06-30, Briggsy: "wording is great") — C3 fully closed. (Follow-on, non-blocking: a designer pass on per-spouse help repetition in `Paired` steps — filed in TODO.)

| Date | Issue | Tier | Recommendation | Conf. | Action |
|---|---|---|---|---|---|
| 2026-07-03 | **The N=1 cold-read package** (Briggsy, live) — the budget side-door inconsistency, "Not now", the tier answer never seen again, the one-way back-to-single, the cryptic month/year check, the band's fade real-estate + faint horizontals, and the ladder (drawer click, worthless tooltip, no hover, the open-dots-above-the-line "doesn't hold" riddle) | **N=1 override** (tone authority — supersedes the council's one-pull-down drawer, the cohort FADE, and the open-dot dip encoding) | **ALL SHIPPED same session (3 batches, live-verified):** (1) the itemize invite lives IN the spend question (accounts-step grammar; R8 never-a-gate holds) + `Cancel` + a self-explanatory period check + the essentials/extras readback (sheet + governed face); (2) the band ENDS at the dead-cohort line (`truncateFanAtThinCohort` — the TwoFutures law; kills the fade real estate AND deflates the parked $20M squash: `?seed=dip` ceiling $20M→$6M, horizon 119→93) + one grid weight; (3) the ladder ALWAYS ON with the fan (both graphs share the right pane via a display:contents wrapper), the band-grammar scrub readout (ONE describeMark sentence = aria + readout), QUIET dips at their true rung (the story in words, never a plot riddle), no `<title>`, the bar label a margin rung-anchor. CF6 is ANSWERED by the scrub; CF3's dead lower-left is closed by the graphs-right/words-left rebalance. | N=1 | **executed** (1782 tests / 99 files; all gates green) |
| 2026-06-29 | Web-design + implementation review of the 2 shipped result surfaces — *first council with the 3 new design elders* (design-engineer, motion-designer, a11y-auditor) | council-decided (split) | **SHIP both — honesty floor holds, no veto, red team could-not-break.** FIX-NOW executed + LIVE-verified: (1) added the missing `:hover` on `.fod-ladder__summary` (the date route's primary drawer control had only `:focus-visible`) — text+chevron lift `--ink-muted`→`--ink`; (2) **CF1** — persistent `sr-only <h1>{appTitle}` gated on `began`, so every in-app view has ONE top-level heading (result views were starting at `<h2>`, no `h1`) — verified h1→h2, single h1, 1×1px; (3) `.band-modal__close` hover (optional ride-along). **DROPPED** the inert `.band-enlarge-surface` hover the architect/advocate wanted (red team: `currentColor`=0 in src/viz → would assert a phantom affordance — calm-but-wrong-in-miniature). **KILLED, stay dead:** inline-style CSP risk + missing focus ring (both refuted at the live DOM). *(2026-07-03 note: the `.fod-ladder__summary` hover died with the drawer — the ladder is always-on now.)* | 9/10 | **executed** (h1 + ladder hover live-verified; typecheck+lint+1356 tests green) |

## ⚑ Awaiting your eye (design review — 2026-06-29; cold-read = N=1 you) — **updated 2026-07-03**
The council ruled the SUBSTANCE of each already won — only the **tone** read is yours.
- **CF3 — RESOLVED 2026-07-03:** the graphs-right/words-left rebalance (both graphs in the right pane, actions welded under the words) closed the dead lower-left.
- **CF4 — the coincident "7 of 10":** the verdict and the survivor echo both read "7 of 10" close together on laptop. The survivor reading STAYS regardless — purely a tone read. STILL YOURS.
- **CF6 — ANSWERED 2026-07-03 by the ladder scrub** (hover any dot → its full worded reading). Re-read with your eye on the live surface.
- **CF5 — LARGELY RESOLVED 2026-07-03 by the dead-cohort truncation** (the $20M balloon lived in the dead tail; `?seed=dip` now tops at $6M). A residual tone read on long-horizon seeds is yours if it still bugs you.
- **CF2 — band per-year $ range in the a11y tree:** an ENHANCEMENT (WCAG 1.1.1 already met — the band has a descriptive label), not a blocker. The documented AT-reader portfolio-range-parity item.

## ⚑ HARD pre-Act-3 gate (council 2026-06-29, insight 048) — **ENGINE + RENDER HALVES SATISFIED 2026-07-03; the N=1 cold-read is the open half**
The gate demanded a REAL engine-produced non-monotone seed through the LIVE ladder + an N=1 cold-read. **Delivered the honest way** (U10, 5-agent mechanism hunt after 10 monotone probe grids ≈ 1,300 candidate sims):
- **The gate's own named mechanism is UNREACHABLE**: every *pure conversion/premium* channel is structurally monotone — the §7 working-year clamp + `windowGate`'s working-year ACA zeroing make each extra work year dominate (~2:1) anything a Y-invariant conversion can torch, and per-path MC variance smears the cliff crossing out of the aggregate statistic (three-leg dominance argument, hunt prover 2026-07-03).
- **The channel that DOES invert — the budget collision**: budget line windows are RETIREMENT-anchored while the conversion schedule is ABSOLUTE-year, so a go-go-years travel line slides across the fixed conversion window as the candidate date moves — a k-dependent MAGI-collision tent over the 400%-FPL cliff. `?seed=dip` (a plausible 57/57 couple) lands lifestyle `confirmed-date@5` with `nonMonotoneOffsets [0,1,2]` at BOTH tiers, floor monotone (lifestyle-specific by mechanism) — pinned in `devSeeds.test.ts`.
- **The warning was SEEN to draw** (live Chromium 2026-07-03): three open dip dots above the bar + one "Doesn't hold" tell + the worded hero note — and the first real multi-dip render exposed (and fixed) a label-garble + bar-label collision no synthetic single-dip fixture could show.
- **STILL OWED — Briggsy's cold-read at `?seed=dip`:** does the dip read as *honest recalibration*, never "just retire later"?

| Date | Issue | Tier | Recommendation | Conf. | Action |
|---|---|---|---|---|---|
| 2026-06-29 | Portfolio multi-holding schema — fold `holdings[]` into `ScenarioV3` before U8, or defer? | council-decided | **DEFER the build — deferral is verified FREE, not a v3→v4 migration.** The codec is a tolerant reader (additive-within-v3 optional field, the `hsa`/`contributions` precedent; `scenarioCodec.ts:21-23,:248-249,:361`), so `holdings?` lands LATER on `EnteredAccount` WITH its UI writer. **Ship U8 on the existing `ScenarioV3` unchanged — U8 is NOT blocked.** Recorded shape: `EnteredAccount.holdings?: Holding[]` = `{ label, blend: TickerClassification, valueToday }`. **Hawk veto FIRED + HONORED:** `account.valueToday` stays required + stored authoritative; holdings derive the **blend only**; residual *visibly reconciled, never silent* (rejects the doc's derive-balance rec). Sub-1 opt-in expander RATIFIED. Corrected the doc's false-migration framing. **Dissent (minimalist):** per-holding weight as %-sum-to-100 (no residual) — resolved with the writer; dollars is the lead. | 9/10 | **executed** (decision doc corrected + shape/invariant/gate-obligations recorded; U8 unblocked) |
| 2026-06-28 | D2c confidence-curve drawer — reading-semantics + presentation | council-decided | **Discrete integer-rung ODDS-LADDER, not a smooth curve** (≤11 per-offset markers): bar drawn at TRUE y=8.5 (never the rung-9 gridline); bound-only v1 (`round(qlb·10)`, plot≡text single-sourced); crown = durable longest-clearing-suffix bound to `offsetYears`, **never** max(curve); the ACA-cliff dip rendered ABOVE the bar (clearing offsets) + worded "doesn't hold", no red gash; curve one-pull-down but the non-monotone SENTENCE stays first-frame; height/shape encode confidence (CVD-safe, one vermilion crown accent redundant-only). Advocate's "dip below the line" DISCARDED as a confirmed contract inversion (`dateSearch.ts:308`). | 8/10 | **executing** spine · **surfaced** ⚑ no-date plot |

## ⚑ Awaiting your eye (D2c no-date plotted curve)
- **The plotted no-date curve is DEFERRED from v1 (Honesty Hawk scoped veto, 2026-06-28).** False belief it kills: a scared reader crowning an above-the-0.85-line dot on a result whose headline says there is *no* durable work-optional date ("I can retire in 2 years"). v1 ships the worded **"how close"** line instead (e.g. *"your best year reached about 7 of 10, but it doesn't hold through [window top] — no durable work-optional date in the next N years"*). **Dissent preserved:** architect + craftsman + fiduciary (4 of 6) wanted to ship the *guarded* plot now — the household told "no date" is the one most desperate to know 7-of-10-climbing vs 2-of-10-flat. **What flips it:** the plotted no-date version returns only behind BOTH (a) a swept planted-fail proving no above-bar no-date dot carries any pickable/crown affordance AND every clearing offset wears a first-class "doesn't hold" tell, AND (b) **your N=1 cold-read** that the flagged dots don't read as "just retire here." No test alone clears it — the failure lives in the reader's eye.

| Date | Issue | Tier | Recommendation | Conf. | Action |
|---|---|---|---|---|---|
| 2026-06-28 | What to build next? (full-authority prioritization) | council-decided | **Survivor readout — wire it live** (unanimous 9/10; closes a live calm-but-wrong gap: the widow's-cliff picture was dark) → SHIPPED + verified live | 9/10 | **executed** (wired; 1320 tests green; seen live at ?seed=retired) |
| 2026-06-28 | Dead-cohort scrub note wording | council-decided + ⚑ final-word yours | **"Too few couples to show a range."** (was "Few couples reach these years") — names the honest *why*, width-verified 164/200px in real Source Sans 3, catastrophe-gated | 8/10 | **executed** (string + gate-scope; you picked the word) |
| 2026-06-28 | $20M band-scale squash — cap the date-route fan axis? | council-decided + ⚑ yours-to-close | **Don't build the cap** — leave the axis honest-as-is; serve comprehension via the non-axis scrub readout, not the scale lever | 8/10 | executed (stand-down) · surfaced (tone + reproducibility) |

## ⚑ Awaiting your eye
- **$20M band-scale squash (2026-06-28):** cap lever resolved (don't build — see TODO). **(a)** STILL YOURS: tone cold-read on the real render — is the honest-as-is squash tolerable, or add a *non-axis* comprehension aid? **(b)** RESOLVED by independent verification (2026-06-28): the ~26% swing is the **expected** provisional-tier (2k paths) → final-tier (16k paths) change — a finer p90 tail estimate, and the UI re-draws (doesn't morph, `FuckOffDate.tsx:152`); the `88.88M→65.42M` figures appear **nowhere in code** (an ungrounded elder anchor — verified, not believed). One latent note for **U8**: `seed` is persisted but `tier` is NOT, so a completed date-route scenario could reload at the coarser provisional axis — handle at Save/reload. The council reversed a unanimous open-to-cap *after measuring against real code* — dissent + "what would flip it" preserved in that run's task output.

> **How the council reached this:** opening 6/6 to cap (0.72–0.8) → rebuttal round caught the unmeasured claim → 5/6 flipped, Honesty Hawk veto turned *against* the cap. Genuine reversal, grounded in `src/viz/bandData.ts`, `answerView.ts`, `bandData.test.ts`. Cost: ~1.12M tokens, ~24 min wall-clock (full council). Lever for cheaper runs: `weight: "light"`.
