# Council Log — the digest

Every Council of Elders verdict lands here, newest first. This is Briggsy's **after-the-fact review surface** under maximum-autonomy: scan it to see what the council decided and acted on without stopping you.

- **⚑** = executed at `yours-to-close` (a taste / direction call acted on at high confidence) — the ones you're most likely to want to eyeball or reverse.
- **action**: `executed` (council resolved it, pilot acted) · `surfaced` (brought to Briggsy — low confidence, veto, or hard-stop).
- Reverse anything: it's all reversible by design (hard-stops never auto-execute).

How it's run: `/council <issue>`, or the pilot auto-convenes whenever it would otherwise park a non-trivial judgment call. Mechanism: `.claude/skills/council/` (playbook + 9 elder charters) → `.claude/workflows/council.js` (debate engine).

| Date | Issue | Tier | Recommendation | Conf. | Action |
|---|---|---|---|---|---|
| 2026-06-29 | Web-design + implementation review of the 2 shipped result surfaces — *first council with the 3 new design elders* (design-engineer, motion-designer, a11y-auditor) | council-decided (split) | **SHIP both — honesty floor holds, no veto, red team could-not-break.** FIX-NOW executed + LIVE-verified: (1) added the missing `:hover` on `.fod-ladder__summary` (the date route's primary drawer control had only `:focus-visible`) — text+chevron lift `--ink-muted`→`--ink`; (2) **CF1** — persistent `sr-only <h1>{appTitle}` gated on `began`, so every in-app view has ONE top-level heading (result views were starting at `<h2>`, no `h1`) — verified h1→h2, single h1, 1×1px; (3) `.band-modal__close` hover (optional ride-along). **DROPPED** the inert `.band-enlarge-surface` hover the architect/advocate wanted (red team: `currentColor`=0 in src/viz → would assert a phantom affordance — calm-but-wrong-in-miniature). **KILLED, stay dead:** inline-style CSP risk + missing focus ring (both refuted at the live DOM). | 9/10 | **executed** (h1 + ladder hover live-verified; typecheck+lint+1356 tests green) |

### ⚑ Awaiting your eye (design review — 2026-06-29; cold-read = N=1 you)
The council ruled the SUBSTANCE of each already won (full 0..10 ladder axis, show the survivor reading, no axis cap) — only the **tone** read is yours. The new design elders judge implementation craft, never your gut.
- **CF3 — two-pane lower-left whitespace: calm, or dead?** Worst on the DATE route with the ladder CLOSED (~60% of the lower-left is blank); opening the ladder rebalances it. (The parked "calm-vs-dead" question, now seen live on the real render.)
- **CF4 — the coincident "7 of 10":** the verdict and the survivor echo ("…still covers you") both read "7 of 10", close together in the left column on laptop. The survivor reading STAYS regardless (cutting it to de-duplicate would be calm-but-wrong) — purely a tone read.
- **CF6 — D2c below-line dots carry no per-dot numeral** (only the crown shows "9 of 10"; the others read by position + AT alt-text). Restraint vs. completeness — your call.
- **CF5 — the $20M squash tone** (date-route median pinned near $0). The axis cap is council-settled "don't build" (2026-06-28); only the tone read remains.
- **CF2 — band per-year $ range in the a11y tree:** an ENHANCEMENT (WCAG 1.1.1 already met — the band has a descriptive label), not a blocker. The documented AT-reader portfolio-range-parity item.

### ⚑ HARD pre-Act-3 gate (council 2026-06-29, insight 048)
The ACA-cliff dip "doesn't hold" tell (`OddsLadder.tsx:206-219`) is green in `curveMarks.test.ts` but has **never rendered from a real engine-produced non-monotone seed** (unreachable in v1 — proportional drawdown + no conversions is always monotone). **Before the Roth-conversion lever ships (Act 3):** drive a real non-monotone ACA/IRMAA seed through the LIVE render AND pass an N=1 cold-read. "Tests green" is never "the warning was seen to draw." (Filed in the Act-3 plan + TODO.)

| 2026-06-29 | Portfolio multi-holding schema — fold `holdings[]` into `ScenarioV3` before U8, or defer? | council-decided | **DEFER the build — deferral is verified FREE, not a v3→v4 migration.** The codec is a tolerant reader (additive-within-v3 optional field, the `hsa`/`contributions` precedent; `scenarioCodec.ts:21-23,:248-249,:361`), so `holdings?` lands LATER on `EnteredAccount` WITH its UI writer. **Ship U8 on the existing `ScenarioV3` unchanged — U8 is NOT blocked.** Recorded shape: `EnteredAccount.holdings?: Holding[]` = `{ label, blend: TickerClassification, valueToday }`. **Hawk veto FIRED + HONORED:** `account.valueToday` stays required + stored authoritative; holdings derive the **blend only**; residual *visibly reconciled, never silent* (rejects the doc's derive-balance rec). Sub-1 opt-in expander RATIFIED. Corrected the doc's false-migration framing. **Dissent (minimalist):** per-holding weight as %-sum-to-100 (no residual) — resolved with the writer; dollars is the lead. | 9/10 | **executed** (decision doc corrected + shape/invariant/gate-obligations recorded; U8 unblocked) |
| 2026-06-28 | D2c confidence-curve drawer — reading-semantics + presentation | council-decided | **Discrete integer-rung ODDS-LADDER, not a smooth curve** (≤11 per-offset markers): bar drawn at TRUE y=8.5 (never the rung-9 gridline); bound-only v1 (`round(qlb·10)`, plot≡text single-sourced); crown = durable longest-clearing-suffix bound to `offsetYears`, **never** max(curve); the ACA-cliff dip rendered ABOVE the bar (clearing offsets) + worded "doesn't hold", no red gash; curve one-pull-down but the non-monotone SENTENCE stays first-frame; height/shape encode confidence (CVD-safe, one vermilion crown accent redundant-only). Advocate's "dip below the line" DISCARDED as a confirmed contract inversion (`dateSearch.ts:308`). | 8/10 | **executing** spine · **surfaced** ⚑ no-date plot |

### ⚑ Awaiting your eye (D2c no-date plotted curve)
- **The plotted no-date curve is DEFERRED from v1 (Honesty Hawk scoped veto, 2026-06-28).** False belief it kills: a scared reader crowning an above-the-0.85-line dot on a result whose headline says there is *no* durable work-optional date ("I can retire in 2 years"). v1 ships the worded **"how close"** line instead (e.g. *"your best year reached about 7 of 10, but it doesn't hold through [window top] — no durable work-optional date in the next N years"*). **Dissent preserved:** architect + craftsman + fiduciary (4 of 6) wanted to ship the *guarded* plot now — the household told "no date" is the one most desperate to know 7-of-10-climbing vs 2-of-10-flat. **What flips it:** the plotted no-date version returns only behind BOTH (a) a swept planted-fail proving no above-bar no-date dot carries any pickable/crown affordance AND every clearing offset wears a first-class "doesn't hold" tell, AND (b) **your N=1 cold-read** that the flagged dots don't read as "just retire here." No test alone clears it — the failure lives in the reader's eye.

| 2026-06-28 | What to build next? (full-authority prioritization) | council-decided | **Survivor readout — wire it live** (unanimous 9/10; closes a live calm-but-wrong gap: the widow's-cliff picture was dark) → SHIPPED + verified live | 9/10 | **executed** (wired; 1320 tests green; seen live at ?seed=retired) |
| 2026-06-28 | Dead-cohort scrub note wording | council-decided + ⚑ final-word yours | **"Too few couples to show a range."** (was "Few couples reach these years") — names the honest *why*, width-verified 164/200px in real Source Sans 3, catastrophe-gated | 8/10 | **executed** (string + gate-scope; you picked the word) |
| 2026-06-28 | $20M band-scale squash — cap the date-route fan axis? | council-decided + ⚑ yours-to-close | **Don't build the cap** — leave the axis honest-as-is; serve comprehension via the non-axis scrub readout, not the scale lever | 8/10 | executed (stand-down) · surfaced (tone + reproducibility) |

### ⚑ Awaiting your eye
- **$20M band-scale squash (2026-06-28):** cap lever resolved (don't build — see TODO). **(a)** STILL YOURS: tone cold-read on the real render — is the honest-as-is squash tolerable, or add a *non-axis* comprehension aid? **(b)** RESOLVED by independent verification (2026-06-28): the ~26% swing is the **expected** provisional-tier (2k paths) → final-tier (16k paths) change — a finer p90 tail estimate, and the UI re-draws (doesn't morph, `FuckOffDate.tsx:152`); the `88.88M→65.42M` figures appear **nowhere in code** (an ungrounded elder anchor — verified, not believed). One latent note for **U8**: `seed` is persisted but `tier` is NOT, so a completed date-route scenario could reload at the coarser provisional axis — handle at Save/reload. The council reversed a unanimous open-to-cap *after measuring against real code* — dissent + "what would flip it" preserved in that run's task output.

> **How the council reached this:** opening 6/6 to cap (0.72–0.8) → rebuttal round caught the unmeasured claim → 5/6 flipped, Honesty Hawk veto turned *against* the cap. Genuine reversal, grounded in `src/viz/bandData.ts`, `answerView.ts`, `bandData.test.ts`. Cost: ~1.12M tokens, ~24 min wall-clock (full council). Lever for cheaper runs: `weight: "light"`.
