# Insights — The Back Nine

Hard-won, non-obvious engineering lessons for this project. One file per insight,
numbered `NNN-kebab-slug.md`. Added via `/distill` after debugging something whose
root cause wasn't obvious; surfaced via `/brief` before related work.

**Citation format:** reference an insight by its **full relative path + title slug**
(e.g. `docs/insights/001-some-lesson.md`), not a bare number — and cross-repo
citations carry the full path too (e.g. `projects/burned/docs/insights/072-...`),
never just `burned/072`. These sibling-project lessons are sometimes cited in
shorthand; decode each by its full path: `burned/NNN` → `projects/burned/docs/insights/NNN`;
`DND NNN` → `projects/archive/do-not-disturb/docs/insights/NNN`;
`AJS NNN` → `projects/ai-journey-stats/docs/insights/NNN`.

## Transferable discipline carried in from sibling projects

No domain prior art exists, but the engineering discipline transfers. The
load-bearing ones (see `CLAUDE.md` for how each applies here):

- **One canonical constants table** — `projects/burned/docs/insights/` 057/061/063.
- **No in-range default fallbacks** — `projects/burned/docs/insights/062`.
- **Absence-tests need presence companions** — `projects/burned/docs/insights/027`.
- **Determinism gates self-test against a planted positive** — `projects/burned/docs/insights/070`.
- **Engine↔reader contract drift at a serialization boundary** — `projects/burned/docs/insights/042`.
- **Stale Vite HMR dev-500** — `projects/burned/docs/insights/072`.
- **Externally-derived golden fixtures** — `projects/archive/do-not-disturb/docs/insights/012`.
- **JSON-persisted Infinity/NaN → null sentinels** — `projects/archive/do-not-disturb/docs/insights/009`.
- **Clean-clone discipline** — `projects/ai-journey-stats/docs/insights/008`.
- **Eager module-level singletons (never minted in render)** — `projects/ai-journey-stats/docs/insights/003`.

## This project's insights

- [001 — strict CSP vs Vite/PWA inline scripts + workbox-window peer](001-strict-csp-vs-vite-pwa-inline-scripts.md)
- [002 — Trusted Types breaks `new Worker(new URL(...))`](002-trusted-types-breaks-new-worker.md)
- [003 — ESLint engine-purity bypasses (global objects, dynamic import, eval, .mts)](003-eslint-engine-purity-bypasses.md)
- [004 — monorepo CI: action-setup reads repo root + workflow self-trigger](004-monorepo-ci-pnpm-and-workflow-self-trigger.md)
- [005 — verify review findings before folding; reviewers should be read-only](005-verify-review-findings-and-readonly-reviewers.md)
- [006 — a convergence probe that contradicted the algebra was sampling the wrong regime](006-gross-up-convergence-probe-wrong-regime.md)
- [007 — cap-gains stacking raised the gross-up's worst-case k to ≈0.74 and made the contraction rest on regime-disjointness](007-capgains-stacking-raised-convergence-k-and-broke-automatic-contraction.md)
- [008 — a NaN survives `?? default`, so an unguarded sibling input stream becomes a calm-but-wrong crash](008-nan-survives-nullish-coalescing-in-an-unguarded-input-stream.md)
- [009 — cross-verifying a transcribed ~3,000-cell federal reference table (eCFR XML + Pub 590-B diff + symmetry + cross-table identity)](009-cross-verifying-a-transcribed-federal-reference-table.md)
- [010 — a NaN passes a tolerance/range guard (every comparison with NaN is false); knowing the `??` form (008) didn't prevent the `>` form](010-nan-passes-a-relational-guard-because-every-comparison-with-nan-is-false.md)
- [011 — externally-deriving a golden fixture through a solver whose API returns only aggregates (the zero-return read-off trick)](011-externally-deriving-a-fixture-through-an-aggregate-only-solver-api.md)
- [012 — ceil-quantizing MAGI against an integer cliff is a no-op vs a raw compare (it does NOT absorb float noise)](012-ceil-quantizing-against-an-integer-cliff-is-a-no-op-vs-a-raw-compare.md)
- [013 — a discontinuity in a sourced table silently breaks a root-finder's monotonicity premise (segment the bracket at the jump)](013-a-table-discontinuity-silently-breaks-a-root-finders-monotonicity-premise.md)
- [014 — a threshold whose location depends on an evolving state variable needs its CROSSING year tested, not just static positions (the survivor ACA cliff drop)](014-a-mid-simulation-state-change-moves-a-threshold-so-test-the-crossing-year-not-just-static-positions.md)
- [015 — a generative break-the-code adversary finds mutation-survival seams that the value lenses + adversarial-verify structurally cannot (the first /ultramode-code-review spin)](015-a-generative-adversary-finds-mutation-survival-seams-that-checks-and-verify-cannot.md)
- [016 — a security-header gate needs a header-applying harness, a no-policy control arm + mutation, and EXACT-directive assertions or it's theater (the CSP browser-enforcement build)](016-a-security-header-gate-needs-a-control-arm-mutation-and-exact-directive-match-or-its-theater.md)
- [017 — a confirming review-fold loop converges by finding-CLASS, not zero count; even confirmed prescriptions need source-verification before folding](017-a-confirming-review-loop-converges-by-finding-class-not-zero-count.md)
- [018 — an amendment's blast radius is the CONSUMERS of the superseded premise, not the producers of the new content (grep for premise phrases, not requirement numbers; verbatim transplants by line-range extraction; additive commits have a zero-removals invariant)](018-an-amendments-blast-radius-is-the-consumers-of-the-superseded-premise.md)
- [019 — a crashed verifier is not a refutation: a verify-stage aggregation that treats missing votes as "discard" silently loses real findings (score ABSTAIN, read the failures block, resume the run)](019-a-crashed-verifier-is-not-a-refutation-missing-votes-must-abstain-not-discard.md)
- [020 — a fail-loud guard gated on its FIRST consumer silently fails to protect the SECOND consumer of the same fragile invariant (gate on the property, not the consumer)](020-a-guard-gated-on-its-first-consumer-does-not-protect-the-second-consumer-of-the-same-invariant.md)
- [021 — anchors + monotonicity left 45/50 transcribed cells silently mutable: structural invariants admit SMOOTH corruptions, so pin transcribed vectors in FULL (the 015 mutation-survival lesson, specialized to data)](021-structural-invariants-admit-smooth-corruptions-pin-transcribed-vectors-in-full.md)
- [022 — provenance is per-FIGURE, not per-document-family: frozen-base formula figures (§109's 150%×the-2024-catch-up), first-bump staleness (the §108 IRA catch-up), and a figure cited to a document it isn't in (the §223(b)(3)(B) HSA catch-up)](022-provenance-is-per-figure-frozen-base-formulas-and-first-bump-staleness.md)
- [023 — an external-derivation panel validates ARITHMETIC, not RULE SELECTION: the conventions you hand it are an unchecked trust root (the C2 ledger-RMD fixture's near-miss on the JLLS gap-11 switch)](023-an-external-derivation-panel-validates-arithmetic-not-rule-selection.md)
- [024 — an unattributable aggregate can neither be vetted nor safely rejected: carry per-entity attribution through the wire (the C2 wave-2 dead-slot trilemma; a verify-panel dissent is design input)](024-an-unattributable-aggregate-can-neither-be-vetted-nor-safely-rejected.md)
- [025 — a plan's enumerated test scenario is a HYPOTHESIS: derive that the mechanism exists in THIS engine before building the fixture, and record the reactivation trigger when retiring a premise-false one (the C3 cliff-straddle + averaged-handoff pair; insight 023 lifted to plan altitude)](025-a-plans-test-scenario-is-a-hypothesis-derive-the-mechanism-before-the-fixture.md)
- [026 — your FIX for a confirmed finding is also a hypothesis: feed the verifiers the disposition and let them attack IT (the C3 verify stage overturned the P1 instrument + the decideTrack guard; "adjusted" is a first-class verdict)](026-your-fix-for-a-confirmed-finding-is-also-a-hypothesis-verify-dispositions-not-just-findings.md)
- [027 — a guard family's trigger predicate must match the HAZARD CREATOR's domain, not a correlated proxy (the §7-clamp/§3b-guard split: income-blind creator, earned>0 guards — the zero-income-worker P1; insight 020's class, third recurrence)](027-a-guard-familys-trigger-predicate-must-match-the-hazard-creators-domain-not-a-proxy.md)
- [028 — finite inputs do not bound the computation: float overflow voids a convergence proof — and the green test pinning it (the M6 saturation claim, refuted; the ENGINE_MAX_* domain bounds + the per-path finiteness seam; insight 026 on a THEORY)](028-finite-inputs-do-not-bound-the-computation-float-overflow-voids-convergence-proofs.md)
- [029 — an equality assertion on a structurally-zero surface discriminates nothing: a planted zeroing of the hsa collection survived 567 green tests (presence companions for field PLUMBING; insights 015/021's class at the copying layer)](029-an-equality-assertion-on-a-structurally-zero-surface-discriminates-nothing.md)
- [030 — an async cancel/lock authority that acts only at its END is fiction: synchronous token bump at entry + generation checks after every await + local ref captures (the U4 session race cluster; lens diversity is ANGLE diversity)](030-an-async-cancel-authority-must-be-synchronous-and-generation-checked-across-every-await.md)
- [031 — a committed credential must outlive the cancellation of the op that minted it: firstSave returns the phrase whenever the vault write committed (the point-of-no-return owns the reporting obligations)](031-a-committed-credential-must-outlive-the-cancellation-of-the-op-that-minted-it.md)
- [032 — a committed source artifact without a test-time bind is not a pin: integral guards (window counts, S(90) anchors) + spot cells left 106/110 mortality and ~332/340 Damodaran values silently mutable, incl. the optimistic-direction mutant (the pin-pass review fold; insight 021 sharpened — the bind is a TEST that re-derives every cell)](032-a-committed-source-artifact-without-a-test-time-bind-is-not-a-pin.md)
- [033 — verify a gate's TARGET when WRITING the gate, not when clearing it: table4c7.html never existed (TR table numbering ≠ filename, the phantom propagated to 5 docs), bit-exact Bengen is unattainable at any price (his own tail was estimated; SBBI edition-revises), and the "bot-blocks curl" blocker dissolved on one real recon attempt](033-verify-a-gates-target-when-writing-the-gate-not-when-clearing-it.md)
- [034 — independent verification agents apply CONFLICTING conventions and the harness's own manifest is an error source: the fold owns cross-prescription consistency — resolve against the file's documented rules, never the more confident agent (the tickerBlend real-asset split; the constants sweep's only STOP indicted the workflow's inventory, not the data)](034-cross-agent-convention-conflicts-must-be-resolved-against-the-files-own-rules-before-folding.md)
- [035 — a live-updating region ABOVE interactive content shifts tap targets mid-gesture: reserve its box (the answer strip resized on every commit and a segment tap landed one control off — caught by the retargeted CSP e2e walk; "calm" is a hit-target property, not just tone)](035-a-live-updating-region-above-interactive-content-shifts-tap-targets-mid-gesture.md)
- [036 — a gate reading a render-closure snapshot validates ONE COMMIT STALE when blur and action share a task: snapshots are for rendering, gates read the store (the period force-confirm let an ambiguous spend straight past Continue; advance() now reads getSnapshot())](036-a-gate-reading-a-render-closure-validates-one-commit-stale-when-blur-and-action-share-a-task.md)
- [037 — the orchestrator's OWN "pre-verified" finding is still a hypothesis: a partial-chain trace manufactures false confidence (the D1 review's self-found MAGI-drop traced producer+consumer but not the UI writer that zeroes the slot nor the engine gate that rejects loudly — refuted; route your own findings to a refuter and tell it to read the files you didn't)](037-an-orchestrator-found-finding-is-a-hypothesis-trace-the-full-chain.md)
- [038 — a single-hue band swallows a same-hue overlay line under CVD: two colors that each clear the 0.10 floor against the background collapse to 0.024 as a COMPOSITE (blue series-1 line over the blue band, lost in its dark half under deuteranopia); probe the composite not just the tokens, and MEASURE the planted-fail — the design law's "forbidden" vermilion/bluegreen pair was vacuously above floor, the real one is matched-L red/green at 0.011 (U6 palette)](038-a-single-hue-band-swallows-a-same-hue-overlay-line-under-cvd.md)
- [039 — one missing domain guard on a pure sub-engine input leaked BOTH a silent NaN and a NEGATIVE benefit dollar: an unguarded claimAge made `adjustOwnBenefitAnnual` return NaN (non-finite, fell through the relational branch) AND −$600/yr (claim 47 extrapolated `reductionFactor` to a negative numerator); one finite/INTEGER/range chokepoint guard closes both + the fractional-month case. An unwired pure core owns its own domain guard (don't defer to validateParams); a schedule that extrapolates past its window needs a RANGE bound, not just finiteness — and 8/10 lenses found the NaN, only the boundary-adversary found the negative (the diverse panel earned its seat)](039-an-unguarded-actuarial-window-leaks-both-a-nan-and-a-negative-benefit.md)
- [040 — an integration seam must realize a PLANNED input the stochastic timeline can preempt: the §202 survivor base was built from the deceased's PLANNED claim age, crediting DRCs a worker who died before claiming never earned (plan-70 dying at 68 → 1.24× not 1.08×) — an OPTIMISTIC overstatement of the survivor floor on the early-widowhood paths the unit exists to harden. The pure core was correct (it trusts claimAge as realized); realizing the plan against the path is the SEAM's job. `realizedClaimAgeAtDeath` caps at age-at-death, floored at FRA. Every survivor-branch fixture used an already-claimed deceased, so the death-before-claim path was structurally untested — a discriminating test must drive the PREEMPTION, and a nonzero/preemptible value through the seam (the excess gates + own-wins survived mutation at 915/915)](040-an-integration-seam-must-realize-a-planned-input-the-stochastic-timeline-can-preempt.md)
- [041 — a plan's own mechanical step can be obsoleted by a decision made LATER in the same effort: RESTRUCTURE-PLAN step 4 said "rename Phase→Act in ~18 source comments" but the roadmap's ID scheme (authored later, M2) keeps code in P-form joining on the immutable U-key — a literal rename would have drifted ~128 `Pn·Um` keys + the insight-corpus frontmatter; ground-truth-first (read the live roadmap before the rote step) caught it, and step 4's REAL residue was the dead-premise comments insight/018 predicted as code-side siblings (model.ts:200/408/551/553, db.ts:12 — wrong for 8 days). Re-verify each rote-looking step against the live authoritative doc; precedence breaks the tie — the landed doc post-dates the plan and wins. The "a plan's X is a hypothesis" family (025, 037)](041-a-plans-mechanical-step-is-a-hypothesis-obsoleted-by-a-later-decision-in-the-same-effort.md)
- [042 — a doc cleanup re-authored "present-tense from the ledger" proves COHERENCE, not CURRENCY: weeks after the M2–M6 restructure, a cold-read of the fresh README found 4 source-checkable bugs (stale 942/44 vs live 962/45; a "25 constants…SEC" summary CONFLATION — SEC=0 in constants/, it backs the fund-blend in tickerBlend.ts, while heavily-cited SSA was omitted; a present-tense survivor-backstop overclaim for unbuilt U8; a wrong "what's next"). A cleanup that runs no suite and greps no src/ structurally can't catch facts that drifted in code — and the stat lived on TWO surfaces with no guard, violating the engine's own "a number is never re-typed" rule. Gate shipped: `pnpm verify:doc-stats` reads each surface's claim and asserts it against vitest's OWN collection (`vitest list`) — a glob UNDERCOUNTS (misses scripts/__tests__/**). Mirrors externally-derived fixtures: ledger-authored proves coherence, engine-computed proves typing — neither proves correctness-against-reality. Family: 033, 041](042-a-doc-cleanup-from-the-ledger-proves-coherence-not-currency.md)
- [043 — a domain guard that fails LOUD on one degenerate input but CALM on its sibling returns a plausible wrong answer: `yForDollars` threw on a non-finite `dollarMax` but `return PLOT.bottom` on a non-positive one, rendering a healthy fan (p10 $1M/p90 $2M) BYTE-IDENTICAL to total ruin (the $0 baseline) — the calm-but-wrong cardinal sin from the U6-render review. A degenerate scale/denominator has no honest state behind it (unlike a real $0 datum), so it must fail loud, never resolve to a drawable coordinate; the asymmetry IS the tell. Live because it's a pure leaf any caller hits — unlike the unbuilt-producer-seam findings that are latent until U7 lands (reachability triage). Made `dollarMax<=0` + `xForYear horizonYears<=0` throw; strengthened the sibling `isFixedLattice` to reject inverted/negative/non-finite percentiles (proven by new bandData.test.ts). The finiteness-first family (008/010/039/028); reachability cross-ref 042](043-a-domain-guard-loud-on-one-degenerate-input-fails-calm-on-its-sibling.md)
- [044 — a "this can never happen" comment is a CLAIM about a gate, not a fact; sourced from intuition it HIDES a latent crash: `resolveBandData`'s seam asserted "initialPortfolio > 0 always, so dollarMax > 0 can never be violated by a real fan" — but `validateParams`' `finiteNonNeg` admits `initialPortfolio === 0` (a $0-portfolio, SS-funded decumulation household; only the accumulation construct rejects $0), so an all-$0 fan → `maxP90=0` → `dollarMax=0`. The seam guard `dollarMax >= maxP90` is trivially true at `0=0`, so it didn't enforce the strict `dollarMax > 0` the downstream `yForDollars` needs (043's sibling) — the throw deferred from the documented producer seam to the pure renderer mid-draw, on a real household, AND the test echoed the false premise while only exercising a $1.4M fixture (unguarded + untested). Made the seam fail loud on `!(dollarMax > 0)`; corrected the comment to the validator's real domain. The lesson: trace a "can never happen" comment to the gate that would enforce it, never to intuition — a degenerate the validator admits but the comment excludes is a crash the comment is worse-than-silently hiding. Why 4 lenses converged (017 — the false comment was a beacon); family 020/043/029/032](044-a-can-never-happen-comment-is-a-claim-about-the-gate-not-a-fact.md)
- [045 — a counterfactual differenced AT a transition event understates (or INVERTS) the change when a relevant stream's onset is decoupled from the event's timing: the U7 survivor income step-down differenced household income at the first-death year `fd`, but for a death after retirement but before SS claiming the all-alive leg has $0 SS (neither claimed yet) while the survivor already draws a §202 widow benefit at 60 → the diff goes NEGATIVE (income "rising" at widowhood), biasing the median DOWN = the cardinal understatement (an adversarial-verify pass empirically reproduced −$2,753/mo). "Anchor the diff at the event to isolate it" is a trap when the differenced streams have onsets independent of the event — measure at the STEADY-STATE year (the later of the event and every stream's onset, in the survivor's living window) so both would-be benefits are in pay status, plus a `max(0,…)` floor for the residual edge. The `≥0` guard test was vacuous (`pia=0` structurally zeroed the very §202 mechanism that breaks it — 029); the fix is regression-guarded by a §202-present $1,000/mo golden a raw fd-anchor renders negative and a floor-only fix renders 0. Family 029 (structurally-zero fixture) + 040 (a stochastic timeline preempts a planned input)](045-a-counterfactual-differenced-at-an-event-understates-when-a-streams-onset-is-decoupled-from-the-event.md)
- [046 — a restore codec that checks FINITENESS but not RANGE silently passes a multiplied-away scalar the engine can't backstop, in the optimistic (cardinal-sin) direction: the U8 ScenarioV3 codec gated the income entity fractions (survivorPct/taxableFraction/exclusionFraction) for finiteness only, so a GCM-authentic blob with survivorPct=5 decoded ok:true → the survivor "inherits" 500% of a pension → overstated survival on a friend's saved plan. These scalars are MULTIPLIED AWAY at compile (survivorGross = gross×survivorPct), so validateParams never sees them (KTD-4); the engine's finiteNonNeg backstop only catches values that drive a vector NEGATIVE, so a positive out-of-range multiplier escapes — and model.ts + sanity.ts explicitly name the codec "the third [0,1] gate," the SOLE one on the restore path (session installs decoded.scenario directly). Fix: needUnitFraction [0,1], regression-guarded + boundary-0/1-valid. The lesson: when a value is multiplied away before the next domain gate, the layer that last sees the raw scalar is the sole range gate, a non-negativity backstop is NOT a range gate, and a doc that names a layer "the Nth gate" is a contract — test a finite out-of-range value, not just null/NaN. Caught by a 4-lens adversarial pass (3 converged, 1 empirically reproduced). Family 010 (finiteness) + 029 (the null-only test gave false coverage)](046-a-codec-that-checks-finiteness-but-not-range-passes-a-multiplied-away-value-the-engine-cannot-backstop.md)
- [047 — a TIERED (provisional→final) consumer re-fires identity-keyed contracts the STATIC consumer never stressed: the D2 date band reused the spine's once-per-landing focus announce + draw-once-then-morph, both green on the spine because its two recomputes are byte-identical. The date route's final tier crowns a DIFFERENT offset/scale than provisional → (1) focusSignal flipped and `focusHeading` re-fired (focus theft + scroll-jump on the hero) — fix = announce only on the undefined→defined edge; (2) the re-draw `key` on ConfidenceBandPanel (which OWNS the enlarge-modal + focus state) remounted the panel on a scale change, destroying an open modal + dropping focus to `<body>` — fix = key the INNER band(s) on the scale, never the node that also owns interaction state. The lesson: when a new consumer is tiered/evolves-over-time where the original was static, re-audit every contract keyed on identity-or-change (focus keys, React remount keys, morph-vs-redraw); and scope a remount key to the thing that must re-draw, never to a node that also owns modal/focus state. A diff-scoped review can't see it — the bug is in the new-caller × old-stability-assuming-contract cross-product. Family 020 (a contract correct for consumer #1, wrong for #2) + 035 (a changes-over-time surface breaking a static interaction)](047-a-tiered-consumer-re-fires-identity-keyed-contracts-a-static-one-never-stressed.md)
- [048 — an honesty gate INLINED in a render path the test env can't drive is untested despite a green suite: the band scrub readout's dead-cohort dollar withdrawal (`thin = cohortFraction < COHORT_FADE.full` → withhold crisp dollars on a vanishing cohort, the cardinal-rule guard) was wedged inside the non-exported `ScrubReadout`, and the scrubber only mounts via `onPointerMove → locate → getScreenCTM()`, which jsdom returns null for — so NO test could reach it and a gate inversion ships with 1313 green tests, even though every SIBLING decision in the module (nearestLatticeIndex, cohortFadeOpacity, placeReadoutBox) was a pure planted-fail-tested helper. Fix: extract the DECISION to pure exported seams (isThinCohort + composeReadoutLines, planted-control: thin ⇒ the dollar figures are ABSENT), the component keeps only kind→class wiring. Secondary same review: placeReadoutBox's magic 0.6·PLOT_W flip threshold was WIDER than the box-clip boundary, so the opaque readout box clamped back over the live scrubber rule at the dead-center vertices, and the edge-only test was vacuous (the clamp never fired at i=0/i=48) — fix = derive the flip from box-fit + sweep ALL 49 vertices. The lesson: a correctness/honesty gate in a render path jsdom can't mount is effectively untested no matter how green the suite — push the decision out to a pure seam, cover the wiring live. Family 029 (a vacuous assertion on a structurally-zero/never-executed surface) + 015 (mutation-survival seams) + 038 (the same band's honesty arm needs a planted-fail)](048-an-honesty-gate-inline-in-an-undrivable-render-path-is-untested-despite-a-green-suite.md)
- [049 — a rework that changes a guarded invariant's PRECONDITION must re-audit every path that ESTABLISHES it: the 2026-06-30 recovery rework made both credentials user-chosen, retrofitting the `recovery == daily` negative-pairing guard onto `firstSave` ONLY — but the mirror mint path `restoreVault` (both plaintexts in scope, re-mints the passphraseWrap identically) silently regressed from *structurally impossible* (old system-minted 128-bit phrase couldn't equal a daily passphrase) to *unchecked*, collapsing the guarantee on the survivor's-door path with zero lines of it edited. The audit trigger is mechanical: a changelog "added guard X to foo" ⇒ grep every OTHER site that mints/asserts the same invariant and prove it under the new precondition. First ultramode review of the U8 recovery-rework arc; 3 lenses converged on ①. Secondary same review: ③ codec ticker-blend finiteness-only → uncaught `stockWeightForBlend` crash (mirror its ≥0∧sum>0 domain — 027), ⑤ non-exhaustive unlock switch swallowing new reasons into the no-alarm generic (house `never`-default, fail-loud), ④ ExportConfirm dropping the export-failure arm. Latent-until-SLICE-2 (restore UI + hydration unbuilt) is still a spine bug — guard the mint/codec now, never defer to unwritten UI (048). Family 020 (guard on consumer #1 ≠ protects #2) + 027 (hazard-creator's domain) + 046 (finiteness ≠ range, here on a rate whose ceiling is a domain judgment, not a guess — burned/062)](049-a-rework-that-changes-a-guarded-invariants-precondition-must-re-audit-every-path-that-establishes-it.md)
- [050 — a Monte-Carlo confidence fan is structurally BLIND to a deterministic seedless input, so it cannot backstop an over-optimistic one — a grounded range gate is the SOLE defense: a `fixed-pct` COLA compiles to a seedless per-year overlay (otherIncome.ts:51-56) that lifts EVERY path's income floor identically, so the fan narrows toward success and can never widen to flag a fat-fingered 30%/yr (~$689M/yr, calm-but-wrong "10 of 10"). "The MC run will catch it" is false for any input that doesn't vary across draws. The Council of Elders (2026-07-01) settled the grounded ceiling the U8 review (049) refused to guess: one cited raw-absolute constant COLA_PCT_MAX=0.05 (parsePercent('5')===0.05 exact → a real 5% rider clears the inclusive bound, empty false-reject set; kills the 0.2-0.5 sin band) + floor -1 allowing [-1,0) (decaying = conservative; turns an engine crash into calm 'corrupt', 027), HARD at BOTH gates off ONE src/shared constant, source-bound COLA_PCT_MAX>=inflationMean. A RATE's bound is a grounded domain judgment (cite it), never the [0,1] reflex a fraction earns. Family 046 (finiteness ≠ range on a multiplied-away scalar, here a rate) + 044 (which guard actually SEES the value) + 049 (grounded-not-guessed)](050-a-confidence-fan-is-blind-to-a-deterministic-seedless-input-so-a-range-gate-is-the-sole-defense.md)
- [051 — a RIDER folded into a decision-reviewed commit inherits none of that review's scrutiny, AND a wiring-repair fix ships its own silent-regression vector unless a test asserts the association RESOLVES: the colaPct commit (050) settled a money gate whose *number* the Council vetted — but its a11y rider (an `ERROR_OWNER_FIELD` map repairing a dangling `aria-describedby`, the invalid field pointed at `err-income-<field>` while the one FieldError always rendered at `income.save`) was reviewed by NOBODY. The unit's first ultramode CODE review found the money gate CLEAN (adversarial boundary/float + NaN/Inf/overflow + correctness all ZERO) and 100% of the residual in the rider: (1) the aria-association had no regression test — colaPct, the field the unit exists for, was the ONE field whose error-channel wiring was unasserted while its sibling accountEntry.test.tsx:128-129 had the exact guard; two mutations (delete the map entry / drop the range arm of `invalid=`) shipped green, one breaking the color-blind three-channel law. (2) the map newly exposed an orphan alert on a `taxableFraction` error whose field sits in the COLLAPSED advanced tier. Fixes: an `expectFieldBoundToAlert` helper locks the association (`.split(' ').toContain(alert.id)` — the review's suggested bare-id equality would FALSE-FAIL on a help-text field's two-id describedby, a fix-is-a-hypothesis catch, 026), `save()` re-reveals the advanced tier on an advanced-tier error, and needColaRate reuses the shared `colaRateInRange` (three gates byte-identical, 020 made structural). Two edges: a bug-fix's regression guard is PART of the fix (the just-fixed field is the one most likely left unasserted); and a DECISION review (council/design) is NOT a code review — grep the diff for changes outside the decided scope and review THOSE. Family 026 (the suggested fix is a hypothesis) + 048 (a green suite that never drives the real association) + 033 (verify the gate's target) + 035 (the a11y-craft family) + 020 (one of N gates protects none)](051-a-rider-folded-into-a-decision-reviewed-commit-inherits-none-of-that-reviews-scrutiny.md)
- [052 — a catch block spanning the durability point-of-no-return reports a COMMITTED write as "didn't finish": one try/catch wrapping both the IndexedDB commit and the post-commit bookkeeping catches a late error and surfaces "save failed" over a vault that IS on disk — one try per durability regime (before vs after the point of no return)](052-a-catch-spanning-the-durability-point-of-no-return-reports-a-committed-write-as-unfinished.md)
- [053 — a rejected React.lazy chunk import is CACHED for the session and Suspense has no error arm: an offline chunk-miss blank-screens the app stickily (a reload is the only recovery) — the lazy boundary needs an error boundary with a retry, and the chunk should be precached](053-a-rejected-lazy-chunk-import-is-sticky-and-suspense-has-no-error-arm.md)
- [054 — a deterministic refusal's remedy AFFORDANCE is part of the honesty contract, and a test authored WITH the feature pins its bugs as the spec: the read-only re-save rendered a "Try again" that deterministically re-refuses (the copy itself said "reload"), and the feature-authored test even picked the broken key — classify every failure key TRANSIENT (offer retry) vs DETERMINISTIC (the primary action must BE the copy's stated remedy) and grep sibling surfaces of the same failure class](054-a-remedy-affordance-is-part-of-the-error-honesty-contract-and-a-feature-authored-test-pins-its-bugs.md)
- [055 — two independently-ratified rules collide on an input class neither ruling considered: compose by the error-direction law and pin BOTH behaviors, never special-case one rule to satisfy the other (the plan's inert-degenerate law × the council's err-sticky law both fire on a single `other`-essentials line; resolved by letting it err STICKY = conservative)](055-two-ratified-rules-collide-on-an-input-class-neither-considered-compose-by-error-direction.md)
- [056 — a verdict that names WHICH source feeds a composite surface must re-key EVERY field of the composite, not just the headline one: "the DateBand crowns off the FLOOR track" re-keyed only the offset the sentence named; the fan-sink and outcomeState kept riding the FULL track (a tag from one world over evidence from another) — enumerate every field and ask which source its plumbing actually reads](056-a-verdict-naming-a-composite-surfaces-source-must-re-key-every-field-not-the-headline-one.md)
- [057 — a verify gate that reads a pre-built artifact verifies the PREVIOUS build: `verify:bundle` reads `dist/` without rebuilding, so it reported the pre-U9b size until `pnpm build` ran — a stale dist is a false green; always build before any dist-reading gate](057-a-verify-gate-reading-a-prebuilt-artifact-verifies-the-previous-build.md)
- [058 — a reconciliation invariant with MANY input-writers isn't closed by fencing ONE writer: the council made the spend field read-only to kill "the second writer" of annualSpendingReal, but the invariant reads TWO inputs (budget lines + oopMedical) and the OOP-medical step was an un-audited writer that desynced the scalar under a governing budget → the engine's backstop demoted a confident answer to indeterminate (live-repro 6k→30k). Caught by the whole-file review because oopStep wasn't in the diff — its RELATIONSHIP to the new invariant changed. Enumerate every input the invariant reads and every path that writes each. Family 020 (guard on consumer #1 ≠ protects #2, here on the WRITE side) + 049 (a rework promotes a pre-existing editor into a writer that must now reconcile)](058-a-reconciliation-invariant-with-many-input-writers-is-not-closed-by-fencing-one.md)
- [059 — a shared parse primitive that collapses "empty" and "invalid" into ONE sentinel swallows a typo on the one field where blank is VALID: `IntegerField`'s `→ number | undefined` folds cleared and garbage both to `undefined` (correct for a REQUIRED field: both error) but the budget line's through-year routes `undefined → lifelong`, so `2.5`/`-3` silently became a valid-blank encoding with no error affordance (insight 054), asymmetric with every other field. Fix = preserve the distinction AT the primitive (`blankAllowed`: empty ⇒ undefined, non-empty-invalid ⇒ NaN) — the consumer can't recover what the primitive already discarded. A feature-authored test masked it (the through-year error test drove the FROM field)](059-a-shared-parse-primitive-that-collapses-empty-and-invalid-swallows-a-typo-where-blank-is-valid.md)
- [060 — a `useEffect(…, [])` mount-effect binding a ref to a node inside an open-gated portal binds NULL in the app's real closed-at-mount shape and never re-runs: the sheet's blocked-Apply announcer bound at mount, but the live-region node lives inside `open && (…)` and both real mounts start CLOSED → null → silent-dead announce (green in an always-open test harness). Fix = a callback ref that binds/unbinds when the node actually mounts. The tell is a TESTING one — drive the real closed→open sequence, not the convenient always-open. Family 047 (a contract silently correct only because the default consumer never stressed it) + 048 (a behavior the default harness structurally can't reach)](060-a-mount-effect-binding-a-ref-inside-an-open-gated-portal-binds-null-in-the-apps-real-shape.md)
- [061 — a nesting-breaking CSS edit passes dev serving AND the live eye because browsers ERROR-RECOVER — only the build minifier is a strict parser, and the loud syntax failure masks the quiet bug (rules stranded OUTSIDE their @media, applying globally): an edit anchored on a block's opening line re-emitted its own close, stranding four rule groups at global scope + an orphan `}`; dev + full live Chromium verification stayed green (the stranded rules were coincidentally near-inert at the tested widths), `pnpm build`'s lightningcss alone refused — and a balanced-brace variant of the same mistake fails NOWHERE. Re-read from the edit to the block's closing brace; audit SCOPING on any CSS build failure, don't just patch the brace. Family 001/057 (dev-vs-build asymmetry)](061-a-nesting-breaking-css-edit-passes-dev-and-the-live-eye-because-browsers-error-recover.md)
- [062 — non-monotonicity in a swept objective needs a DUAL-CLOCK collision; every single-clock channel sweeps monotone: 10 grids + a 5-agent prover showed pure conversion channels structurally monotone on the date sweep (work-year dominance ~2:1 · the conversion drains its own MAGI motor · MC variance smears the per-path cliff out of the aggregate) — the real dip is retirement-anchored budget windows sliding across the absolute-year conversion window (a k-dependent overlap TENT over the 400%-FPL cliff, `?seed=dip`); ask what two schedules ride different clocks, and check whether the decision statistic AVERAGES a per-path discontinuity away before betting a gate on it](062-non-monotonicity-needs-a-dual-clock-collision-single-clock-channels-sweep-monotone.md)
- [063 — a crashed verify stage reads as a CLEAN result: `Array.from({length}, mapFn)` INVOKES the mapFn, handing `parallel()` live promises instead of thunks — ten P0-P2 verifiers died pre-run and `.filter(Boolean)` rendered the crash as `verified: []` (= "nothing material") while the failures block held the truth; read failures BEFORE trusting a phase's empty output (family 019: a crashed verifier is not a refutation), and build deferred lists with a mapper that RETURNS the thunk](063-a-crashed-verify-stage-reads-as-a-clean-result-arrayfrom-mapfn-invokes-thunks.md)
- [064 — the first REAL-cardinality render is its own test tier: five green-under-jsdom defects (adjacent dip-label garble, bar-label×dot collision, sheet flex-squash, a viewBox-only SVG at height 0, a thin-cohort median spike) were all structurally invisible to single-instance/unit-lightness fixtures — defect classes have a MINIMUM cardinality/geometry, so when a surface's first real instance of a shape arrives, walk it live before the cold-read and back-fill fixtures at the real cardinality (extends 029: a structurally-UNDERSIZED fixture asserts less than it appears to)](064-the-first-real-cardinality-render-is-its-own-test-tier-single-instance-fixtures-cannot-see-collisions.md)
- [065 — a fixed em-reserve is a GUESS that drifts (font × leading × longest-string × width — any drift = the exact layout shift the reserve existed to prevent): reserve by STACKING the real content — all alternatives in one grid cell, visibility-hidden except the active one — so the browser computes the true max-wrap per width for free (the ladder readout jumped on every hover at 2.6em; hardened 035's "reserve the box" with the right HOW; audit any swap-on-hover box under long strings)](065-a-fixed-em-reserve-is-a-guess-reserve-by-stacking-the-real-content-hidden.md)
- [066 — a hermetic stub that SWALLOWS props leaves the router glue mutable — echo the props into the DOM: both real findings of the sweep review were well-tested leaves joined by unpinned glue (App.test stubbed IntakeApp to a marker, so inverting `readOnly={entry.notice !== null}` or dropping deriveResultSave's 3rd arg stayed green); a props-ECHO stub (render the routed prop into a data-attribute) converts a hermetic mock from a coverage hole into a mutant killer at zero cost — audit every vi.mock for computed-but-unechoed props (family 048; sibling finding = 027's intent-vs-effect proxy: `seed != null` disarmed the R19 guard on a bogus key → key provenance on the APPLIED effect via a state flag inside the apply block)](066-a-hermetic-stub-that-swallows-props-leaves-the-router-glue-mutable-echo-the-props.md)
- [068 — a cliff-removed helper reused for a MARGINAL readout reports a phantom slope past the cliff: `slidingScalePtc` deliberately leaves the 400%-FPL branch to its solver, so the U11 shadow-rate's finite-difference probe through it quoted a smooth ~10¢ subsidy drag for an anchor whose credit was already zero (and a probe window can straddle the 133% applicable-% jump) — conservative-direction, so nothing downstream noticed; rule = every NEW consumer of a discontinuity-stripped primitive must answer "who applies the branch here?", gating derivatives on the branch's own predicate or bounding the probe away from the table's OWN jump list](068-a-cliff-removed-helper-reused-for-a-marginal-readout-reports-a-phantom-slope-past-the-cliff.md)
- [067 — an invited door routed into a shared mandatory-gate component inherits the gate's no-escape: the optional backup re-offer door mounted the first-save ceremony's ExportConfirm (deliberately cancel-free) and became a trap — exit = export or reload+re-unlock, while the sibling ColdStart door had onBack for the identical changed-my-mind case; fix = an optional onCancel ("Not now") with the STRICT default so the mandatory mount stays un-skippable — escape affordances belong to the MOUNT's obligation level, not the component; for every new route into an existing flow, walk the exits ("what does backing out commit them to?"), and remember a host test that stubs the flow hides its escape topology](067-an-invited-door-routed-into-a-shared-mandatory-gate-inherits-its-no-escape.md)
- [069 — comprehension holes are a defect class tests CANNOT see (only a cold reader finds them): the ultramode-reviewed Healthcare sheet carried seven one-family holes — undefined referents ("the cliff"), unquoted quantities ("runs ~$174,300 under it"), unattributed derived figures ("your income" for a plan-derived number), mechanism-frame vocabulary ("marketplace help" even INVERTED for the reader; "per enrolled spouse" → "the two of you"), garden-path appositions on load-bearing figures, era-naked same-named figures ($65,100 vs $43,700 read as a data bug), and missing anchors (the step's +$1,100 without the before) — all invisible to 1,930+ green tests BY CONSTRUCTION (tests pin strings; they can't detect a correct string failing to communicate); the law is codified (memory dont-make-users-think + back-nine-design) and FLEET-ENFORCEABLE (Sonnet-5 finders + render-context refuters swept copy.ts: 19 real / 2 refuted — the refuter stage matters, an adjacent-line referent is NOT a violation); sibling law content-outranks-aesthetics (the container yields, content is never cut to fit); budget a live cold read as a GATE for every new user-facing surface](069-comprehension-holes-are-a-defect-class-tests-cannot-see.md)
- [070 — an absence-wait cannot SYNCHRONIZE (green both before and after the awaited event, so it passes vacuously when the element never mounts on that surface): the vertical-fit gate's "wait for the final tier" was toHaveCount(0) on provisional tags the result hero NEVER renders — the gate measured the PROVISIONAL frame, protected only by the spine's accidental tier-invariance; caught by the adversarial review (3 lenses independently, 6/6 refuters), fixed by waiting for a PRESENCE only the awaited event mints (the ModelAnswer.tier stamp → data-answer-tier="final", REQUIRED field) + proven both directions (hardcoded-tier mutant reds store pin AND e2e; the dip arm 6.8s→37.0s = the wait demonstrably WAITS); prefer presence-of-a-minted-state over absence-of-a-transient, demand the wait's runtime move when the awaited work slows, and NAME the accidental invariant that was protecting you (extends 016/029 falsifiability to the synchronization layer)](070-an-absence-wait-cannot-synchronize-it-is-green-before-and-after-the-awaited-event.md)
- [071 — a modal-close focus-restore guard reading focus position at exit-complete must exempt the dying dialog's OWN subtree: U12's sheet→sheet steal-guard ("skip restore if focus is inside a sheet") suppressed EVERY family sheet's plain-close restore in real Chromium — onExitComplete can fire while the dying dialog is still ATTACHED with focus on its own Close button, which the bare predicate reads as "another modal owns focus"; jsdom detaches BEFORE the callback, so the component pin was structurally blind (048/064 at the focus-timing layer) and only the live walk caught focus stranded on body; fix = "focus is inside an X **that is not me**" (dialogRef.contains(active) exempts self), re-proven live both directions; generalize to ANY don't-act-if-someone-holds-it check inside a teardown callback (scroll-lock counts, body-class cleanup, aria-hidden siblings — the dying instance never counts itself), and pin every modal-close focus contract with one real-browser arm](071-a-focus-restore-guard-reading-focus-at-exit-complete-must-exempt-the-dying-dialogs-own-subtree.md)
- [072 — a hysteresis hold over a MONOTONE-MEANING display must be direction-gated and pair-coupled: U12's sticky seam held ANY one-step edge-landing flip, so a FALLING reading kept the rosier prior (prev over-funded + raw 0.85 → "more than enough… better than 9 in 10" over a true 85% — the 9-cap blinded the count guard, the echo's xOfTen-keyed disclosure never fired) and two independent holds composed engine-impossible pairs ({9, borderline}); 8 of 12 lenses converged (017's beacon), the "conservative by construction" comment was a claim about a gate that existed for only half the input space (044); fix = the conservative-direction law on ALL THREE holds (count · word via the engine's exported STATE_OPTIMISM_RANK · the DOLLAR hold nobody flagged — the 054 sibling sweep at fold time) + SYMMETRIC coupling (hold only beside stable-or-held siblings ⇒ the pair is provably prev's or raw's); the proof is a full-grid PROPERTY SWEEP whose readings AND legal-pair set derive from summarize() itself — hand-enumeration missed the mirror leak ({9, borderline} via state-holds-while-count-adopts at 0.98) inside this very fix](072-a-hysteresis-hold-over-a-monotone-meaning-display-must-be-direction-gated-and-pair-coupled.md)
- [073 — a wall-time stamp inside the save payload breaks every byte-identity consumer, and every same-day test stays green: U13's `savedAt` entered a payload (`scenarioFromDraft` output) that is simultaneously the disk bytes, resultSave's JSON.stringify dirty-compare operand, AND the round-trip-guard subject — the first NON-deterministic field in a triple role every deterministic vintage stamp tolerates, so an untouched next-day session would read permanently dirty while every within-one-run identity assertion passes vacuously (both sides stamp the same epoch-day); fix = ONE exported order-preserving normalizer (`scenarioIdentity`, model.ts) imported by BOTH consumers + the fixture the calendar would have written (an aged savedAt reads clean on identical content, dirty on a real edit — planted-fail both directions); rule = before any non-deterministic field enters a compared payload, enumerate its identity CONSUMERS and route them through one normalizer in the SAME change](073-a-wall-time-stamp-in-the-save-payload-breaks-every-byte-identity-consumer-invisibly-same-day.md)
- [074 — a constant's metadata field nothing consumes prices nothing: U13's senior-bonus sunset staleness note narrated a re-pricing the engine never performs — `seniorBonus.sunsetAfter` exists in the sourced metadata but `seniorBonusFor(filing, count65, magi)` has no year parameter and NOTHING in the tax math reads the marker, so the "this reading prices the years as they stand now" note was calm-but-wrong-OPTIMISTIC over a byte-identical recompute; the ratified premise ("already deterministic inside the engine") survived 23 council agents + the build + a live verify because every stage inherited it from the metadata LOOKING like behavior; fix = clock removed + the engine sunset unit filed REQUIRED behind a 2028-01-01 build tripwire (the reVerifyEveryBuild pattern for dated provisions); rule = before writing any sentence that DESCRIBES engine behavior, grep the CONSUMER of the constant it narrates — metadata records the law, only a consumer makes the engine do it, and ratification records agreement, not verification](074-a-constants-metadata-field-nothing-consumes-prices-nothing.md)
- [075 — a fix that re-states an already-applied tier's values is a no-op the passing environment cannot reveal: the U13 vault-return fit arm failed on ubuntu ONLY (text-metric drift pushed the R13 disclaimer 13px past the fold) and the first whitespace fix set the exact token values the ≤840px density tier already applies — computed values identical, CI red at the byte-identical 804, while every local gate stayed green (it was green BEFORE the fix too, so local green carried zero information; the drift-sim control had !important-forced a gap world that never exists at that height); fix = step ONE TOKEN BELOW the tier AND prove the delta pre-ship (computed rowGap 12→8, disclaimer 776→744, sim-drift 772≤791 — then CI green); rule = for a failure you cannot reproduce, the only meaningful pre-ship evidence is a MEASURED DELTA in the failing regime or its simulation, and before any tiered CSS fix, read what the cascade already resolves to at the failing condition (033's fix-side mirror)](075-a-fix-restating-an-already-applied-tiers-values-is-a-no-op-the-passing-env-cannot-reveal.md)
- [076 — a NEW fail-loud guard deep in the engine retroactively narrows the upstream gate's admission contract: the sunset unit's integer-year throw in `seniorBonusFor` (correct, insight-010-conformant) made `validateParams`'s `Number.isFinite(startCalendarYear)` predicate STALE without touching its file — a finite 2026.5 passed the R19 gate whose own header promises "indeterminate, never a crash" and crashed mid-path in the year loop; green tests can't see it (fixtures pass integers, the guard's tests exercise the primitive, not the gate); caught by the review's numerical adversary, fixed to `Number.isInteger` + a planted-fail arm in the R19 battery (mutant-proven); rule = whenever a unit ADDS a throw under a calm-refusal gate, the unit's definition of done includes sweeping that gate's admission predicate against the engine's NEW throw surface (049's precondition lesson inverted — the downstream check strengthened, the upstream promise lagged)](076-a-new-deep-fail-loud-guard-retroactively-narrows-the-upstream-gates-contract.md)
- [077 — a verify-stage vote that discards a finding on a materiality TIE (both refuters agreeing the gap is REAL) silently loses the review's one live catch: the sunset review's aggregation required real∧material per vote and majority-voted the conjunction, so a 1–1 materiality split on a unanimously-REAL gap read as a refutation — insight 019's shape at the vote-math layer (there a crash read as refutation; here a severity disagreement did); the coordinator caught it only by reading the REJECTED pile's vote texture, hand-tracing to source (the gate's own contract comment made it material), and folding it; rule = aggregate `real` and `material` SEPARATELY, surface every unanimously-real finding to the coordinator regardless of the impact vote (reachability-vs-contract-honesty is a project-values judgment refuters grade against different standards), and never declare a review clean from the confirmed count alone](077-a-materiality-tie-on-an-agreed-real-gap-is-a-hand-verification-trigger-not-a-rejection.md)
- [078 — a defect-class sweep by CODE SEAM misses siblings that render the same MEANING through their own chrome: the U13 one-time-base family kept yielding survivors across TWO deliberate sibling sweeps (hero+floor fixed at the U13 fold → the band caught live on the first ?vault=datestale walk → TwoFutures + the C2 sentence caught by the review's caller adversary → the odds LADDER still escaped, caught hours later by the Caddie panel's False-PASS Hunter: hero "6 years out · 9 of 10" over aria "Stopping in 8 years … your date") because every sweep enumerated consumers of a SEAM (dateAnchor, the shared chrome) while the ladder renders offsets through its OWN chrome with zero shared imports; rule = enumerate a meaning-class off the RENDERED SURFACE (list every element that speaks the corrupted dimension, then verify each), and treat making a blocked surface walkable as a bug-finding act — the whole family was caught within hours of the aged vault becoming drivable, never by the 2,159-test suite (054's structural cousin: same-meaning siblings share no words and no imports)](078-a-defect-class-sweep-by-code-seam-misses-siblings-that-render-the-same-meaning-through-their-own-chrome.md)
- [079 — a state seed that NORMALIZES its payload is only as truthful as the fields its current consumers read: the aged-balances clause read `savedAt` from the persist machine (contract: "the LAST COMMITTED model") but the hydrate path seeds it through `scenarioFromDraft`, which re-stamps savedAt to TODAY — invisible for two units because the state's only consumer compares through the savedAt-BLIND `scenarioIdentity`, then the FIRST consumer of the unread field inherited the fabricated value and the clause silently never rendered; caught ONLY by the real-browser `?vault=datestale` fit arm (every unit test injected the derived prop directly and stayed green); fix = splice the raw disk model's own savedAt into the seed (absent on legacy vaults — suppression over fabrication); rule = a normalizer at a state seed makes every field it re-mints a quiet lie waiting for its first reader — seed disk-contract state with the disk's own bytes for every fabricated field, and audit provenance at every seed site before trusting a long-unread field's type doc (073's sibling at the seed layer; the ship-gate arm is the only oracle class that can see it)](079-a-state-seed-that-normalizes-its-payload-is-only-as-truthful-as-the-fields-its-current-consumers-read.md)
- [080 — a disclosure predicate that complements ONE producer's gate becomes a lie when a SECOND producer of the same flag ships (and its compliance-citing docstring deflects every reviewer): `medicareUnpriced` keys the "Medicare isn't priced in yet… never up" note off AGES — the exact complement of the INTAKE gate, true at write time because the intake was the only producer of `healthcareEnabled` — but `dateSearch.ts:222` is a SECOND producer (forces the flag true on every date candidate), so a still-working all-65+ household reads the note over numbers Medicare already moved; it survived 13 U11 ultramode lenses + 6 recon readers + 9 council openings because the docstring CITES insight-027 compliance and the citation satisfied every reviewer — only the council red team's refuter checking the claim against the OTHER route's params builder caught it; fix (hawk-armed) = key the disclosure off the run's ACTUAL route-aware pricing decision, mutant-proven by an age-mutation-holding-pricing witness; rule = a complement-of-a-gate predicate carries the hidden premise "this gate is the flag's ONLY producer" — re-audit it whenever a new producer ships (020 mirrored to producers), and sweep "compliant with rule N" comments as claims, not facts (044's class)](080-a-predicate-complementing-one-producers-gate-lies-when-a-second-producer-of-the-flag-ships.md)
- [081 — single-sourcing by re-deriving a producer's INPUTS forks at its first early return (only the producer's OUTPUT cannot drift; a regression witness unseen RED can be vacuous twice): the Medicare unit — BUILT to kill 080's proxy class, citing 080 in its comments — recurred the class one level up by deriving the disclosure from the gate's shared input predicate; buildOverlay's degenerate early-return ($0 accounts/no premium/no income ⇒ NO overlay) fires upstream of the medicareOnly branch, so the SS-only all-65+ household read "Medicare's premiums are already in these numbers" over a run that priced nothing (6-lens convergence); fix = `spineMedicarePriced` reads `buildSpineParams(d)?.overlay?.healthcareEnabled` (the producer's OUTPUT) + a chain of pins ending in a source-bind on Result's derivation line — because BOTH first witnesses were VACUOUS for independent reasons (an un-planted hero surface, 029's class; a sheet channel whose account precondition destroys the divergence itself) and only planting the mutant and demanding RED exposed them; rules = sweep every mirror for input-re-derivation whenever a producer gains an early return (020 at the derivation level; the "never drift" comment is 044's class), and never trust a witness you have not seen go red](081-single-sourcing-by-re-deriving-a-producers-inputs-forks-at-its-first-early-return.md)
- [082 — when the whitespace reservoirs hit floor, the next honest fit rung is the subordinate-disclosure TYPE REGISTER: the extras unit grew the medicare pair past a frame whose gaps (round 3) and leading (snug) were already at their sanctioned floor, and content cuts are forbidden — so round 4 stepped the pair into the R13 disclaimer's OWN `--text-sm` register (the SAME register an equal-rank subordinate already wears in the same frame — hierarchy preserved, no novel size, words untouched, two-pane only/phone byte-identical, ~40–50px), and round 5 paid ubuntu's wrap-drift with arithmetic SCOPED to the on-typical modifier frames (row-gaps → 2px, leading snug → tight); the ladder is ORDERED (whitespace → leading → subordinate register) and every rung ships as a computed-style pin red on EVERY platform (075's standing encoding); shrinking a hero, minting a new size, or cutting words stay forbidden — density pays, meaning never does](082-when-whitespace-reservoirs-hit-floor-the-next-fit-rung-is-the-subordinate-disclosure-type-register.md)
- [083 — parallel agents in ONE working tree couple through the tree itself: the extras build's seed-tuner wrote its drift probe as a temp test at the project root ("reliably picked up by vitest" — meaning every TEAMMATE's vitest/lint run collects the throwaway too, and its mid-window deletion makes a teammate's runs non-comparable), its python rewrite silently flipped devSeeds.ts CRLF→LF (a 914-line diff hiding six real edits; core.autocrlf=false restores nothing), and the sibling team's staleness.test.ts pins were written EXPECTING the tuner's exact retired-seed edit before it landed (coordination that held by shared dispatch context, not by mechanism); rules = scratch probes live outside the collected globs (scratchpad/temp/worktree isolation) or carry delete-before-handoff + a join-point pinned-suite re-run, whole-file writers preserve EOL byte-for-byte, and any fixture surface two agents touch is a decide-before-dispatch charter fact — enumerate tree-level channels (collection globs, EOL, lint sweeps, fixture pins) like API contracts before dispatching into one tree](083-parallel-agents-in-one-working-tree-couple-through-the-tree-itself.md)
- [084 — an oversized StructuredOutput call TRUNCATES after its first property and the retry loop dies at the cap re-truncating every attempt: the state-tax pre-build council lost THREE opening seats (hawk/fiduciary/a11y — each wrote the ten-question issue as an ESSAY into the schema call; "must have required property 'reasoning'" on every retry; the agents diagnosed the cutoff themselves) while six tighter-writing siblings passed — output-size-dependent, so it presents as flaky seat crashes, not a law; a RETRY cannot fix structural truncation (the hawk guard cycled correctly and still lost; unguarded seats would be filter(Boolean)-discarded, 019's violation); fix (efd8585e) = the SIZE LAW in every schema-emitting prompt (per-field word budgets, one-two sentences per question, depth in deliberation TEXT before the call — "the call is the verdict summary, not the essay") + the recovery pattern (patch ONLY the dead seats' prompts + un-run phase templates, resume with resumeFromRunId + identical args — the healthy majority replays from cache); proof = the resumed 21-agent council and the same-day 53-agent ultramode review, both under the law, zero schema crashes; generalizes to ANY workflow schema with several required string fields, and to schema design itself (few bounded fields, never "write your full analysis into this property")](084-an-oversized-structured-output-call-truncates-after-its-first-property-and-dies-the-retry-loop.md)
- [085 — a doctored dev fixture must survive the same fail-loud gates as an organic household (every vault doctor needs an ENGINE-ACCEPTANCE pin): the statestale plant aged startCalendarYear −2y into a year the engine's own priced-state lower bound (simulate.ts:640 — the prior fold's guard, whose comment names "an aged dev plant" as the anticipated caller, written five days before one hit it) correctly REFUSES, so the vault demoted to the R19 calm indeterminate and rendered NO verdict while every unit test stayed green (the plant's tests proved the clock fires + the badge reads clean — never that the engine ADMITS the scenario); caught only by the fit builder's insight-033 live drive BEFORE the arm was pinned; fix = the light doctor (savedAt + the state stamp alone, the F2 supersession) + the missing test class as Arm 2 (hydrate → buildSpineParams → validateParams accepts → a REAL outcomeState) + the fail-loud pair completed at the review fold (agedStateProfile throws on a schedule-less FL; doctorStaleVault throws on a priced base); rules = a doctored fixture is a NEW PRODUCER of persisted state whose consumer chain includes the engine's gates — age only within the domain the engine prices (a household predating the feature it carries is a contradiction), pin engine-acceptance for every doctor, and when a deep fail-loud guard ships (076's shape) sweep the DEV-FIXTURE producers too](085-a-doctored-dev-fixture-must-survive-the-same-fail-loud-gates-as-an-organic-household.md)
- [086 — splitting a copy key into predicate-gated variants ORPHANS every renderer not re-pointed in the same commit (the stale variant becomes a live false claim): S5.2 split spendHelp into priced/unpriced twins and gated the intake renderer, but the AssumptionPanel spend row — a SECOND renderer of the same key — kept its hardcoded `helpKey="spendHelp"`, telling a priced NC household "State income tax isn't priced yet… keep that bill inside" days after the intake said "leave that bill out" (a flat false claim + the federal double-count invitation, 080's shape one layer up: a second CONSUMER of a split string); every test stayed green (the unit's mutants proved the gated renderer's branch, nothing enumerated the OTHER renderers of the pre-split key) and the face was unreachable by any walk until the seed increment made it renderable — S3's walk smoke caught it on the answered-panel face's first-ever capture; fix = mirror the predicate, then (the review fold) hoist it to ONE shared home (`spendHelpKeyFor`, intakeMap) both input surfaces import — agreement BY CONSTRUCTION, both directions component-pinned on both surfaces; rules = the SPLITTING commit greps every render site of the OLD key and re-points or justifies each (the old key staying legal is what makes the orphan silent — live code telling yesterday's truth), and prefer a shared predicate helper over a comment-held mirror (this pair had already drifted once)](086-splitting-a-copy-key-into-gated-variants-orphans-every-renderer-not-re-pointed-in-the-same-commit.md)
