---
title: "Phase 1 — Foundation"
type: feat
phase: 1
parent: docs/plans/back-nine-mvp/roadmap.md
date: 2026-06-04
status: active
deepened: 2026-06-05
doc-reviewed: 2026-06-05
coded:
code-reviewed:
---

# Phase 1 — Foundation

**Goal.** Stand up the buildable PWA skeleton, the deterministic Monte Carlo engine validated against known-good reference cases, the **two-control substrate** every recommendation will stand on (pluggable withdrawal-sequencing policy + the tax-and-accounts overlay), the **income-aware healthcare overlay** that makes ACA-PTC / IRMAA / HSA visible across the Medicare line, and the encrypted local store that makes the at-rest promise provable. Nothing user-facing ships in this phase — but the engine's answer must be *right*, the overlays must reduce **byte-identically** to the validated spine when off, and the store's guarantee must be *proven* before any surface consumes them.

**Why this is Phase 1.** The two hardest, highest-risk surfaces — *correctness* (a calm-but-wrong number, and later a calm-but-wrong **recommendation**, fail the bar worse than no tool — R25) and *trust* (R16: provable before spoken) — are foundations everything else stands on. The thesis reset transferred the load onto honesty + engine validation, so the substrate the solver will later search (sequencing policy, tax overlay, healthcare overlay) is built and validated **here, before any control surface or solver exists** — the controls (P3) and the solver (P4) *drive* this substrate; they never re-implement decumulation. U1–U3 are independent of U4 (parallelizable) and have zero monorepo precedent, so they carry the most risk and must be locked first.

> Paths are relative to `projects/the-back-nine/`. References: `(origin: …)` → `docs/brainstorms/the-back-nine-requirements.md`; `(findings §StrandN)` → `docs/research/foundation-findings-2026-06-03.md` (engine validation = **§Strand 4**; tax numbers = **§Strand 5**); `(healthcare doc)` → `docs/research/pre65-healthcare-aca-hsa-2026-06-04.md`. **Every dated tax/health constant, crypto param, and validation percentage lives in those research docs and is read through `src/engine/constants/` — never re-stated here.**

---

## Phase-1 cross-cutting contracts

*These six contracts span the units below. They are named once here so a reader sees the seams, then enforced in the unit that owns each. Several are seams later phases (Controls, Solver) explicitly consume; this section is their producer.*

1. **The determinism / CRN contract (owned by U1; consumed by U2, U3, every P3 control, and the P4 solver).** The engine is a pure function of `(params, seed)`. The seed is a single integer in `mulberry32`'s signed-32-bit domain, **injected by the caller** (never generated inside `src/engine`), and stored as a first-class field of the saved model. The **draw schedule** — the count and ordering of normal draws per path/year — is a pure function of the path/horizon **dimensions only, never of the financial inputs**: the normals matrix is allocated to the **maximum** cohort horizon up front and indexed by **absolute year**, and the Box-Muller transform is **stateless** (no cached spare — the textbook cached-spare pattern makes the Nth normal depend on call parity, silently desynchronizing two CRN candidates). A fixed seed therefore reproduces a byte-identical matrix of normals **regardless of which input changes** — so financial / longevity inputs select *which* draws are consumed, never *how many* are generated or their order. The **CRN-safe regime-shifters** — the inputs that change *which* draws are read but never the schedule — are an enumerated, exhaustive set: **{the joint→survivor two-regime boundary, the earned-income bridge, the tax-and-accounts overlay (U2), the healthcare overlay (U3), the death-order conditional filter}**. Each earns concrete CRN tests in its owning unit. The seed must survive U4's encrypt → persist → decrypt round-trip **bit-identically**. **Seed-stability** (same inputs + same seed → identical raw distribution and identical raw rounded headline) is U1's pure guarantee **on a single JS engine** — byte-identical normals + raw distribution hold within one browser build, but plain-TS transcendentals (`exp`/`log`/`pow`) are **not** bit-identical across engines, so the *displayed* `X of 10` is made cross-engine-robust by `confidence.ts` **quantizing the headline statistic to a coarse grid before the band-edge decision** (below), not by relying on bit-identical floats; **rounding hysteresis** is a *stateful* property (it needs the prior displayed value) and lives in the P2 recompute orchestration (`memoryModel.ts`), **not** in pure `confidence.ts`. This is the structural fact that lets the P4 solver rank K candidate strategies on **identical futures** — signal, not RNG luck.

2. **The single-shared-market-draw rule (owned by U1; enforced in U1/U2/U3; now more load-bearing).** **All account buckets — pre-tax / Roth / taxable / HSA — share ONE market-return draw per simulated year** (the same normals stream as the spine). Buckets differ **only** in tax treatment, never in return assumption. Splitting the portfolio into buckets tempts an implementer to shock each bucket with its own draw — which would multiply draws-per-year, change the draw-schedule **dimension**, and silently break CRN. Per-bucket draws are **forbidden**. Tax / RMD / conversion / healthcare effects are all deterministic **post-draw arithmetic**. This makes "no asset-location" a *structural* guarantee (not a copy promise — the chapter-two asset-location capability, if ever wanted, must be a *deterministic per-bucket tilt on the one shared draw*, never a separate draw), and it is what lets the solver rank candidates on identical per-path returns.

3. **The shared per-year-cash-term transform seam (owned by U1; populated by the bridge in U1, the tax overlay in U2, the healthcare overlay in U3).** There is **one** per-year update function the validated decumulation uses; everything else is a **deterministic transform of the cash-flow term, indexed by absolute year, consuming ZERO random draws**, fed into that same function so within-year order-of-operations (withdrawal vs return application vs rebalance) is shared by construction and can never drift between the spine and an overlay. The transforms compose: the **earned-income bridge nets the withdrawal DOWN** (`netWithdrawal = max(0, spending − earnedIncome)`, never crediting a dead earner, never contributing surplus back); the **tax overlay grosses it UP** (tax + RMD + conversion increase the cash needed); the **healthcare overlay grosses it UP further** (net ACA premium / IRMAA surcharge are spending). **The reduce-to-spine golden invariant binds every transform:** when an overlay is OFF it reduces **byte-identically (same seed) to the Trinity/Bengen-validated decumulation distribution** — the golden cases are **never** perturbed. Each unit states its own exhaustive OFF condition and owns the byte-identical test.

4. **The in-memory → encrypted-persistence seam (owned by U4; consumed by P2 intake).** Nothing touches disk until the user saves and sets a passphrase. This is enforced **structurally** — the store exposes no write path reachable before a key exists — not by caller discipline. At rest, the AES-GCM ciphertext (protected by the PBKDF2-600k passphrase derivation) guards the disk; the cleartext model held in JS memory during an unlocked session is guarded by U0's CSP. The P1 store proves the seam at its own layer; P2 adds the end-to-end intake assertion. (Gates R15/R16.)

5. **The R19 split (engine numeric half owned by U1; intake semantic half handed to P2).** The **engine** guards its own numeric domain — no `NaN`/`Infinity` escapes a percentile; degenerate-but-coherent inputs return an honest extreme (or the defined indeterminate state), never a crash. **Semantic plausibility** (retirement age < current age, spending beyond any plausible portfolio, the control/budget/health input surfaces) is the R19 **intake / control-layer** concern owned by P2/P3. Neither layer assumes the other validated. The control surfaces (P3·U10 conversion clamp, P3·U9 budget, P3·U11 health inputs) are downstream R19 surfaces this contract names but does not own.

6. **The canonical year-keyed constants module (owned by U0; read by U1/U2/U3, the plan, every test, and the copyGuard allowlist).** Every dated tax/health constant — 2026 brackets, the standard deduction + age-65 addition + senior-bonus, RMD birth-year ages, SS provisional-income thresholds, ACA applicable-% + FPL cliff + SLCSP handling, IRMAA brackets + 2-yr lag, HSA limits, Part B premium — lives in **one** year-keyed module `src/engine/constants/`, each figure carrying its **source citation** (`findings §Strand 5` / the healthcare doc) and a **`directional-until-pinned`** marker (and, for the ACA legislative item, a **re-verify-every-build** gate). Plan, engine, tests, and the copyGuard allowlist all read this module — a constant is **never re-typed** (`burned/057,061,063`: one canonical table, no hand-duplication). The reduce-to-spine invariant means the spine itself reads **no** constant from this module (it is tax-free) — the module is consumed only by the overlays, so a constants change can never perturb a golden case.

---

- [ ] **Unit 0: Project scaffold, conventions, PWA shell, CI**

**Goal:** A buildable, deployable React 19 + TS + Vite 8 PWA skeleton with the monorepo's conventions, the engine-purity lint, a strict CSP, a CI that gates on tests, the **canonical constants module**, and the full directory map the roadmap's Output Structure names — so every later unit lands in a place that already exists.

**Requirements:** Enabler for all; R13 (static honest-limits disclaimer surface); R15/R16 (the CSP that guards the in-memory model).

**Dependencies:** None.

**Files:**
- Create: `package.json`, `vite.config.ts`, `tsconfig.json`, `eslint.config.js`, `index.html`, `src/main.tsx`, `public/manifest.webmanifest`, `vercel.json` (CSP + security headers)
- Create: `src/engine/constants/index.ts` (the canonical year-keyed table — directional-until-pinned; cross-cutting contract #6) + `src/engine/constants/__tests__/constants.shape.test.ts`
- Create (dir scaffolding so later units land in existing dirs — empty/placeholder per the roadmap Output Structure): `src/engine/{sequencing.ts, taxOverlay.ts, healthOverlay.ts, solver/, validation/, reference/}`, `src/crypto/`, `src/store/`, `src/intake/`, `src/budget/`, `src/viz/`, `src/ui/`, `src/shared/`
- Create: `CLAUDE.md` (model on `projects/burned/CLAUDE.md`); `docs/insights/README.md` (this project's own insight index; mirror burned's)
- Create: `.github/workflows/verify-the-back-nine.yml` (in the monorepo `.github/`, path-scoped to `projects/the-back-nine/**`)
- Test: `src/shared/__tests__/scaffold.smoke.test.ts`; a `verify:bundle` budget sentinel script

**Approach:**
- Adopt the convergent baseline verbatim (verified current across siblings): React 19.2.4, **TS `~5.9.3` (tilde, patch-only — a TS minor can't silently change type-checking under a correctness-critical engine)** + the strict-plus tsconfig (`noUncheckedIndexedAccess`, `noFallthroughCasesInSwitch`, `noImplicitOverride`), Vite 8, Vitest 4 (`globals:false`), `pnpm@10.30.3` (pin `packageManager: pnpm@10.30.3` verbatim), flat **ESLint 10**, **no Prettier**, `motion` v12. Add the engine/store runtime deps **`comlink`** (the worker boundary) and **`idb`**; add the passphrase-strength estimator (**`zxcvbn-ts`** or equivalent — the strength gate is seated in P2·U8, but the dependency is pinned here). Pin every new dep at scaffold (zero monorepo precedent for comlink/idb).
- **Vite 8 variant is decided, not a coin-flip:** adopt **`rolldownOptions`** per burned's documented Vite 8 convention (legacy `rollupOptions` is not the standard). Also `resolve.tsconfigPaths: true` (built-in; no `vite-tsconfig-paths` plugin) and `import.meta.dirname`.
- `vite-plugin-pwa` in **`prompt`** mode (NOT `autoUpdate` — `skipWaiting`+`clientsClaim` can reload a tab mid-encrypt-write and tear an IndexedDB write). `useRegisterSW` → "Update ready" toast. `globPatterns` includes all shell assets **including the hashed engine-worker chunk** (so the offline shell precaches it); raise `maximumFileSizeToCacheInBytes` if any asset is large. Pin a `vite-plugin-pwa` version whose peer range includes Vite 8. **U0 ↔ U4 reload seam:** the update-acceptance handler must defer `skipWaiting`+reload until any in-flight write commits (the store exposes a "no write in flight" signal) — `prompt` mode removes the *automatic* tear but a **user-accepted** update mid-write is still a hazard. The signal *interface* is defined here; the integration test that exercises it lands in P2 (where the real save flow exists).
- **The canonical constants module (cross-cutting contract #6) is born here.** `src/engine/constants/` is a year-keyed table — every figure carries `{ value, citation, directionalUntilPinned: true, legalBasis? }`. The ACA legislative item carries an additional `reVerifyEveryBuild: true` flag — **and the flag is enforced by CI, not advisory metadata:** a checked-in `aca-last-verified.json` records the date + the verified legislative status, and `verify-the-back-nine.yml` **fails the build** if that record is older than the build window (or the status is unconfirmed), so a build cannot ship with a stale ACA assumption merely because no human remembered to check. (This is the build-time half of the gate the roadmap Validation Gates and P4·U17's gate-red fallback both consume — the ACA status can flip the whole pre-65 model and invert which strategy wins, so its re-verification is mechanical, not a checklist item that degrades over time.) **The spine reads NOTHING from it** (the spine is tax-free); only the overlays do. A shape test asserts: every entry has a citation + the directional marker; no overlay number is inlined elsewhere (grep-style assertion that the constants are single-sourced — `burned/057,061,063`). Pinning a number = flipping its marker after confirming against the named primary (the U1/U2/U3 exit gates).
- **ESLint must be rewritten, not cloned.** burned's `eslint.config.js` is the *structural* template (flat config, typescript-eslint, `no-restricted-imports` + `no-restricted-properties` idiom) — but its rules target burned's `client/server/shared` dirs. the-back-nine's layers are `engine/crypto/store/intake/budget/viz/ui/shared`. Rewrite: **layer import boundaries** (`engine` must not import `ui`/`store`/`intake`/`viz`; `engine` is pure); and the **engine-purity ban** — forbid the global `Math` RNG, `crypto.getRandomValues`, `Date.now`/`new Date`, and `performance.now` inside `src/engine/**` (the engine must read no entropy source, clock, or environment — those pass a weak-RNG-only lint while still breaking purity). Extend the weak-RNG ban to `src/crypto/**` too (recovery-phrase entropy must be CSPRNG — see U4). Use `eslint.config.js` (a `.ts` flat config needs ESLint's `jiti` dep, which burned does not ship — not worth a new dependency when the rules are what matter).
- **Vendor, do not cross-import.** Copy burned's `rng.ts` into `src/engine/` rather than importing across the project boundary, and **commit** reference fixtures (never gitignored/generated-at-build): a value/type import from a sibling — or a generated-into-gitignored-dir fixture — is green locally and `TS2307`-cascades or vanishes on a clean CI clone (`ai-journey-stats/008`). Prove the clean-clone path by hiding any sibling artifact, not by trusting local green.
- **CI deliberately diverges from the `ai-journey-stats` template.** That workflow is path-scoping / Node-22 / pnpm / `--frozen-lockfile` gold — but it is **build-only and explicitly does NOT run tests**. For the-back-nine the **engine and crypto ARE the product**, so `verify-the-back-nine.yml` MUST run **typecheck + vitest + the bundle sentinel** on CI, not just `vite build` (without it, U1's "tests pass on CI" and U4's trust evidence are false by construction).
- **Content Security Policy (the in-memory trust boundary, gates R15/R16).** At-rest encryption protects the disk; it does **nothing** for the cleartext model in JS memory during an unlocked session — any injected script could read it with no passphrase. Ship a **strict CSP via HTTP response headers** (`vercel.json`), not just a `<meta>` tag: `script-src 'self'` (no inline, no `eval`), `connect-src 'self'` (blocks exfiltration to any third-party endpoint), `object-src 'none'`. Keep the dependency surface deliberately narrow (React, `idb`, `motion`, `comlink`, `zxcvbn-ts`) — every runtime dep is same-origin code with reach into the in-memory model. (XSS is the in-scope runtime vector; the disclosed "malicious server code-update" threat stays scoped out.) **The CSP guards the in-session model against XSS-injected scripts, NOT against browser extensions** — extensions run in a privileged context CSP does not constrain and can read the page heap (the decrypted model, keystrokes on the passphrase field, the recovery phrase) without a CSP violation; this is an **accepted out-of-scope risk** for a personal single-device tool, named so no downstream R15/R16 copy presents the CSP as a complete in-session guard. **Deployment-context note:** `connect-src 'self'` is safe for the Vercel deployment; if the tool is ever self-hosted on `localhost`/a LAN hostname, a DNS-rebinding attacker could reclassify a local address as "self" — add the Private-Network-Access header and re-audit `connect-src` before any such deployment (recorded in `CLAUDE.md`).
- **Engine-worker emit + precache (no Rolldown precedent).** Instantiate the worker via `new URL('./engine.worker.ts', import.meta.url)` with `{ type: 'module' }`; confirm Vite 8 / Rolldown emits it as a separate hashed chunk and that the chunk is in the PWA `globPatterns` — a build-time failure to emit/resolve the worker surfaces only on a clean CI build (`ai-journey-stats/008` class).
- `CLAUDE.md` names `docs/brainstorms/the-back-nine-requirements.md` as the contract and sets a bundle budget. Conventions to record: cross-repo insight citations carry the **full relative path + title slug**, not a bare number; a frozen-token Vite dev 500 that survives reloads while `pnpm build` passes is a **stale-HMR artifact** (restart Vite, never edit code — `burned/072`).
- **Static "informational and educational — not legal, tax, or investment advice; validate big, irreversible moves with a professional" disclaimer** in the app shell (R13). It is a Unit-0 constant **outside** the copyGuard's input (P2·U7) — a mandatory directive-shaped disclaimer must stay legal while imperative mood is forbidden in verdicts. Kept on **honesty** grounds, not a Terms requirement (R13 reset).

**Patterns to follow:** `projects/burned/CLAUDE.md`, `projects/burned/vite.config.ts` (rolldown Vite 8 convention), `projects/burned/eslint.config.js` (structure only — rewrite rules for our layers), `projects/burned/scripts/verify-prod-bundle.ts` (`verify:bundle` precedent), `.github/workflows/verify-ai-journey-stats.yml` (path-scoping shape only — we add the test gate), `ai-journey-stats/008` (clean-clone discipline), `burned/057,061,063` (one canonical constants table). **PWA: `projects/undercover-mob-boss/vite.config.ts` is a STRUCTURAL reference ONLY — borrow its plugin wiring / `globPatterns` shape; it uses `registerType: 'autoUpdate'`, the mode we explicitly reject. Do NOT copy its `registerType` or its plugin version; use `prompt` + the `useRegisterSW` toast.**

**Test scenarios:**
- Happy path: `pnpm build` produces a PWA that loads offline (app shell precached) on a second visit.
- Edge: bundle-budget sentinel fails the build when initial JS exceeds the budget (test against a planted oversize import).
- Edge (constants shape): every constants entry carries a citation + the `directionalUntilPinned` marker; the ACA legislative entry carries `reVerifyEveryBuild`; no overlay number is inlined outside the module (single-source assertion, `burned/063`).
- Integration: service worker registers in `prompt` mode and surfaces an update toast rather than auto-reloading (verify no `skipWaiting`).
- Integration: **CI runs the test suite** — a planted failing vitest test fails the `verify-the-back-nine.yml` run (proves CI is not build-only).
- Integration (**ACA re-verify gate**): `verify-the-back-nine.yml` **fails the build** when `aca-last-verified.json` is older than the build window or its status is unconfirmed — a planted stale/unconfirmed record turns the run red (proves the `reVerifyEveryBuild` flag is CI-enforced, not advisory metadata).
- Integration: **clean-clone integrity** — build/typecheck succeed after hiding any sibling artifact the engine might lean on (no cross-project import / no gitignored fixture; `ai-journey-stats/008`).
- Integration: **CSP browser-enforced** (`e2e/csp.spec.ts`, gated by `pnpm verify:csp` in CI) — `scripts/serve-dist-with-headers.ts` serves `dist/` with `vercel.json`'s exact headers (`vite preview` does NOT apply them) and a real Chromium blocks an injected inline `<script>` AND a cross-origin `fetch` exfil (`connect-src`) — each proven non-vacuous by a no-CSP control arm — while the engine module worker still constructs under `worker-src 'self'`.
- Integration: **worker chunk emitted + precached** — a clean build emits the hashed `engine.worker` chunk and it appears in the precache manifest.
- Integration (layer-boundary lint): a planted `import` of `src/ui` from `src/engine` (and a planted `Math.random` / `Date.now` / `crypto.getRandomValues` inside `src/engine`) each fails the lint.

**Verification:** Clean clone → `pnpm install --frozen-lockfile` → `pnpm build` succeeds on CI (Node 22, `pnpm@10.30.3`); CI also runs typecheck + vitest + the bundle sentinel (a planted failure turns the run red); the built app loads offline; the disclaimer is visible; the strict CSP is browser-enforced (the `verify:csp` Playwright gate blocks an injected inline script + still constructs the worker, proven against a no-CSP control); the constants module is the single source of every overlay number and every figure is marked directional; and a stale/unconfirmed `aca-last-verified.json` fails the build (the ACA re-verify gate is enforced, not advisory).

---

- [ ] **Unit 1: Monte Carlo engine core + validation contract + sequencing substrate** *(the correctness unit)*

**Goal:** A pure, deterministic TypeScript Monte Carlo engine that models a married couple's joint-and-survivor longevity, decumulates through a **pluggable per-year drawdown policy** (the second control's substrate), and emits a distribution the UI reads as "X of 10" + the dollar adjustment — validated via **two distinct modes**: a deterministic historical-sequence backtest (a self-consistent oracle + a directional check on the published Trinity/Bengen figures) and the Monte Carlo path (asserted as a band strictly below the historical anchor). U1 owns the CRN seam, the shared cash-term transform seam, longevity-with-survivor-identity, the earned-income bridge, and the pluggable-policy substrate that U2 (tax) and U3 (health) hang on and that the P4 solver searches.

**Requirements:** R3 (distribution of futures), R9 (sequencing-as-a-control substrate), R14 (plain-language reading), R19 (engine's numeric-domain half), the correctness success criterion.

**Dependencies:** U0.

**Files:**
- Create: `src/engine/rng.ts` (vendored mulberry32 + stateless Box-Muller), `src/engine/longevity.ts` (cohort joint-survivor, retains per-path survivor identity), `src/engine/sequencing.ts` (the named drawdown policies — the second control's substrate), `src/engine/simulate.ts` (MC spine + pluggable policy + the shared cash-term seam), `src/engine/historical.ts` (the deterministic backtest oracle), `src/engine/confidence.ts` (distribution → X-of-10 + dollar + outcome-state, pure), `src/engine/engine.worker.ts` (Comlink boundary), `src/shared/model.ts` (the single plaintext shape + the outcome-state enum)
- Create: `src/engine/reference/` (Trinity/Bengen fixtures — **committed**, generated via cFIREsim-open on a pinned dataset; self-consistent + directional, not published-exact)
- Test: `src/engine/__tests__/{rng,simulate,longevity,sequencing,confidence,historical,worker}.test.ts`, `src/engine/reference/__tests__/validation.test.ts`

**Approach:**
- Port `mulberry32` + the CSPRNG/seeded split from burned's `rng.ts` (**vendored, not cross-imported**); add a **stateless** Box-Muller normal transform (absent in burned): consume two uniforms per draw and **do not cache the spare** across calls. **The engine never generates its own seed:** `crypto.getRandomValues` is called by the caller (the P2 `memoryModel` orchestration layer) and the 32-bit seed is **injected** into `simulate`/the worker; `src/engine` reads no entropy source, clock, or environment (enforced by the U0 lint). A saved scenario stores its seed in `model.ts`; the seed must round-trip through U4's encryption bit-identically (contract #1).
- Lognormal sim with **log-drift μ = arithmetic_mean − σ²/2** (the #1 MC bug is naïve arithmetic compounding — findings §Strand 4). Conservative real-return + inflation defaults (Pfau/Kitces ~3–3.5% real in high-CAPE), all user-overridable, all read from the methodology-default layer (not the tax constants module). **The σ in the σ²/2 correction is the *log-return* σ, not the simple-return σ** (they differ at equity vol); the engine seam **declares whether `arithmetic_mean`/`sigma` arrive in simple- or log-space and converts consistently** — silently mixing a simple-return mean with a log-return σ biases every percentile (a second-order sibling of the naïve-compounding bug).
- **CRN seam (contract #1):** `simulate` allocates its normals matrix to the **maximum** cohort horizon up front, indexed by **absolute year**, so the draw schedule is a pure function of path/horizon **dimensions only**. The enumerated CRN-safe regime-shifters (survivor boundary, bridge, tax overlay, health overlay, death-order filter) select *which* draws are consumed, never *how many* or their order. Two parameter sets under one shared seed consume identical normals path-for-path — the substrate the solver ranks K candidates on.
- **Joint-and-survivor longevity from COHORT tables (not period):** `P(last survivor alive) = pₓ + pᵧ − pₓ·pᵧ`. **HARD requirement (contract #1):** longevity must **sample per-path, per-spouse death years AND retain which spouse dies first per path** (or expose per-path death-order). The closed-form mixture **alone does NOT retain survivor identity** — and the death-order conditional filter (P3·U10 / P4) is unbuildable without it. SSA cohort source = `table4c7.html` (explicitly **not** the period table `table4c6.html`). Two-regime horizon (joint → survivor): survivor-SS step-down (keep larger benefit) + survivor-spending ratio. **The survivor-spending ratio is grounded, not guessed** — because it scales the survivor's spending and so rides the **Tier-1 survival floor** (the solver's hero metric), it must be **grounded to a citable retirement-research range** (e.g. the Blanchett survivor-spending / "two-thirds-to-three-quarters" literature), **source-stamped like every other constant** with a `directionalUntilPinned` marker, and ships a defensible default near **~75%**. It stays **editable**, but its **dangerous direction is documented**: too *low* understates the survivor's need — the unsafe direction for the survivor product — so a low edit carries a calm note, and the default is never a silent un-sourced number under the hero metric. Never a fixed "to-age-90". **The couple survival figure is derived from the two *sex-specific* cohort curves through the formula — never a hardcoded couple number:** a single symmetric individual rate is arithmetically inconsistent (0.25 + 0.25 − 0.0625 = 0.4375, not the ~0.53 a sex-differentiated pair yields), so the longevity test asserts *internal consistency* (couple prob = the formula applied to the two shipped curves), not a constant. Independence is assumed (real spousal mortality is positively correlated), which **mildly overstates** last-survivor probability — acceptable because it errs *safe* for a survival floor, documented so it isn't mistaken for precision.
- **The pluggable drawdown policy — sequencing as a first-class engine parameter (net-new core, R9).** `simulate` takes a per-year **drawdown POLICY** that decides which bucket funds each year's net withdrawal. The named set is **`{proportional, taxable-first, pre-tax-first, bracket-fill}`** (locked in the roadmap's resolved Open Questions, D4). The policy is the **substrate both the manual control (P3·U10) and the solver (P4·U15) drive** — neither re-implements decumulation; they select a policy (or a custom order) and the engine runs it. **The policy is a function of the per-year bucket state + the tax/health overlay context, consuming ZERO draws** — it decides *which bucket*, not *what return*. In P1 the policy operates against the U2 bucket abstraction; with the buckets collapsed to one pool and the overlay OFF, **every policy reduces to the single-pool decumulation** (the policy choice is inert when there is only one bucket — part of the reduce-to-spine condition). `bracket-fill` is the tax-aware policy (fill ordinary income to a target bracket edge before drawing tax-free) and so depends on U2's overlay; in P1 it is wired to the seam, its tax behavior validated in U2.
- **Earned-income bridge regime.** `simulate` accepts a per-person **earned-income** stream (flat real income). Each person's earning window is **`[year 0, min(retirementYear, sampledDeathYear))`, evaluated per path** — income **truncates at death** (**never credit a dead earner** — this matters precisely in the survivor scenario the product exists to model honestly). The bridge is a **pre-withdrawal adjustment to the per-year withdrawal term** (`netWithdrawal = max(0, spending − earnedIncome)`, a clamp at zero — no surplus contributed back; **not** a parallel accumulation engine) fed into the **same** per-year update function the validated decumulation uses (contract #3). MVP-minimal: flat real income, net-against-spending only; no salary-growth curve, no surplus-contribution modeling (post-MVP, surfaced as an editable assumption). **Reduce-to-spine condition: `earnedIncome = 0` for both spouses in every simulated year** — then `netWithdrawal == spending` and the engine reduces exactly to the pure-decumulation path. (Equal-but-future retirement years with nonzero income still net in the pre-retirement years, so retirement-year equality is **not** the golden-case condition — earned-income-zero is.)
- Outputs: the full terminal-value distribution + percentiles + depth-of-failure (not a binary pass/fail) → `confidence.ts` maps it to "X of N" (**display denominator pinned at 10** — a P2 display contract, but the raw→display rounding target is owned here; top-of-scale renders as the over-funded near-ceiling, never a bald "10 of 10"), the dollar adjustment ("trim ~$Y/month"), and the **outcome state**. **Never "probability of failure"** (Kitces framing). `confidence.ts` is **pure** (single run → reading): it owns the **stateless** first-answer rounding rule (`priorHeadline = null`, with defined band-edge tie-breaking) — and **quantizes the headline-determining statistic to a coarse grid (well outside last-ULP noise) BEFORE the band-edge decision**, so the displayed `X of 10` is robust to a single-ULP transcendental (`Math.exp`/`Math.log`) difference between browser JS engines. This is the load-bearing guard for the cross-engine reproduction promise: byte-identical normals + byte-identical raw distribution is a **same-JS-engine** guarantee (plain TS transcendentals are not bit-identical across V8/SpiderMonkey/JavaScriptCore — an IEEE-754 reality; `findings §Strand 2` flags the broader cross-browser float-determinism gap, of which this is the engine-level cause), so a user who screenshots a headline in Chrome and reopens the PWA in Safari must still see the same `X of 10` even though the raw percentile may differ in its last ULP — the quantization, not bit-identical floats, is what keeps the screenshot promise honest. (A true cross-engine bit-identical requirement is the concrete trigger that would promote WASM from fast-follow to load-bearing — roadmap.) It emits raw continuous values **plus margin metadata for every rounded display output** — the X-of-N headline (distance to the next band edge) **and** the dollar figure (distance to the next display-rounding step) — so a stateful caller (P2/P3) can layer sticky rounding on **both** (the failure mode is one being sticky while the other flickers, since they render in one sentence). It does **not** itself implement cross-edit hysteresis.
- **The outcome-state set (engine-owned, single-sourced in `src/shared/model.ts`):** `on-track / borderline / off-track / indeterminate / over-funded / already-failing` for the single-metric first answer. The engine is the sole authority that selects the state and owns all band edges, the indeterminate selection, and the **10/10-honesty clamp**. (The P3 budget split adds a two-tier lexicographic *reading* over the same states — not a 7th state.)
- **Two validation modes (the correctness contract):**
  - **(A) Historical-sequence backtest** (`historical.ts`) — replays fixed historical return sequences. The engine's *anchor*, asserted two ways: **(1) self-consistency** — reproduces **our own committed golden fixture** (generated on a pinned dataset) byte-for-byte, so an engine regression fails loud; **(2) directional** against the *published* Trinity/Bengen figures (lands near them, right band), **not** exact equality. **Why not exact:** fixtures are generated with **cFIREsim-open** (Shiller data → long-term **government** bonds), but Trinity's published 95% uses long-term **corporate** bonds, a series cFIREsim-open lacks — so it yields a faithful self-consistent oracle on a known dataset but cannot reproduce Trinity's published corporate number exactly. Treat the published figures as **directional until the exact source series are pinned**.
  - **(B) Monte Carlo** (`simulate.ts`) — validated against a **band only**: an i.i.d. MC runs *more pessimistic* than the historical backtest (findings §Strand 4), so the MC result must land inside the asserted band **and strictly below** the historical anchor. Reserve byte-for-byte equality for mode (A)'s self-consistency check. **The Mode-B band assertion must run on historically-calibrated return/vol moments** (comparable to the historical anchor) — **not** the conservative Pfau/Kitces production defaults, which would drive MC far below the band and fail the test for the wrong reason. And **"strictly below" holds only in the stress region the band defines**: at low withdrawal rates both regimes approach ~100% and MC need not be strictly below (MC-vs-history is two-sided per Kitces — MC overstates *tail* risk while sometimes overstating sustainable income in the 70–90% range; assert the relation only where the band lives).
- **Trinity fixture spec (vintage-locked):** 1926–1995 window, S&P 500 + **long-term CORPORATE** bonds, inflation-adjusted withdrawals, success = balance > $0 at horizon end, annual rebalance (findings §Strand 4). When generating with cFIREsim-open, restrict to this vintage; the bond series will be Shiller **government** until a true corporate series is pinned.
- **Bengen caveats (all four, findings §Strand 4):** tag the fixture **1966 cohort** (not 1968); 50% S&P 500 / 50% **intermediate-term GOVERNMENT** bonds; **DIRECTIONAL survive/fail only** until the exact Ibbotson intermediate-government dataset is pinned; and **never cross-validate** the Trinity 50/50 (corporate) figure against the Bengen 50/50 (government) figure.
- **Phase-1 exit gates (block calling a fixture "golden"; findings §Strand 4 "Still-unverified"):** **(1)** the SSA cohort curves were grounded-search-verified, **not parsed from the SSA tables** (SSA bot-blocks automated fetch) → confirm against the actual `table4c7.html` via manual fetch + a committed snapshot before the longevity curves are load-bearing (this is load-bearing for the survivor differentiator and every survivor headline the controls/solver later render); **(2)** Bengen's bit-exactness depends on the exact Ibbotson intermediate-government dataset → pin before relying; **(3)** Trinity's published number depends on a long-term-corporate series cFIREsim-open lacks → pin before relying. Until each clears, its fixture is **directional** (marked in code). Name who/what clears each gate before P1 closes.
- **Worker boundary contract:** runs in `engine.worker.ts` behind Comlink, a **single long-lived instance** created/wrapped once and reused across recomputes (the future solve budget is measured against reuse, not per-run spawn). **Result shape:** the large numeric arrays (terminal-value distribution, percentile series, per-path depth-of-failure) return as typed-array buffers via `Comlink.transfer`; the small derived fields (X-of-N integers, dollar adjustment, outcome-state enum) travel by ordinary structured clone and are **not** in the transfer list. Transferred buffers are **detached** on the worker side — the worker retains none for reuse and allocates fresh per run (protects the CRN path + the future solver's K-candidate batching). **Error propagation:** a thrown engine error surfaces as a defined **calm** result (the tri-state `pending | resolved-distribution | calm-error`), never a hung promise / unhandled rejection / dead worker; the worker stays alive and reusable. **No-worker posture:** because the engine is pure TS, a worker-construction failure falls back to a **main-thread run** returning the **same distribution**; P1 proves only the same-distribution equivalence, the UX consequence (disable live recompute on fallback) is a P2/P3 concern noted here so they own it.
- **Engine's R19 half (contract #5):** `simulate` validates/clamps params at the worker boundary and returns the defined **indeterminate** state for invalid input rather than computing — no `NaN`/`Infinity` escapes a percentile or headline.

**Execution note:** Implement test-first against the reference cases — the golden fixtures are the contract. Build mode (A) `historical.ts` first so the anchor exists before the MC band is asserted below it. Build `sequencing.ts` against the single-pool reduce-to-spine case first (policy inert on one bucket), then wire it to the U2 bucket abstraction.

**Patterns to follow:** burned's `rng.ts` (determinism discipline, the load-bearing `|0` coercions); `burned/055+070` (determinism gates fail loud + self-test against a planted positive); `burned/027` (absence-tests need presence companions — pair every CRN/invariant absence-assertion with "the sim ran N paths and at least one stressed the condition"); `burned/042` (engine↔reader contract-drift precedent for the worker boundary); `archive/do-not-disturb/012` (externally-derived golden fixtures — derive expected numbers by an independent path, never via the engine's own formula).

**Test scenarios:**
- **Mode A — self-consistency:** the historical backtest reproduces our committed Trinity-vintage golden fixture byte-for-byte (a regression fails loud); the result lands **directionally near** the published 50/50 95% (right band), not asserted as exact until the corporate series is pinned.
- **Mode A — diagnostic (the landmine), directional:** historical 100%-bond / 4% / 30-yr lands in the **§Strand-4 bond band** — materially below the 50/50 case and far above the wrong low range — proving inflation/volatility handling is correct (asserted as a band, not exact equality to a tilde'd figure).
- **Mode A — extra anchors (optional):** historical 75/25 and 100%-stock land in their §Strand-4 directional bands — catch allocation-handling bugs the 50/50-vs-bond pair alone would miss.
- **Mode B — MC band:** the MC 50/50 / 4% / 30-yr lands inside the asserted §Strand-4 band **and strictly below** the historical anchor — a result *equal to* the historical 95% is a bug (MC must run more pessimistic).
- **Mode B — MC bond, directional:** the MC 100%-bond case fails materially more than the 50/50 case and is demonstrably **not "safe"** (directional, not an equality).
- Edge (log-drift): +50% then −50% yields two-year **cumulative −25%** / **annualized geometric −13.4%** (correct, distinct labels — findings §Strand 4).
- Edge (seed-stability, same-engine): same inputs + same seed → byte-identical raw distribution and byte-identical raw rounded headline across repeated runs on one engine.
- Edge (cross-engine headline robustness): a simulated **last-ULP perturbation** of the headline-determining statistic (modelling a transcendental difference across browser engines) does **not** flip the quantized `X of 10` — the coarse pre-band-edge quantization absorbs sub-grid noise, so the screenshotted headline reproduces across engines even when the raw percentile differs in its last bit.
- Edge (first-answer rounding): `confidence.ts` with `priorHeadline = null` and a raw value **exactly on a band edge** produces a defined, stable headline (the stateless tie-break is pinned; P2/P3 stickiness builds on this).
- Edge (CRN under a regime-shifting input — the hardest case): two parameter sets differing only in the **survivor-spending ratio** (which moves the two-regime boundary), under one shared seed, consume normals **identical path-for-path** for the overlapping years (proves the max-horizon / absolute-year allocation holds when the survivor boundary moves). **Paired presence companion** (`burned/027`): assert the sim ran N paths and at least one path's survivor boundary actually shifted between the two arms.
- Edge (sequencing policy is CRN-neutral): the four named policies, run under one shared seed against an identical multi-bucket state, consume **identical** per-year market draws path-for-path (the policy decides *which bucket*, never *what return* — contract #2). And: with buckets collapsed to one pool, all four policies produce the **identical** distribution (policy inert on one bucket — part of reduce-to-spine).
- Edge ($0 portfolio + positive spending): honest **already-failing** / "0 of N" (coherent-but-dire, never a crash or NaN).
- Edge (NaN hygiene): no `ln`/`exp` of a non-positive escapes into a percentile.
- Edge (R19 engine half): each of {negative portfolio, `NaN` param, `Infinity` spending, horizon ≤ 0} → the defined **indeterminate** outcome, no crash, no `NaN`/`Inf` anywhere in the distribution.
- Edge (outcome states): the **10/10-honesty clamp** — an all-paths-survive distribution renders as the **over-funded** near-ceiling, never a bald "10 of 10"/100%; **indeterminate** — minimal/missing-assumption inputs yield the indeterminate state (the expected first answer), not a falsely confident number.
- Error path: a forced internal sim error surfaces as the defined **calm** error result and the worker remains alive/reusable; a simulated worker-construction failure falls back to a main-thread run returning the **same distribution**.
- Integration (worker transport, `worker.test.ts`): an in-worker run with `Comlink.transfer` returns a distribution **byte-identical** to the same seeded in-thread run (no truncation/detachment of transferred buffers); the transferred-buffer **layout** `simulate.ts` emits matches `confidence.ts`'s reader, with a `NaN`/zero sentinel so a misread channel fails loud rather than rendering "0 of N" (`burned/042`).
- Integration (couple longevity): at-least-one-survivor-to-90 probability materially exceeds a single life (sanity vs findings §Strand 4); survivor-SS step-down lowers the verdict vs ignoring it; **per-path survivor identity is retained** — a query for "paths where spouse X died first" returns a non-empty, deterministic sub-population (the death-order filter's precondition); and the couple at-least-one-survivor figure **equals the formula applied to the two sex-specific cohort curves** (internal consistency), never a hardcoded constant.
- Edge (bridge reduces to decumulation — the golden-case invariant): with **`earnedIncome = 0` for both spouses**, `netWithdrawal == spending` every year and the distribution is **byte-identical** to the pure-decumulation run (same seed). Distinct non-equality case: equal-but-future retirement years with nonzero income net in the pre-retirement years, so they are **not** byte-identical — assert that explicitly so an implementer doesn't code "equal retirement years ⇒ identical".
- Edge (bridge directional, partial coverage): `0 < income < spending` ⇒ `netWithdrawal > 0` and the terminal distribution is **monotonically weakly-better path-for-path under CRN** than the no-bridge run; a year where income exceeds spending **clamps net withdrawal at zero** (never a contribution back).
- Edge (earner dies mid-bridge — the survivor landmine): in a path where the longevity draw kills the earning spouse **before** their retirement year, earned income is credited **only through the death year** (window truncates at `min(retirementYear, sampledDeathYear)`).

**Verification:** Mode-A reproduces our committed golden fixtures byte-for-byte and lands directionally near the published figures (exact-equality gated on pinning the Trinity-corporate / Bengen-Ibbotson / SSA datasets); Mode-B MC tests land in-band and below the historical anchor; the four named policies are CRN-neutral and reduce-to-spine on one bucket; longevity retains per-path survivor identity; all run on CI (fail-loud, self-tested against a planted wrong number per `burned/070`). A 1k-path run completes well under the recompute budget in the reused worker. The seed survives U4's persistence round-trip and reproduces the identical headline after reload (asserted in U4).

---

- [ ] **Unit 2: Tax-and-accounts overlay** *(the first control's tax substrate)*

**Goal:** A CORE engine unit (`src/engine/taxOverlay.ts`) that turns the tax-blind spine into a tax-aware one — per-person account buckets, ordinary-income tax on withdrawals + RMDs + conversions, SS provisional-income taxation as a per-year fixed-point, and the MFJ→single filing switch at the first death — as a **deterministic per-year cash-term transform that consumes ZERO draws and reduces byte-identically to the validated spine when off**. This is the substrate the manual Roth/sequencing controls (P3·U10) and the solver (P4·U15) move; it is built and validated here, before any control surface exists.

**Requirements:** R9 (conversion + tax-aware sequencing substrate), R24-adjacent (the tax half of the income picture healthcare couples into), R19 (engine numeric half on the bucket arithmetic), the correctness success criterion (tax-math directional fixtures).

**Dependencies:** U0 (constants module), U1 (the CRN seam, the shared cash-term transform seam, the bucket-aware sequencing substrate, the joint→survivor boundary).

**Files:**
- Create: `src/engine/taxOverlay.ts` (the per-year deterministic transform: buckets, ordinary tax, RMD, SS-tax fixed-point, MFJ→single)
- Create: `src/engine/reference/` tax fixtures (the §Strand-5 2026 MFJ + single brackets, standard deduction + age-65 + senior-bonus, Uniform Lifetime Table divisors — **committed, not generated**, **read from `src/engine/constants/`**, marked directional until pinned, same discipline as Trinity/Bengen)
- Modify: `src/engine/simulate.ts` (invoke the overlay on the shared cash-term seam), `src/shared/model.ts` (per-person pre-tax / Roth / taxable bucket fields + **per-person birth year** + filing-status / bracket-vintage / app-default-version stamp fields, as the **schemaVersion-2 field shape** — defined here, first **written to disk by Phase 3** (P3 contract #5), since the spine first answer runs with these overlays OFF)
- Test: `src/engine/__tests__/taxOverlay.test.ts`

**Approach:**
- **The overlay is the structural sibling of the earned-income bridge (contract #3):** a per-year **deterministic transform of the cash-flow term, indexed by absolute year, fed into the SAME per-year update function**, consuming **ZERO** random draws — CRN-safe by the bridge's identical argument. The bridge **nets down**; the overlay **grosses up** (tax increases the cash needed; a conversion adds a tax bill paid from the taxable bucket).
- **Per-person buckets: pre-tax / Roth / taxable.** Ordinary-income tax on pre-tax withdrawals + RMDs + the conversion; tax-free Roth growth; capital-gains/qualified-dividend stacking from taxable withdrawals (§Strand 5 IN-scope). **All buckets share the ONE existing per-year market draw (contract #2)** — buckets differ only in tax treatment, never return assumption; tax/RMD/conversion are deterministic post-draw arithmetic. This is what makes `bracket-fill` (U1's tax-aware policy) computable and structurally forecloses asset-location.
- **RMD age is birth-year-derived, NEVER a flat 73** (§Strand 5, SECURE 2.0): 72 (≤1950) / 73 (1951–1959) / 75 (1960+, effective 2033). Per person, keyed off the model's birth-year field (derived in P2 intake from calendar year − age; a ±1yr birth-month approximation is sufficient for the discrete threshold). RMD = the IRS Uniform Lifetime Table divisor on the prior-year-end pre-tax balance. RMD is a **forced-distribution mechanic, not a tax** — so "taxes off" alone does **not** silence it (see the OFF condition). **The Uniform Lifetime Table is not universal:** when the sole beneficiary is a spouse **more than 10 years younger**, the IRS Joint Life & Last Survivor table (Pub 590-B Table II) applies and yields a *smaller* RMD — flat Uniform Lifetime overstates forced income for the age-gapped couples this product explicitly models. And **the RMD is non-convertible** — past RMD age it must be distributed as ordinary income *first* and cannot be satisfied by (or reduced by) a Roth conversion; the manual control (P3·U10) and solver (P4·U15) consume this as a hard legality constraint.
- **SS provisional-income taxation as a per-year bounded FIXED-POINT.** Taxable SS depends on provisional income, which depends on the year's other ordinary income — including the **gross-up** withdrawal needed to cover spending + tax, which depends on the tax: a circularity. Resolve it per simulated year as a bounded fixed-point — **iterate provisional-income → taxable-SS → tax → gross-up → re-converge** to a stable value (a few passes), deterministic, reading **zero** draws (CRN-safe). The convergence rule is pinned in the engine seam (iterate-to-stable or a fixed small pass count). The MFJ/single thresholds are **frozen, not inflation-indexed** (§Strand 5) — constants with **no staleness clock**.
- **MFJ→single switch at the sampled first death = the joint→survivor two-regime boundary U1 already models** (NO new boundary; the empty-nest retired couple gets **no QSS grace** — files single the year after the first death, §Strand 5). The survivor's same real dollars fall into ~half-width single brackets with ~half the standard deduction — the "tax cliff" that is the recommendation's emotional headline story (rendered in P3/P4).
- **Standard deduction + age-65 addition + senior-bonus** (the senior-bonus carries its MAGI phase-out and its **2028 sunset** — a guaranteed-to-go-stale provision; §Strand 5). All figures via the constants module; none inlined.
- **GOLDEN INVARIANT — reduce-to-spine, with an EXHAUSTIVE OFF condition (contract #3).** The overlay reduces byte-identically to the spine when, and only when, **buckets collapsed to one pool AND conversion = 0 AND ordinary-tax off AND RMD-inert** (no forced distributions). RMD-inert is the fourth clause **not** because an active RMD perturbs the total — under contract #2 (one shared draw, no asset-location) a forced pre-tax→taxable relocation moves money between two *identically-growing* buckets and is provably total-neutral with tax off — but as a **clean-fixture guarantee**: it keeps the golden anchor unambiguously the spine and the auxiliary ledger trivially the single pool. An active tax-off RMD is exercised *separately* and asserted at the **ledger** level; only the M3 ordinary-income tax makes an RMD change the total. Under all four, `netWithdrawal == spending` and the terminal-value distribution is **byte-identical (same seed)** to the Trinity/Bengen-validated decumulation — the golden cases are never perturbed (the direct analogue of the bridge's `earnedIncome=0` invariant). The overlay is ON only when a control (P3) or the solver (P4) turns it on.
- **Both arms / both candidates run at identical tax fidelity** — there is no tax-blind arm. The only difference between a "with-conversion" and "without-conversion" candidate is the conversion event; both decumulate through the overlay. (A tax-blind delta is **sign-inverted** — it sees only the cash drain of paying conversion tax, so every conversion looks worse. This unit exists to prevent that.)
- All MVP tax scope and numbers live in **§Strand 5**, via the constants module: IN — brackets, standard deduction + age-65 + senior-bonus, RMDs, SS-taxation, cap-gains stacking; OUT-but-disclosed — NIIT, state (neither moves with sequencing or conversion). The death-time **§1014 basis step-up is a *lever-sensitive* effect that is IN, not omitted** — it *does* move with the lever (sequencing/conversion changes which account is preserved into the estate: taxable is stepped up at death, inherited pre-tax is fully taxable to heirs as IRD, Roth is tax-free) and a disclosed omission can *invert* the after-tax ranking, so by the IN/OUT falsifiable test it belongs IN: a **first-order §1014/IRD adjustment** is modeled into the P4 *leave-more* objective (after-tax-to-heirs) at a **disclosed assumed heir bracket**, never a gross/pre-heir-tax figure. U2's role is to expose the per-bucket basis/character the adjustment reads (taxable basis, pre-tax IRD, Roth tax-free); the full estate model stays chapter-two (canonical enumeration shared with roadmap Scope + P4 contract #4/#7 / U16). The fixture carries its **OBBBA-2025 legal basis as provenance metadata** (so a statutory bracket change reads as a vintage bump, not silent inflation drift).

**Execution note:** Test-first on the **reduce-to-spine invariant** and **CRN stability across the MFJ→single transition** (the golden anchors), then the directional tax-math fixtures (a known conversion in a known year → known incremental tax), then the SS-tax fixed-point convergence. Run the golden test with at least one **born-pre-1960** spouse so the RMD path is exercised, not only RMD-exempt ages.

**Patterns to follow:** U1's earned-income-bridge overlay shape (the tax overlay is its sibling — grosses up vs nets down); U1's CRN seam + the single-shared-draw rule; `archive/do-not-disturb/012` (externally-derived tax-math fixtures — compute the expected incremental tax by hand/spreadsheet, never via the engine's own formula); `burned/062` (no in-range default fallbacks — a `?? 22%` default that overlaps a plausible bracket makes a missing input indistinguishable from a measurement, fatal inside the SS-tax fixed-point; use out-of-range sentinels or fail loud); `burned/027` (pair the reduce-to-spine absence-assertion with a presence companion that the overlay actually did tax work in the ON case).

**Test scenarios:**
- **Golden invariant (the anchor):** buckets collapsed to one pool + conversion = 0 + taxes off + **RMD-inert** → the overlay-on-with-OFF-conditions run is **byte-identical (same seed) to the spine distribution** (the bridge analogue — proves no spurious delta, no engine drift). **Presence companion** (`burned/027`): with the overlay genuinely ON (multi-bucket, a conversion, taxes on, an active RMD), the distribution **differs** and at least one path actually paid RMD-forced tax.
- **CRN across the survivor transition:** two candidates differing only in **conversion amount** (hence bucket split + tax) consume normals **identical path-for-path across the survivor MFJ→single bracket transition** (proves the overlay consumes zero draws and buckets share one draw/year). Monotonic delta with the conversion amount, no jitter (asserted on the raw pre-clamp delta).
- **Single-shared-draw:** two candidates with different bucket splits consume the **same** per-year market draws (buckets change only tax treatment, never the draw schedule — contract #2).
- **Tax-overlay directional math:** a known conversion in a known year produces the expected incremental ordinary tax (MFJ schedule), including an RMD-forced distribution and the SS-provisional-income effect; survivor years use the single-filer schedule. Expected numbers computed independently (`archive/do-not-disturb/012`), marked directional until §Strand-5 primaries are pinned.
- **SS-tax fixed-point:** the per-year iteration converges to a stable taxable-SS / gross-up within the pinned pass count; a constructed circular case (high spending + SS + a conversion) converges and is deterministic across repeated runs; **no in-range default** stands in for an unconverged value (it fails loud, `burned/062`).
- **RMD age birth-year-derived:** a born-1960+ spouse gets RMD age 75 (a wider conversion window); a born-1955 spouse gets 73. **An active RMD with taxes OFF is TOTAL-NEUTRAL — it does NOT break byte-identity:** under contract #2 (one shared market draw, no asset-location) the pre-tax and taxable buckets grow by the *identical* return, so a forced pre-tax→taxable relocation is a no-op on the portfolio TOTAL — `terminalReal`/`depletionYear` come from `stepYear` on the (RMD-untouched, tax-off) gross withdrawal and stay byte-identical to the spine. The RMD is asserted at the **ledger** level (pre-tax drawn down, taxable raised by the same forced excess); the visible *distribution* difference is the M3 presence companion, where the ordinary-income TAX on the RMD makes it bite and the cash actually leaves the portfolio. **RMD-inert is retained as a required clause of the golden OFF condition** as a clean-fixture guarantee (so the golden anchor is unambiguously the spine), NOT because an active RMD alone perturbs the total.
- **bracket-fill policy (U1 substrate, validated here):** the `bracket-fill` drawdown policy fills ordinary income to the target bracket edge before drawing tax-free, producing lower lifetime tax than `pre-tax-first` on a constructed case (the policy's tax behavior, deferred from U1).
- Edge (R19 on buckets): a conversion clamped to `[0, remaining pre-tax pool]` accounting for cumulative multi-year conversions; degenerate bucket amounts rejected — the engine-numeric half (the inline-surfacing UI half is P3·U10).
- Edge (constants single-source): every tax figure the overlay uses comes from `src/engine/constants/` and is marked directional; none is inlined in `taxOverlay.ts`.

**Verification:** The overlay reduces byte-identically to the spine under the exhaustive OFF condition (and demonstrably does tax work in the ON case); CRN holds across the MFJ→single transition; the directional tax-math fixtures match independently-computed numbers; the SS-tax fixed-point converges deterministically with no in-range fallback; RMD age is birth-year-derived and the RMD-inert condition is proven necessary; `bracket-fill` is tax-correct; every number routes through the constants module marked directional.

---

- [ ] **Unit 3: Healthcare overlay** *(income-aware ACA / IRMAA / HSA — the hardest net-new unit)*

**Goal:** A CORE engine unit (`src/engine/healthOverlay.ts`) that makes healthcare cost **income-dependent and continuous across the Medicare line** — pre-65 ACA-PTC as a per-year fixed-point with explicit cliff branching, post-65 IRMAA as a 2-year-lagged feed-forward, and HSA as a fourth account bucket — as a **deterministic per-year cash-term transform that consumes ZERO draws, shares the one market draw, is CRN-safe, and reduces byte-identically to the validated spine when off**. ACA-MAGI and IRMAA-MAGI are **two distinct calculators**. Built and validated here because a disclosed omission of a cliff **inverts which strategy wins** (D6, R24) — the solver may not optimize over a healthcare effect it cannot see.

**Requirements:** R24 (income-dependent healthcare across the Medicare line), R9 (the health half of the income picture the controls/solver move), R19 (engine numeric half on the MAGI / cliff arithmetic), the correctness success criterion.

**Dependencies:** U0 (constants module + the ACA legislative re-verify gate), U1 (CRN seam, shared cash-term seam, per-path death-order identity, the four-bucket draw rule), U2 (AGI / taxable-income feed the MAGI calculators consume; the MFJ→single survivor boundary; the funding-source order that breaks the loop).

**Files:**
- Create: `src/engine/healthOverlay.ts` (two MAGI calculators, the ACA fixed-point + cliff branch, the IRMAA lagged feed-forward, the HSA bucket)
- Create: `src/engine/reference/` health fixtures (ACA applicable-% table + FPL cliff + a known SLCSP case, IRMAA brackets + surcharges, HSA limits, Part B premium — **committed**, **read from `src/engine/constants/`**, marked directional until pinned; the ACA fixtures gated on the legislative re-verify)
- Modify: `src/engine/simulate.ts` (invoke the overlay on the shared cash-term seam, after the tax overlay), `src/shared/model.ts` (HSA bucket fields per-person/owner-age-keyed; a per-year **out-of-pocket-medical-cost** stream the HSA's qualified spend is capped to; per-filing-unit MAGI-history state for the IRMAA lag **plus the two pre-sim seed years' actual IRMAA-MAGI** when the sim starts within 2 years of age 65; the SLCSP-benchmark **and the separate enrolled-premium** user-input fields; the enhanced-subsidy scenario toggle — the **schemaVersion-2 field shape**, defined here, first **written to disk by Phase 3** per P3 contract #5)
- Test: `src/engine/__tests__/healthOverlay.test.ts`

**Approach:**
- **Two distinct MAGI calculators — do NOT reuse one number** (healthcare doc §4a):
  - **ACA-MAGI** = AGI + tax-exempt interest + **non-taxable SS** + excluded foreign earned income (so the **full** SS benefit effectively counts).
  - **IRMAA-MAGI** = AGI + tax-exempt interest, **NO SS add-back**.
  - Qualified Roth distributions, return of basis, cash, and HSA spending count toward **neither** — so the engine's **funding-source order materially changes loop strength** (this is exactly why sequencing is a control).
- **Pre-65 ACA-PTC as a per-year FIXED-POINT (the same shape as U2's SS-tax fixed-point).** `PTC = max(0, SLCSP_benchmark − applicable_pct(FPL%) × ACA-MAGI)`; **`allowed_PTC = min(PTC, enrolled_premium)`** and **`net_premium = max(0, enrolled_premium − allowed_PTC)`** — floored at zero, never cash back; `gross_wd = spending + taxes + net_premium`; `MAGI = f(gross_wd, other_income)` — MAGI on both sides. **Enrolled premium and the SLCSP benchmark are two separate inputs** (PTC keys off the benchmark; the bill is the enrolled plan — a sub-benchmark Bronze plan must not yield PTC > premium = negative net); if the MVP simplifies to enrolled = SLCSP, state it so `net_premium = applicable% × MAGI` is the documented assumption. The per-MAGI-dollar clawback is the **applicable % only on the flat 300–400% FPL band (~0.10 in 2026)** and is **larger in the sliding bands** (`p + I·dp/dI`, ~13–16%); the funding loop adds a tax gross-up (effective factor ≈ clawback ÷ (1 − marginal_rate)) — still a contraction < 1, so it converges, **but make a bisection on a monotone funding-gap residual the PRIMARY solver** (the map is non-smooth: the `max(0,·)` floor, the cliff, band-boundary kinks, the SS-torpedo and LTCG-stacking kinks). Iterating a MAGI↔PTC circularity is IRS-precedented — cite **Pub 974 / Rev. Proc. 2014-41 only as precedent that iteration is legitimate**, NOT as the method that solves *this* loop (they solve the distinct SEHI-deduction↔PTC loop).
- **DETECT-AND-BRANCH the 400% FPL cliff explicitly** (healthcare doc §4b): a naive iterator **oscillates** across the cliff (one dollar over → all PTC lost). Detect the crossing, compute the **just-under** and **just-over** funding solutions, and pick the cheaper — never rely on smooth convergence over the discontinuity. The **under-cliff branch is a constrained solve (MAGI ≤ 4.00 × FPL, optimum sitting at the threshold) that may be INFEASIBLE** once all MAGI-invisible funding (Roth/basis/cash/HSA) is exhausted — on infeasibility take the over-cliff branch, never oscillate or emit an in-range default (`burned/062`); the **over-cliff branch pins PTC = 0 → net premium = full premium → a direct linear solve**, no fixed-point iteration. The cliff-aware ceiling, not the tax bracket, is the binding constraint in ACA years — so **when the overlay is ON, `bracket-fill`'s target ceiling becomes `min(tax-bracket edge, the ACA-subsidy-aware MAGI line pre-65 / the next IRMAA step post-65)`**, never the raw bracket top (the policy consumes the overlay's binding ceiling).
- **2026 base case = the 400% FPL cliff is ON** (enhanced subsidies expired 12/31/2025, unre-enacted as of 2026-06-04). "Enhanced subsidies" = a **scenario toggle** (a model field), never hard-coded "no enhanced forever." **The legislative status GATES ALL ACA fixtures and must be re-verified at every build** (the constants module's `reVerifyEveryBuild` flag on this item; healthcare doc Exit-Gate #1 blocks the entire pre-65 module).
- **The SLCSP benchmark premium is a USER INPUT (an age-banded assumption), never synthesized** — it is ZIP/age-specific; every serious tool punts it to input; it is the single biggest honesty lever (healthcare doc §4b, OUT-but-disclosed). The engine optimizes *around* it; it does not predict it. **The benchmark covers only the marketplace-enrolled member(s):** when a spouse enrolls in Medicare they drop out of the SLCSP coverage family, so in an age-gap year the benchmark is the **single-enrollee** assumption, not a household-of-two value (household MAGI is unchanged) — couples to the IRMAA overlap below.
- **IRMAA = a 2-YEAR-LAGGED feed-forward, NOT a fixed point** (healthcare doc §4c). Store a **per-filing-unit MAGI history**; in year *t*, look up the bracket from **IRMAA-MAGI[t−2]** and add the surcharge (Part B + Part D) **× the count of spouses currently Medicare-enrolled** — ×1 in age-gap years when only the older spouse is 65+, ×2 once both are; **never a hardcoded ×2** — measured against the still-MFJ thresholds while both are alive, to year-*t* healthcare cost — a deterministic delay carried in state, no within-year circularity. Hard **per-person step-cliffs** (no smooth phase-out): $1 over → the full bracket surcharge. The "danger years" start at 63–64 (a conversion then hits the bill at 65–66). The second-order self-funding loop (a surcharge funded by a withdrawal that bumps MAGI[t→t+2]) is **tiny — MVP may ignore it and document the omission** (OUT-but-disclosed). **Pre-65 and post-65 are NOT mutually exclusive at the household level:** in age-gap years one spouse runs the ACA fixed-point (current-year MAGI) while the other runs this IRMAA feed-forward (MAGI[t−2]), both keyed to the same year's household income — the overlay evaluates each spouse's regime independently per year and sums. **The MAGI[t−2] history must be SEEDED from real inputs:** when the sim's first year is within 2 years of either spouse's age 65, the two pre-sim years' actual IRMAA-MAGI are **required model inputs** — never defaulted to zero, which would silently undercharge the first 1–2 Medicare years (`burned/062` fail-loud).
- **HSA as a 4th account bucket** (healthcare doc §1): triple-advantaged, earmarked medical. Covers out-of-pocket + (**owner 65+**) Medicare premiums tax-free — **NOT ACA marketplace premiums** in the normal case (the trap — qualifies only via COBRA / unemployment; do not let the model overstate "HSA covers healthcare premiums"). **Medicare enrollment ZEROES HSA contributions** (the 6-month Part A retroactive-lookback trap for 65+ who delay Medicare while contributing). The 65+ Medicare-premium privilege is keyed to the **HSA OWNER's age**, not the spouse's. HSA spending is **MAGI-invisible to both calculators ONLY for qualified-medical withdrawals** — capped at the year's qualified-medical cost (a modeled out-of-pocket-medical stream + owner-65+ Medicare premiums). A **post-65 *non-qualified* withdrawal is ordinary income that RAISES both ACA-MAGI and IRMAA-MAGI** (the 20% penalty is waived after 65, the income inclusion is not), so it is **not** a loop-breaking source — the solver must never treat all HSA outflow as MAGI-free (income-laundering). Contributions (pre-Medicare) are MAGI-*reducing*; qualified spending is MAGI-*neutral* — the asymmetry is why the funding-source order changes loop strength. It shares the **one market draw** like every other bucket (contract #2).
- **Couple / death-order interaction** (healthcare doc §4e): on the first death the survivor flips **MFJ → single thresholds (~half)** — but the two regimes flip on **different clocks**: the **ACA-FPL basis flips to single in the year filing status changes** (current-year, immediate), while the **IRMAA side applies the threshold table matching the filing status of the MAGI[t−2] return**, so the single table first bites the IRMAA bill **~2 years after** the first death (the death year itself may still be MFJ) — applying single IRMAA thresholds immediately mistimes the widow-penalty spike. *(Directional until pinned to the SSA primary — exit gate.)* The survivor can then be hit with a surcharge on the *same* income. **Wire this into U1's death-order conditional filter** (it uses the same survivor boundary U2 keys MFJ→single on — no new boundary). **Inflation-index the thresholds when projecting forward** (ACA applicable-% and the first four IRMAA brackets adjust annually; IRMAA top tier frozen through 2027) — static thresholds silently catch more people each year.
- **GOLDEN INVARIANT — reduce-to-spine (contract #3).** The overlay reduces byte-identically to the spine when **healthcare modeling is off** (no ACA premium, no IRMAA surcharge, no HSA bucket — the spine's tax-free, healthcare-free decumulation). Consumes zero draws; shares the one market draw; CRN-safe. The golden Trinity/Bengen cases are never perturbed. It is ON only when a control (P3·U11) or the solver (P4) turns it on; it composes **after** the tax overlay on the shared cash-term seam (ACA premium / IRMAA surcharge are spending the tax overlay's gross-up does not include).
- All numbers live in the **healthcare doc** (folding into §Strand 5), via the constants module, marked directional-until-pinned — the **legislative item is the build-gating one**. The IN/OUT line is **falsifiable**: a health effect is IN iff a per-year conversion/withdrawal dollar moves it through ACA-MAGI (current year) or IRMAA-MAGI (two years forward) under the survivor filter. OUT-but-disclosed: the SLCSP level itself, CSR out-of-pocket richness, state subsidy wraps, the IRMAA second-order loop, SSA-44 appeals (healthcare doc §5).

**Execution note:** Test-first on the **reduce-to-spine invariant** and **CRN stability** (the golden anchors), then the **ACA fixed-point convergence + the explicit cliff branch** (the hardest piece — a constructed just-under/just-over case the naive iterator would oscillate on), then the IRMAA 2-year lag (a conversion at 63 → the surcharge at 65), then the HSA bucket rules. Gate every ACA fixture behind the legislative re-verify before treating it as anything but directional.

**Patterns to follow:** U2's SS-tax fixed-point shape (the ACA fixed-point is its sibling — same iterate-to-convergence, zero draws); U1's CRN seam + the four-bucket single-draw rule; U1's death-order conditional filter (the survivor MFJ→single flip rides it); `archive/do-not-disturb/012` (externally-derived ACA/IRMAA fixtures — compute the expected PTC / surcharge independently); `burned/062` (no in-range default fallbacks — fatal inside the ACA fixed-point and across the cliff branch); `burned/021` (validate-before-mutate — the cliff branch must check legality of the just-over solution before committing, or stage→commit); `burned/027` (pair the reduce-to-spine absence-assertion with a presence companion that the overlay actually moved a premium/surcharge in the ON case).

**Test scenarios:**
- **Golden invariant (the anchor):** healthcare modeling off → the overlay run is **byte-identical (same seed) to the spine distribution**. **Presence companion** (`burned/027`): with healthcare ON (an ACA premium, an IRMAA surcharge, an HSA bucket), the distribution differs and at least one path actually paid a net premium and at least one paid a surcharge.
- **CRN:** two candidates differing only in conversion amount (hence ACA-MAGI / IRMAA-MAGI) consume normals **identical path-for-path** across the 63→65 boundary and the survivor MFJ→single flip (proves zero draws, one shared draw).
- **Two MAGI calculators are distinct:** a case with non-taxable SS yields **ACA-MAGI > IRMAA-MAGI** by exactly the non-taxable SS portion (proves they are not the same number); qualified Roth / HSA spend move **neither**.
- **ACA fixed-point + cliff branch (the hardest test):** a constructed case just under the 400% FPL cliff converges in ≤3 passes; a case where the premium-funding withdrawal would push MAGI **across** the cliff triggers the explicit branch (just-under vs just-over funding solutions, the cheaper picked) and does **not** oscillate; a **high-spend case with no feasible under-cliff solution** returns the over-cliff (PTC=0, linear) branch without looping; a **sub-benchmark enrolled plan** yields net premium ≥ 0 and PTC ≤ enrolled premium (never cash back); **no in-range default** stands in for an unconverged value (`burned/062`).
- **The cliff inverts a ranking (the load-bearing test, D6):** a conversion that looks beneficial under a tax-only model becomes net-negative once it knocks the household over the 400% FPL cliff (the >100% effective-rate case from the healthcare doc) — proving the omission *inverts which strategy wins*, not just blunts a delta.
- **IRMAA 2-year lag:** a conversion at age 63 raises IRMAA-MAGI[63], and the surcharge appears in the year-65 healthcare cost, not at 63 or 64; a constructed step-cliff case shows $1 over a bracket → the full bracket surcharge (hard cliff, no phase-out).
- **IRMAA enrolled-count + seeding:** an age-gap couple (one 66, one 63) pays IRMAA **×1** in the overlap year against the MFJ threshold (not ×2), while the 63-year-old simultaneously receives ACA-PTC — both off the same household income; and a sim starting at age 65 with seeded MAGI[63]/MAGI[64] produces the correct first-year surcharge, while an unseeded near-65 start **fails loud** rather than charging $0.
- **HSA bucket rules:** HSA covers out-of-pocket + (owner 65+) Medicare premiums tax-free but **NOT** an ACA marketplace premium in the normal case (the trap); Medicare enrollment **zeroes** the contribution from that month (the 6-month Part A retro case produces excess); the owner-65+ Medicare-premium privilege does not apply when only the spouse is 65+; a **qualified-medical** HSA withdrawal is MAGI-invisible to both calculators, but a **post-65 non-qualified** HSA withdrawal raises **both** MAGIs (the laundering negative test — general spending cannot be routed MAGI-free through the HSA).
- **Survivor flip:** on the first death the survivor's IRMAA and ACA-FPL thresholds halve (MFJ→single), riding U1's death-order filter — a surcharge can land on the survivor at the same income that was clear under MFJ.
- **Enhanced-subsidy toggle:** flipping the scenario toggle removes the 400% FPL cliff and changes the ACA fixed-point's result (the calculus flips) — proving the cliff is a toggle-able scenario, never hard-coded.
- Edge (R19 on health inputs): a missing/degenerate SLCSP benchmark is rejected (engine half; the inline-surfacing UI half is P3·U11); a negative FPL% is rejected.
- Edge (constants single-source + legislative gate): every ACA/IRMAA/HSA figure comes from `src/engine/constants/`, marked directional; the ACA legislative item carries the `reVerifyEveryBuild` flag and no ACA fixture is treated as golden until re-verified.

**Verification:** The overlay reduces byte-identically to the spine when off (and demonstrably moves a premium/surcharge when on); CRN holds across the 63→65 boundary and the survivor flip; the two MAGI calculators are provably distinct; the ACA fixed-point converges and the cliff branch does not oscillate; the cliff-omission inversion is demonstrated (the case that justifies D6); IRMAA is a correct 2-year lag with hard cliffs; the HSA bucket honors the ACA-premium trap, the Medicare-zeroes-contribution rule, and owner-age keying; every number routes through the constants module marked directional, with the ACA legislative item build-gated.

---

- [ ] **Unit 4: Encrypted local store + key lifecycle + recovery/export/restore** *(the trust unit)*

**Goal:** The trust layer — derive an AES key from a passphrase, encrypt the model at rest in IndexedDB, lock/unlock, and provide the recovery-phrase + encrypted-export/restore mechanism. **This unit makes R16's promise provable** and hands the passphrase-strength enforcement reason forward to P2·U8.

**Requirements:** R15, R16, R17, R18; R19 (store's no-falsely-confident-restore half).

**Dependencies:** U0. (Independent of U1–U3 — **parallelizable**.)

**Files:**
- Create: `src/crypto/kdf.ts` (PBKDF2-600k + HKDF), `src/crypto/cipher.ts` (AES-GCM), `src/crypto/recoveryPhrase.ts`, `src/store/db.ts` (idb), `src/store/session.ts` (key + decrypted model in memory, lock/unlock), `src/store/backup.ts` (export/restore)
- Create/extend: `src/shared/model.ts` (the persisted record shapes + the `schemaVersion` field — shared with U1/U2/U3; the **"never depleted" persisted sentinel** decision lands here)
- Test: `src/crypto/__tests__/{cipher.roundtrip,kdf,recoveryPhrase}.test.ts`, `src/store/__tests__/{session,backup}.test.ts`

**Approach:**
- `importKey('raw', utf8(passphrase), 'PBKDF2')` → `deriveKey({PBKDF2, salt, iterations:600000, SHA-256}, …, {AES-GCM,256}, extractable:false, ['encrypt','decrypt'])`. **Keys live in memory only — never persisted unwrapped** (passphrase-each-session model). Params live in the constants/`findings §Strand 2` — do not alter.
- **The 600k-iteration derivation must never freeze the UI (the rendered-pending-state contract).** 600,000 PBKDF2-HMAC-SHA256 iterations is a ~0.5–1.5s computation on a mid-tier phone — and it runs on **every** unlock, passphrase-set, recovery-unlock, and re-derive on lock. `crypto.subtle.deriveKey` is async and most browsers run the work off the main thread, but that is **implementation-dependent, not guaranteed by spec**, so the UI contract does not rely on it: every derivation renders an explicit calm **"unlocking… / securing…" pending state** owned by the lock/unlock state machine (and P2·U8's passphrase surface), and a test asserts no silent multi-hundred-ms unresponsive window. If the U4 spike measures real main-thread jank during derivation on the reference device, the fallback is a **dedicated crypto worker** (a message boundary distinct from the engine worker) — named here so it is a decided fallback, not an implementation surprise.
- **Key hierarchy (a planning EXTENSION, not a §Strand 2 derivation).** A stable random **data key (DK)** encrypts the model; the passphrase-derived key and the recovery-derived key each **wrap DK independently**. **Why (the load-bearing reason):** there is exactly **one** copy of the model (encrypted under DK), so the recovery path can **never** recover a *stale* copy. The flat alternative — store the model twice, once per credential — risks the two copies drifting (a save updates the passphrase copy but the recovery copy lags), so the surviving spouse could restore an out-of-date plan; that is the worst failure for a product whose reason to exist is the survivor case (R17/R18). DK also makes the common save path **one** encryption, not two, and makes a passphrase change a cheap re-wrap.
- **DK handling (keep the key non-exportable).** DK = **raw random bytes** (`crypto.getRandomValues`, 256-bit) imported as a `CryptoKey` with `extractable:false`. "Wrapping" DK = **AES-GCM `encrypt()` of the raw DK bytes** under each credential key — **not** WebCrypto `wrapKey()`, which would require DK `extractable:true` and let any injected script `exportKey('raw', dk)` and exfiltrate it, bypassing PBKDF2 entirely. DK encrypts exactly one plaintext stream (the model) with random 96-bit IVs; the per-vault save count stays far below the 2³² random-IV safe bound.
- **The at-rest store schema holds THREE record types:**
  - **model:** `{ iv(12B), ciphertext }` — the model encrypted under DK (no KDF salt; DK is raw key bytes).
  - **passphraseWrap:** `{ salt(16B), iv(12B), wrappedDataKey }` — DK wrapped under the passphrase-derived key.
  - **recoveryWrap:** `{ salt(16B), iv(12B), wrappedDataKey }` — DK wrapped under the recovery-derived key.
  Each wrap carries its **own** fresh salt + IV. The plaintext-before-encrypt begins with an integer **`schemaVersion` (= 1) as its first field**, written from v1; decrypt reads `schemaVersion` before any other field and branches/refuses on an unknown version (the migration enabler — it must exist from v1 or later migration is impossible; the decrypt branch anticipates the full ladder — **v1→v2** (Phase 3: buckets / budget / vintage stamps) **→v3** (Phase 4: `seedB` / chosen-goal / saved-recommendation)).
- **The "never depleted" persisted sentinel — DECIDED HERE, before the schema locks** (`archive/do-not-disturb/009`): `Infinity`/`NaN` silently become `null` through `JSON.stringify`/IndexedDB. The engine's "portfolio never depleted across the horizon" outcome must persist as an **explicit out-of-domain integer sentinel or a tagged-union discriminant** (e.g. a defined `{ kind: 'never-depleted' }` variant), **never `Infinity`/`NaN`/`null`**. The decode path maps the sentinel back to the engine's never-depleted state; a bare `null` is treated as corruption, not as never-depleted. This pins the serialization the U1 outcome-state set round-trips through.
- **Fresh IV + salt on EVERY encryption and re-encryption.** A fresh 12-byte IV per encryption, never reused; every re-wrap (passphrase change, recovery re-wrap, restore-sets-new-passphrase) mints a **fresh 16-byte salt AND a fresh 12-byte IV** — none carried over.
- **Atomicity.** Every multi-record mutation (first save's model + both wrap records; a passphrase re-wrap that writes the new wrap and deletes the old) commits as **one IndexedDB transaction** — all-or-nothing, leaning on IndexedDB's auto-abort/rollback so a tear never leaves a half-updated vault (a partial first save that wrote the passphrase wrap but not the recovery wrap would strand the survivor — catastrophic, discovered only at recovery time). A save reports success **only after** the write commits. **Expose a model-only re-encrypt entry point** — re-write the model record under the **existing in-memory DK** with a fresh IV, **wraps untouched** (DK is unchanged by a schema migration; re-minting wraps is reserved for credential ops) — the single-record degenerate case of the transaction, so the P3 **v1→v2** and the P4 **v2→v3** vault upgrades inherit this atomicity instead of re-implementing it.
- **Honest lock (no zeroization overclaim).** JS/WebCrypto cannot byte-scrub a `CryptoKey` handle or a string. On lock the session **drops its only references** to the key(s) and the decrypted model (and any derived copies), making them GC-eligible and requiring a fresh passphrase-derived key for every subsequent decrypt/encrypt. We claim *reference-drop + mandatory re-derive*, **not** cryptographic zeroization — so no downstream R15/R16 copy overstates it.
- Wrong passphrase → GCM auth-tag failure surfaced as a calm "that passphrase didn't unlock this", not a stack trace. **No artificial lockout** — unlimited retries (in a no-server model the real protection is PBKDF2-600k + the encrypted blob; a client-side lockout adds zero real security while breaking legitimate access). **The meaningful attacker is offline:** they have extracted the IndexedDB blob and are running PBKDF2 on their own hardware, never clicking *Unlock* in the browser — so no UI-layer lockout (client or server) defends against them, only KDF hardness does. This is named explicitly so a future contributor does not "harden" by adding a server-enforced rate-limiter, which would require a server and break the local-first architecture without improving security. Distinguish "wrong passphrase" (structurally valid record, GCM fail → retry) from "data damaged" (record absent/truncated/malformed → point to restore).
- **Recovery phrase spec:** entropy source = `crypto.getRandomValues` **only** (the U0 weak-RNG lint extends to `src/crypto/**`); a named, vetted wordlist (**BIP-39 English, 2048 words**); **12 words = 128 bits**. **Recovery-key derivation (pinned):** the phrase already carries full 128-bit entropy, so derive the recovery-wrapping key with **HKDF-SHA-256** (key-*expansion*, the correct primitive for high-entropy input — PBKDF2-600k's password-*stretching* adds nothing against a 128-bit secret) with **IKM = the phrase bytes, salt = the 16-byte `recoveryWrap` salt, and a pinned `info` string for domain separation** (`info = utf8('the-back-nine/recovery-wrap/v1')`, the `/v1` enabling future rotation). The `info` parameter is **mandatory, not optional**: it is what prevents a key collision if a second HKDF use over the same phrase is ever added, and is pinned as a constant in `src/crypto/kdf.ts` alongside the PBKDF2 params. The phrase is generated **once** and is **immutable** for the life of the data in the MVP; rotation is out of scope. **Display threat:** the phrase is a full credential with a screenshot/phishing threat model — the P2 display must instruct "store offline, don't screenshot" and prefer a print/PDF presentation over selectable text. **Non-selectability is a UX defense (casual shoulder-surfing, accidental clipboard), NOT DOM isolation** — a11y-tree text is readable by OS UI-Automation and browser extensions regardless — so the real mitigation is minimizing exposure (a view-then-dismiss display) and not persisting the phrase in the JS heap after the display step; no downstream copy may present non-selectability as a cryptographic control (the a11y caveat is owned by P2·U8).
- Recovery + export + restore are one mechanism: **export = encrypted blob** carrying an explicit **format-version field** from day one; restore = import file + phrase → set new passphrase (re-wrap, fresh salt+IV). **Mandatory export at first save** — the first Save flow produces/confirms the recovery phrase and the encrypted export file as part of reaching "saved"; no "remind me later" bypass (that path is where users get burned; the export pair is the real durability backstop).
- **In-place recovery unlock (the plain survivor case).** When the local vault is **intact** but the passphrase is unknown/forgotten, the surviving spouse unlocks **in place** — derive the recovery key from the phrase → unwrap DK from `recoveryWrap` → decrypt the model → prompt to set a new passphrase (re-wrap with fresh salt+IV). This is the survivor's primary door; file-restore is only for the device-wiped case. **The set-a-new-passphrase step is a MANDATORY BLOCKING gate** — **no write path is reachable until the new `passphraseWrap` is re-minted**. Otherwise the survivor could save data while the on-disk `passphraseWrap` still encodes the forgotten passphrase, silently degrading the vault to **recovery-phrase-only** access (if the phrase is then lost, the data is unrecoverable) — the worst outcome for the survivor product.
- **The seam, enforced structurally (contract #4) — one predicate, stated once.** The persistence API is **gated by construction** on the **conjunction**: a writable store handle requires **a derived session key AND a current (or in-session-just-minted) `passphraseWrap`**. Stating it as "writable iff a key is derived" alone is the trap the recovery-unlock path exposes — recovery-unlock derives a key and decrypts the model yet writes must stay blocked until the new `passphraseWrap` is re-minted (above), so "a key exists" is true while "writable" must still be false. Binding both clauses into the **same** seam (not a second hand-enforced rule layered on top) is what makes the survivor-stranded catastrophe structurally impossible: the same gate that refuses an unkeyed write refuses a recovery-unlocked write before the new `passphraseWrap` exists. There is no reachable cleartext/unkeyed/stale-credential write path. (The P1 evidence R15/R16 require, independent of the P2 intake test. The in-memory model is additionally guarded by U0's CSP.)
- **Durability is best-effort + backstopped.** The encrypted write commits **before** `navigator.storage.persist()` is attempted; `persist()`'s boolean is **advisory only** and never rolls back a successful save. `store.load()` returns a clean **"no vault present"** result (not an exception) for both first-run and post-eviction (Safari ~7-day idle eviction) → restore is a **first-class entry point**. A `persist()` denial or `QuotaExceededError` surfaces a calm "use your export" state, never a false "saved".
- **The no-key-leak gate is single-sourced** (`burned/063`): the security assertion imports the **same** record-shape definition the serializer writes, so adding a field can't silently bypass the "no raw key material" check.
- **The passphrase-strength enforcement reason is U4's, the gate is seated in P2·U8 — name the seam handed forward.** PBKDF2-600k is the **only** brute-force defense, so a weak passphrase voids the entire at-rest story. U4 hands forward a **mandatory min-entropy gate at passphrase-set** (no weak-passphrase bypass) to P2·U8, which owns the UI estimator (`zxcvbn-ts`) + the calm inline feedback. U4 states the contract: no `deriveKey` from a passphrase below the threshold, which is **PINNED at plan level — `zxcvbn-ts` score ≥ 3 AND an independent length ≥ 12** (the library is pinned at U0; this is the sole at-rest defense, so it is a security-grade decision, not deferred). P2·U8 owns only the UI estimator and the calm inline feedback, not the threshold or library choice.
- **Multi-tab (minimal guard):** serialize vault writes with `navigator.locks` (Web Locks); single-active-vault. **The store also EXPOSES the `no-write-in-flight` signal (the interface U0 declares and P2·U8 consumes) — U4 is its producer:** the session/store API surfaces an `isWriteInFlight()` query (true during an in-flight IndexedDB transaction, false on commit), so U0's PWA update-acceptance handler can defer `skipWaiting`+reload until a write commits and never tear a mid-encrypt vault. Without U4 producing it, U0's interface and P2's wiring have nothing to read — so it is owned here, with a test (below). **Concurrent-unlock semantics (named, not left undefined):** Web Locks serializes individual write transactions but does **not** stop two tabs from each unlocking and racing a save (the second tab's write could silently clobber the first's edits — a lost save, catastrophic for the survivor). A **`BroadcastChannel` single-active-session detection** closes this: a second tab attempting to unlock an already-unlocked vault is told *"this plan is open in another tab"* and stays **read-only** rather than opening a second writable session. Fuller multi-tab coordination (live cross-tab model sync) is explicitly out of MVP scope; single-active-**writer** is not.
- **Preconditions:** a passphrase change is available only while unlocked or when supplying the recovery phrase; a lock requested during an in-flight write is queued until commit then drops the key + model; a lock requested during an in-flight **engine run** lets the run finish but **discards its result** (never rendered post-lock).

**Execution note:** Test-first on the crypto round-trip, the wrong-passphrase path, the atomic-write/tear paths, the in-place recovery unlock, the mandatory-new-passphrase gate, and the restore failure paths — these are the trust guarantees.

**Patterns to follow:** Greenfield for the monorepo; `idb` (jakearchibald) for IndexedDB; `findings §Strand 2` is the spec for params + the recovery model (NOT the DK indirection — that's the roadmap's extension); `burned/063` (single-source the no-leak gate); `archive/do-not-disturb/009` (decide the JSON-persisted "never depleted" sentinel before the schema locks).

**Test scenarios:**
- Happy path: encrypt model → persist → reload → unlock with passphrase → byte-identical model (including the **seed** field — proving U1's determinism survives the round-trip and reproduces the identical headline; and the **never-depleted sentinel** round-trips to the engine's never-depleted state, not `null`).
- Error path: wrong passphrase → decrypt fails cleanly (GCM auth-tag), no partial/garbage model; Nth wrong attempt still permits N+1 (no lockout); a correct passphrase then succeeds.
- Error path: truncated/corrupted ciphertext → GCM rejection surfaced as the calm "data damaged" state (distinct from "wrong passphrase"), never a crash.
- Edge: passphrase change re-wraps DK without invalidating the recovery phrase; the new wrap record's salt **and** IV both differ from the old.
- Edge: IV/salt freshness — re-encrypting the same model twice yields a different IV **and** salt **and** ciphertext.
- Edge: a blob whose plaintext `schemaVersion` is unknown/newer → calm "saved by a newer version" state, never a mis-parsed model.
- Error path (atomicity): a write aborted mid-transaction leaves the prior decryptable blob intact; an interrupted first save leaves **no** partial vault; an interrupted passphrase swap leaves the **old** passphrase still working (never a state where neither old nor new unlocks).
- Error path (model-only re-encrypt): the model-only re-encrypt entry point re-writes the model record under the existing DK with a fresh IV and **wraps untouched**; an interrupted re-encrypt leaves the prior model decryptable (the P3 v1→v2 upgrade inherits this).
- Error path (durability): a forced `persist()` = false / `QuotaExceededError` → the save reports failure with the calm "use your export" state, in-memory model intact, no partial record.
- Edge (no vault): `load()` against an empty/cleared object store → clean "no vault present" state with the restore path reachable, no throw (first-run and post-eviction alike).
- Integration: **in-place recovery unlock** — passphrase forgotten + **intact** vault → the recovery phrase alone unwraps DK, decrypts the model, and the **mandatory new-passphrase gate blocks every write path until the `passphraseWrap` is re-minted** (a planted write attempt before the new passphrase is set is refused); afterward the recovery phrase still works.
- Integration: export encrypted blob → wipe IndexedDB (models Safari eviction) → restore from file + recovery phrase → set new passphrase → original model recovered; and export → change passphrase → restore the **earlier** export with the unchanged phrase still succeeds (phrase immutability).
- Error path (restore): valid file + **wrong** recovery phrase → calm "that recovery phrase doesn't match this backup" (GCM fail); corrupted/non-Back-Nine file → calm "this backup file looks damaged" (format-version checked **before** decrypt), never a stack trace.
- Security (negative pairing — proven, not believed): recovery phrase **without** the export file recovers nothing *from a file*; export file **without** the phrase recovers nothing.
- Security: **DK is non-exportable** — while unlocked, `exportKey('raw', DK)` is not possible (imported `extractable:false`; "wrapping" uses `encrypt()` of raw bytes, never `wrapKey()`).
- Edge: lock mid-session drops **both** the in-memory key and the decrypted model (asserted behaviorally — no method returns/uses the prior key or model — **not** by inspecting wiped bytes); a lock during an in-flight engine run discards that run's result.
- Security (passphrase-strength seam): `deriveKey` is unreachable for a passphrase below the **pinned floor** (`zxcvbn-ts` score ≥ 3 AND length ≥ 12; the UI estimator is P2·U8) — a planted weak passphrase (strong-but-short, or long-but-common) cannot reach a write path.
- Edge (no-write-in-flight producer): the store's `isWriteInFlight()` reads **true** during an in-flight transaction and flips **false** on commit; a simulated service-worker update event fired during a stub write **defers** the reload until commit (the signal U0's handler + P2·U8's integration test wire to — proven at U4's own layer here).
- Edge (concurrent unlock): a second tab unlocking an already-unlocked vault is detected via `BroadcastChannel` and is **read-only** — a planted write from the second session is refused, so it cannot clobber the first tab's unsaved edits.
- Edge (KDF pending contract): a `deriveKey` call renders the "unlocking…/securing…" pending state and leaves no silent unresponsive window (asserted behaviorally via the state machine, not by timing the off-thread work).
- Security: the store exposes **no** write path reachable before a key exists (the P1 home of the magic-moment-first seam).
- Security (write-gate conjunction): a session with a derived key but **no current `passphraseWrap`** (the recovery-unlocked state before re-mint) is refused a write by the **same** seam that refuses an unkeyed write (contract #4 — one predicate, both clauses).
- Security assertion: no **raw/unwrapped** key material ever appears in any IndexedDB record — only the three defined record types, asserted against the single-sourced record-shape definition (`burned/063`).

**Verification:** A full save→reload→unlock and an export→wipe→restore cycle both reproduce the model (including the seed and the never-depleted sentinel); an in-place recovery-phrase unlock of an intact vault recovers the current model and **cannot write until the new passphrase is set**; the model-only re-encrypt is atomic; an interrupted write leaves the prior vault intact; a `grep` of the persisted store shows no **unwrapped** key material — only the `wrappedDataKey` blob, which **is** the raw DK bytes AES-GCM-encrypted, recoverable **only** with the passphrase, so the at-rest protection is exactly **PBKDF2-600k strength and no more**. `extractable:false` is stated plainly as preventing **script** exfiltration of an *unlocked* DK only; it provides **no** additional protection against an offline attacker who already holds the blob (that attacker brute-forces the passphrase directly, never touching `exportKey`) — the sole at-rest boundary is the AES-GCM ciphertext protected by the PBKDF2-600k derivation, which is why the passphrase-strength floor is the real security boundary. *(This is the evidence R16 requires before any at-rest privacy claim is honest.)*
