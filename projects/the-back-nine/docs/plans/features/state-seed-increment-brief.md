---
title: "The state-carrying seed increment — build brief"
doc-type: build-spec
status: shipped
---

# The state-carrying seed increment — build brief

**Status: SHIPPED 2026-07-16** — built at `6009be12`, ultramode-folded the same evening at `350319a8`, both CI-green by explicit run id, and cold-read the same night (panel wf_fc34db61-46c, 78 agents; four cards PILOT-CLEARED, zero fixes from the walk). Per-unit build status lives in the roadmap's You-Are-Here table (the **State-tax** row), never here. Pilot-decided under Briggsy's full-decision-rights GO; no council convened — no one-way doors, no framing forks: every deliverable was DEV-only or a test. The parent law is [`state-tax-build-spec.md`](state-tax-build-spec.md) (ratified 2026-07-15) — where this brief is silent, the spec + house idioms govern. The requirement it closes was filed at `docs/caddie/cold-read-log.md:213`: the four unwalkable priced faces ride "a state-carrying seed increment (seed + walk targets + fit arms + outcome pins) before their first cold read" — marked ✓ CLOSED on that line the day this shipped.

## Mission

Made walkable, fit-pinned, and engine-outcome-pinned:

1. the PRICED verdict affirmation + narrowed-residual SET (the NC/PA/FL sentences — **both route mounts**)
2. the PRICED panel-row face (the answered `retirement-state` seat)
3. the CVD selected-picker face
4. the `stalenessStateTax` gate note

Plus the cards' noted coverage artifact: the ANSWERED-`'elsewhere'` face (the unpriced direction the 2026-07-15 bundle never showed the lenses).

The increment's own discipline caught TWO real defects before exposure, each of which became a filed insight: the aged-vault plant shape ([085](../../insights/085-a-doctored-dev-fixture-must-survive-the-same-fail-loud-gates-as-an-organic-household.md) — the F2 supersession below) and the AssumptionPanel spend row's false "isn't priced yet" on a priced household ([086](../../insights/086-splitting-a-copy-key-into-gated-variants-orphans-every-renderer-not-re-pointed-in-the-same-commit.md) — the insight-080 class one layer up; the panel now mirrors the intake's draft-keyed predicate, hoisted at the fold to one home, `spendHelpKeyFor` in `intakeMap.ts:981`).

## Settled forks (as shipped; a later deviation needs a dated supersession block in this file)

**F1 — five new seeds; ZERO edits to existing seeds.**
- `nc` / `pa` / `fl` / `elsewhere` = clones of `retiredOnTrack` (66/65 both retired — the state clause renders ONLY inside the `medicarePricedNote` block, `ConfidenceStatement.tsx:445-458`, so the household must be all-65+) with `retirementState: 'NC' | 'PA' | 'FL' | 'elsewhere'` respectively.
- `datenc` = clone of `stillWorkingAllMedicare` (the `date65` seed) + `retirementState: 'NC'` — the date route's affirmation mount (`FuckOffDate.tsx:406-419`) rides its OWN producer (`dateStatePriced`, `intakeMap.ts:798`); insight 080's lesson: the second producer gets its own live witness.
- Existing seeds stay **byte-identical**: `?seed=retired`'s UNANSWERED panel-row face is a walked, regression-live surface (Card 2, chair fix #1 — `assumptionStateUnsetNote`). `date65`'s purpose (the insight-080 no-false-unpriced witness) is pinned; never repurpose it.
- Registration: draft const + `DEV_SEEDS` entry (`devSeeds.ts` — the only dispatch table); the registry auto-iterates into the validator-acceptance test.

**F2 — the stale variant is `?vault=statestale` (base seed `nc`), riding its OWN light doctor.**
- `AGED_PLANTS` carries `statestale: { base: 'nc', doctor: doctorStateStaleVault }`; the light doctor is `doctorStateStaleVault` (`devSeeds.ts:1261`). It moves TWO things and nothing else: `savedAt` −150d (a mid-Feb-2026 save, the same calendar year as the UNTOUCHED `startCalendarYear` 2026, so the save is coherent) and the household's own state profile diverged one rate-step back via `agedStateProfile` (`:1214`), built on the `taxVintageDetail` "pre-OBBBA dev fixture" precedent that `doctorStaleVault` still carries (`:1190`). Tax/health/blend stamps stay FRESH (a 2026 save carries 2026 vintages). `stale`/`datestale` outputs are byte-identical to before — their bases carry no state, and that is pinned.
- **The plan's original shape was superseded in flight on 2026-07-16, and the reversal is load-bearing for anyone tempted to re-route this plant.** F2 first sent `statestale` through the FULL `doctorStaleVault` (−2y `startCalendarYear` → 2024). S2's insight-033 live drive caught it refusing to render: the engine's own priced-state lower bound (`simulate.ts:640-643`, from the 2026-07-15 ultramode fold — its comment names "an aged dev plant" as the exact anticipated caller) correctly REFUSES a priced-NC household whose year-0 precedes NC's earliest rate row (2026), so the affirm recompute demoted to the R19 calm indeterminate and no verdict hero rendered. The engine was right; the plant was organically impossible (no 2024 save could carry `retirementState` — the field shipped 2026-07-15). The state-stamp divergence logic therefore lives in the light doctor alone (one home). Both halves of the guard are now CODE, not prose: `doctorStaleVault` THROWS on a priced-state base (`devSeeds.ts:1170`) and `agedStateProfile` THROWS on a schedule-less priced state (`:1219` — FL passes `isPricedState` yet has a null rate schedule and can never diverge, the fold's 6-lens convergence). Insight 085 is this defect's write-up.
- The clock's firing predicate is `staleness.ts:470-476` (saved stamp present ∧ household state priced ∧ `stateProfileKey` diverges). FL never fires (constitutional $0) — that's why the base is `nc`.
- The plant hydrates with a **CLEAN badge** (the drifted-vault clean-badge law) AND fires `controls.stateTaxMoved` → the `stalenessStateTax` note (`copy.ts:1288`) at the gate, in ISOLATION — every other clock stays quiet, which is what makes face #4 a clean cold read. The final hero renders the NC clause on a real engine-proven verdict; it read BORDERLINE at ship, and reads on-track since S.L. 2026-41 cut NC's 2027+ rates (see F3).

**F3 — purpose pins (`devSeeds.test.ts`; the house idiom; insight 081 — pin the producer's OUTPUT).** Every pin reads the producer's output and the engine-proven RELATION to the state-absent twin (`DEV_SEEDS.retired`, the same household minus the state term, same seed ⇒ same CRN paths), never a hand-typed golden dollar.
- Every new seed pins `pricedStateForRun(draft)` === its state (or `undefined` for `elsewhere`) via the REAL producer (`intakeMap.ts:808`) — never draft truthiness.
- `nc`: the engine INEQUALITY vs the twin (the `'order'` seed idiom, `devSeeds.test.ts:403`) — NC prices a strictly HIGHER lifetime tax, the flat rate biting both the pretax draw and the realized gains on the taxable bucket the household forms from reinvested RMD surplus. **The band crossing this seed was minted for is gone, and it was not a regression.** Until 2026-08-02 the NC drag pushed the twin's on-track down across the edge to BORDERLINE (0.8425 vs 0.8585); S.L. 2026-41 § 44.1(a) cut NC's 2027+ rate from the held-forward 3.99% to 3.49/3.24/2.99, a real enacted tax cut, so the drag no longer reaches the edge and both twin and `nc` land on-track. The inequality is the load-bearing pin and still bites; only its magnitude moved. The standing C3 law ("re-tune the account knob on drift, never loosen the pin") deliberately CANNOT be applied here: `ncAffirmation` is a pure spread of `retiredOnTrack` with one field changed and the twin IS `DEV_SEEDS.retired`, so giving `nc` its own accounts to force a crossing would destroy the same-household-one-difference invariant that makes the comparison honest. Restoring a band-crossing state face needs a NEW purpose-built seed with its own state-off twin — filed for Briggsy's call, not silently invented.
- `pa`: **the working-memory guess of byte-identity was REFUTED by the engine, and the true relation is what shipped.** At qualified age (both 65+) PA exempts the IRA withdrawal and SS, but it taxes the realized capital gains on the taxable bucket at 3.07%, so PA lifetime tax sits slightly ABOVE the twin — a small non-zero state tax with survival byte-identical and the verdict unmoved. That makes the shipped "usually a small piece" copy engine-honest rather than a guess.
- `fl`: PRICED (`pricedStateForRun === 'FL'`) yet BYTE-IDENTICAL to the twin — `stateIncomeTax` early-returns a structural literal 0 on the null rate schedule, so the "no state income tax — nothing to add" affirmation ships as an honest fact, not an unbuilt-state omission.
- `elsewhere`: `pricedStateForRun === undefined` (an explicit roster member, a persisted fact distinct from never-asked absent, but not in `PRICED_STATES`) and byte-identical to the twin — the reduce-to-spine MEMBERSHIP witness (spec S2.5).
- `datenc`: the async date-route pin (`devSeeds.test.ts:569`, on the provisional-tier / 120s-timeout idiom the `date65` arm established at `:197-245`), `dateStatePriced` === `'NC'`, the route-aware union agreeing, and a real crowned date.
- `statestale`: a pure staleness-reader test (injected epoch day) proving the doctored vault fires `stateTaxMoved` in isolation, the clean-badge hydrate arm, and — the arm insight 085 says was missing — an ENGINE-ACCEPTANCE pin: hydrate → `buildSpineParams` → the validator accepts → a real resolved `outcomeState`, coupled by construction to the `nc` arm's expectation.

**F4 — fit arms (`e2e/vertical-fit.spec.ts`; house MUSTs: `gotoSeedFinal`, presence companions, one-visible-disclaimer, density tier, `WALK_FLOOR`).** All shipped as specified; the gate grew 17 → 28 arms in this increment. The PA and FL clauses are pinned at BOTH laptop tiers (`REAL` 1536×791 and the fit-law floor `TIER` 1280×800, `reviewSurface.ts:16`/`:20`); the NC, `datenc` and `statestale` clause pins are REAL-tier only (the NC frame itself still rides the matrix's 3-viewport sweep).
- `nc` joined the `SPINE_SEEDS` matrix (`{seed: 'nc', medicareNote: true}`) → the auto 3-viewport set; plus a residual-text pin (contains "North Carolina") at the REAL tier. Its state clause is OUTCOME-independent by construction (`statePricedNote` reads `pricedStateForRun` off the built params, never the verdict), which is why the rate cut that moved this seed's verdict left every arm green.
- `pa`, `fl`: bespoke arms at BOTH laptop tiers (1536×791 + 1280×800): `assertResolvedSpine` + frame fit + their residual text ("Pennsylvania leaves most retirement income untaxed" — the LONGEST variant, the wrap-risk arm; "Florida has no state income tax") + snug-leading where the house asserts it.
- `elsewhere`: one REAL-tier arm — the unpriced `verdictMedicareResidual` monolith's distinctive clause ("a real yearly bill") still rendering, each of NC/PA/FL negatively pinned absent, frame fit.
- `datenc`: one REAL-tier date-route arm — the fod trio presence + the affirmation text + the ORDER contract (graphs → in-frame disclaimer → doors LAST).
- `statestale`: a vault arm on the `?vault=stale` block shape (`:1176`): gate CTAs in-frame + the ISOLATION pinned by COUNT — `.reentry-notes p` has exactly ONE note and it is the `stalenessStateTax` line by name, with each distinctively-worded sibling clock pinned dark (count is the airtight proof; the federal-tax wording is a case-insensitive substring of the state note, so a federal negative would be fragile) + affirm → the final-tier frame law, the priced residual naming North Carolina on the writable stale return and not only on fresh `?seed=nc`.
- Mutation-proofing: three planted mutant families (state-clause swap → red; note suppression → red), red then reverted, recorded in the commit body. **Never `git checkout --` to revert a planted mutant on a dirty tree — Edit only.**

**F5 — caddie walk targets (`e2e/caddie-walk.spec.ts`; "increment 5" in the walk's own header and in the caddie SKILL's target list).**
- `seed:nc|pa|fl|elsewhere|datenc` ride the existing `walkSeed` grammar (landing + door walk — the Assumptions-door capture IS the priced/answered panel-row face).
- `vault:statestale` rides `walkVaultReturn` and joined the doors condition (`caddie-walk.spec.ts:992-1000` — the gate note is the target); it also captures the update arc.
- The CVD selected-picker face: `walkIntakeFork` picks NC and captures the PICKED picker as a device-scale crop AND under each CVD arm via `captureCvdRegion`. The read law held: the selected segment stays distinguishable in EVERY arm without hue (the weight+fill+ring redundancy, `intake.css:367-372`). The fold re-pointed the fork's NC pick onto the pick helper — the raw `check({force})` was the sr-only 1px phone-flake idiom that helper exists to avoid.
- The walk never runs concurrently with the full suite.

**F6 — doc sync (the stats-single-source law).** Landed in the same commit: TODO.md's dev-seed header block, README, the roadmap row, and the caddie SKILL's known-targets list, with `verify:doc-stats` green.

## Ship gates (all run, all green)

`pnpm typecheck` · `pnpm lint` · full `pnpm test` · fresh `pnpm build` THEN `pnpm verify:bundle` (insight 057 — never a stale dist; the DCE proof held: the dev passphrase grep in `dist/` stayed 0 and the new seed keys are absent from dist) · `pnpm verify:fit` · `pnpm verify:doc-stats` · `verify:aca` / `verify:state-tax` untouched-green. Committed, pushed, **CI confirmed green by explicit run id.** The `/ultramode-code-review` fold followed at `350319a8` (wf_01354175-291, 31 agents, 11 raw findings with 2 refuters each): five dispositions folded — the two fail-loud throws of F2, the `spendHelpKeyFor` single home, the narrowed −150d coherence comment, and the fork walk's pick helper — with two inflated claims REFUTED and thrown out (the "silent drift ships false help" claim dies on both surfaces' own truth-table tests; the "−150d renders incoherent" claim dies on the elapsed line's <365d suppression). The Caddie pre-walk closed the batch (wf_fc34db61-46c, 78 agents, bundle `temp/caddie/state-faces-prewalk/`, logged at `docs/caddie/cold-read-log.md:183`): four cards PILOT-CLEARED, zero fixes. Per the standing rule, dialect findings STAPLED to O14 (the "tighter" hedge-word ambiguity, twice refuter-confirmed on `nc` and `statestale`; PA's no-dollar "usually a small piece" as rule-38 detail-hunger) and the `elsewhere` uncapped-grade framing question was filed as O15 for Briggsy — not fixed as riders.

## Landmines (carry-forward, binding)

- **Insight 079:** the vault plant splices the disk's own bytes over fabricated fields — `statestale` rides the EXISTING splice machinery unchanged. Its five unit arms (`devSeeds.test.ts:599-731`) pin the state-stamp divergence, the isolation, engine acceptance, the clean badge and the two throws; the −150d `savedAt` splice itself is NOT pinned by any assertion (the `datestale` −760d splice is, at `:312`). A future re-tune of the −150d anchor moves silently — pin it if that anchor ever carries weight beyond the "earlier this year" elapsed wording.
- **Insight 073:** this increment added no persisted fields (the stamp already shipped); the doctored stamp keeps the savedAt-blind identity CLEAN.
- **Insight 033:** verify each gate's target actually renders BEFORE pinning it — drive each seed live once before writing its arm. This is the landmine that fired: it is what caught the F2 plant shape, and insight 085 is the write-up.
- **Insight 075:** any fit fix proves a MEASURED delta in the failing regime before shipping.
- **Insight 082:** if a new frame overflows, degrade down the ordered ladder (whitespace → leading → subordinate type register) — NEVER content cuts.
- **Harness:** ports 4190 (fit) / 4195 (caddie) dedicated, `--strictPort`; http + `127.0.0.1` never `localhost`; the `testMatch`/`testIgnore` mirrors are load-bearing; never import constants from a `*.spec.ts` (shared constants live in `reviewSurface.ts`); masked currency fields never `fill()`; fit specs are outside tsconfig `include` — a type error there won't surface in `pnpm typecheck`.
- `devSeeds.ts` is DEV-only, DCE'd from prod — confirmed by the dist grep after the fresh build, and any future seed work re-runs it.

## What a later reader still needs to know

- **`?vault=statestale` may only ever ride a light doctor.** Both guards throw now, so a wrong re-route fails loud rather than demoting silently to the R19 indeterminate; do not "simplify" the plant back onto `doctorStaleVault`.
- **`nc` no longer crosses the confidence band.** The inequality pin is the invariant; the verdict literal is coupled to the `nc` arm's expectation and moves with the priced NC schedule. A band-crossing state face needs a new purpose-built seed with its own state-off twin — Briggsy's call.
- **Two harness residuals from the pre-walk, filed not fixed:** the answered picker row sits below the Assumptions dialog's internal scroll in the panel crop (the pick's checked state was verified via aria, not pixels — scroll-to-row before the crop), and walk arms need pick-provenance annotations so cold readers cannot reconstruct impossible cross-arm sequences.
