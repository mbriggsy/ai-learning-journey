---
title: The Back Nine — Roadmap (where we are, what is next)
doc-type: roadmap
status: living
created: 2026-06-17
updated: 2026-07-18
derives-from: [docs/product.md]
---

# The Back Nine — Roadmap

This is the single maintained source of build-truth for the repo. The four acts below trace the work from the deterministic engine to the recommendation surface; the **You-Are-Here table** is the honest per-unit status the rest of the docs point to. When a doc and this table disagree about what is built, this table wins.

The *why* and the *what* live in [docs/product.md](product.md). How the engine holds the cardinal rule — and the load-bearing invariants — live once in [docs/architecture.md](architecture.md). This doc is the map between them: which requirement landed in which unit, what is shipped, and what is next.

> The volatile next-action queue lives in `TODO.md`, not here. This doc moves at the speed of *units shipping*; `TODO.md` moves at the speed of *sessions*.

## The four acts

The product grows in four strictly-dependent acts. The first two answer the question; the last two are the differentiator.

**Act 1 — The Engine** (`docs/plans/1-engine.md`). The two hardest surfaces — correctness and trust — plus the overlays both controls stand on. A deterministic Monte Carlo spine validated against Trinity/Bengen, joint-and-survivor longevity, the zero-draw tax and healthcare overlays, the encrypted-at-rest store, and the accumulation date-search (the fuck-off-date search). Nothing user-facing ships in this act. **Done and pinned.**

**Act 2 — Where You Stand** (`docs/plans/2-first-answer.md`). The magic moment end-to-end: the ~5-minute account-level guided setup, the colorblind-safe visualization, the plain-language confidence statement, the state-adaptive first answer (the fuck-off date for a still-working household; the spine statement for an already-retired one), and the first encrypted Save. **Complete (2026-07-02)** — residual N=1 tone reads are parked non-blocking in `TODO.md`.

**Act 3 — The Levers You Hold** (`docs/plans/3-controls.md`). The itemized, time-boxed budget builder plus both manual controls — withdrawal sequencing and Roth conversion — driving the Act-1 overlays, with the healthcare surfaces and the sharpen/re-entry loop. A shippable cold-read milestone in its own right, *before* the solver exists. **COMPLETE — U9–U13 all shipped (U13 2026-07-10 under the clearance grant; the aged-ladder re-base SHIPPED 2026-07-10 night — TODO item 0 ✓)** (per-unit truth in the You-Are-Here table below). Act 4 (Solver & Recommendation) is underway — **U14 (the validation harness) and U15 (the solver core) shipped 2026-07-18**; U16 (the recommendation surface — the actual differentiator) is next.

**Act 4 — The Recommended Route** (`docs/plans/4-recommendation.md`). The new layer and the actual differentiator: the solver validation harness, then the solver that ranks candidate strategies on identical futures, then the recommend-second surface, then stale-recommendation handling. **Validation is built and passing before the solver is allowed to recommend.** **Not started** (`src/engine/solver` and `src/engine/validation` are `.gitkeep`-only).

The act-to-act dependency is strict — each act stands on the one before — but the Act-1 overlays feed Act 3 directly, and the validated spine feeds Act 4 directly.

## The ID scheme

The docs and the code share a small set of stable IDs. They are **internal join-keys** — the thread that ties a requirement to the unit that builds it to the test that proves it to the insight that hardened it. They are **never shown to the user**, and they are **never renumbered**: the numbers are wired into the source comments, the test names, the insight files, and the git history, so a renumber would silently rot every one of those references with no gate to catch the drift. Cosmetic labels (an act's name) change freely; these keys do not.

| Key | Name | What it identifies |
|---|---|---|
| **R**`n` | Requirement | A product contract (R1–R40) — *what* must be true. The ledger is in [product.md](product.md). |
| **U**`n` | Unit | A build unit (U0–U17, globally unique across all acts) — *how* it gets built. |
| **C**`n` / **D**`n` | Unit tracks | The accumulation tracks: **C** at engine altitude (C1–C3), **D** at intake/answer altitude (D1–D2). They extend the U-numbering without renumbering it. |
| **Act**`n` | Act | One of the four build chapters (Act 1–4). The code comments say `P`n (Phase) for the same chapter — the globally-unique unit key is the unambiguous join. |
| **M**`n` | Milestone | A sub-step inside a unit (e.g. `U3·M5`); lives in code comments, not the build tables. |
| **§**`n` | Section | A numbered section inside a plan or doc (e.g. `architecture §7.2`). |

Compound refs read left-to-right: `Act 2 · U7` is Act 2, Unit 7; `U3·M6` is Unit 3, Milestone 6. Because the unit key is globally unique, a doc that says `Act 2 · U7` and code that says `P2·U7` join unambiguously on `U7`.

> **The unit-track `C` is not the code variable `C_dest`.** `C2` is an engine-altitude accumulation *unit*; `C`/`C_dest` in `taxOverlay.ts` is the per-bucket *contribution* amount. Same letter, unrelated.

## The You-Are-Here table

The single source of build-truth. Status is honest, not aspirational — the closed set: `shipped` means coded **and** reviewed; `in-progress` means partially landed; `planned` means scoped and build-ready with zero code; `scoping` means the shape is still being worked out, not yet build-ready; `not-started` means the directory is `.gitkeep`-only.

The C-units and D-units are the accumulation tracks — they extend the U-numbering without renumbering it (see [The ID scheme](#the-id-scheme)). C-units land at Act-1 engine altitude; D-units reshape Act-2 intake and the answer surface.

### Act 1 — The Engine (shipped + pinned; closed 2026-06-11)

| Unit | What it delivers | Status | Note |
|---|---|---|---|
| **U0** | Scaffold, conventions, PWA shell, CI; engine-purity lint; strict CSP | shipped | The burned toolchain; strict CSP ships via HTTP headers, not a meta tag |
| **U1** | MC engine core: determinism, CRN single shared draw, joint-survivor longevity retaining survivor identity, Trinity/Bengen externally-derived fixtures, sequencing as a pluggable policy | shipped | The validated spine; the survivor-spending ratio is grounded + dangerous-direction-documented |
| **U2** | Tax-and-accounts overlay: buckets, ordinary tax/gross-up, RMD birth-year, SS provisional-income fixed-point, MFJ→single; reduces-to-spine when off | shipped | Zero-draw deterministic transform |
| **U3** | Healthcare overlay: ACA-MAGI + IRMAA-MAGI calculators, ACA pre-65 fixed-point + cliff/enhanced toggle, IRMAA 2-yr lag, HSA 4th bucket; reduces-to-spine when off | shipped | Zero-draw; `verify:aca` gates the legislative status. HSA *spend* (the resumed U3·M5, "B1") is decumulation-side and not yet built |
| **U4** | Encrypted store + key lifecycle + recovery/export: PBKDF2-600k, AES-GCM, recovery credential, mandatory export/restore, schemaVersion | shipped | The migration ladder covers the v2-with-accounts shape. The v1 BIP-39 recovery phrase was superseded in U8 by a second user-chosen passphrase (council 2026-06-30) — [architecture.md §7.3](architecture.md) |
| **SS sub-engine** | Social Security spousal/survivor benefit math | shipped | Reviewed; the review caught + fixed a cardinal-sin optimistic survivor-floor bug |
| **C1** | Contribution-limit + ticker-blend constants (Notice 2025-67 / Rev. Proc. 2025-19 / issuer-or-EDGAR; pinned to primaries) | shipped | Feeds the accumulation projection and the household stock/bond/cash blend |
| **C2** | Accumulation projection: signed contribution inflow on the one continuous absolute-year CRN timeline; presence-keyed reduce-to-spine | shipped | Empty phase (`Y == 0` / construct-absent) reduces byte-identically to plain decumulation |
| **C3** | The date-search (`dateSearch.ts`): exhaustive, non-monotone-robust sweep over the household work-stop offset; quantized-lower-bound selection — **the fuck-off date** | shipped | Three first-class outcomes per track: confirmed date / window-edge-unconfirmed / no-date-in-window |

### Act 2 — Where You Stand (complete 2026-07-02; residual tone reads parked in `TODO.md`)

| Unit | What it delivers | Status | Note |
|---|---|---|---|
| **U5 / D1** | Account-level guided setup (the U5 reshape): surface-early, single entry pass; the in-memory orchestrator + R19 sanity | shipped | Cleared the N=1 laptop cold-read on every screen. The broader D1 date/answer surface is still ahead |
| **U6** (foundation) | Colorblind-safe viz primitives: palette / scale / CVD probe | shipped | The CVD-safe foundation, not the band render |
| **U6** (render) | The confidence-band / projection-fan RENDER | shipped | Built (direction B: Lead + Drawer + click-to-enlarge); **review DONE** (`/ultramode-code-review`, 11 lenses — insight 043); **app-integrated live via D2** (both routes, 2026-06-28) and the binding two-pane first-frame cold-read **cleared** (2026-06-29, "calm, not an ambush — ship"). Residual optional tone reads (CF3/CF4/CF5 squash) are parked non-blocking in `TODO.md` |
| **U7** | Confidence statement surface + outcome-state system + survivor readout; copyGuard born here | shipped | Path B. Built, **N=1 cold-read-cleared**, and **`/ultramode-code-review`-cleared** ([insight 044](insights/044-a-can-never-happen-comment-is-a-claim-about-the-gate-not-a-fact.md)): `ConfidenceStatement` (verdict-first, wires the band on demand), the `resolveBandData` producer + its fail-loud seam, the non-color verdict signals, `outcomeStates`, the `copy` verdict-grammar, `copyGuard`, the `?preview` harness. The survivor-conditioned engine surface — e1 (survival fraction) + e1b (income step-down) + e1c (the `confidence.ts` survivor reading) — is **built + adversarially verified** (2026-06-27; a 4-lens pass caught + fixed a P1 understatement where an fd-anchored counterfactual went negative for pre-claim deaths — fixed with a steady-state anchor, regression-guarded). The **`SurvivorReadout` UI (e2)** is **built + cold-read cleared + review-hardened** (2026-06-27 — the quieter second statement; three cold-read calls resolved, and a focused 4-lens review caught a regression-net hole over the $0-cliff suppression, now boundary-pinned). The live-app wiring **LANDED** (2026-06-28, council-chosen): the survivor readout now mounts beneath the live spine verdict — `engineWire`/`fromWire` carry `survivorConditioned` + `survivorReading` presence-keyed (the `bandFan` mirror); the $0-portfolio band-suppression + insight-035 obligations were handled in D2. |
| **U8** | First-Save ceremony + recovery model + decrypt-on-return (unlock · forgot-passphrase recovery · restore-from-backup · view-only 2nd tab · edit-and-re-save · the PWA update-apply ceremony hold) | shipped | **COMPLETE (2026-07-02).** The ceremony (passphrase → recovery word → atomic `firstSave` → mandatory export), the council recovery rework (user-chosen 2nd passphrase, PBKDF2-600k — the BIP-39 phrase was DOA), and the full decrypt-on-return arc: probe → Unlock/Recovery/Restore flows (all live-verified in real Chromium, incl. a genuinely damaged vault), provisional-first hydration, the Fork B commit→export update-apply hold, the Fork C(ii) standing view-only banner (two-tab live-verified), and the proper edit-and-re-save through `session.save()` (edit-aware badge; the hydrated session starts saved). Three ultramode reviews across the arc; the closeout review's 7-lens convergence (the read-only reload-not-retry law) fixed + live-verified. The portfolio-holdings field stays **ratified — build-DEFERRED** (additive-within-v3, `docs/decisions/portfolio-holdings.md`) |
| **D2** | The state-adaptive first answer surface: date-first for not-yet-retired, spine-first for already-retired; the two-pane laptop layout | shipped | Same calm voice, one intake flow; only the lead answer changes. **Built (2026-06-27, cold-read cleared):** the elevated `FuckOffDate` surface — the date-first magic moment (hero headline in the "fuck-off date" voice — Briggsy's call), the three first-class outcomes + free-today, the conservative odds (single-sourced with the strip via `dateOdds.ts`), the window-edge + non-monotone disclosures, and the **date↔confidence tradeoff** (R28 — an earlier lower-odds point, suppressed when a priority disclosure already shows). Reached at `?preview=date`. **Now COMPLETE (2026-06-28/29, council-decided + cold-read-cleared):** the on-demand odds drawer (D2c — a discrete integer-rung odds-ladder + a y-axis "X of 10" scale, NOT a smooth curve), the **state-adaptive routing** + the **live-wiring** (`runDateSearch` → the surface), and the **two-pane laptop layout** (cold-read cleared 2026-06-29: "calm, not an ambush — ship"). See [`docs/council-log.md`](council-log.md). |
| **R40** | Other income in retirement (pension / rental / annuity / alimony / other) — engine + intake | shipped | U1–U4 shipped (types · compile + goldens · the atomic engine integration · the **intake UX + the KTD-9 copy half**); U5 doc-reconcile done — R40 complete. Build steps: [docs/plans/2-first-answer.md](plans/2-first-answer.md) |

### Act 3 — The Levers You Hold (complete — opened 2026-07-02, closed 2026-07-10; the state-tax follow-up unit shipped 2026-07-15)

| Unit | What it delivers | Status | Note |
|---|---|---|---|
| **U9** | Budget builder: itemized, time-boxed, essentials/discretionary; the two-tier lexicographic headline reading | shipped | **U9a (ENGINE) + U9b (UI) SHIPPED 2026-07-02** (council-ratified, runs wf_f18bbb26-389 + wf_67d45818-f65). **U9a:** the persisted `budget?` line items (additive-optional within v3), the `src/budget` compile (sticky rule + OOP-medical injection), the two-track evaluation on ONE shared CRN draw set (a genuine second decumulation+overlay pass), `Distribution.floor` + `floorReading`, the genuinely-split date-search floor/lifestyle tracks with the floor-crowned band, the fail-loud oopMedical containment gate, the reconciliation invariant. **U9b:** the pure decision layer (`floorRelief` · `composeDateSplit`/`isFloorLifestyleInverted` · `anchorTarget`/`commitBudgetPatch`), the render layer (`TwoTierHeadline` relief line — word+count only, no dollar; `FuckOffDate` floor/lifestyle split — lifestyle hero, essentials-covered subordinate, floor-crowned band, R27 inversion disclosure), the `BudgetBuilder` sheet (portal/trap/restore, R19 timing, reconciliation readout netting OOP, the ONE Result door, the read-only governed spend question) — **`/ultramode-code-review`-cleared** (9 lenses + 3-angle adversary, 16 agents; 3 findings folded incl. a P1 OOP-medical second-writer desync + the endYear honesty-affordance, both live-verified) and **live-verified in real Chromium** (both routes, phone + two-pane laptop, dev seeds `?seed=budget/datesplit/datemixed`). The R27 floor>lifestyle inversion is unreachable in v1 (proportional-drawdown MAGI coupling; render arm fixture-pinned, reactivates with U10 conversions). Only owed: Briggsy's Q2 two-tier tone cold-read (a formal Act-3 exit condition, non-blocking) |
| **U10** | Manual withdrawal-sequencing control + Roth-conversion lever (both drive the U2 overlay); require-the-hedge lint introduced | shipped | The persisted `rothConversion?` lever + `'custom'` order (additive-within-v3), `roth.ts` two-arm on ONE seed (CRN by construction; without-arm byte-identical golden), the `runTwoArm` wire + latest-wins preview seam (zero write paths), the sequencing + lever sheets over the two-futures median chart (string-free, dead-cohort-truncated), the require-the-hedge gate reading the single-sourced `HEDGE_TOKENS`. **Ultramode-reviewed** (13 lenses + per-finding adversarial verification, 23 agents — 7 verified findings folded, zero false alarms). **The hard gate's engine+render halves SATISFIED via `?seed=dip`** (the budget-collision mechanism — pure conversion channels proven structurally monotone; plan supersession block + council-log carry the record); the dip SEEN to draw live **and cold-read CLEARED (Briggsy, 2026-07-08 — the dip verdict, the two-futures grayscale/delta, and the vermilion double-duty all passed)**. `bracket-fill` shipped with U11 ("Low-tax room first"). The two-futures chart gained the fan's full axis + scrub grammar 2026-07-08 (fcc35556). |
| **U11** | Healthcare surfaces (ACA cliff + enhanced toggle + re-verify gate; IRMAA cliffs; HSA entry) over the U3 overlay | shipped | Built 2026-07-03 (council-ratified design → the magiLandscape rail/readout module, engine-derived cliff-aware bracket-fill ceilings + the picker join, the per-year healthReadout wire emission, `enhancedSubsidies?`+`healthcareVintage` additive-in-v3, the Healthcare door/sheet + shadow-rate readout + regime what-if lever, the post-65 unpriced-Medicare disclosures; ultramode-reviewed, live-verified, `?seed=health`). **The ship-gating cold-read batch CLEARED 2026-07-08** (all six stations walked; its two findings — the laptop one-frame regression and the bare comparison chart — were fixed, live-verified, and pushed the same day: 6f8305de + fcc35556). **The council-REQUIRED post-65 Medicare pricing engine unit SHIPPED 2026-07-10** (council wf_4c8cd836-b22, 9/10 no veto; the executable shape is `docs/plans/features/medicare-pricing-build-spec.md`): the all-65+ household now prices base Part B + full IRMAA (the `medicareOnlyPriced` intake branch — one engine gate, ACA self-skips on `pre65 > 0`), the containment premise ratified ADDITIVE (entered spending excludes Part B/IRMAA — the tool adds them on top; `spendHelp` carries the precise boundary), and the age-keyed unpriced-Medicare disclosure is RETIRED for a route-aware priced-affirmation + narrowed residual shipping together (insight 080 — the old age-keyed note was a live false statement on the date route, which had priced Medicare since D2's candidate builder). Real-flat Part B stays a disclosed modeling choice + an Act-4-solver-BLOCKING forward landmine (architecture §7.2); the 2028 IRMAA top-tier re-index rides a dated tripwire test. **The ask-for-Medicare-extras unit SHIPPED 2026-07-11 + CLOSED 2026-07-12** (council wf_efc6ece2-675, 8/10; spec `docs/plans/features/medicare-extras-build-spec.md`): each person's own Part D/Medigap/MA premium asked via the 3-arm payment fork (blank-start, the affirmed-$0 MA arm, the conservative-HIGH primary-sourced ~$203 typical), priced per person as the F3 heterogeneous vector (never count×avg — planted-mutant RED ×3), the spendHelp boundary flipped fund-first, disclosed in BOTH homes off BUILT params (insight 081), with per-person provenance stamps + the extras-typical staleness clock. Fresh ultramode review folded its one confirmed finding (the hero on-typical composer battery + the disclosure-view dedup + the overlay length backstop) and closed O11 as resolved; the codified Caddie panel's first run chaired five cards PILOT-CLEARED with two fixes (the panel's unanswered-fork standing line — the rule-38 details home; the spend-step's keep-state-tax-inside instruction). His eye rides the 2026-07-12 tape row. |
| **U12** | Sharpen loop + assumption editing + power-user escape hatch | shipped | **Built + live-verified 2026-07-09 early** (council-ratified wf_dff75c2f-9e3, 8/10 — 8 forks ratified, F1 market-moments deferred to visible-only, the narrow hawk veto honored-and-widened; the plan's dated in-body supersession block carries the shipped shape). The sticky-rounding seam (`resolveStickyDisplay` over the three emitted margins incl. the NEW `stateMarginToEdge`, session-only, notify-once), the F9 demotion (a not-validly-present required fact — incl. the veto's clear-to-0 — commits the tier-less `inputs-incomplete` arm, never a held stale verdict; **live-proven**: spend→0 demotes to the calm strip and recovers), the R7 compile-enforced `assumptionRegistry`, the AssumptionPanel-as-escape-hatch (the Review door seat; reachable on inputs-incomplete/fallback/compute-error; governed spend routes to the budget sheet), the CSS-only verdict crossfade reading the sticky triple, the standing market-floor line (the probe killed the per-edit floor heuristic — 30 CRN-paired runs), the four carry-ins, `verify:fit` 14/14 incl. the new panel-extent arm. 16 planted mutants red-then-reverted across the build. **The 2026-07-09 ultramode review (12 lenses + 5-angle adversary panel, 70 agents) FOLDED: the direction-blind sticky holds (8-lens convergence — a falling reading could hold the rosier verdict) fixed under the conservative-direction law + symmetric coupling with a full-grid property sweep; the panel echo re-joined the hero on one composer (verdictSentence.ts); the remount focus-steal + via-panel restore-to-<body> + double-announce closed (live-proven); the survivor-ratio floor rule; the panel period provenance. 13 more mutants red; 2108 tests / 118 files. The F1 defer was KEPT by Briggsy 2026-07-09 — market moments stay visible-only; the sourced-bounds flip condition stays filed.** **The cold-read batch CLEARED 2026-07-10 (Briggsy, via the Caddie’s first pre-walked card — docs/caddie/cold-read-log.md): “I think everything is fine … other than that good to go” — the truer-picture line (THE formal Act-3 exit condition) passed untouched. His one finding — the withdrawal-order sheet’s chart spoke years while the fan speaks ages — was FIXED + eye-verified same session: the TwoFutures x-axis now derives from the fan’s own canonical decade-age rule (`deriveDecadeAgeTicks`, one home) behind a geometry-aware endpoint-collision guard (`visibleXTicks`). U12 SHIPS.** |
| **U13** | Returning-user re-entry + per-surface staleness (tax/healthcare vintages, budget line items, the date answer's fixture clocks) | shipped | **Built + live-verified 2026-07-09** (council-ratified wf_a1ef7e25-64d, 9/10 no veto; the executable shape is `docs/plans/features/u13-build-spec.md` — it supersedes this plan's U13 section where they conflict, each supersession dated). The three phases: the persisted substrate (`savedAt` epoch-day range-gated + `TaxVintageV3`/`DateVintageV3` stamps + the Q7 saved-era app-default map + the ONE `scenarioIdentity` savedAt-blind normalizer), the pure per-surface reader (`src/store/staleness.ts` — reads the RAW-decoded vault at unlock, absent-stamp = not-applicable), and the re-entry gate (`ReEntry.tsx` GATES the reveal — per-bucket read-back, a prompt never an attestation, fired clocks named; the hero wears the one-line standing echo; the date hero gained the wall-time-anchored calendar framing). `?vault=stale` drives the whole surface live. **The 2026-07-09 ultramode review (13 lenses incl. a 4-angle adversary panel, 44 agents, per-finding refuters — wf_44cdf86d-b71) FOLDED:** the senior-bonus sunset "re-priced" note REFUTED against source and REMOVED (the engine never read `sunsetAfter` — the crossing changes nothing about a saved answer; the REQUIRED engine sunset unit was filed + build-tripwired, and **SHIPPED 2026-07-09 same-day**: council wf_a5ccd576-048 8/10 no veto — the deduction stack is calendar-year-gated to [2025..2028] via the constant's own consumed window, DND-012 edge/gain-shelter/full-solver-crossing batteries, the tripwire retired in the unit's commit, architecture §7.1/§8 updated), the hero echo split onto `rulesMoved` (a lapsed budget window no longer fires a false "rules changed" over a byte-identical recompute), the contribution clock route-gated quiet for retired households (+ the route-true `stalenessBlendSpine` line), the whole clock subsystem unified on the household's LOCAL calendar (the Dec-31 UTC skew), the floor line anchored like the hero (aged-vault time-base parity), the J1 survivor-door composition pinned (App.test recover→hydrate arm), the drifted-vault CLEAN-badge law pinned (glue-mutant proven), and `verify:fit` gained the `?vault=stale` gate+echo arm (15/15, frame-mutant proven). 7 planted mutants total this unit, all red→reverted. **SHIPPED 2026-07-10 (evening) under the clearance grant** (Briggsy's live grant, memory `project-back-nine-cold-read-clearance-grant`): the four-card batch resolved — 3 PILOT-CLEARED with five copy/mechanic fixes folded (route-true gate intro, the FRA frame gloss, "fund snapshots" purged, the ladder crown's shape sentence, the O7 focus-ring exemption) + the `?vault=datestale` plant unblocking the floor's arrived arm live, the aged band/TwoFutures/C2 re-based to ONE time base (the U13 doctrine applied to the charts), and the same-day oracle sweep (O3 council-closed + probe-PASSed, O4 verified, O5 exact ticks, O6 round-half elapsed). **The aged card is HARD-FLAGGED, not cleared:** the odds ladder is the two-time-bases family's third sibling (un-re-based) — filed REQUIRED at TODO item 0 with the design fork named; every organic vault is same-day (elapsed 0), so the shipped domain is unaffected until real vaults age (2027-01-01). His card-based reads stay welcome any time — the tape rows are unscored and his verbatims supersede. |

| **State-tax** | Exact per-state decumulation income tax for the priced roster {NC, PA, FL} — the intake best-guess state + assumption seat, the gross-up coupling, Roth conversions priced per state, per-household disclosure retirement | shipped | **Built + reviewed + pre-walked, all 2026-07-15** (ultramode fold wf_e485b96d-85a → 388b8821, 53 agents, zero false-alarm survivals; Caddie pre-walk wf_96397de6-244 — 3 cards PILOT-CLEARED, two chair fixes live re-verified; the state SEED INCREMENT followed 2026-07-16 — seeds `nc`/`pa`/`fl`/`elsewhere`/`datenc` + `?vault=statestale`, its own ultramode fold 350319a8 + 78-agent pre-walk). *(Row re-stamped 2026-07-18 — it had sat at "review pending" after both phases closed.)* **Built 2026-07-15** (pre-build council wf_d04148cb-1e5, 8/10 EXECUTE over the wf_7bc1f76d-6cc primary-source research packet; the executable shape is `docs/plans/features/state-tax-build-spec.md`). The year-keyed primary-sourced {NC, PA, FL} constants (**the hawk veto holds NC at 3.99% flat ALL years — never the uncertified 3.49%/2027; `verify:state-tax` carries the ~Aug-2026 certification checkpoint**), the pure `stateIncomeTax` primitive joining the SAME gross-up fixed point (k re-derived ≈0.78 with NC/PA's SS exemption dropping the torpedo multiplier from the state term; `GROSS_UP_MAX_PASSES` 128→192; the state-ON worst-corner stress sweep), reduce-to-spine `+0` keyed on PRICED_STATES MEMBERSHIP (absent / 'elsewhere' / unbuilt SC byte-identical to the golden spine — proven), NC conversions fully taxed vs PA $0 at 59.5+ (under-59.5 TAXED — the conservative arm; household-granularity age gate, documented), the NC survivor SD-halving ($25,500→$12,750) crossing-year external fixture, the R19 enum gate both-ends, the intake state step BEFORE spend (the 4-arm vertical SegmentedControl reused — no new component), the R7 'retirement-state' editable seat, `retirementState` + `StateTaxVintageV3` persisted (codec-vocab-gated, staleness-clocked route-true, `scenarioIdentity`-blind), the four "state isn't counted" disclosures SPLIT into clause-parts dying per-household on the producer's-output roster-gated `pricedStateForRun` (the bare `isDateRoute` disjunct stripped — the red-team fold; six planted mutants RED with named killing tests), the outcome-scoped affirm+residual SET (NC/PA/FL each named honestly), state-aware `spendHelp` (the double-count inversion closed), and `verify:state-tax` mirroring `verify:aca` (RED-proven on planted-stale AND missing-record). The CSP + Caddie intake walks learned the step (CSP now proves NC pricing under the enforced headers). **2351 tests / 125 files, every gate green.** Ultramode review + the Caddie pre-walk are the open phases. |

### Act 4 — The Recommended Route (U14–U15 shipped — the oracle gates everything after it; the solver core ranks through it)

| Unit | What it delivers | Status | Note |
|---|---|---|---|
| **U14** | Solver validation harness: optimality/ranking oracle, ranking-stability-under-CRN, grade calibration, held-out-seed reporting — **gates U15** | shipped | **Built 2026-07-18 (the Act-4 runway's GO; the executable shape is `docs/plans/features/act4-u14-validation-harness-build-spec.md` — council wf_d873be6e-5b2 over audit wf_1369c7e7-698).** S0 the pin pass: `directionalKind` splits certification-pinnable (BLOCKS the household) from methodology-substrate (ships-disclosed); 15 state entries + medicareExtrasTypical PINNED (the extras 2026 refresh — Medigap-G typical $164→$205 conservative-high off the Telos +12–26% filings, combined ≈$244, vintage `extras-2026b`; the `?seed=dip` knife-edge re-tuned 658k/282k to restore nm=[0,1,2]); `ncRateSchedule` stays the SOLE directional state entry (the ~Aug-2026 certification, hawk's veto); `medicareCostTrend` = the first live `Unsourced` sentinel + the `PART_B_PRICING_MODE` consumption witness (a sourced-but-unconsumed trend still blocks — insight 074/081); the consumed-set derivation is import-audited + SOURCE-BOUND (witness table + doc-row exclusion test). S1 `solver/candidates.ts`: the SHARED enumerator (4 searched policies; cliff-anchored amounts just-under every ACA/IRMAA/bracket rail via the shipped magiLandscape maps — continuous-monotone bisection, insight-013-compliant; RMD-first legality filter rejects over-headroom amounts as sentinels; `applyCandidate` = the one apply seam, dimension-invariance pinned). S2 the oracle: 7 committed hand-derived fixtures (constant-rate conventional-optimum w/ the bracket-fill→pre-tax-first +∞-degrade BYTE-witness · bracket-fill-to-22%-top hb 0.23 · ACA-cliff sign-inversion (the healthcare-BLIND counterfactual from the engine's own premium surface crowns the over-cliff loser) · §1014/IRD gross-vs-after-tax EXACT anti-alignment · no-change routing by provenance · NC (the state term FLIPS the optimal anchor 22%→12%-top — why the precondition refusal is load-bearing) · PA (byte-identical to the state-absent twin)), each test-time re-derived from the canonical constants by closed forms; planted-wrong-best fails loud with a control arm. S3 K-candidate CRN: dims-invariance + the perturbation law + survivor-crossing presence via the engine's own minted stamp, vacuity-guard red-armed. S4 grades: all-members-beyond-band rule (stability built in), min-B 16k floor, the conversion-near-tie DEMOTION calibrated 0.02 from the MEASURED class (0.0101–0.0118 ± 0.0016 at 16k×5 — CRN resolves the margin, so the demotion is genuinely about the §3–§5 shape residual; the real-engine proving case fires), display-tenth collapse, named-driver probe (the cliff fixture's crown genuinely flips under the enhanced-subsidies probe; `sampling-noise-near-tie` otherwise), the S4.5 disclosure seam. S5 held-out: canonical splitmix seedB (planted seedA+1 structurally refused; identity-derivation caught by the decorrelation control), the deterministic B-family, the ε SPLIT (A-side CRN-difference tolerance vs B-side display band, with the level-band-over-declares demonstration). S6 the token: `OracleClearedToken` mintable only on branded Oracle+Stability reports + the four live clauses — **NC-blocks-while-FL/PA-mint on the same build; every conversion-bearing candidate set is trend-blocked until the sourcing unit lands; the withheld-reason enum names the TRUE reason.** No user-facing surface — no Caddie walk; the oracle IS the reviewer. **The ultramode fold (2026-07-18 night, wf_d04f0891-7fc + fresh refuters wf_a6e6a13b-6dd): 13 advisory dispositions folded, no P0/P1** — the perturbation law byte-compares the FULL decision surface (`decisionSurfaceIdentical`) + carries a moved-witness with an inert-world red arm (insight 093); the conversion demotion is axis-guarded (pay-less-tax refuses until a tax-unit margin is calibrated — U15); past-horizon conversion windows THROW at the apply seam; empty-roster / zero-case-report refusals; the mint's ε leg drivable red (`_epsilonRequired`); the ACA freshness window joined the ε list; `xOfTenClamp` is the one canonical clamp home (the displayTenth mirror died); `CASE_I_TOP_MARGINAL` table-read; heirBracket + expected() asserted; the FRA prose de-staled. The 4 crashed refuters (abstentions, insight 019) AND the 3 findings the script's confidence-threshold had silently dropped (insight 092) all got fresh refuters — zero crashes under the 084 size law. 6 planted mutants red→reverted. |
| **U15** | Solver core: named-policies × conversion-grid search on identical CRN draws; lexicographic objective; byte-identical selection; the WASM measurement gate | shipped | **Built 2026-07-18 (serial fleet over `docs/plans/features/act4-u15-solver-core-build-spec.md` — pre-build council wf_45d9d56b-d1c, high 8/10 RATIFY-W/-AMENDMENTS; the honesty-hawk veto HONORED).** S0 closed BOTH mint bindings the veto killed: a pure `solverRunFingerprint(params,candidates,ranking)` derives every ranking-affecting input (per-bucket balances · goal · heirBracket · budget · horizon/dates · people · overlay flags · the full ordered candidate-id list — a fail-closed SUPERSET of the enumerated list) and joins BOTH the token's `mintedOver` AND the stability report, so `solve()` refuses a fingerprint mismatch (the moved-witness battery red-arms every input class, insight 093); `solverCandidateId` widened with provenance arms (`grid:`/`conventional:`/`baseline:`) so the modal `taxable-first:0` grid-vs-baseline collision is injective; `SOLVER_CODE_VERSION` minted. S1 `objective.ts` a THIN adapter (consumes the harness `rankCandidates`/`tier2` — never a parallel scorer that drifts between fixtures). S2 the dispersed-world verdict = **INTRACTABLE → the architect's default ships**: the mean RANKS (the zero-vol oracle battery genuinely oracles `expectedRankingIds`; a dispersed mean↔median ordering FLIP is hand-derived on the record but cannot become an engine oracle without buying tractability with tolerance — insight 091), the mean's skew DISCLOSED via `leaveMoreSkewDisclosure` (insight 092), never a second ranking authority (contract #4). S3 `search.ts` RANKS, never re-decumulates — the shared enumerator + `evaluateCandidates` make the K-candidate CRN identity true BY IMPORT; both seed-sets (A selects, B displays+grades); the labeled baselines located by provenance. S4 `select.ts` DETERMINISTIC shrinkage of each seed-A advantage over the CONVENTIONAL prior (never the user's own habit — laundering the status quo), keyed to the already-built CRN-difference SE (no new constant): the insight-025 calibration case + the zero-shrinkage identity collapsing onto `rankCandidates`; the surplus-regime flag; the demotion-axis refusal FAIL-CLOSED (unreachable-live, planted-seam-tested). S5 the solve entry: `solve(token,input)` two-clause gate (REQUIRED token — a compile error without it — + the fingerprint identity refusal), conversions trend-blocked BY DESIGN with every withheld lever NAMED + directioned (insight 092 — an abstention, not a pass), the seed-B display discipline (no seed-A curse-biased score ever renders), the tier-less pending arm in `memoryModel`, the wire (three seed-B distributions transfer, every scalar/grade/lever clones), the chosen-goal model field (additive-in-v3, an explicit unset sentinel — never a Tier-1-only tie-break crowned as advice). S6 `cancel.ts` (the UNCONDITIONAL commit-epoch guard — a superseded solve NEVER commits — + the injected cooperative-abort seam threaded through both orchestration + solve, coarse per-stage checkpoints; the live worker-epoch transport + finer granularity deferred to the profile's numbers), `profile.ts` (PURE, injected clock — **the dev-laptop worst case measured 72.4 s / 45.9× a single simulate (healthcare-priced — the review fold corrected the S6 healthcare-blind 48.1 s under-measurement) at 16k paths × 45 yr / 8-candidate roster, the linear budget shape confirmed; the reference-device measurement is the WASM trigger + the pin event**), the sentinel-guarded fallback ladder (interactive/full-precision tier + coarse-then-refine + the candidate ceiling, all −1 knobs FAIL-CLOSED until the reference profile pins them — burned/062). No user-facing surface (U16's) — no Caddie walk; the harness IS the reviewer. +124 tests across 10 new files. |
| **The Medicare-cost-trend sourcing unit** | The standing conversion blocker lifted: the sourced V.E2 trend table, `PART_B_PRICING_MODE: 'trended'`, disaggregated IRMAA surcharge scaling, the Q4d SE-multiple re-calibration — conversions rank end-to-end | shipped | **Built 2026-07-19 (pre-build council wf_c673339e-257, 8/10 RATIFY, the hawk's base-only-scope veto HONORED; the executable shape + dated stamps: `docs/plans/features/medicare-cost-trend-build-spec.md`).** Shape (c) rank-flip-probe-CONFIRMED (no flip vs the smooth twin; the 013 fallback never fired); per-year real Part B (horizon-matched deflation, pre-anchor clamp, C0 ultimate tail); Part B surcharges scale by the cost-share identity, Part D + extras held-and-DISCLOSED; the `part-b-trend` staleness clock + the annual Trustees re-verify tripwire; Q4d = 10×SE measured on Medicare-bearing 16k×5 worlds; seeds re-tuned under record-before-retune; the residual three-string swap; 6 mutants red→reverted; the oracle-case audit proved every committed case trend-unexposed. 2719 tests / 145 files, every gate green. |
| **U16** | Recommendation surface: recommend-second, confidence-grading, comparative transparency with the runner-up retained, objective ≡ headline, the 10/10→surplus pivot, hedge-on-headline lint | shipped — built + live-smoked 2026-07-22 (6f927862 + 263b1053); ultramode-folded 30b5ae85 (5 defect families fixed, seed-B inversion guard + re-solve promise + blocked steer); Caddie pre-walked 2026-07-23 with FIVE chair fixes (the full-width beat, the coherent stale card, the few-minutes pending line, the disabled confirm, the $X.XM dialect); S5 interactive tier DEFERRED on record (dated spec stamp); his eye rides the tape | The actual differentiator. Pre-build council wf_8d4c6f65-415 RATIFIED 8/10 (hawk veto honored: delta-as-hero, spine confidence source-bound); spec `docs/plans/features/act4-u16-recommendation-surface-build-spec.md`. §S0.2 near-tie inversion stress gate: **NO FIRE** (36/36 reps conversion-positive under the block-bootstrap historical shape — the richer-draw deferral ratified on the record); §S0.1 fallback knobs PINNED from the reference-device measurement (4000 / 5 / 2, rank-stability-keyed), `assertFallbackCalibrated` passes. **S1–S4 landed 2026-07-22:** the store solve router + `solverRunFingerprint` invalidation (stale-not-lying demotion), the invited doors-region affordance + `GoalPicker` (sheetShell selectors, the ControlSheet focus contract) + the full-precision `.solve-pending` breathe, and the committed honesty arc — the `RecommendationGrade` lockup (delta-as-hero comparative, spine-confidence source-bound by reference, one `.cs-swap` crossfade key), the Q5 withheld render (NC named / ~August / refusing-to-guess, coupling caveat), the no-change/surplus compose (no fabricated dollar hero), `assertObjectiveMatchesHeadline` on the render path, the retained runner-up one tap down, the leave-more skew median quote, the R7 disclosure nets, and the RESERVED inert save slot (no live Save ships in U16). §S5 interactive tier **DEFERRED on the record** (the winner-cannot-flip proof cannot be made + the wall-#2 degeneracy — the full-precision breathe stands, the pinned knobs + fail-closed guard stay shipped). **THE LIVE-DISPATCH BLOCKER IS CLOSED (2026-07-22, the seam-(i) fleet):** the intake-layer `buildSolveRequest` (`src/intake/solveDispatch.ts`) now builds the draft→`SolveRequest` and the optional `buildSolveDispatch` is wired into `appModel` — every request input SOURCE-BOUND to a shipped seam: the year-0 `ConversionAnchorContext` derived from built params by `src/engine/solver/solveAnchor.ts` (SS/RMD/ongoing-taxable/ACA-cliff/IRMAA rails each read from the engine's own year-0 seam), the RMD math re-homed to the SINGLE producer `src/engine/rmd.ts` (shared with `taxOverlay`, the consumedConstants witnesses moved with it), candidates from the shipped `enumerateCandidates`, `ranking` from `chosenGoal` + the R7 `solverAssumedHeirBracket` default (0.24, methodology-substrate, R7-editable), `seedA` the minted shared-draw CRN seed, `tieTolerance` 0. **LIVE-SMOKED end-to-end in real Chromium (2026-07-22):** `?seed=nc` → affordance → GoalPicker → the `.solve-pending` breathe → the committed WITHHELD render (NC named, ~August, refusing-to-guess register); `?seed=surplus` → the delta-as-hero surplus recommendation. The fit gate proves the organic dispatch fires (the pending well 112px aligns the committed grade lockup 110px, IN-FRAME at 1536×791 + 1280×800). **Recorded deviations:** (a) v1 recommend-second is ALL-RETIRED-ROUTE only — `solveInvitable` guards `!isDateRoute` (the working/date-route base is the crowned date offset, a follow-up increment; both fit-gate affordance seeds are all-retired, so the posture is unregressed); (b) NO no-change WITNESS SEED ships — `noChange` is structurally unreachable for a natural live household (a beneficial conversion always exists) and `subTenthCollapse` needs full 16k-path convergence no registered seed reaches, so the no-change RENDER stays covered synthetically in `RecommendationSurface.test.tsx`. Full battery green: typecheck · lint · **2880 tests / 155 files** · bundle (255.0 KiB) · aca · state-tax · csp · fit (40, the affordance IN-FRAME posture 2 + the pending/CLS + the F-A full-width right-rail arms) · doc-stats. Ultramode review + Caddie pre-walk + Briggsy's eye are the open phases. |
| **U17** | Stale-saved-recommendation handling: a saved rec is an executed action → re-solve under current fixtures; staleness reads "the action we recommended may no longer be advised" | in-flight | Council-ratified 2026-07-24 (wf_f4ced3c8-2f6, hawk veto honored ×3); the executable shape is `docs/plans/features/act4-u17-saved-recommendation-build-spec.md`. **S0–S4 SHIPPED** (the plan-clock rename + one arrived predicate · the Roth echo speaks the calendar year on read AND write · the aged band geometry: passed markers withdraw at the array, the elapsed segment demotes under a static mask, the premise line + rendered re-confirm control gate the aged fan structurally, the arrived idiom split strict · the record carries its OWN era, compared by one comparator against two subjects, verdict enumerated never a remembered dollar · a staleness clock names itself only where the run was EXPOSED, exposure injected from the run's own built params). All five stages carry dated build stamps in the spec. **S5 next** (the save gesture + the v3 write, landing together — the spec's S5 section was AMENDED 2026-07-26 with the copy register that moved from S4, the four mint obligations, and two corrections to how the stage had been described), then S6 (`?vault=datearrived` + the walk of both aged plants) and S7 (the two riders, own review scope; Q7a gated on `rendered_B − rendered_A === rendered_delta`). |

### Cross-cutting features

| Feature | What it delivers | Status | Note |
|---|---|---|---|
| **Social Security** | Spousal/survivor benefit sub-engine (shipped above) + the claim-age intake | shipped (engine) | Claim-age as a *solver-optimized control* is chapter two. Mechanics: [docs/architecture.md](architecture.md) §7; build: [docs/plans/1-engine.md](plans/1-engine.md) |
| **Other income (R40)** | Generic per-person non-earned income stream (pension/rental/annuity/alimony/other) | shipped | U1–U4 + the U5 doc-reconcile (done) — R40 complete. Build steps: [docs/plans/2-first-answer.md](plans/2-first-answer.md) |
| **Portfolio holdings** | Per-account exact stock/bond/cash %; ticker → blend; the household blend the engine consumes | scoping | Accounts take an exact stock/bond/cash % that collapses to the one household blend the engine consumes. Folds into U8. Requirement: [product.md](product.md) R37 |
| **"Just me" single-user mode** | A single-person (non-couple) household path | planned/deferred | Named, deferred |

## Requirements → unit trace

Every requirement maps to an act/unit. Numbers are immutable (R1–R40); the prose is re-framed for clarity, the mapping is not. `SC` = the success-criteria list in [docs/product.md](product.md). The C/D-unit detail lives in [docs/decisions/accumulation-fuck-off-date.md](decisions/accumulation-fuck-off-date.md).

| Req | Where |
|---|---|
| **R1** primary question is the face | Act 2 · U7 |
| **R2** plain-language confidence statement, survival-vs-lifestyle separation, no color-alone | Act 2 · U7 (single metric), Act 3 · U9 (the two-tier essentials/lifestyle reading) |
| **R3** distribution of futures | Act 1 · U1 |
| **R4** detail on demand, never unsolicited | Act 2 · U7, Act 3 · U10–U12, Act 4 · U16 |
| **R5** guided one-question intake — the guided intake is the account-level setup of R35, one flow for both user states | Act 2 · U5 → D1 (the U5 reshape) |
| **R6** power-user escape hatch | Act 3 · U12 |
| **R7** every assumption (and every recommendation input/reasoning) visible+editable | Act 3 · U12, Act 4 · U16 |
| **R8** input mirrors output; refinement *sharpens* (narrows on precision, shifts on a correction) | Act 2 · U5, Act 3 · U12 |
| **R9** propose a strategy over two coupled solver-optimized controls (sequencing + conversion) | substrate Act 1 · U1–U2; manual Act 3 · U10; solver Act 4 · U15 |
| **R10** recommend-*second* flow (spine first, then strategy, comparative reasoning on demand, user tunes/overrides) | Act 4 · U16 |
| **R11** calm, invited; never a nagging alert | Act 3 · U10, Act 4 · U16 |
| **R12** recommends, but every recommendation probabilistically framed; certainty banned; hedge required | copyGuard Act 2 · U7, extended Act 3 · U10 + Act 4 · U16 |
| **R13** optional honest-limits note (honesty grounds, not a Terms requirement) | Act 1 · U0 (static), Act 4 · U16 |
| **R14** plain not dumbed-down | Act 2 · U7, Act 4 · U16 |
| **R15** no marketing privacy claim; honesty-about-architecture survives | Act 1 · U4 |
| **R16** encrypted at rest + local access guarded (PBKDF2-600k acceptable) | Act 1 · U4 |
| **R17** survivor recovery load-bearing (phrase + mandatory export, two-person posture) | Act 1 · U4, Act 2 · U8, Act 3 · U13 |
| **R18** export/back-up for durability | Act 1 · U4, Act 2 · U8 |
| **R19** manual inputs sanity-checked, never falsely confident | engine half Act 1 · U1; intake half Act 2 · U5; control surfaces Act 3 · U10; budget Act 3 · U9 |
| **R20** itemized, time-boxed budget (essentials=floor / discretionary=surplus) | Act 3 · U9 |
| **R21** lexicographic objective (survival floor → user-chosen surplus goal; objective metric ≡ headline metric) | Act 4 · U15 (objective), Act 4 · U16 (headline pivot) |
| **R22** every recommendation grades its own confidence; hedge rides the headline | Act 4 · U14 (calibration), Act 4 · U16 (render) |
| **R23** comparative depth (why this beat the runner-up; retain the runner-up) | Act 4 · U15–U16 |
| **R24** income-dependent healthcare across the Medicare line (ACA-PTC / IRMAA / HSA) | overlay Act 1 · U3; surfaces Act 3 · U11 |
| **R25** cardinal honesty: calm-but-wrong is the sin; the bar rises for a recommender; "just for friends" never softens validation | validation Act 4 · U14; copyGuard everywhere; N=1 cold-reads |
| **SC** correctness two-tier (engine number right + recommendation right vs an optimality/ranking oracle, ranking-stability, grade calibration) | Act 1 · U1 (number), Act 4 · U14 (recommendation) |
| **R26** the fuck-off date = the existing engine swept over the household work-stop **date-offset `Y`** (never a household "age"); **non-monotone-robust** exhaustive sweep, never a bisection | C3 (`dateSearch.ts`) |
| **R27** the answer is **two dates** (floor + lifestyle) from the lexicographic objective; one-sided window-floor semantic | C3 (engine) + D2 (surface); the two-track split rides Act 3 · U9's degenerate-collapse |
| **R28** both dates **confidence-graded**, never hard lines; re-grade on strategy override | C3 + D2 |
| **R29** framing **adapts to user state** (date for not-yet-retired; spine confidence for already-retired) | D2 (state-adaptive first answer) |
| **R30** model the pre-retirement accumulation phase (contributions + growth → retirement-onset balance + basis) | C2 (accumulation projection) |
| **R31** contributions **per-account, flat-real, stop at the tested date**; employer **match** captured (pre-tax even on a Roth 401k) | C2 (engine) + D1 (intake) + C1 (limit constants) |
| **R32** v1 **projects**, does not optimize accumulation; solver stays decumulation-only | Scope Boundaries (product); C3 (date-search ≠ solver) |
| **R33** healthcare **OFF during accumulation, ON at the tested date** — per-candidate cost-stream construction (the engine has no retirement gate) | C3 (`buildCandidateParams(Y)`) + C2 |
| **R34** accumulation **inherits the engine invariants** — ONE continuous absolute-year draw timeline (CRN); one per-path future end-to-end; empty phase reduces byte-identically | C2 (the load-bearing engine contract; the CRN + reduce-to-spine invariants are canonical in [architecture.md](architecture.md)) |
| **R35** the first answer from a **~5-min account-level guided setup**, surface-early, single entry pass, both user states | D1 (intake reshape) |
| **R36** account **values user-entered; no live price lookup** | D1 + Scope Boundaries |
| **R37** per-ticker holdings **collapse to one household blend**; bundled ticker→asset-class table + manual classification; basis per account, not per lot | C1 (`tickerBlend.ts`) + D1 (entry + manual fallback) |
| **R38** HSA **contributions → accumulation**; HSA **spend → decumulation** (the resumed U3·M5) | C2 (contributions) + B1 (U3·M5 spend) |
| **R39** new PII inherits encryption + the schema ladder (additive `schemaVersion` bump) | C2/D1 schema fields; consumed by Act 1 · U4's migration ladder |
| **R40** generic per-person ongoing non-earned income stream (pension/rental/annuity/alimony/other); reduces-to-spine byte-identically; opt-in off the guided path | Act 2 (engine + intake); build steps [docs/plans/2-first-answer.md](plans/2-first-answer.md) |

## Validation gates

These are the gates a fixture or recommendation must clear before any downstream surface treats it as golden. The full engine-validation detail (golden derivations, the SSA cohort pin, the dataset pins) lives in [docs/architecture.md](architecture.md) and [docs/research/engine-validation-and-tax.md](research/engine-validation-and-tax.md); the per-command surface is below.

| Gate | What it checks | Command |
|---|---|---|
| **Typecheck** | `tsc --noEmit` — must pass before any browser touch | `pnpm typecheck` |
| **Tests** | 3110 vitest across 161 files (Vitest 4, `globals:false`); externally-derived goldens, absence-tests paired with presence companions | `pnpm test` |
| **Lint** | Layer-boundary + engine-purity rules (no clock/entropy/env inside `src/engine/**`) | `pnpm lint` |
| **ACA re-verify** | Fails the build if the enhanced-subsidy legislative entry is stale/unconfirmed — live, possibly-retroactive policy | `pnpm verify:aca` |
| **Bundle budget** | Initial-JS byte sentinel ≤ 300 KiB entry JS; **currently 242.5 KiB** | `pnpm verify:bundle` |
| **CSP enforcement** | A real Chromium blocks an injected inline script + a cross-origin fetch, while the engine worker still constructs — each with a no-CSP control arm | `pnpm verify:csp` |
| **Vertical fit** | The one-frame fit law (`--laptop-fit-height` + the doors-only degrade contract), the two-mount disclaimer swap, the density-tier boundary, the date route's order contract, and the 68rem honesty floor — asserted in a real Chromium on the dev server's `?seed=` routes (reflow-dominated; a token-sum gate is structurally blind). Every assertion family mutation-proven RED before shipping | `pnpm verify:fit` |
| **Doc-stat drift** | README + roadmap "NNN tests across NN files" must match the live suite (vitest collection) — catches both staleness and cross-surface drift | `pnpm verify:doc-stats` |

Solver-blocking gates (Act 4, not yet live): the **optimality oracle** (hand-computable known-best cases, including the after-tax leave-more inversion and the no-change case) must exist and pass **before the solver is allowed to recommend** — enforced structurally as a harness-minted "oracle-cleared" token, taken as a required parameter, withheld until every check passes **and** every rec-relevant primary is pinned (`directionalUntilPinned === false`) **and** ε is calibrated. Skipping validation or recommending on directional fixtures is a *compile* error, not a discipline check. The date-search carries the non-monotone-robust contract and the empty-phase byte-identity gate (both shipped with C2/C3). The N=1 cold-read judges *tone*; the automated oracle judges *correctness* — never the reverse.

## What is next

**Act 2 is complete (2026-07-02).** The whole magic-moment arc is live and review-hardened: the account-level guided setup (U5/D1), the confidence band integrated on both routes (U6), the confidence statement + survivor readout (U7), the state-adaptive first answer with the two-pane laptop layout (D2), other income (R40), and the full save/recovery/return arc (U8 — first-Save ceremony, the recovery-passphrase rework, unlock/recovery/restore, view-only second tab, edit-and-re-save, the update-apply hold). Residual N=1 tone reads are parked non-blocking in `TODO.md`.

**Act 3 (Controls, U9–U13) is the live act** — U9 + U10 + U11 + **U12 are shipped** ([docs/plans/3-controls.md](plans/3-controls.md)); U12’s cold-read batch CLEARED 2026-07-10 (the bad-news truer-picture transition — a formal Act-3 exit condition — passed untouched; his one consistency finding fixed same session) via the Caddie’s first pre-walked card; **U13 SHIPPED 2026-07-10 evening under the clearance grant — ACT 3 IS COMPLETE** (the Medicare-card walk closed its final batch 2026-07-11). The 2026-07-08 gate batch cleared (the dip verdict + U11’s six stations). **Cold-read cadence since 2026-07-11: the batched-oracle grant** — chaired Caddie cards ship (no per-surface park); Briggsy’s eye audits any-time + at the pre-exposure Briggsy-bar gauntlet. Act 4 (Recommendation) — the actual differentiator — follows Act 3.

The live, session-level next-action queue is in `TODO.md`.
