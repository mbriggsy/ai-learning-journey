# arm-c enlarged-text — STOPS
"c3 FIRED — I STOPPED, and c4 + c5 were NOT applied.\n\nAll 8 arms are red, and the offenders are squarely inside the PROTECTED set the spec's docblock (vertical-fit.spec.ts:16-20) names under the Honesty-Hawk veto: the VERDICT (div.survivor__verdict / p.survivor__word), the priced-Medicare AFFIRMATION and narrowed RESIDUAL (p.cs-medicare-note / p.cs-medicare-residual), the BAND (div.cs-band / aside.band-drawer), and the R13 DISCLAIMER (footer.disclaimer). This is not the sanctioned unprotected tail: .result-quiet-row is already excluded by `assertFrameFits(page, true)`, and .result-backup-door / .rec-record appear in NO arm's offender list.\n\nWhat I did NOT do, deliberately:\n- did NOT add any flag to SanctionedDoors (excluding a subtree there also excludes its whole ancestor chain, which would silently un-protect the R13 disclaimer — the exact failure the report was hunting);\n- did NOT raise --laptop-fit-height (tokens.css:118) — and it would not have helped: frameReport measures the live window.innerHeight, not the token;\n- did NOT relax, soften or scope-narrow assertFrameFits;\n- did NOT touch docs/architecture.md's §12 'The gate is real Chromium' bullet (c4). In THIS tree that bullet is at :326, NOT the :327 the skeptic prescribed — the skeptic's own refutation of the verifier's :326 is wrong against today's file, and I would have written the fit sentence into the right bullet only on a green arm. Writing it now would plant a false claim;\n- did NOT touch docs/backlog.md's 'The app on someone else's device' third bullet (c5). Its '(Partial, 2026-09-05 …)' parenthetical is at :768 in this tree (the skeptic said :669; the heading is :762, not :663 — the whole entry has moved ~99 lines). Its live sentence 'The fit law itself is still unmeasured under a raised default' is now the only thing that is no longer true — it IS measured, and it FAILS. That rewrite belongs to whoever lands the council's ruling, not to a red arm.\n\nTHE FRAMING CALL FOR THE COUNCIL (mine to surface, not to make): the one-frame law and a 24 px browser default are, on today's layout, mutually exclusive — the answer is a ~3.5-frame scroll. So either (a) the one-frame law binds only at the default and the honest answer at an enlarged default is a DOCUMENTED SCROLL — in which case this arm should assert ORDER (graphs → in-frame disclaimer → doors), the contract the DATE route already uses at vertical-fit.spec.ts's dip/datenc arms, rather than fit; or (b) the law binds and the layout owes a large-text regime. I have no live in-file precedent to hand them for a record-only arm: the KNOWN-BREACH arm this spec once had is gone (only the back-reference at vertical-fit.spec.ts:135 survives).\n\nCONSEQUENCE FOR LANDING — READ THIS BEFORE MERGING THE PATCH: the patch as delivered makes `pnpm verify:fit` RED (8 failures) and therefore reds the CI job that runs it. It must NOT be landed unchanged on main. It is delivered as the INSTRUMENT that produced the measurement above, which is what c3 asked for."

# MEASURED
"ROOT FONT (the falsifiability pin), measured on every one of the 8 arms via getComputedStyle(document.documentElement).fontSize: 16 px on the bare `/` before the CDP send, 24 px on the seed route after it. Identical on all 8 — no arm measured a 16 px frame.\n\nPER-ARM RESULT (seed | viewport | root@baseline→after | elements walked | offenders | PROTECTED offenders with their bottom px). Every arm FAILED. Viewport height is the budget frameReport compares against (vertical-fit.spec.ts frameReport's `window.innerHeight`).\n\n1. budget | REAL 1536×791 @2.5dpr | 16→24 | counted=81 | 67 offenders | band div.cs-band@1177 / aside.band-drawer@1177; verdict div.survivor__verdict@1347, p.survivor__word@1347; Medicare affirmation p.cs-medicare-note@1768; Medicare residual p.cs-medicare-residual--typical@2085; R13 footer.disclaimer@3048 (its <p>@2976)\n2. budget | TIER 1280×800 | 16→24 | counted=81 | 66 offenders | div.cs-band@1174; div.survivor__verdict@1341, p.survivor__word@1341; p.cs-medicare-note@1760; p.cs-medicare-residual--typical@2077; footer.disclaimer@3040 (<p>@2968)\n3. retired | REAL 1536×791 @2.5dpr | 16→24 | counted=70 | 55 offenders | div.cs-band@1177; div.survivor__verdict@1347, p.survivor__word@1347; p.cs-medicare-note@1700; p.cs-medicare-residual@1892; footer.disclaimer@2854 (<p>@2782)\n4. retired | TIER 1280×800 | 16→24 | counted=70 | 54 offenders | div.cs-band@1174; div.survivor__verdict@1341; p.cs-medicare-note@1695; p.cs-medicare-residual@1886; footer.disclaimer@2849 (<p>@2777)\n5. health | REAL 1536×791 @2.5dpr | 16→24 | counted=68 | 53 offenders | div.cs-band@1177; div.survivor__verdict@1347, p.survivor__word@1347; footer.disclaimer@2596 (<p>@2524). NO Medicare pair — correct, `health` is the ACA-door household (SPINE_SEEDS medicareNote:false).\n6. health | TIER 1280×800 | 16→24 | counted=68 | 52 offenders | div.cs-band@1174; div.survivor__verdict@1341; footer.disclaimer@2591 (<p>@2519)\n7. nc | REAL 1536×791 @2.5dpr | 16→24 | counted=70 | 55 offenders | div.cs-band@1177; div.survivor__verdict@1347; p.cs-medicare-note@1700; p.cs-medicare-residual@1892; footer.disclaimer@2854\n8. nc | TIER 1280×800 | 16→24 | counted=70 | 54 offenders | div.cs-band@1174; div.survivor__verdict@1341; p.cs-medicare-note@1695; p.cs-medicare-residual@1886; footer.disclaimer@2849\n\nMAGNITUDE: 52-67 of the 68-81 measured elements are below the fold — 76-83% of the page. The R13 disclaimer's bottom lands at 3.2×-3.8× the viewport height (e.g. budget/TIER: 3040 px against an 800 px frame). This is not a marginal breach; at a 24 px default the resolved answer is roughly a 3.5-frame scroll.\n\nHOW MEASURED: the block logs `[24px-root fit] seed=… WxH root=…→…px: counted=… viewport=… offenders=<desc>@<bottom>` from a `frameReport(page, true)` call placed BEFORE `assertFrameFits(page, true)` (so a green arm still leaves evidence). Raw runs: <scratchpad>/run24.txt, run24b.txt, run24c.txt.\n\nDETERMINISM: three independent runs of the 8 arms produced byte-identical seed/viewport/root/counted records (diff of the extracted keys is empty) and identical offender bottom px.\n\nCONTROL AT 16 px: the pre-existing spine arms over the SAME four seeds and the SAME two viewports, calling the SAME assertFrameFits(page, true), pass 8/8 in 6.9 s. So the frame fits at the default and breaks at 24 px.\n\nWHY IT BREAKS (grepped in this tree, not assumed): tokens.css declares BOTH scales in rem — the type scale --text-xs 0.8125rem … --text-hero clamp(2.875rem,…,4rem) (tokens.css:56-62) AND the spacing scale --space-1 0.25rem … --space-12 6rem (tokens.css:80-91). `grep -rnE \"font-size:\\s*[0-9.]+px\" src --include=*.css` returns NOTHING; `html` (base.css:16-22) and `:root` declare no font-size at all; the only html/:root/body font-size is `body { font-size: var(--text-base) }` (base.css:28), itself rem. `grep -rnE fontSize src --include=*.tsx --include=*.ts` returns nothing. So a raised browser default multiplies type AND whitespace by 1.5×, while the budget — `window.innerHeight` in frameReport — does not move at all. (--laptop-fit-height: 800px, tokens.css:118, is the token the CSS is tuned to; it never reaches the assertion.)\n\nCI COST: the 8 arms ran in ~11 s (server reused) to ~29 s (cold) locally on 6 workers, one engine solve each — the same order as the 8 existing 16 px REAL/TIER spine arms (6.9 s). The verifier's landmine #12 also mis-stated the harness budget: playwright.fit.config.ts's timeout is 180_000, not 120 s."

# LANDMINES
"1. THE PRESCRIPTION'S DOC ANCHORS ARE BOTH WRONG IN THIS TREE, IN OPPOSITE DIRECTIONS. c4: the §12 'The gate is real Chromium' bullet with 'the text grows when the reader raises their browser font' is at docs/architecture.md:326 here — the verifier's number, not the skeptic's :327. The skeptic's refutation ('WRONG LINE… :326 is the band-annotation-block bullet') does not hold against today's file. c5: docs/backlog.md's entry heading is :762 and the '(Partial, 2026-09-05 …)' third bullet is :768 — the skeptic said :663/:669, ~99 lines stale. Whoever lands c4/c5 must re-grep both, not trust either number.\n\n2. THE VERIFIER'S HELPER ANCHORS FOR c1 ARE STALE; THE SKEPTIC'S ARE RIGHT. In this tree: SPINE_SEEDS :61-73, assertResolvedSpine :77, frameReport :111 (window.innerHeight :119), WALK_FLOOR :166, assertFrameFits :168, the spine-matrix `for` opens :248 and closes :285, SHOWCASE arm :274-283. reviewSurface.ts: REAL :16, REAL_DPR :18, TIER :20, settleLayout :41, gotoSeedFinal :76 (it ENDS with settleLayout at :81 — do not double-settle).\n\n3. LANDMINE #12'S TIMEOUT NUMBER IS WRONG. playwright.fit.config.ts's per-test timeout is 180_000, not 120 s (:44). The wall-clock worry it raises is also unfounded for this arm: 8 arms, ~11-29 s, one solve each.\n\n4. THE CHEAP FIRST NAVIGATION IS LOAD-BEARING, AND IT IS A DELIBERATE DEVIATION FROM THE LITERAL PRESCRIPTION. The skeptic's c1 body calls `gotoSeedFinal` TWICE (navigate → set → re-navigate), which pays two full engine solves per arm for a baseline read that does not depend on the answer at all. I kept the ordering law (send between two navigations) but made the first navigation a bare `page.goto('/')`. Anyone 'restoring' the second gotoSeedFinal doubles this arm's CI cost for nothing.\n\n5. THE PIN IS THE ONLY THING STANDING BETWEEN THIS ARM AND A VACUOUS GREEN. e2e/ is outside tsconfig's include and eslint has no e2e glob, so a renamed CDP method, a flat `{ standard: 24 }` param (rejected — the shipped shape at chart-text.spec.ts:714 is `{ fontSizes: { standard, fixed } }`), or a dropped `await` would all fail SILENTLY and the arm would re-measure the 16 px frame, which the control proves FITS. Never delete or weaken the `expect(at24, …).toBeGreaterThan(at16)` line.\n\n6. IF THE COUNCIL RULES 'DOCUMENTED SCROLL', DO NOT REACH FOR SanctionedDoors. Adding a flag there excludes the offender's whole ANCESTOR CHAIN (vertical-fit.spec.ts:126-139), which on these arms means .cs-band, .survivor and their parents — it would un-protect the verdict and the disclaimer wholesale. The DATE route's ORDER contract (graphs → in-frame disclaimer → doors) is the shape to copy instead.\n\n7. PORT 4190 WAS OCCUPIED AND `reuseExistingServer` IS TRUE LOCALLY. I ran on 4193 by temporarily rewriting all four `4190` occurrences in playwright.fit.config.ts and restoring from a sha256-verified backup; the patch does not touch that file. If a future run reuses a dev server left on 4193 from these runs, it is serving THIS worktree — but the same setting means a server on 4190 could silently serve someone else's tree. Always confirm which tree answered.\n\n8. THE MUTANT'S RED LANDS AT THE PIN, NOT AT THE FIT ASSERTION — so the mutant alone does NOT prove the 16 px frame fits. That is why the separate 16 px control run exists; do not drop it from the evidence chain when re-verifying."

# docTruth
"No docs/*.md file was changed (c4 and c5 are blocked by the stop). Everything below is a comment or an assertion message I added to e2e/vertical-fit.spec.ts, each re-verified in THIS tree today.\n\n1. 'The chart-text gate already proves the CHARTS follow the reader's browser font up (e2e/chart-text.spec.ts, its 20px-root arms)' — TRUE: chart-text.spec.ts:1407-1423 asserts min font px at a 20 px default > at 16 px; its 320-arm tests at :1472/:1495 assert `floor > 16`.\n\n2. '…the FRAME law had never been measured under a raised default at all' — TRUE: `grep -n setFontSizes e2e/*.spec.ts` before my edit hit ONLY chart-text.spec.ts:713-714. docs/backlog.md:768 says the same in prose.\n\n3. 'This re-drives the two one-frame spine arms above — REAL and the TIER floor, never the tall SHOWCASE' — TRUE by construction: the loop is `for (const vp of [REAL, TIER] as const)`; SHOWCASE (spec :274-283) is untouched.\n\n4. 'Chromium-only: Page.setFontSizes is CDP, and this harness's one project is chromium' — TRUE: playwright.fit.config.ts:51 is `projects: [{ name: 'chromium', … }]`.\n\n5. 'BOTH scales are rem (tokens.css --text-xs … --text-hero, --space-1 … --space-12)' — TRUE: tokens.css:56-62 (rem / clamp-in-rem) and :80-91 (rem).\n\n6. 'src/**/*.css declares no literal-px font-size anywhere' — TRUE: `grep -rnE \"font-size:\\s*[0-9.]+px\" src --include=*.css` returns nothing. (I rewrote an earlier draft of this sentence after checking: the three `font-size` lines that mention px do so only in a trailing comment beside `var(--text-base)`.)\n\n7. '`html` and `:root` declare no font-size at all, and the one `body` declaration is `font-size: var(--text-base)` — itself rem' — TRUE: base.css's `html` block (:16-22) sets only color-scheme + -webkit-text-size-adjust; the only html/:root/body font-size in src is base.css:28. My FIRST draft said 'no html/:root/body font-size override', which was FALSE (body does declare one) — I caught and corrected it before the final run.\n\n8. '`frameReport` measures every box against the LIVE `window.innerHeight`, which no font setting grows' — TRUE: frameReport's evaluate opens with `const vh = window.innerHeight` (vertical-fit.spec.ts:119) and compares `r.bottom > vh + 0.5`.\n\n9. '(--laptop-fit-height is the token the layout is TUNED to; it never reaches this assertion.)' — TRUE: `grep -rn laptop-fit-height` hits only src/ui/styles/*.css + Result.tsx comments; no e2e file reads it. This corrects the verifier's landmine #7, which conflated the token with the test's budget.\n\n10. 'the two orders this repo has proven green are \"send, then navigate\" (chart-text.spec.ts's setRootFont20, called before its first gotoSeedFinal) and \"send, then reload\" (its raiseRootFont)' — TRUE and freshly re-read: setRootFont20 at chart-text.spec.ts:712-715 with its own comment 'Per-target emulation, so it survives navigation: call it BEFORE the first gotoSeedFinal'; raiseRootFont at :719-724 does setRootFont20 + page.reload(). I deliberately did NOT write the skeptic's stronger sentence ('binds on a NAVIGATION, never retroactively') — I never measured the no-navigation case, so asserting it would have been an unsourced claim.\n\n11. 'Falsifiability (insight 016, this spec's docblock above)' — TRUE: the spec docblock at :47-48 cites insight 016 by name.\n\n12. 'with the send removed the arm still renders, still resolves, and the 16px frame FITS — so a silently-ignored… CDP call would pass having measured nothing new' — PROVEN in two halves: the mutant reached the pin (so it rendered and resolved through gotoSeedFinal's final-tier wait), and the 16 px CONTROL run over the same 4 seeds × 2 viewports passed 8/8. I ran that control specifically because this sentence would otherwise have been an assumption.\n\n13. '(mutant-proven: drop the send and that line reds, \"root 16px → 16px\")' — TRUE, quoted verbatim from the mutant run's failure output.\n\n14. Inline: 'RECORD the walk before asserting it: a PASSING assertion prints nothing, so this line is the standing evidence of what was actually measured at the raised root.' — TRUE of the code as written (frameReport is called before assertFrameFits). My first draft said 'not only the first arm that threw', which was loose — each arm is its own test and throws independently; corrected.\n\n15. Assertion message 'Page.setFontSizes never took (root Xpx → Ypx) — this arm would measure the 16px frame' — TRUE, and it is the message the mutant printed."