// Placeholder — the tax-and-accounts overlay (buckets, ordinary tax, RMD birth-year,
// SS provisional-income fixed-point, MFJ->single): a deterministic per-year cash-term
// transform that consumes ZERO draws and reduces byte-identically to the spine when off.
// Filled in P1·U2. See docs/plans/back-nine-mvp/phase-1-foundation.md (Unit 2).
export {}
