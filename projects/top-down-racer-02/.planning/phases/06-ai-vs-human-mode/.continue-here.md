# Continue Here — Phase 6, Plan 04

**Paused:** 2026-03-02
**Branch:** main
**Last commit:** d71152c4 (WIP: pause Phase 6)

## Where We Left Off

Plan 04 is the final plan of Phase 6. It was **not started** — we were about to execute when the session paused. Three design changes were agreed this session and folded into Plan 04 scope.

## This Session's Work

No code changes — only planning docs updated:
- `06-CONTEXT.md` — Gap Timer section marked REPLACED, Win/Loss section rewritten
- `STATE.md` — Two new decision entries
- Auto-memory `phase6-decisions.md` — New HUD and Finished Screen sections

## Design Changes Agreed (fold into Plan 04 execution)

### Change 1: Drop GapTimerHud popup
- Delete `src/renderer/GapTimerHud.ts`
- Remove from `HudRenderer.ts` (showGap, tick, reset, import)
- Remove from `GameLoop.ts` (onGapUpdated callback, trackHumanCheckpoints gap logic)
- Remove from `ScreenManager.ts` (onGapUpdated wiring)
- Delete `tests/ai/gap-timer.test.ts`

### Change 2: Add minimal AI stats to HUD in vs-ai mode
- In `HudRenderer.ts`, add AI section showing AI total race time + AI best lap
- Position: tucked under existing human timing stats (top-right area)
- HudRenderer needs access to AI world state (via getter, same pattern as WorldRenderer's setAiStateSource)

### Change 3: Finished screen — total time primary, best lap secondary
- In `OverlayRenderer.ts` `updateFinished()` (lines ~1158-1175)
- Primary: total race time (`totalRaceTicks`) — who finished all laps faster = overall winner
- Secondary: best lap (`bestLapTicks`) — faster single lap
- Both shown with deltas
- Currently only compares `bestLapTicks` — needs rework
- Need AI total race time fed to OverlayRenderer (currently only gets `aiBestLapTicks`)

### Original Plan 04 tasks (still needed)
1. TrackSelectScreen leaderboard display (human vs AI best per card)
2. ScreenManager AI best recording with dedup cache
3. Delete BestTimes.ts shim
4. Full test suite regression check
5. Human-verify checkpoint (update criteria for new behavior)

## Key Files to Touch

| File | Action |
|------|--------|
| `src/renderer/GapTimerHud.ts` | DELETE |
| `src/renderer/HudRenderer.ts` | Remove gap timer, add AI stats section |
| `src/renderer/OverlayRenderer.ts` | Rework updateFinished() comparison logic |
| `src/renderer/GameLoop.ts` | Remove onGapUpdated, expose AI timing for HUD |
| `src/renderer/ScreenManager.ts` | Remove gap wiring, feed AI total time to overlay |
| `src/renderer/screens/TrackSelectScreen.ts` | Add leaderboard display (per Plan 04) |
| `src/renderer/BestTimes.ts` | DELETE |
| `tests/ai/gap-timer.test.ts` | DELETE |

## Test Status

- **374/374 tests pass** (17 test files)
- TypeScript compiles clean

## Blockers

None.

## How to Resume

```bash
cd C:\Users\brigg\ai-learning-journey\projects\top-down-racer-02
/gsd:resume-work
# Then execute Plan 04 with the three design changes folded in
```
