# Task Completion Checklist

After completing a task:
1. Run `pnpm test` to verify all tests pass
2. Run `pnpm exec tsc --noEmit` to verify no TypeScript errors
3. Screenshots and temp files go in system temp directory, NOT project root
