# .env.example — Copy to .env and fill in values
# NEVER commit .env to git

# Gemini API — get key from https://aistudio.google.com/apikey
# Requires billing enabled for Imagen 4 (~$0.06/image)
# Also used for narrator TTS (Gemini 2.5 Flash TTS — free tier eligible)
GEMINI_API_KEY=
