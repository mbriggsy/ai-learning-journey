# .env.example — Copy to .env and fill in the project-local values below.
# NEVER commit .env to git.
#
# SHARED secrets now live in the REPO-ROOT .env (see ../../.env.example):
#   GEMINI_API_KEY — Imagen 4 assets + Gemini 2.5 Flash TTS narrator.
#   Get a key: https://aistudio.google.com/apikey (billing on for Imagen 4).
# The generate-assets / generate-narrator scripts load it via
#   tsx --env-file=../../.env --env-file-if-exists=.env
#
# Project-local values (keep these here):
VITE_PARTYKIT_HOST=localhost:1999
STITCH_API_KEY=
