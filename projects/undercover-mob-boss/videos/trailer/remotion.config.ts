import { Config } from '@remotion/cli/config';

// Point to the game project's public/ directory so all game assets
// (images, audio, fonts) are accessible via staticFile()
Config.setPublicDir('../../public');
